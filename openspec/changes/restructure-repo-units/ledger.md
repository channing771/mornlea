# Ledger: restructure-repo-units

裁决与进度记录。每任务一行裁决（SPEC/QUALITY），评审循环以 subagent-driven-development 为准。

| 日期 | 任务 | 裁决 | 记录 |
|---|---|---|---|
| 2026-09-04 | S0 change 立项 | PLANNED | proposal/design/specs delta（repository-code-organization：3 ADDED + 18 MODIFIED）/tasks（T1–T8）完成；模块切割以 archcheck `allowed` 表为权威输入验证为 DAG；用户裁决：全单元入 `packages/`、engine 保留原名、services 改 agent 服务族（companion 居 `packages/agent/companion`） |
| 2026-09-04 | T1.1–1.4 实现+评审 | PASS | 实现：unit_boundary_test.go（六单元边表+drift+动态激活）、卫生清理（client.test/mesh.test/空 golden 目录/.gitignore/make_demo_gif 路径）、git mv companion → packages/agent/companion（含 Python 仓库根定位深度 +1 的 9 处必需同步）、verify-native-artifact.sh 收敛 CI 三处重复块 + frontend job 改 make frontend-check（corepack enable 替代 action-setup）。定向验证全绿（archcheck/companion-agent-check/integration/frontend-check）。评审四偏离全 ACCEPT；P2 三条：①脚本测试可补钉 test -f/重定向语句（后续）②project-identity 直改已回退、改走本 change delta spec ③commit 时确认脚本可执行位。裁决：shared→∅ 空边表与 design 一致；go.mod require 解析器核验正确（replace/exclude 不误判）；corepack ubuntu 权限前提已核实 |
| 2026-09-04 | T2.1–2.2 实现+评审 | PASS | 实现：engine → packages/engine 整体 git mv（127 文件）；Makefile/ci.yml/.gitignore/cgo 双包三要素/archcheck 六个测试/guard.mjs 六正则/AGENTS 链/docs/openspec specs 路径全量同步（含 grep 查漏：lod oracle、app 测试 schema 读取、visual.mjs repoRoot 4→5、baseline_test sourcePath）；deploy-dylib.sh 提取（if [ -f ] 守卫替代吞错 || true，fail-fast 方向正确）。cgo 深度裁决：包仍在 internal/ 两层，${SRCDIR}/../../packages/engine 正确，S4 迁包后再变三层。评审 P1 一行（visual-golden README 活路径）已由控制会话修复；P2：历史文档旧路径留 T8.3 确认、deploy-dylib.sh 入库需可执行位。验证：make rust/rust-check、nativeabi/mesh/client/lod/app/archcheck 定点、guard 20/20、frontend-check、make build 全绿 |
| 2026-09-04 | T3.1 实现+评审 | PASS | 实现：go work init（. + packages/contracts）、contracts 模块化（零依赖 module + 根 go.mod require+replace 保 GOWORK=off 单模块世界）、import/路径全量改写（server/companion 测试/archcheck 三表/pyproject force-include/mcp_contract.py 分段路径）、CI 分片自检双向补 packages/contracts。关键实证：go.work 下 `go test ./...` 不跨嵌套模块（47 包不含 contracts），各阶段须显式纳管新模块，S8 收敛为循环。评审三偏离全 ACCEPT；P2-a：companionGoProductionRoots 与扫描根在 T4/T5 移包时须同步（BFS 图外 import 静默跳过会变松）；P2-b：S3–S7 期间 make dev-check 不含 contracts 模块（CI 已覆盖，T8.2 收敛）。单元边界检查首激活（contracts→∅）PASS |
