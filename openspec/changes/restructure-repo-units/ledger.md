# Ledger: restructure-repo-units

裁决与进度记录。每任务一行裁决（SPEC/QUALITY），评审循环以 subagent-driven-development 为准。

| 日期 | 任务 | 裁决 | 记录 |
|---|---|---|---|
| 2026-09-04 | S0 change 立项 | PLANNED | proposal/design/specs delta（repository-code-organization：3 ADDED + 18 MODIFIED）/tasks（T1–T8）完成；模块切割以 archcheck `allowed` 表为权威输入验证为 DAG；用户裁决：全单元入 `packages/`、engine 保留原名、services 改 agent 服务族（companion 居 `packages/agent/companion`） |
| 2026-09-04 | T1.1–1.4 实现+评审 | PASS | 实现：unit_boundary_test.go（六单元边表+drift+动态激活）、卫生清理（client.test/mesh.test/空 golden 目录/.gitignore/make_demo_gif 路径）、git mv companion → packages/agent/companion（含 Python 仓库根定位深度 +1 的 9 处必需同步）、verify-native-artifact.sh 收敛 CI 三处重复块 + frontend job 改 make frontend-check（corepack enable 替代 action-setup）。定向验证全绿（archcheck/companion-agent-check/integration/frontend-check）。评审四偏离全 ACCEPT；P2 三条：①脚本测试可补钉 test -f/重定向语句（后续）②project-identity 直改已回退、改走本 change delta spec ③commit 时确认脚本可执行位。裁决：shared→∅ 空边表与 design 一致；go.mod require 解析器核验正确（replace/exclude 不误判）；corepack ubuntu 权限前提已核实 |
| 2026-09-04 | T2.1–2.2 实现+评审 | PASS | 实现：engine → packages/engine 整体 git mv（127 文件）；Makefile/ci.yml/.gitignore/cgo 双包三要素/archcheck 六个测试/guard.mjs 六正则/AGENTS 链/docs/openspec specs 路径全量同步（含 grep 查漏：lod oracle、app 测试 schema 读取、visual.mjs repoRoot 4→5、baseline_test sourcePath）；deploy-dylib.sh 提取（if [ -f ] 守卫替代吞错 || true，fail-fast 方向正确）。cgo 深度裁决：包仍在 internal/ 两层，${SRCDIR}/../../packages/engine 正确，S4 迁包后再变三层。评审 P1 一行（visual-golden README 活路径）已由控制会话修复；P2：历史文档旧路径留 T8.3 确认、deploy-dylib.sh 入库需可执行位。验证：make rust/rust-check、nativeabi/mesh/client/lod/app/archcheck 定点、guard 20/20、frontend-check、make build 全绿 |
| 2026-09-04 | T3.1 实现+评审 | PASS | 实现：go work init（. + packages/contracts）、contracts 模块化（零依赖 module + 根 go.mod require+replace 保 GOWORK=off 单模块世界）、import/路径全量改写（server/companion 测试/archcheck 三表/pyproject force-include/mcp_contract.py 分段路径）、CI 分片自检双向补 packages/contracts。关键实证：go.work 下 `go test ./...` 不跨嵌套模块（47 包不含 contracts），各阶段须显式纳管新模块，S8 收敛为循环。评审三偏离全 ACCEPT；P2-a：companionGoProductionRoots 与扫描根在 T4/T5 移包时须同步（BFS 图外 import 静默跳过会变松）；P2-b：S3–S7 期间 make dev-check 不含 contracts 模块（CI 已覆盖，T8.2 收敛）。单元边界检查首激活（contracts→∅）PASS |
| 2026-09-04 | T4.1 实现+评审+修复 | PASS | 实现：12 包+tuning 切入 packages/shared（git mv + 约 250 文件 import 改写 + cgo 深度三层）；archcheck 五测试文件同步（allowed 双表/companion 图/identity/platform/source_guards/baseline）；Makefile GO_TEST_MODULES 循环纳管；race-changed 跨模块闭包实测；CI 分片并入 shared。评审五偏离全 ACCEPT（worldgen 草测试迁 internal/lod 保 shared 零兄弟依赖、shared 无 contracts require 以 tidy 为准、klauspost 钉回 v1.19.1、删 guard.mjs 假同步注释、CI 三处连带路径）；P1（Makefile for 循环吞非末位退出码）由修复子代理按处方修复并以最小复现证明（修复前 exit 0 → 修复后 exit 2）+ make test-race-short 全绿；P2-a tasks.md 4.1 文本已修订；P2-b guard.mjs 旧 native 清单留 T8 清扫；test-timing-discipline 分片 union 张力裁决为补 MODIFIED delta（模块中性措辞，已入 change）。验证：shared 15 包 -short、archcheck、vet 三模块、lod、race-changed 43 包 race、CI 自检 48 包、GOWORK=off build、openspec validate 全绿 |
| 2026-09-04 | T5.1 实现+评审 | PASS | 实现：sim 四包+fluid+storage 七包+server(+persistence)+cmd/mornlea-server 切入 packages/server（494 文件 git mv）；go.mod require+replace shared/contracts；archcheck 全套同步（allowed 双表/persistence 反向禁令/identity mutation/platform token 扫描根/companion 生产根——兑现 T3 P2-a）；Makefile/CI/race-changed 四模块编排；新建 packages/server/AGENTS.md。关键偏离（控制会话裁决维持至 S6）：server 28 个测试文件（client import 28 处、mesh/assets 4 处）驱动的 Memory/TCP 集成测试使 packages/server 暂 require 根模块（replace ../..），生产零污染经 grep+Linux 依赖闭包双重实证；S6 裁决方向见下行。评审五偏离全 ACCEPT；P2-1 实测 28 文件（实现者低估为 20）；P2-2 AGENTS 措辞 S6 已改；P2-3 companion dialogue 负载 flake 在案（重负载勿并行）；事故披露：sed 曾波及 .claude/worktrees/feat-ui-changes 283 文件，已证明≡HEAD 后还原干净。验证：server 17 包 -short、根模块 15 包、archcheck、make archcheck、companion-agent-integration、race-changed 42 包、CI 自检 48 包、GOWORK=off 双向 build、guard 20/20；build-linux-server 本机环境不可跑（macOS 无 Linux 交叉 C 工具链，CI ubuntu 执行，本地以 GOOS=linux go list -deps+禁入正则等价覆盖） |
| 2026-09-04 | T6.1 实现+评审 | PASS | 实现：client 六域包+cmd/mornlea 切入 packages/client（161 R+178 RM）；S5 遗留 server→根模块边正式化为 server→client 测试专用（require 集精确断言门禁化，P2-4 兑现）；新源码守卫 server_client_boundary_test.go 双向禁令（server 生产禁 client、client 域库含测试禁 server、cmd/mornlea 装配子树放行）+5 drift。**重大架构事实与裁决（控制会话追认）**：cmd/mornlea/app 与 benchmark 生产代码进程内装配权威 Host（单机/远程同路径的既定架构），allowed 表的 go list 枚举从未含 ./cmd 故设计初稿 DAG 漏此边；落地 client → {shared, server}（server 边仅 cmd 装配消费），域库闭包实证零 server 包、cmd 闭包 14 server 包；delta spec/design/tasks 已由实现者按 openspec 规则先行修订；评审独立评估维持豁免、否决第七模块方案（三层钉死+与 tools 组合入口/测试豁免两先例同构）。golden：captureGoldenDir 系 cwd 相对常量（make 从仓库根发起），无需改，visual-check 24 场景 0 像素差。验证：client 12 包 -short、archcheck（含新守卫）、GOWORK=off 三模块独立 build/vet、go mod verify、race-changed 全绿、CI 自检 48 包、gofmt/openspec validate 通过；旧前缀 mornlea/internal/ 在 packages/ 下零残留 |
| 2026-09-04 | T7.1–7.2 实现+评审+修复 | PASS | 实现：tools 模块立项（perfcheck/agent-board+web/gfxspike/composite；agent-board findRepoRoot 改锚 go.work）、internal/archcheck → packages/audit（枚举重构：repositoryRoot/workspaceModules/listWorkspacePackages，perModule 收窄）、根 go.mod/go.sum 解散、go.work 六 use 终态、internal//cmd//web/ 目录消失、CI 分片 client/server/rest+全集自检六模块 union 48 包、cache key 五 go.sum。评审六偏离全 ACCEPT（根模块不加 tools require——sync 即剪、tools require 集为上界子集、mornlea-server config 豁免系旧盲区收敛、scripts/AGENTS.md 无 composite 段、config.yaml 一并更新、mutation 落点 tools）。P1（Linux wildcard 静默跳过整包 darwin-only 的 gfxspike 致 linux-server 必红）由修复子代理解决——并修正评审推荐方案本身缺陷（`/gfxspike/...` 尾缀仍 wildcard 不吃 -e，唯直接点名 `./gfxspike` 生效，Linux 以 ImportPath 完好+imports 空带错列出，正反断言均过）；顺带 P2 四小项（GOWORK=off fail-closed、Compact 去重、.gitignore gfxspike 条目、config.yaml client ABI 行）。新增 TestWorkspaceUseSetMatchesUnitModules（use 集⇄模块集双向）。验证：六模块 vet、tools/audit -short、make archcheck、test-multiplayer、agent-board npm ci+build、race-changed（闭包 25 包）、GOWORK=off 三小模块独立构建、openspec validate 87/87 | 
