These are implementation tasks, all still pending. Direct Cargo commands run from the repository root using the toolchain pinned by `packages/engine/rust-toolchain.toml`; reconcile the explicit pin if that file changes before implementation. New crate and named test targets below are planned outputs, not currently runnable commands. Register each named integration target with non-empty contract cases before using it as evidence; inspect `-- --list` and reject zero discovered tests. Add the failing scenario first, then the minimum implementation, and commit each verified node. Record coverage and the exact code SHA in `ledger.md`. Refine any node exceeding a session into explicit capability tasks before implementing it.

## 1. Inventory and replay identity

- [ ] 1.1 Reconcile current registries/code with `testdata/runtime-migration/contracts.json`; add failing coverage, version and missing-family tests in `packages/tools/cmd/runtime-oracle/`, then implement the inventory reader. Include all protocol/save/input/event/kernel families and provenance. Validate with `go test ./packages/tools/cmd/runtime-oracle -run TestContractInventory -count=1` and `go test ./packages/tools/cmd/runtime-oracle -list TestContractInventory`; verify tests are discovered.
- [ ] 1.2 Add deterministic oracle traces and isolated temp-save execution in `packages/tools/cmd/runtime-oracle/`, with rejection tests for incomplete identity, live-path writes, malformed input and incomplete traces. Validate with `go test ./packages/tools/cmd/runtime-oracle -race -count=1`; record corpus digests in this change's ledger.

## 2. Rust shared contracts

- [ ] 2.1 Register `mornlea_domain`, `mornlea_protocol`, and `mornlea_storage` in `packages/engine/Cargo.toml`, create their scoped guides and non-empty `runtime_contract` integration targets, and pin dependency directions. Validate target discovery with `rustup run 1.97.1 cargo metadata --manifest-path packages/engine/Cargo.toml --no-deps --format-version 1` and `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_domain -p mornlea_protocol -p mornlea_storage --test runtime_contract --locked -- --list`; record intended red cases before implementing them.
- [ ] 2.2 Port shared identity/value/input/event semantics into `packages/engine/crates/mornlea_domain/` against the inventory, including invalid ranges and deterministic ordering. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_domain --test runtime_contract --locked`.
- [ ] 2.3 Port framing, negotiation and packet-family codecs into `packages/engine/crates/mornlea_protocol/`, one inventory family per tested commit, preserving bytes and malformed-input failures. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_protocol --test runtime_contract --locked`.
- [ ] 2.4 Port versioned save records and supported migration codecs into `packages/engine/crates/mornlea_storage/`, including standalone entity families and corrupt/partial data. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_storage --test runtime_contract --locked`.

## 3. Kernel reuse and acceptance

- [ ] 3.1 Reconcile numerical inventory with `packages/engine/crates/mornlea_engine/`; test and expose safe Rust APIs for existing kernel functions without changing established ABI behavior or duplicating implementations. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_engine --locked` and the affected kernel oracle suites; record complete inventory coverage in `ledger.md`.
- [ ] 3.2 Port inventory-listed Go-only numerical kernels, including `packages/shared/pathfind/`, into `packages/engine/crates/mornlea_engine/`, one bounded algorithm per red/green task and commit. Before each port, freeze deterministic tie-breaking, budgets, cancellation, overflow and unreachable-result cases in a non-empty `numerical_migration` integration target; extend tasks with exact additional algorithms discovered by the inventory. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_engine --test numerical_migration --locked -- --list` and `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_engine --test numerical_migration --locked`. Offline Go comparison uses the same identified corpus; missing algorithms block F1 rather than falling back online.
- [ ] 3.3 Add and run a non-empty `differential_replay` target under `mornlea_domain` with test-only protocol/storage/kernel dependencies to compare normalized offline oracle fixtures; reject empty/missing families and source identity mismatch. Validate with `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_domain --test differential_replay --locked -- --list` followed by `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_domain --test differential_replay --locked`. Verify no production dependency cycle or online writer is introduced.

## 4. Closeout

- [ ] 4.1 Reconcile the full capability inventory and prerequisite evidence in this change's `ledger.md`; leave completion blocked for missing supported cases. Review `packages/engine/AGENTS.md` and new crate guides, and promote only verified cross-task rules into both architecture skills. Validate with `go test ./packages/audit -count=1` and the full named suites above; record discovered and executed test counts.
- [ ] 4.2 Run formatting and complete implementation stage gates: `rustup run 1.97.1 cargo fmt --manifest-path packages/engine/Cargo.toml --all --check`, `make rust-check`, `make dev-check`, `make test-race`, and `openspec validate --all --strict --no-interactive`. Record failures as blockers, not exclusions; retain the rollback result and fixture/report identities in `ledger.md` before syncing specs or archiving.
