## Execution And Review Protocol

以下四组仅镜像批准实施计划的 Tasks 2–5；admission、OpenSpec sync/archive、长期文档同步、backlog/Discussion、PR、CI 与 worktree cleanup 不属于本 active checklist。每组 MUST 由 fresh implementer 按 red-green-refactor 完成，并分别接受独立 SPEC 与 QUALITY 评审；命令、exit、摘要、SHA、findings、修复轮次和 `Ruling: 决定 — 理由 — 原问题` MUST 写入 `ledger.md` 后才可勾选。

## 2. 物品、配方、持久兼容与耐久 seam

目标文件：`internal/core/combat.go`、`internal/core/combat_test.go`、`internal/core/item.go`、`internal/core/item_test.go`、`internal/core/bed_test.go`、`internal/core/recipe.go`、`internal/core/recipe_test.go`、`internal/core/recipe_shape_internal_test.go`、`internal/storage/player_codec_test.go`、`internal/storage/chunk_chest_test.go`、`internal/storage/chunk_drop_test.go`、`internal/storage/companion_codec_test.go`、`internal/sim/mining.go`、`internal/sim/mining_test.go` 及本 change 的 tasks/ledger。

- [ ] 2.1 在 `internal/core/combat_test.go` 和 item/bed tests 写 RED：锁定 `CombatTargetPlayer=1`、`CombatTargetHostile=2`、0/3 非法；六个 ItemID `47..52`、`ItemIDMax=53`、stack limit 1、完好剑耐久 59/131/250、broken mapping、damage 4/5/6、其它物品和损坏剑 damage 2，并保持 `ItemBed=46`。运行 `go test ./internal/core -run 'Test(CombatTargetKind|Sword|ItemID)' -count=1`，预期因 kind、常量与 helpers 尚不存在而失败。
- [ ] 2.2 在 `internal/core/combat.go` 和 `item.go` 最小实现 append-only combat kind、`Valid()`、六个 sword ItemID、现有 item switches、`IsIntactSword(ItemID) bool` 与 `WeaponDamage(ItemID) int32`；不得增加 registry struct、配置、显示名表或公共 `DamageTool`。
- [ ] 2.3 格式化 core 改动并运行 `go test ./internal/core -run 'Test(CombatTargetKind|Sword|ItemID|Durability|Broken)' -count=1`，结果 MUST 通过。
- [ ] 2.4 在 `internal/core/recipe_test.go` 与 `recipe_shape_internal_test.go` 写三条 recipe RED：ID 17/18/19，纵向 `material, material, stick`，3×3 三列平移，满耐久产物；横放、倒放、错料、多料拒绝；未知 ID 从 `RecipeIronSword+1` 开始。运行 `go test ./internal/core -run 'Test.*SwordRecipe' -count=1`，预期因 recipe 尚未登记而失败。
- [ ] 2.5 在 `RecipeBed=16` 后追加 `RecipeWoodenSword=17`、`RecipeStoneSword=18`、`RecipeIronSword=19`；每条固定 `Width:1, Height:3, Mirror:true`，材料为橡木木板/圆石/铁锭加木棍，产物耐久 59/131/250；matcher 上界延伸到 `RecipeIronSword`，固定计数改为 19，不增加 `RecipeIDMax`。
- [ ] 2.6 在 storage tests 新增 `TestPlayerCodecCurrentSchemaRoundTripsSwordItems`、`TestChunkCodecRoundTripsSwordItemsInChests`、`TestChunkCodecRoundTripsSwordItemDrops`、`TestCompanionCodecCurrentSchemaRoundTripsSwordItems`，覆盖六个 ID、部分磨损完好剑、耐久 0 损坏形态和 `core.ItemIDMax` invalid case；不改 shared binary/golden fixtures，不升级 schema。运行 `go test ./internal/storage -run 'Test.*SwordItems' -count=1`，若失败只修通用 ItemID 校验。
- [ ] 2.7 在 `internal/sim/mining_test.go` 写 `TestMiningIntactSwordsDoNotConsumeDurability` RED：三把部分磨损完好剑分别成功采掘 dirt，方块移除但完整 stack 不变、无耐久导致的额外 inventory dirty；保持镐、锄、作物×锄头和伙伴采掘断言。运行 `go test ./internal/sim -run 'TestMiningIntactSwordsDoNotConsumeDurability' -count=1`，预期当前通用耐久路径会失败。
- [ ] 2.8 在 `internal/sim/mining.go` 提取 sim-private `consumeToolDurabilityAt(*actorState, uint8, core.ItemID) bool`，只接受合法 hotbar slot、相同 item、Count 1 和有耐久上限的 stack；`N>1` 恰减一，`N<=1` 原子转 broken/Count 1。既有 wrapper 先排除 `core.IsIntactSword`，其余调用新 helper；Task 3 只能在 accepted player hit 的冻结身份重验后调用。
- [ ] 2.9 运行 Task 2 focused gates 并取得 fresh SPEC/QUALITY 评审：`gofmt -w internal/core/combat.go internal/core/combat_test.go internal/core/item.go internal/core/item_test.go internal/core/bed_test.go internal/core/recipe.go internal/core/recipe_test.go internal/core/recipe_shape_internal_test.go internal/storage/player_codec_test.go internal/storage/chunk_chest_test.go internal/storage/chunk_drop_test.go internal/storage/companion_codec_test.go internal/sim/mining.go internal/sim/mining_test.go`；`go test ./internal/core ./internal/storage ./internal/sim -race -count=1`；`go test ./internal/archcheck -count=1`；`openspec validate --all --strict --no-interactive`；`git diff --check`。全部通过并记录 ledger 后提交 `feat: register tiered swords`。

## 3. 统一冻结、仲裁与战斗结算

目标文件：`internal/sim/combat.go`、删除 `internal/sim/hostile_melee.go`、`player.go`、`hostile.go`、`hostile_action.go`、`command.go`、`engine_step.go`、`hunger.go`、`mining.go`，combat/capacity/resolution/weapon/knockback/exhaustion/hostile/lifecycle/mining tests，`internal/server/hostile_manager.go` 与 tests，以及本 change 的 tasks/ledger。

- [ ] 3.1 在 `internal/sim/combat_capacity_test.go` 等写 RED：72 actor/72 intent 成功；用双 actor/双 intent 夹具和 `actorLimit=1`、`intentLimit=1` 证明下一次 append overflow；overflow 时 health、velocity、四类 cooldown、inventory、fatigue、`TickResult.CombatHits` 不变，唯一允许 active player `meleeSuppressedMining=false`；锁定玩家第 1/11 tick 与 hostile 20 tick。运行 `go test ./internal/sim -run 'TestCombat(Snapshot|Intent|PlayerCooldown|HostileCooldown|Cooldowns)' -count=1`，预期失败。
- [ ] 3.2 把 player `meleeCooldownTicks` 拆成不持久的 `attackCooldownTicks`、`hurtCooldownTicks` 并在 reset 清零；在 `command.go` 新增领域 `CombatHit{Session SessionID, Damage uint8, TargetKind core.CombatTargetKind}`，把 `TickResult.CombatHits []CombatHit` 放在 `Craftings` 后、`Tick` 前；领域事实不得包含 tick、target ID、位置或武器。
- [ ] 3.3 在 `internal/sim/combat.go` 建立 `maxCombatActors=72`、`maxCombatIntents=72`、private `combatActor`/snapshot/intent tagged values、生产 `advanceCombat` 与测试 seam `advanceCombatWithLimits(result, actorLimit, intentLimit) bool`；生产 limit 固定 72/72，不增加配置项或 interface。
- [ ] 3.4 在 `combat_test.go` 写 mixed-target RED：`(ray distance, TargetKind, stable ID)` 全序、player 等距优先 hostile、同 kind 最小 ID、非零 pitch、固体严格在目标表面前阻挡、表面等距不阻挡、流体穿透、最近受保护目标不穿透，以及冻结后改变位置/yaw/pitch/health/栏位不改写已冻结结果。运行 `go test ./internal/sim -run 'TestPlayerCombat(Target|Protected|Frozen|Ray)' -count=1`，预期当前 player-only producer 失败。
- [ ] 3.5 在 `combat_resolution_test.go` 写 hostile-first reservation、同 kind 最小 ID、loser 零副作用、A→B/B→A 和 player↔hostile 不同 victim 互杀 RED。运行 `go test ./internal/sim -run 'TestCombat(Reservation|Mutual|Loser)' -count=1`，预期缺少全局 reservation 而失败。
- [ ] 3.6 在 weapon/knockback/exhaustion/mining tests 写 RED：empty/ordinary/broken=2、wood=4、stone=5、iron=6、最后一点转损坏、player 攻击 hostile；miss、遮挡、超距、cooldown、目标保护、reservation loser、frozen slot mismatch 均不耗耐久、不收 fatigue、不抑制采掘、不发 hit；XZ 单位方向乘 0.35、保留 Y，水平重合使用 attacker yaw 且有限。运行 `go test ./internal/sim -run 'Test(PlayerCombatWeapon|CombatReservationLoser|Combat.*Mining|Combat.*Exhaustion|CombatKnockback)' -count=1`，预期失败。
- [ ] 3.7 严格按事务顺序实现 `advanceCombatWithLimits`：清 `meleeSuppressedMining`；构造 snapshot，overflow 返回 false；只在 copies 中递减 cooldown；先 hostile ID 后 player SessionID 追加 intent，overflow 返回 false；提交递减；固定数组 hostile-first reservation；稳定 settlement。player 只用冻结 snapshot 做 3 格选择并在选择后检查 hurt cooldown；hostile 只解析 manager target 并重验 active/alive/same-dimension/≤1.8。删除平行 `hostile_melee.go`，不用 map 或额外排序。
- [ ] 3.8 accepted intent 在任何 live 写前完整解析 attacker、target、维度和冻结栏位；提交顺序固定为 damage → 0.35 additive horizontal knockback → 来源相关 cooldown → player 100 milli exhaustion/采掘抑制 → frozen intact sword 耐久 → `sim.CombatHit`。player attack/hurt 为 10，hostile attack/player hurt 为 20；hostile 增加正伤害钳零私有入口；最后一点剑伤害先成立再损坏。运行 `go test ./internal/sim -run 'Test(CombatReservation|CombatMutual|CombatLoser|PlayerCombatWeapon|Combat.*Mining|Combat.*Exhaustion|CombatKnockback)' -count=1`，结果 MUST 通过。
- [ ] 3.9 把 hostile 生命周期固定为 spawn/action/movement → unified combat → burn → hostile deaths/drop → distant despawn → player deaths；新增 health=1、burn 到期、`DistantTicks=599` 同 tick 测试，证明先掉腐肉再移除。`hostile_manager.go` 删除 `AttackCooldown==0` 前置过滤，范围内每 tick enqueue，sim 递减后唯一准入；`AttackCooldown=1` 同 tick 命中。同步旧函数注释为 `advanceCombat`。
- [ ] 3.10 验证 `TickResult.CombatHits` 按 player `SessionID` 升序、每 session 每 tick 至多一条，hostile attack 不产生 hit。运行 `go test ./internal/sim -run 'TestCombatHits' -count=1` 与 `go test ./internal/server -run 'TestHostileChase' -count=1`，结果 MUST 通过。
- [ ] 3.11 运行 Task 3 focused gates 并取得 fresh SPEC/QUALITY 评审：格式化本组全部 Go 文件；`go test ./internal/sim ./internal/server -race -count=1`；`go test ./internal/archcheck -count=1`；`openspec validate --all --strict --no-interactive`；`! rg -n 'advancePlayerMelee|advanceHostileMelee|damageHostileTarget' internal/sim --glob '*.go' --glob '!*_test.go'`；`git diff --check`。全部通过并记录 ledger 后提交 `feat: unify authoritative melee combat`。

## 4. CombatHit 协议、私有发布与 Memory/TCP parity

目标文件：新增 `internal/network/message_combat.go` 与 tests，修改 network packet/registry/codec/golden/fuzz/现有边界 tests、`internal/network/tcp/transport_consistency_test.go`、`internal/server/publication.go`、新增 publication/parity tests、协议 pin tests、根 `AGENTS.md`，以及本 change 的 tasks/ledger。

- [ ] 4.1 在 `message_combat_test.go` 写 wire RED：protocol 32、S→C ID 25、ID 26 unknown、10-byte round trip；tick `0x0102030405060708`/damage 6/hostile kind 2 的 payload 必须为 `08070605040302010602`；拒绝 tick 0、damage 0/21、kind 0/3、所有截断、任意尾随和 wrong state。运行 `go test ./internal/network -run 'TestCombatHit' -count=1`，预期因消息不存在而失败。
- [ ] 4.2 新增固定 10-byte `network.CombatHit{ServerTick uint64, Damage uint8, TargetKind core.CombatTargetKind}` 与 `Validate`；encode 固定 `u64/u8/u8`，decode 在任何分配前要求 payload 恰好 10 bytes，值域为 tick>0、damage `1..core.MaxHealth`、kind.Valid()。
- [ ] 4.3 把 `ProtocolVersion` 升为 32，只追加 S→C ID 25 和对应 validation/control codec；既有 IDs、packet 形状、C→S registry 不动。更新协议/handshake golden `1f→20`、`1f01026e6f→2001026e6f`，unknown boundary 25→26，fuzz seeds 加合法 CombatHit 和 current/previous hello。
- [ ] 4.4 格式化 network 改动并运行 `go test ./internal/network/... -run 'Test(CombatHit|ProtocolVersionPinned|ServerPacket|CodecGolden|Transport)' -count=1` 与 `go test ./internal/network -run '^$' -fuzz '^FuzzSmallPacketCodec$' -fuzztime=5s`；结果 MUST 通过且无 crash/acceptance drift。
- [ ] 4.5 在 `internal/server/combat_hit_publication_test.go` 写 publication RED：攻击者接收 inventory/container mirror → `PlayerState` → `CombatHit`；wire tick=`result.Tick`；victim、旁观者、trusted observer 不收；只够 `PlayerState` 的慢 session 在追加 hit 时按既有策略断开，健康 session 仍收到自己的 hit。运行 `go test ./internal/server -run 'TestCombatHitPublication' -count=1`，预期 publication 尚未投影而失败。
- [ ] 4.6 在 `publishLocalResult` 保持 inventory/crafting/furnace/chest 顺序，只在当前 session 的 `PlayerState` enqueue 成功后扫描 sorted `result.CombatHits` 并私发匹配 session 的 `network.CombatHit{ServerTick:result.Tick,...}`；`PlayerState` 或 hit enqueue 失败立即走既有 close 策略。不得增加重试、dedupe map、战斗专用队列或 trusted observer 特例。
- [ ] 4.7 扩展 player→player 与 player→hostile parity：player melee 使用 durability=2 iron sword，从目标 `PlayerState` 读取 health/velocity、攻击者 inventory mirror 读取 sword stack、攻击者 wire 读取 player-kind hit，以相邻 hit tick 10-tick 间隔验证节奏；sword combat 比较 hostile health/velocity、剑耐久/损坏、hostile-kind hit。Memory/TCP transcript 去掉绝对 tick origin 后相等，各 transport tick 严格递增，不使用缺 durability 的 `PlayerHash`。先 `make rust`，再运行 `go test ./internal/server -run 'Test.*(MeleeParity|SwordCombatParity)' -race -count=1` 与 `go test ./internal/network/tcp -run 'Test.*TransportConsistency' -race -count=1`。
- [ ] 4.8 把根 `AGENTS.md` 协议基线改为 v32；README、兼容性、gameplay、progress 留给 Task 6。运行本组全部 gofmt；`go test ./internal/network/... ./internal/sim ./internal/server ./cmd/mornlea-server ./cmd/mornlea/app -race -count=1`；`go test ./internal/archcheck -count=1`；`openspec validate --all --strict --no-interactive`；`git diff --check`。取得 fresh SPEC/QUALITY 评审并记录 ledger 后提交 `feat: publish confirmed combat hits`。

## 5. 客户端确认反馈、HUD、音频与 capture

目标文件：`internal/audio/cue.go` 与 tests、`internal/render/drop.go` 与 tests、HUD atlas/UV/marker/renderer/layout tests，新增 `cmd/mornlea/app/combat_feedback.go` 与 tests并修改 app message/lifecycle/frame/accessor/protocol/render 文件和 `AGENTS.md`，修改 capture 场景/接口/hostile order并新增 sword scene/test/golden、更新 capture `AGENTS.md`，以及本 change 的 tasks/ledger。

- [ ] 5.1 在 `cmd/mornlea/app/combat_feedback_test.go` 写 RED：tick 1 接受并 arm 6，tick 1/0 忽略，tick 2 重置 6；`AfterRender(false)` 不减，六次 true 后不可见；`Reset` 清 tick 和帧；input/health/inventory 不调用状态机。运行 `go test ./cmd/mornlea/app -run 'TestCombatFeedback' -count=1`，预期状态机不存在而失败。
- [ ] 5.2 新增 Darwin-tagged `combatFeedback{lastServerTick uint64, remainingFrames uint8}`，固定 `combatMarkerFrameCount=6`，实现严格递增 `Observe`、`ArmMarker`、`MarkerVisible`、只在 rendered true 减帧的 `AfterRender` 与全清 `Reset`；值直接属于 `Application`，不复用 `a.serverTick`，不增加 animation manager。
- [ ] 5.3 写 app message/lifecycle RED：只有严格新 `network.CombatHit` 播放一次 cue 并 arm；同 tick `PlayerState{Reset:true}` 先清旧状态后续 hit 仍接受；disconnect、菜单、新 session、authoritative reset 全清；共享 session reset 同时清 hostile mirror。运行 `go test ./cmd/mornlea/app -run 'TestApplicationCombat|Test.*Session.*Reset' -count=1`，预期失败。
- [ ] 5.4 在 `DrainServerMessages` 的 `PlayerState` 分支后消费 `network.CombatHit`，仅 `Observe` 返回 true 时 `playLocalCue(audio.CueCombatHit)`；`PlayerState.Reset` 清 audio/combat，`resetSessionOwnedState` 清 combat 并在 non-nil 时 reset hostiles；不得新增重复 lifecycle 调用。
- [ ] 5.5 在 audio test 先锁定 append-only cue、1323 samples、520→180 Hz、amplitude 10500 和 PCM SHA-256 `17752cdda0232ebb88b0e6db1e39fa4a4889e5469bac0c28a07044b677710dae`，再在 `CueWaterSplash` 后追加固定参数并复用 `[cueCount][]int16` 与现有队列。运行 `go test ./internal/audio -run '^TestCueCombatHitPCM$' -count=1`，结果 MUST 通过。
- [ ] 5.6 在 HUD tests 写 marker RED：无 inventory 确认也产生 4 个白色不透明 untextured quad；中心 `(width/2,height/2)`，上下 `2×8`、左右 `8×2`，内缘 `4*hudScale`；marker-on 最大关闭/打开 100/261、`<=267`；warmed `Prepare` 零分配且实例在 framebuffer 内。运行 `go test ./internal/render/hud -run 'Test.*(CombatMarker|FixedCapacity|ReusesLayout|Responsive)' -count=1`，预期缺少 marker 输入而失败。
- [ ] 5.7 给 `HotbarRenderer.Prepare` 在 chat 与尺寸参数之间追加 `combatMarker bool`，既有调用传 false；新增 `appendCombatMarker` 在 health/oxygen/hunger/chat 后只追加四个 quad，公式严格使用 `(4+8/2)*scale`。不得改 `maxHotbarQuads`、glyph offset、upload bytes、atlas、shader 或 pipeline。
- [ ] 5.8 在 `app_frame.go` 把 marker 纳入 `hudVisible` 并传 `Prepare`；只在现有 native render 结果后调用 `AfterRender(rendered)`。零 framebuffer、entity overflow、name-tag/HUD prepare error 在 native render 前返回，不扣帧；扩展 frame tests 锁定四个失败边界。
- [ ] 5.9 在 `render.ItemColor` 固定 switch 增加六个原创、alpha=1、两两可区分且 intact/broken 不同的 sword colors；测试 nonzero、互异、drop 可见。HUD atlas 宽由 `ItemIDMax` 自动 960→1056，不改生产计算；旧 800 说明改为动态宽，UV stability probes 为 `hotbarTextureWidth`、`+16`、`+32`、2048、4096。运行 `go test ./internal/render ./internal/render/hud -run 'TestSword|Test.*Atlas' -count=1`。
- [ ] 5.10 在 capture tests 写 `sword-combat` RED：selected iron sword `Durability:125`；合法 UUIDv4 远端玩家经 spawn/state 沿受击方向移动 0.35；固定 camera/time；marker visible；`PinVolatile` 收敛后 re-arm。顺序严格 `ai-companion,sword-combat,hostile-mob,water-surface-slope`，总数 24，far-horizon 倒数第二，water-underwater 末项；更新 hostile 原“紧随 ai”断言。运行 `go test ./cmd/mornlea/capture -run 'Test(SwordCombatCaptureState|CaptureSceneOrderAndAICompanionDeterminism)' -count=1`，预期场景/接口缺失而失败。
- [ ] 5.11 只向 app/`SceneApplication` 增加真实消费方法 `ArmCombatMarker`、`ResetCombatFeedback`、`CombatMarkerVisible`；`resetCapturePresentation` 清 combat；`sword-combat` 复用现有开阔草地/air neighborhood helper，`PinVolatile` re-arm。同步 app/capture `AGENTS.md` 的文件地图、最小接口与 reset 责任，不复制会漂移的完整场景清单。
- [ ] 5.12 先 `make rust`，再运行 `make visual-check VISUAL_OUT=build/visual-sword-combat`，预期 baseline 前只因缺 `sword-combat.png` 失败并生成候选；人工确认非满耐久铁剑、远端玩家、marker 与击退关系。批准后运行 `make visual-update VISUAL_OUT=build/visual-sword-combat-update`、`git status --short cmd/mornlea/capture/testdata/golden`、`make visual-check VISUAL_OUT=build/visual-sword-combat-check`；tracked golden MUST 只新增 `sword-combat.png`，任何其它 PNG 变化必须逐图归因并明确批准，绝不放宽阈值。
- [ ] 5.13 运行本组全部 gofmt；`go test ./internal/audio ./internal/render ./internal/render/hud ./cmd/mornlea/app ./cmd/mornlea/capture -race -count=1`；`go test ./internal/archcheck -count=1`；`make visual-check`；`openspec validate --all --strict --no-interactive`；`git diff --check`。结果 MUST 全部通过且 capture 24/24；取得 fresh SPEC/QUALITY 评审并记录 ledger 后提交 `feat: show confirmed combat feedback`。
