## 执行记录

| Task | Implementer | 规格评审 | 质量评审 | 验证 | 裁决 |
| --- | --- | --- | --- | --- | --- |
| 1.1 | fresh implementer | 初审发现参数上界与首批保留证明缺失；修订设计后 re-review 通过 | 初审请求修改；re-review 通过 | `make rust` 通过；Rust 与 Go 定点均按预期 red | ACCEPTED：测试先行，待 ABI 实现使其转绿 |
| 1.2 | fresh implementer | 初审发现 egui callback buffers 丢弃；一轮修复后发现提交顺序相反；第二轮 re-review 通过 | 初审请求修改；两次 scoped re-review 通过 | Rust 176 tests、`make rust`、`go test ./internal/client -race -count=1` 通过 | ACCEPTED：prepared state 保留 callbacks，单次 submit 按 callbacks→main 顺序 |
| 2.1 | fresh implementer | 一次评审通过 | APPROVED | `make rust`、GPU completion focused race、client bridge focused race 通过 | ACCEPTED：Go prepare 在计时前，submit-and-wait 在两个读钟之间 |
| 2.2 | fresh implementer | 一次评审通过 | APPROVED | benchmark short、GPU completion focused race 通过 | ACCEPTED：报告字段断言不再驱动真实 GPU |
| 3.1 | fresh implementer | 初审纠正了自检误标；独立 re-review PASS | 初审请求修改；独立 re-review APPROVED | `go test ./internal/archcheck -count=1` 本轮通过（8.689s）；`openspec validate fix-gpu-benchmark-batch --strict --no-interactive` 本轮 valid；此前 `git diff --check` 通过 | ACCEPTED：controller 已记录独立双评审后勾选 |
| 3.2（范围修订前） | final verification implementer | 已复核 `bounded-benchmark-workload` 的单 command buffer、单次 submit 和单次 completion wait 契约 | `make rust-check` 通过，先前 19 个 `clippy::needless_borrow` 错误阻塞已修复 | 验证员曾记录全量 gate 通过；controller 独立重跑 `go test ./... -race -count=1` 时，`TestMornleaServerProcessReleasesWorldLockAfterSIGTERM` 失败。20 次定点复现 8 次失败，诊断为 `创建服务端 Host: load hostiles: context canceled` | SUPERSEDED：用户批准将已确认的启动取消竞态作为门禁阻断修复纳入 change，最终验证重新执行 |
| 3.2 | fresh implementer | 初审发现两个条件交叉边界及 cleanup 返回排除 cancellation 的测试缺失；一轮补测后 scoped re-review 通过 | APPROVED | focused race、`go test ./cmd/mornlea-server -race -count=1` 与 20 次 SIGTERM subprocess race 均通过；mutation 检查证明新增断言可捕获 `&&`→`||` 和携带构造 cancellation 的退化 | ACCEPTED：仅在 caller 已取消且 `NewHost` 错误匹配 `context.Canceled` 时正常退出，listener/store cleanup 错误保持可观察 |
| 3.3（范围修订前） | final verification implementer | ABI code/契约已复核 | Rust 与格式门禁通过 | 独立 release target 证明两个 v11 export 存在；默认共享 target 的 `make rust` 复制了缺失 export 的旧 dylib，`go test ./... -race -count=1` 因 client 链接失败 | SUPERSEDED：用户批准将 worktree-local Cargo target 作为门禁阻断修复纳入 change |
| 3.3 | fresh implementer | 初审通过 | APPROVED | red：默认 `make rust` 后 `nm` 未找到两项 export；green：默认及显式 `CARGO_TARGET_DIR` 覆盖下均导出两项符号，`go test ./internal/client -race -count=1` 通过 | ACCEPTED：默认 Cargo target 仅属于当前 worktree，cgo release copy 路径和环境覆盖保持不变 |
| 3.3 文档同步 | fresh implementer | 初审纠正 direct Cargo 不是 Makefile 默认目标的表述；一轮 re-review 通过 | APPROVED | `gofmt -l .`、`git diff --check` 与当前指南检索通过 | ACCEPTED：`engine/AGENTS.md` 和 `docs/notes/test-quickstart.md` 与 Makefile target 边界一致 |
| 3.4 | final verification implementer 后由 controller 独立复核 | 已复核 `bounded-benchmark-workload` 的单 command buffer、单次 submit 和单次 completion wait 契约 | Rust 及 Go 全量门禁通过 | `make rust`、`make rust-check`、`gofmt -l .`（空输出）、`go vet ./...`、`go test ./... -race -count=1`、`openspec validate --all --strict --no-interactive`（77 passed, 0 failed）与 `git diff --check` 均本轮通过 | ACCEPTED：controller 以足够超时重新执行全部命令；此前 worker 的 120 秒超时不作为通过依据 |
| 3.3 final gate | final verification implementer | `openspec validate --all --strict --no-interactive` 通过（77 passed, 0 failed），包括 `bounded-benchmark-workload` 与本 change | `make rust-check` 通过；`gofmt -l .` 无输出；`go vet ./...` 通过；`git diff --check` 通过 | `make rust` 通过（3.83s）；`make rust-check` 通过（8.66s）；`gofmt -l .` 通过（0.27s）；`go vet ./...` 通过（0.85s）；`go test ./... -race -count=1` 失败（61.37s）：链接的 `libmornlea_client.dylib` 缺少 `mornlea_client_render_prepare_benchmark_batch` 与 `mornlea_client_render_submit_benchmark_batch`；OpenSpec 严格校验通过（3.46s）；`git diff --check` 通过（0.04s）。task 3.2 已单独 20 次通过 repeated server SIGTERM 测试，未替代任何本行 final gate。 | BLOCKED：全量 race gate 失败，3.3 保持未勾选 |
| 3.4 final verification | fresh final verification implementer | 已复核 `bounded-benchmark-workload`：固定批次单 command buffer、单次 submit/completion wait、仅计 submit-to-completion、固定批次数摊薄且保持无头；proposal/design 与此一致 | `make rust`、`gofmt -l .`、`go vet ./...`、全量 Go race、OpenSpec 严格校验和 `git diff --check` 通过；`make rust-check` 在 `cargo test --workspace --locked` 编译 `mornlea_client` 时被 120 秒执行上限终止，未取得完整通过结果 | `make rust` 通过（release profile 0.42s）；`make rust-check` 失败：`make: *** [rust-check] Terminated: 15`；`gofmt -l .` 无输出；`go vet ./...` 无输出；`go test ./... -race -count=1` 通过（含 `cmd/mornlea/benchmark` 15.767s、`internal/server` 303.808s）；`openspec validate --all --strict --no-interactive` 通过（77 passed, 0 failed）；`git diff --check` 无输出 | BLOCKED：`make rust-check` 未完成，task 3.4 保持未勾选（历史记录，controller 已独立以足够超时重跑并通过，见下一行） |
| 3.5 | fresh implementer | 最终审查 P1/P2 已在既有实现中满足：prepared batch 期间全部资源变更拒绝并经 readback 证明首批保留，crate 文档已为 v11 | APPROVED | `cd engine && cargo test -p mornlea_client --locked`（176 passed，含 `prepared_benchmark_batch_rejects_mutations_and_preserves_first_frame`）、`make rust`、`go test ./internal/client -race -count=1`、`openspec validate --all --strict --no-interactive`（77 passed, 0 failed）与 `git diff --check` 通过 | ACCEPTED：冻结语义与 ABI 文档已与评审批注一致；格式化后 `make rust-check`（fmt/clippy/workspace tests）通过 |
| 3.4/3.5 final gate (controller) | controller | 已复核 `bounded-benchmark-workload` 全契约与本 change proposal/design | Rust 与 Go 全量门禁通过 | `make rust`、`make rust-check`、`gofmt -l .`（空输出）、`go vet ./...`、`go test ./... -race -count=1`（含 `internal/server` 297.158s 全量通过）、`openspec validate --all --strict --no-interactive`（77 passed, 0 failed）、`git diff --check` 均本轮通过 | ACCEPTED：以足够超时独立重跑全部七项门禁；此前历史 BLOCKED 仅作过程记录 |
