# Held tools refinement implementation plan

Spec: specs/first-person-viewmodel/spec.md and design.md in this change. Baseline 442076ca. Follow subagent-driven-development, read AGENTS ancestors. This is a continuation; original implementation-plan.md records the previous iteration.

### Task 1: 立体工具与 HUD 安全构图
Work only in /Users/chen/work/mornlea/.worktrees/first-person-held-items. Read change design/spec, local AGENTS. No subagents. Do not edit planning or unrelated files. Controller handles real foreground inspection separately.
- First write meaningful failing tests then implement solid category models for all registered swords, picks, hoes and broken variants. Independent blade/body, handle, socket/guard, readable side thickness and voxel bevel/edge differentiation. Avoid flat uniformly extruded icon tools. Reuse per-category geometry, preserve material palette from current registry/texture overrides and broken differences. Other items and six-face blocks retain current rendering.
- Adapt rig based on actual logical viewport and FOV, wired through app and capture. Match existing HUD geometry (frontend hud/geometry.ts, hud.css, tokens.css): at640×360 hotbar x82..558,y300..360, status to106px abovebottom, scale=min((W-16)/476,(H-16)/160,1). Account selectedslot9 raise/outline. Test16:9,4:3,640×360 and supported FOV endpoints with full sweep; no HUD overlap, connected grip, readable head and offscreen arm root. May exit right edge. Avoid separate unexplained duplicated frontend constants; reuse/share or paritytest as fits architecture.
- Keep ≤257 instances,96byte ABI18 unchanged, preheated zero allocation. Hide left hand. Confirmed selection only. No server/gameplay changes. Chinese comments. Do not open windows or update goldens.
- Run focused assets/render/app/capture tests, applicable formatting, git diff check. make rust baseline already passed. Update affected local documentation if API responsibility changes. Commit only taskcode/tests/docs with single-line English conventional message. Report exact API fields, red/green commands, commit, concerns to .superpowers/sdd/refinement-plan/task-1-report.md.

### Task 2: 有效点击即时完整挥动
Work only in same worktree. Read Task1 report and change design/spec and applicable AGENTS. No subagents. Controller handles native captures. Keep task1 model/HUD invariants, ≤257/96byte/ABI18 and zero allocations.
- First meaningful failing tests, then connect effective game primary risingedge/heldstate to local presentation. Immediate swing even air or no crack/hit, including empty/tool/block. Clear windup/forward-downstroke/recovery. Use explicit monotonic presentation elapsed (not server tick or framecount) for stable timing and deterministic replay. Held button repeats bounded cadence; release completes currentstroke. Late CombatHit must not duplicate. Authority damage/mining/selection/network unchanged.
- Suppress menu/inventory/chat and first cursor recapture click; sessionreset clears. Test actual app input wiring, rates (30/60/144fps equivalent times), latency/nohit/repeatedclick, longhold and suppression/reset. Keep existing offline capture demonstrations functional with explicit animation inputs and generate representative updated offscreen previews using production path.
- Adjust rig full new motion to preserve Task1 HUD/visibility constraints. Run focused client/render/app/capture tests and git diff check. Document new presentation inputs and update affected local guides. Commit taskonly code/test/docs. Full report to .superpowers/sdd/refinement-plan/task-2-report.md including red/green, precisefields and deterministic capture command.
