## 1. 主手与持物呈现
- [x] 1.1 在 assets 增加与材质刷新一致的有厚度图标缓存，先写 alpha、颜色、覆盖和预算失败测试，再实现；验证 `go test ./packages/client/assets -count=1`。
- [x] 1.2 在 render 与 app 隐藏空闲左手，连接缓存，构建共同握持根、工具分档握点与分面方块；先写失败测试，验证 `go test ./packages/client/render ./packages/client/cmd/mornlea/app -count=1`。
- [x] 1.3 同步 Go/Rust viewmodel 257 实例容量与超量错误测试，保留 ABI 布局；验证 `cargo test -p mornlea_client --locked viewmodel`，执行 `make rust` 并复验涉及 Go 包。
## 2. 视觉与收尾
- [x] 2.1 在 capture 增加无头持物演示，覆盖空手、代表分面方块、全部工具及损坏状态、非方块物品；完整挖掘与攻击动作包含中立、峰值及恢复，输出审查物而不自动更新基线；验证 capture 定点测试与实际 motion demo。
- [x] 2.2 完成逐图目检、16:9/4:3 投影、零分配与预算回归；记录 ledger 并完成任务审查、整分支终审。
- [x] 2.3 收尾执行 gofmt、`make test-race`、`make dev-check`（含六模块 vet 及 `make rust-check` 等价的 Rust fmt/clippy/test） 和 `openspec validate --all --strict --no-interactive`；记录证据，不推送或合并。

## 3. 用户反馈：立体结构、HUD 与即时挥动
- [x] 3.1 工具改为分件立体模型，当前材质和损坏差异保持；先失败测试再实现并定点验证。
- [x] 3.2 适配逻辑窗口、FOV 与完整 HUD 安全区，验证窄屏、常用窗口及完整动作。
- [x] 3.3 接入有效本地主键动作，空挥即时、时间稳定、界面门控和会话重置正确，权威行为不变。
- [ ] 3.4 用户授权的隔离交互窗口同时展示真实 HUD、主手与持物，截图/录制空手和工具点击，完成实际目检与必要修整。
- [x] 3.5 任务审查、整分支终审、相称测试与收尾门禁，记录新证据；不推送或合并。

## 4. 已审核效果图落地
- [x] 4.1 在隔离分支同步当前主线，保留护甲、输入及物品新增行为，执行 make rust 和受影响包定点验证，审查冲突接线。
- [x] 4.2 先写失败测试，再按审核图实现分件拳掌、短斜前臂、立体纹理工具和 HUD 间距；覆盖全注册物品、最坏合法覆盖、完整动作、常用/窄窗口及护甲氧气行。
- [ ] 4.3 生成真实完整 HUD 截图和空手/工具挥动录制，逐图对照审核图并修整；不更新既有基线。
- [x] 4.4 完成任务与整分支审查，gofmt、make dev-check、make test-race、OpenSpec 严格校验并记录实机证据，不推送合并。

- [x] 4.5 按审核图纠正工具右倾、拳掌轮廓、深棕握柄、镐尖和锄刃；修复独立审查发现的剑尖帽头，最终代码为 `30c0396a`，整分支代码审查通过。

当前验收边界：实机静态 HUD 和模型动作图已经生成；真实鼠标点击/按住/释放及界面切换的完整实机动作录制尚未确认，3.4 与 4.3 保持未完成。自动化投影和点击状态测试不能替代这项人工交互验收。最新门禁结果见 `validation.md`，不将旧轮次通过记录当作当前结果。

用户最新范围：暂不需要适配小屏幕。停止后续小屏调整与实机验收，不将其作为本轮完成条件。4.4表示审查及指定门禁已实际执行并记录；`make dev-check`仍有已在主线复现的服务端短测试失败，不表示全绿。


## 5. 严格校对后的桌面构图与动作修正
- [x] 5.1 按独立审计修正底边入画、短厚闭拳、剑镐锄头柄比例和明暗面；TDD及1280原图检查，验证assets/render定点测试并完成任务审查。
- [ ] 5.2 将原地小角摆动改为有前伸/斩击/下劈与连续回收的多轴动作；以真实相位测试完整轨迹、类别差异、HUD/握点/预算/零分配和输入门控，生成桌面连续动作证据并审查。
- [ ] 5.3 实机桌面静帧和点击录制核对，整分支审查、gofmt、make test-race、六模块vet、OpenSpec严格校验，记录未通过或未完成项；不推送合并。

- [ ] 5.4 根据用户实机反馈修正镐尖领先凿击，真实尖端方向TDD、30fps桌面连续证据、定点race与独立复审，完成实机确认。
