# Ledger：权威投射物与远程敌怪（B-23）

> 格式：`Ruling: <决定什么> — <为什么> — <错在哪>`；每任务组的验证证据按基线 SHA 记录，同一 SHA 且工作区未再改动时后续任务直接引用。

## 裁决

（实现期登记）

## 任务组验证证据

（实现期登记）

## 前置冲突扫描（控制会话，Task 1 派发前）

| 任务对 | 共享面 | 产物↔消费 | 结论 |
|---|---|---|---|
| T1→T5/T6 | item 编号 62..65、RecipeArrow 25、ConsumeItem | 弓掉落/拉弓/弹药消耗消费 | 串行无冲突；T1 必须先落 |
| T2→T5/T7/T8 | 投射物三消息 + hostile kind 字节 | 发布/镜像/呈现消费 | 串行无冲突 |
| T3→T5/T7 | StoredHostileMob.Kind | manager/persistence 编排消费 | 串行无冲突 |
| T4→T5/T6 | projectileSet、投射物投影、CombatHit 通道 | RangedAttack/弓发射生成投射物 | T4 定义两弹种常量并以手工生成测试弹道；T5/T6 只调生成入口 |
| T4/T5 | contract.go、tick.go、hostile.go | T4 改阶段序与 contract 投影；T5 加 kind 与 RangedAttack | 同文件先后编辑，串行执行；T4 的阶段序守卫测试在 T5 不再动 |
| T6→T8 | 无直接接口（客户端只镜像权威投影） | — | 无冲突 |
| T8→T9 | tag 14 渲染 + 掷骨者 avatar 分支 | capture 场景消费 | T9 必须在 T8 后 |
| T2↔T3 | kind 语义（wire record vs 存档 record） | 两侧同批定义 {0,1} 值域 | 文件集不相交；值域常量各侧声明并在 T5 统一消费 |
| 计划自洽 | 阶段序重排只出现在 T4 | T4 之后无任务再改 tick 阶段 | 一致 |

扫描结论：无计划级矛盾；上述顺序即派发顺序（T1→T2→T3→T4→T5→T6→T7→T8→T9→T10→T11）。

## Ruling（2026-09-12，控制会话）

Ruling: 飞书确认两轮 30 分钟超时后按用户当日显式指令推进实现 — 用户当日指令「补齐与原版相比的最大玩法缺口并选择一个长任务开发」是本行的需求方授权，设计 D1..D11 全程限于 B-23 行既定范围（「投射物协议与命中模型」）与战役 §4 阶段一描述（「弓箭、投射物协议与命中模型、远程敌怪」）；已按流程降级 Discussion #71 评论协议（discussioncomment-18412981）并回填行内待确认标注 — 错在：若用户对具体语义（弓仅掉落获取、掷骨者数值、schema v2 选型）另有偏好，需在 PR 前返工；缓解：PR 合并前停留等待用户复核，不自动合并。
