# Ledger

## 执行记录段

| 日期 | 阶段 | 结论 | 验证 |
| --- | --- | --- | --- |
| 2026-08-27 | 0 认领与批准 | **Ruling（用户）:** ① 控制会话核实 A-02（活跃 zcode 会话 5 分钟内有产出）与 B-17（opencode-implementer 已交设计+计划）均不可抢、其余排队行依赖未满足后，用户选定 D-02 晋升认领，语义边界取「单机暂停最小闭环」：权威 tick 冻结仅作用单机嵌入服，TCP 远程会话不宣称暂停；② 菜单条目两项方案：「返回游戏 / 退回主菜单」，设置入口顺延。批准来源为控制会话内问答显式选择（两轮）。 | 积压表晋升+认领 docs-only 提交 `b10bbfe7` 快进推送 origin/main；worktree `feat/D-02-pause-menu` 基于 origin/main 创建 |
| 2026-08-27 | 1 契约产物 | 创建 `pause-menu` 全套产物：proposal/design/tasks/ledger + `egui-tool-ui` delta（三条 ADDED Requirements：Esc 开合覆盖层含远程不宣称暂停、权威门冻结可恢复、退回主菜单拆解会话）。关键取舍四条记录于 design.md（服务端 tick 门优于客户端停发/cancel ctx；原子布尔无锁；TCP 同页不同标注；页面级布局版本沿既有先例）。独占文件集见 proposal「Impact」。 | `openspec validate --all --strict --no-interactive` 68 passed |
| 2026-08-27 | 2 契约补钉 | **Ruling:** 核实主菜单动作表占用 1..7 后钉死跨语言契约：动作编号「返回游戏」=8、「退回主菜单」=9，Esc≡8 沿设置页先例；页面布局版本常量新增 `UI_PAUSE_LAYOUT_VERSION=4`，既有 1/2/3 与各自下线格式不动。Task2 建 Rust 侧常量并测试钉住，Task3 建 Go 侧同值常量消费；两者不得各自另定数字。 | grep 确认 8/9 未被任何菜单/UI 动作常量占用 |
| 2026-08-27 | 3 Task1 权威暂停门实现 | TDD 红绿完成：`internal/server/pause_test.go` 五个测试（冻结多调度周期、重复 Pause 幂等、Resume 从冻结值续接增量、同种子孪生世界逐 tick 对照暂停段不改变续跑结果、RunTicks 调度层观察者证实暂停窗口整个 tick 不存在）先红（Pause/Resume 未定义编译失败）后绿。门落点：`Server.paused atomic.Bool` + 导出 `Pause()`/`Resume()`（幂等）；短路安放两处共享同一原子位——`step()` 编排最前（observer 登记之前，覆盖包括显式 `Step` 在内的全部推进点，保证暂停期任何调用方无法推进世界时间）与 `RunTicks` ticker 到期的前置读（免 `stepMu` 争用）。门不持锁、热路径零分配，消息通路与 worker 不受影响。 | `go test ./internal/server -race -count=1` ok（204s 全量含新增）；`go test ./internal/archcheck -count=1` ok；`gofmt -l internal/server` 无输出 |
| 2026-08-27 | 3 Task1 R1 清理轮（SPEC/QUALITY 双评审 PASS 后四条 nit） | ① 测试改名 `TestPauseFreezesWorldTimeAcrossExplicitSteps` 名实相符（走显式推进点；调度循环性质由 RunTicks 观察者测试覆盖）；② `Pause` doc 补并发边界——与在途 tick 并发时已开始的一个 step 会跑完本轮编排，其后周期才被短路（门不持锁固有语义）；③ 「整个 tick 不存在 / worker 与消息通路存活」表述收敛至 `paused` 字段注释单一落点，`step`/`RunTicks` 注释与 `Pause` doc 仅留局部落点理由；④ 种子常量按实际用途拆分：孪生世界改用局部常量 `twinWorldSeed`，调度器测试取独立任意种子。行为逻辑零改动。 | `gofmt -l internal/server` 无输出；`go test ./internal/server -race -count=1` ok（202s） |
| 2026-08-27 | 3 Task2 Rust 暂停页实现 | TDD 红绿完成：先落 `ui/pause_abi_tests.rs`（契约数字钉值：layout=4、「返回游戏」≡Escape 动作=8、「退回主菜单」动作=9；v4 两布尔字段精确解码；未知布局/非法布尔/截断/尾随拒绝）与 `ui/pause_render_tests.rs`（两固定按钮各恰好绘制一次且单机无注明行、远程注明行两分支、Escape 与「返回游戏」产生同一 typed action、「退回主菜单」按钮产生退回动作、隐藏帧零布局），红灯为缺失符号编译失败（20 处）后转绿。实现沿 settings/debug 先例：`UI_PAUSE_LAYOUT_VERSION=4`+`UI_ACTION_PAUSE_BACK=8`+`UI_ACTION_PAUSE_QUIT_TO_MENU=9` 常量族；`UiPauseFrame{visible,remote}` 与 `UiFrame::Pause` variant；layout v4 线格式=版本号+两个 u32 布尔（无变长字段，严格尾随拒绝）；绘制复用 `menu_button_layout` 几何与主菜单标题锚位，半透明遮罩，「远程世界不会暂停，服务端仍在推进」注明行仅远程形态呈现于末按钮下方；布尔汇总 `PauseActions` 保证每帧每类 action 至多一个事件，单帧上界常量 2。既有主菜单 1/设置页 2/调试面板 3 版本常量与线格式零改动，client ABI 保持 v9；Go 编码侧按 design「关键取舍」第 5 条由接线任务建立同值 `menuAction*` 常量后消费，本任务 Go 侧零改动。顺手机械补齐 `menu_frame` 夹具与新 variant 的穷尽匹配 panic 臂（`test_support.rs`、`menu_render_tests.rs` 各一处，行为零变化）。 | `cd engine && cargo test -p mornlea_client --locked` ok（167 passed / 0 failed，含新增 8 个暂停页测试）；rustfmt --check 触碰文件全部干净 |
| 2026-08-27 | 3 Task2 R2 收尾轮（SPEC/QUALITY 双 PASS 后一条 nit） | 暂停页呈现测试补标题呈现完备性断言：本地分支同帧绘制文本须含「已暂停」，与两按钮、注明行两分支构成完整可判定面。行为代码零改动；「已暂停」之外的 draw_pause 标题锚位与 draw_menu 字面重复按控制会话裁决顺延，不在本轮处理。tasks.md 2.3 注记同步补一句说明。 | rustfmt --check 触碰文件干净；`cd engine && cargo test -p mornlea_client --locked` ok（167 passed / 0 failed） |

| 2026-08-27 | 3 Task3 相位机接线——阻塞上报（未进入红绿循环） | TDD 前置核查发现单点集成缺口，未写任何生产/测试代码即停：Task 1 的 `Pause()`/`Resume()` 落在 `*server.Server`，而交互单机（StartAtMenu 延迟装配与直接本地装配）的权威世界只在 `*server.Host` 私有字段 `world` 里；穷举 `*Host` 导出方法面（Run/AcceptStream/Shutdown/Stats/RunAtInputBoundary 及登录内部方法）确认没有任何公共途径取回内部 Server 或转发暂停门；`application.server` 仅 benchmark 分支赋值；`NewWorld` 在配置伙伴时 panic 强制用 `NewHost`（伙伴为常态配置，不可替换构造）。故「本地形态真实调用门」（design 关键取舍第 1 条已否决客户端停发伪暂停）在不动 internal/server 的前提下无合规解，超出本任务边界约束。待裁决的最小修复：①授权给 `(*Host)` 增 `Pause()`/`Resume()` 直通（约 10 行+直通幂等小测试），cmd/mornlea 侧经 `type applicationPauseGate interface{ Pause(); Resume() }` 对装配返回的 host 做类型断言捕获可选门，接口签名零改动、远程形态 nil 安全跳过并照常下发 remote 标志；②或另派微任务交付该直通后继续。相位常量（动作 8/9 同值钉定）、Esc 栈档位序、防重入哨兵、退回主菜单复用 closeClientSession+releaseWorldConnection 同序拆链（含 clientCloseOnce 只耗一次需并用世界连接释放避免二次进入泄漏）、layout v4 段编码（版本 4+两 u32 布尔）、-connect 形态防御性拒绝本地再装配等全套实现与测试映射已定稿，放行即可立即 TDD。 | 生产/测试代码零改动；worktree 停在 f1b9b8c4 干净态；本行仅为可追溯性记录 |

### 版本矩阵（基线 `b10bbfe7`）

全部不变：协议 v27、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` schema v4、engine ABI v7、client ABI v9、benchmark scenario v19。唯一结构变化为 UI 下行布局段内部版本 3→4（仓库自有契约，沿 D-03 先例）；capture 场景表与视觉 golden 零变化。
