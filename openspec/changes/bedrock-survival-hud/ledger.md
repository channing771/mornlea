# bedrock-survival-hud 执行 ledger

本文件记录 subagent-driven-development 的实现、双评审、修复循环与 controller 裁决。只有实际发生且有证据的结果才能回填；`待执行` 不代表通过。每个执行 Task 最多 5 轮修复/复审，超限后由 controller 对每条 finding 单独裁决。

| Task | Implementer | Implementer commit | Spec review | Quality review | 修复轮次 | 验证证据 | Ruling |
|---|---|---|---|---|---:|---|---|
| 1 OpenSpec change | `/root/hud_task1_openspec` | `b67f38e`（候选）/ `978c8f3`（round 1 fix）/ `5c9ce64`（round 2 fix） | `/root/hud_task1_review`：初审 SPEC FAIL；round 1 复审 SPEC PASS；round 2 复审 SPEC PASS | `/root/hud_task1_review`：初审 QUALITY FAIL；round 1 复审 QUALITY FAIL；round 2 复审 QUALITY PASS | 2 | 三次提交前均为 OpenSpec 56/0、diff check EXIT 0；最终 review package SHA-256 `52eb2bc47378ed10f297d85a73cc50c06e58cb0ef31372a1bdc0afe675d253b2`，strict 56/0、范围 diff check EXIT 0 | 通过 |
| 2 atlas icons | `/root/hud_task2_atlas` | `eb440ed`（候选）/ `1b94ee3`（round 1 fix）/ `8d3b4d7`（round 2 ledger-only fix；`BASE` 为 `6886679c44eeb548347bac77aa10bdbaed749ada`） | `/root/hud_task2_review`：首轮 SPEC FAIL（3 个 P2）；round 1 复审 SPEC FAIL（仅 package SHA-256 未闭合）；round 2 复审 SPEC PASS | `/root/hud_task2_review`：首轮 QUALITY FAIL（3 个 P2）；round 1 复审 QUALITY FAIL（仅 package SHA-256 未闭合）；round 2 复审 QUALITY PASS | 2 | `make rust` EXIT 0；红测按预期失败；HUD race、archcheck、gofmt、diff check 均 EXIT 0。round 1 覆盖全枚举 placement 与字面列顺序；round 2 ledger-only 后官方 package 覆盖完整 `BASE..HEAD`，原始 diff SHA-256 `4ad046ef0d15285b9f015dd13e462769e39158b0e63a3790f7979babc4ccb6c9`，复审重新计算一致 | 通过 |
| 3 hotbar/mining | `/root/hud_task3_hotbar` | `4aabc6a`（候选）/ `3e047b5`（打开态修复）/ `ad2c6bb`（round 1 补测）/ `60ddd5b`（round 1 验证）/ `29f4e58`（见证收紧） | `/root/hud_task3_review`：首轮 SPEC FAIL（1 个 P2）；round 1 SPEC PASS | `/root/hud_task3_review`：首轮 QUALITY FAIL（1 个 P2）；round 1 QUALITY PASS | 1 | `make rust` EXIT 0；快捷栏、采掘与 BASE archive renderer 红测均按预期失败；renderer focused、HUD race、archcheck、gofmt、diff check 均 EXIT 0；package raw diff SHA-256 `cfe0d5cd946d66bc8b343f5f50a5b0ba1fd059311af0577a9ccbdfd46f7595bc` | 通过 |
| 4 status/layout/capacity | `/root/hud_task4_status` | `92f0c64`（候选）/ `5e7f970`（round 1 docs fix）/ `a15ac66`（round 1 code fix）/ `04ecf8c`（round 2 comment fix）/ `ade4768`（round 2 verification）/ `02a7a8a`（round 3 downstream fix）/ `3991aed`（round 4 chat anchor fix）/ `6f5cad8`（round 5 fix） | `/root/hud_task4_review`：首轮 SPEC FAIL（P1/P2）；round 1/2/3 SPEC PASS；round 4 SPEC FAIL（新 P2）；round 5 SPEC FAIL（float32 P2） | `/root/hud_task4_review`：首轮 QUALITY FAIL（P1/P2）；round 1 QUALITY FAIL（新 P3）；round 2/3 QUALITY PASS；round 4 QUALITY FAIL（新 P2）；round 5 QUALITY FAIL（float32 P2） | 5 | round 5 修复真实栈高/最宽文本约束并通过 HUD race、integration、archcheck、benchmark v18、strict 56/0、gofmt/diff；独立复审以生产字体复现 `44x40` 与 `109x40` 单 ULP 越界，故五轮用尽时仍 FAIL | `/root`：round 5 finding 有效且不得豁免；这是完成真实栈高约束后才隔离出的 float32 数值根因，不计 Task 4 第六轮，拆为独立 Task 4B。Task 4B CLEAN 后原 3.10 最终闭合 |
| 4B chat float32 strict bounds | `/root/hud_task4b_bounds` | `8e5786d`（计划）/ `18f7b3e`（候选） | `/root/hud_task4b_review`：SPEC PASS / CLEAN | `/root/hud_task4b_review`：QUALITY PASS / CLEAN | 0 | BASE + 新测试与零余量 mutant 均真实 RED；HEAD 固定见证、两组 5×5 scan、reviewer-only 172,800 布局扫描、0 alloc、448 glyph、76/245 与固定 247/700/12288/45888 均 PASS；HUD race、integration、archcheck、benchmark v18、strict 56/0、gofmt/diff PASS；package raw SHA-256 `5864e3555765cb70a55852111d7ff3026a05e596d2374e035710e1ab56cb2f95` | `/root`：独立 Task 4B SPEC/QUALITY 双 PASS/CLEAN，通过；严格界内 MUST 未放宽，Task 5 capture/golden 未改，原 3.10 与 3.14 可勾选 |
| 5 capture/golden | `/root/hud_task5_capture` | `d56181e`（候选）/ `4dcd417`（round 1 fix） | `/root/hud_task5_review`：round 0 SPEC FAIL（P1/P2）；round 1 SPEC PASS / CLEAN | `/root/hud_task5_review`：round 0 QUALITY FAIL（P1/P2）；round 1 QUALITY PASS / CLEAN | 1 | implementer：P1 真实 RED/GREEN，visual-check 15×0 像素差、focused、`cmd/mornlea` race 186.051s、HUD race、archcheck、strict 56/0、gofmt/diff PASS；reviewer：完整 `120dea4..4dcd417` package raw SHA-256 `2bbd90a049a360609f77c076e5c754263d1b6ce83a782a446c46cac6dbe28dac` 一致，15 张逐图、visual-check 15×0、focused、archcheck、strict 56/0 均 PASS | `/root`：round 1 原 P1/P2 均 ADDRESSED，SPEC/QUALITY PASS/CLEAN，Task 5 通过 |
| 6 closeout | `/root/hud_task6_closeout` | `dae6f3c`（候选）/ `180ee30`（Task 6 bookkeeping） | `/root/hud_final_review`：SPEC PASS / CLEAN | `/root/hud_final_review`：QUALITY PASS / CLEAN；Ready to merge: Yes | 0 | review 范围 `08932d9..180ee30`；raw diff SHA-256 `6ba1937abc9909c015f1398b4f58d543f8483f58538b8b6774a1113af959708d`，package SHA-256 `17cae462d5dcdc163e55853787f911e1e94f2e9aa02cd458375606736deded9c`；reviewer HUD race、archcheck、`cmp`、strict 56/0、`gofmt -l`、range diff check 均 EXIT 0；逐图验收 15 张 committed golden，全部最大通道差 0/差异像素 0 | `/root`：接受整分支 SPEC/QUALITY 双 PASS/CLEAN 与 Ready Yes，Task 6 通过；未经用户明确批准不归档、合并或操作 main |
| 6.1 main integration planning | `/root/hud_merge_spec` | `dbb9e811`（planning） | `/root/hud_merge_spec_review`：repair rounds 1–2 后最终 SPEC PASS | `/root/hud_merge_spec_review`：repair rounds 1–2 后最终 QUALITY PASS | 2 | OpenSpec strict `56 passed, 0 failed`；raw diff SHA-256 `a5d5171fb1b7d9d2b2b4ed0c73c0bc330dc65a9f93314bc46a31ffe6eb2e6d55`；package SHA-256 `0b559c4b7d7c0936fcc910fb8f86fb22f8a1b1bb21bfb26a5952369e2d4904ad` | `/root`：approved，semantic merge plan proceeds |

## Finding 与修复记录

| Task | 轮次 | Reviewer | Finding | 修复 commit / 证据 | 复审结论 | Controller ruling |
|---|---:|---|---|---|---|---|
| 1 | 1 | `/root/hud_task1_review` | P1：MODIFIED delta 与新生命契约丢失既有“生命行无背景”语义 | round 1 在生命 Requirement/Scenario、完整 MODIFIED requirement、`hud-hotbar-health` Scenario、design 与 Task 4 红测中恢复；fix `978c8f3` | round 1 复审：ADDRESSED | `/root`：要求最小修复 |
| 1 | 1 | `/root/hud_task1_review` | P1：五个任务组把独立评审排在候选提交之前，无法生成 committed review package | round 1 改为 implementer 自证→候选提交→review package→评审→追加 fix commit/复审；fix `978c8f3` | round 1 复审：ADDRESSED | `/root`：要求 Task 6 同步修复 |
| 1 | 1 | `/root/hud_task1_review` | P2：Task 2、3、5 的 Go 提交前验证遗漏 archcheck | round 1 在 `tasks.md` 1.4、2.5、4.4 增加 `go test ./internal/archcheck -count=1`；fix `978c8f3` | round 1 复审：ADDRESSED | `/root`：要求补齐三处 |
| 1 | 1 | `/root/hud_task1_review` | P2：Task 4 响应式/命中与容量见证测试排在实现之后 | round 1 把两类失败测试分别前移到 3.5/3.6，最小实现后移到 3.7；fix `978c8f3` | round 1 复审：ADDRESSED | `/root`：要求恢复 red-green 顺序 |
| 1 | 2 | `/root/hud_task1_review` | P1：五组任务把单一 task reviewer 扩成分别的 spec reviewer 与 quality reviewer，重复占用评审席位 | round 2 把五处派发统一为一名新的独立 task reviewer 同时给出 SPEC/QUALITY 双裁决，复审由同一 scoped reviewer 双裁决；fix `5c9ce64` | round 2 复审：ADDRESSED | `/root`：只做该项最小修复 |
| 1 | 2 | `/root/hud_task1_review` | 第 2 轮复审无新 P0、P1、P2 或 P3 finding | 复审完整范围 `08932d9..5c9ce64`；review package SHA-256 `52eb2bc47378ed10f297d85a73cc50c06e58cb0ef31372a1bdc0afe675d253b2`；strict 56/0；diff check EXIT 0 | SPEC PASS / QUALITY PASS | `/root`：通过 |
| 2 | 1 | `/root/hud_task2_review` | P2：顶面逐像素测试手写 placement 子集，遗漏 `ItemLightBlock`、`ItemWheatSeeds` 与未来注册项 | `1b94ee3` 改为 `0 <= item < ItemIDMax`，仅跳过 `ItemPlacement` 为 false 的项 | round 1/2 复审：ADDRESSED | `/root`：最小覆盖修复 |
| 2 | 1 | `/root/hud_task2_review` | P2：未锁定五个 UI cell 与物品 offset 的字面列顺序 | `1b94ee3` 断言 `[6]int{0, 1, 2, 3, 4, 5}` | round 1/2 复审：ADDRESSED | `/root`：最小覆盖修复 |
| 2 | 1 | `/root/hud_task2_review` | P2：review package 未包含 committed diff、commit list 与 stat | round 1 由 `review-package` 脚本重生成完整 scratch package | round 1：部分 ADDRESSED | `/root`：要求使用官方脚本 |
| 2 | 1 | `/root/hud_task2_review` | round 1 复审：前两个 P2 ADDRESSED；package 已有 diff、commit list 与 stat，但缺少范围 SHA-256 | `8d3b4d7` 后以脚本生成新 `BASE..HEAD` package，并在包头写入原始 diff SHA-256 | round 2：ADDRESSED | `/root`：最小流程修复 |
| 2 | 2 | `/root/hud_task2_review` | round 2 复审无新 P0、P1、P2 或 P3 finding | 完整范围 `6886679..8d3b4d7` 的 package、SHA-256 与 diff check 均核对一致 | SPEC PASS / QUALITY PASS | `/root`：通过 |
| 3 | 1 | `/root/hud_task3_review` | P2：`renderer_test.go` 未进入候选范围，导致 2.1 的勾选和 renderer 层 TDD 证据不真实 | `ad2c6bb` 新增 renderer 级关闭态/采掘实例前缀见证；在 BASE archive 叠加该测试后真实失败；focused、HUD race、archcheck、gofmt、diff check 均 EXIT 0 | round 1 复审：ADDRESSED，SPEC PASS / QUALITY PASS | `/root`：通过 |
| 3 | 1 | `/root/hud_task3_review` | round 1 复审无新 P0、P1、P2 或 P3 finding | 完整范围 `218bc9c..29f4e58` package 与 raw diff SHA-256 `cfe0d5cd946d66bc8b343f5f50a5b0ba1fd059311af0577a9ccbdfd46f7595bc` 一致，diff check EXIT 0 | SPEC PASS / QUALITY PASS | `/root`：通过 |
| 4 | 1 | `/root/hud_task4_review` | P1：候选把固定 HUD 布局移到 265/13056/46656，却仍标记 benchmark scenario v18，违反主规格锁定的 247/12288/45888 | `5e7f970` 先把 active change 改为 resolved-slot 与 v18 固定契约；`a15ac66` 每个生命/氧气槽只发一个实例，合法最大 76/245，固定布局恢复 247/12288/45888 | round 1 复审 ADDRESSED；round 2 复审保持 ADDRESSED，SPEC/QUALITY PASS | `/root`：不升 v19、不新增 benchmark delta；用视觉等价 resolved single-slot 保持 v18 |
| 4 | 1 | `/root/hud_task4_review` | P2：打开态分隔线 `max(scale*2,1)` 在极窄正尺寸 framebuffer 越过下沿 | `a15ac66` 删除 1px floor；新增 17×800、800×17、16×16、1×1 的打开/关闭全部 quad/glyph finite 且界内回归 | round 1 复审 ADDRESSED；round 2 复审保持 ADDRESSED，SPEC/QUALITY PASS | `/root`：最小修复并覆盖更小正尺寸 |
| 4 | 2 | `/root/hud_task4_review` | P3：容量测试注释仍声称打开态恰好打满上限且容量可随有界实例更新，与合法最大 245、scenario v18 固定 247 矛盾 | `04ecf8c` 只把两处注释改为保留 2 quad 余量且只有显式 benchmark scenario 迁移才能改变固定布局；测试逻辑与生产代码不变 | round 2 复审 ADDRESSED，SPEC/QUALITY PASS | `/root`：最小注释修复，通过 |
| 4 | 2 | `/root/hud_task4_review` | round 2 复审无新 P0、P1、P2 或 P3 finding | 完整范围 `04e62a9..ade4768` package raw SHA-256 `7ac6b350e86f315da2ed33bd11a0eedf7e51e1ed7e7071c80c725aacbaadb6a7`；独立验证全部 PASS | SPEC PASS / QUALITY PASS | `/root`：Task 4 通过 |
| 4 | 3 | `/root/hud_task4_review` | Task 5 integration 暴露 late downstream finding：`cmd/mornlea/chat_test.go` 与 `health_hud_test.go` 仍锁定 Task 4 前 health 16 quad/oxygen 2 quad，与已评审 resolved-slot 各10 quad 契约矛盾 | `02a7a8a` 精确更新所有同类断言与说明，不改生产代码；复审在纯 `git archive HEAD` 中运行 focused 与完整 `cmd/mornlea` race，隔离 Task 5 未提交文件 | round 3 复审 ADDRESSED，SPEC/QUALITY PASS | `/root`：修正测试根因，不放宽断言，通过 |
| 4 | 3 | `/root/hud_task4_review` | round 3 复审无新 P0、P1、P2 或 P3 finding | 完整范围 `04e62a9..02a7a8a` package raw SHA-256 `6412be44786380e5cc03c27589246c7b81ee52e53e3f9915764f201e515207ae`；纯 archive 独立验证全部 PASS，Task 5 三个文件保持原状 | SPEC PASS / QUALITY PASS | `/root`：Task 4 round 3 通过 |
| 4 | 4 | `/root/hud_task5_review` | P2：`ai-companion` 的 accepted chat history `y=244..284` 与关闭态 survival status `y=268..284` 重叠，破坏聊天语义可审查性 | `3991aed` 以同帧 accepted line、open input、confirmed health/depleted oxygen 真实 RED，改为复用 `hotbarRowBounds(false)` 的 status top 向上锚定 | round 4 复审：640×360 重叠 ADDRESSED，但新 P2 使 SPEC/QUALITY FAIL | `/root`：保留 shared anchor，继续修复最大聊天栈越界 |
| 4 | 5 | `/root/hud_task4_review` | 新 P2：640×300 下 open input+六行 history 被 shared anchor 推到 `y=-24`，聊天 scale 未受真实栈高约束 | `6f5cad8` 的表驱动 RED 覆盖 7 尺寸×open/closed；按真实行数/栈高/最宽文本统一缩放，原大幅越界修复，但理论 bound 只向零留一个 ULP | round 5 final：SPEC FAIL / QUALITY FAIL；新 float32 P2，五轮用尽 | `/root`：第五轮不再追加 Task 4 fix；不删行、不隐藏状态、不改容量，后隔离根因交独立 Task 4B |
| 4 | 5 | `/root/hud_task4_review` | P2：理论 fit scale 只向零留一个 float32 ULP，panel 尺寸/坐标与最多 32 glyph 的后续乘加仍可累计越界；实测 `44x40` open CJK 右沿和 `109x40` open 窄 Latin 上沿失败 | `6f5cad8`；review package raw SHA-256 `1c0db7f4fecf406d7f8b97f8f0dced0bab81d4ce94dfa04d4de19e8e25a37541`，生产字体密集扫描复现 | round 5 final：SPEC FAIL / QUALITY FAIL；finding 有效 | `/root`：这是完成真实栈高约束后才隔离出的数值稳定性根因，不计 Task 4 第六轮；拆为 Task 4B，单独最多 5 轮且严格界内 MUST 不放宽 |
| 4B | 0 | `/root/hud_task4b_review` | CLEAN：无 P0、P1、P2、P3；共享 `1/256` pixel slack 修复单 ULP 根因，范围聚焦且契约未放宽 | `8e5786d` / `18f7b3e`；BASE/零余量 mutant RED，HEAD 两个真实见证、5×5 open/closed scans 与 172,800 reviewer-only layouts GREEN；raw SHA-256 `5864e3555765cb70a55852111d7ff3026a05e596d2374e035710e1ab56cb2f95` | SPEC PASS / QUALITY PASS / CLEAN | `/root`：Task 4B 通过，3.14 与依赖其闭环的原 3.10 最终勾选；Task 5 capture/golden 保持不变 |
| 5 | 1 | `/root/hud_task5_review` | P1：`inventory-crafting` 未固定耗损氧气，只呈现生命，无法验收打开容器时生命与氧气同时位于快捷栏下方且避让交互格/配方 | 新窄测试在未改 `capture.go` 时真实 RED：`inventory-crafting 缺少已确认生命与耗损氧气夹具`；`4dcd417` 只复用既有 `captureHUDFixture`，固定 health 5、oxygen `core.MaxOxygenTicks/3` 后 GREEN；新图证明两行完整界内且避让 36 格与 10 行配方 | round 1：ADDRESSED | `/root`：接受最小 capture-only fixture 修复，通过 |
| 5 | 1 | `/root/hud_task5_review` | P2：round 0 `ai-companion` 的 accepted ChatEvent 与生命状态行重叠 | 共享产品布局由 Task 4/4B 的 `3991aed`、`6f5cad8`、`18f7b3e` 修复并独立 PASS/CLEAN；`4dcd417` 更新实际变化 golden，round 1 新图证明 accepted history、open input、生命行严格分离 | round 1：ADDRESSED | `/root`：产品布局修复与最终 capture 验收均闭合，通过 |
| 5 | 1 | `/root/hud_task5_review` | round 1 复审无新 P0、P1、P2 或 P3 finding | 完整范围 `120dea4..4dcd417` package raw SHA-256 `2bbd90a049a360609f77c076e5c754263d1b6ce83a782a446c46cac6dbe28dac` 独立复算一致；15 张逐图与 visual/focused/archcheck/strict 均 PASS | SPEC PASS / QUALITY PASS / CLEAN | `/root`：Task 5 通过，4.8/4.9 完成 |
| 6 | 0 | `/root/hud_final_review` | CLEAN：无新 P0、P1、P2 或 P3 finding | 整分支 `08932d9..180ee30`；raw diff SHA-256 `6ba1937abc9909c015f1398b4f58d543f8483f58538b8b6774a1113af959708d`；package SHA-256 `17cae462d5dcdc163e55853787f911e1e94f2e9aa02cd458375606736deded9c`；15 张逐图和独立命令证据均 PASS | SPEC PASS / QUALITY PASS / CLEAN；Ready to merge: Yes | `/root`：整分支终审通过，收尾完成，等待用户的归档/合并授权 |

## 人工视觉验收

| 执行人 | 候选目录 | 接受的 golden | 拒绝项与根因 | 结论 |
|---|---|---|---|---|
| `/root/hud_task5_capture`、`/root/hud_task5_review`（round 1） | `build/visual-bedrock-survival-hud-update` | `terrain-noon.png`、`hud-hotbar-health.png`、`hud-survival-feedback.png`、`avatar-nametag.png`、`inventory-crafting.png`、`debug-panel.png`、`skylight-tunnel.png`、`block-light-room.png`、`materials-showcase.png`、`target-block-feedback.png`、`oak-grove.png`、`ai-companion.png`、`water-surface-slope.png`、`far-horizon.png`、`water-underwater.png` | 无；`inventory-crafting` 已补低生命与耗损氧气且不覆盖 36 格或 10 行配方；`ai-companion` 的 accepted history、open input、生命行已严格分离 | implementer 与独立 reviewer 均逐图接受全部 15 张；`hud-survival-feedback` 四类反馈同帧清楚，世界/实体/光照/水/LOD 无非 HUD 异常；reviewer visual-check 15×0 像素差 |

## 整分支终审

| Reviewer | 基线与 head | 规格结论 | 质量结论 | 验证证据 | 最终 ruling |
|---|---|---|---|---|---|
| `/root/hud_final_review` | `08932d9240274299cd0b0b1288900120540c636d..180ee3020109da043a4980f50718533d8f3742a6` | SPEC PASS / CLEAN | QUALITY PASS / CLEAN；Ready to merge: Yes | raw diff SHA-256 `6ba1937abc9909c015f1398b4f58d543f8483f58538b8b6774a1113af959708d`；package SHA-256 `17cae462d5dcdc163e55853787f911e1e94f2e9aa02cd458375606736deded9c`；HUD race、archcheck、`cmp`、strict 56/0、`gofmt -l`、range diff check 均 EXIT 0；15 张逐图，全部 0 像素差 | `/root`：接受终审结论，Task 6 通过，未经用户明确批准不归档、合并或操作 main |

## main authoritative-hunger integration 执行记录

- `2026-08-23T02:59:11Z`：任务 `6.2–6.13 main authoritative-hunger integration` 由 fresh implementer `/root/hud_merge_impl` 开始；`planning_base=dbb9e811d9bf3d87aafd56bd1fdb18f0f1107c33`，`main_commit=f9808f4c0c07e17b084a1a5a91ab97f2f925e407`，`common_base=08932d9240274299cd0b0b1288900120540c636d`，`integration_base=b02d8c245c0f1c08c67bd384beedb5bc889747a0`；linked worktree、clean worktree/index、reviewed refs 与 merge-base 未提交 merge 前置检查通过。
- `2026-08-23T03:03:07Z`：合并前基线通过。`make rust` EXIT 0（Rust 1.97.1 locked release）；`go test ./internal/render/hud ./cmd/mornlea ./cmd/perfcheck -count=1` EXIT 0（HUD 2.255s、mornlea 194.022s、perfcheck 2.565s）；`go test ./internal/archcheck -count=1` EXIT 0（2.836s）。
- `2026-08-23T03:03:44Z`：执行 `git merge --no-ff --no-commit f9808f4c0c07e17b084a1a5a91ab97f2f925e407`，按预期 EXIT 1，`MERGE_HEAD=f9808f4c0c07e17b084a1a5a91ab97f2f925e407`。实际 unmerged 清单恰好 22 项且与审批清单逐路径一致：文本/source 8 项 `cmd/mornlea/chat_test.go`、`cmd/mornlea/health_hud_test.go`、`docs/notes/progress.md`、`internal/render/hud/atlas.go`、`internal/render/hud/layout.go`、`internal/render/hud/layout_test.go`、`internal/render/hud/renderer.go`、`internal/render/hud/renderer_test.go`；binary 14 项 `ai-companion.png`、`avatar-nametag.png`、`block-light-room.png`、`debug-panel.png`、`far-horizon.png`、`hud-hotbar-health.png`、`inventory-crafting.png`、`materials-showcase.png`、`oak-grove.png`、`skylight-tunnel.png`、`target-block-feedback.png`、`terrain-noon.png`、`water-surface-slope.png`、`water-underwater.png`。所有权裁决：main 保留 hunger、协议 v24、玩家 schema v7、network/sim/storage/predictor/app wiring 与 benchmark scenario v19；当前分支保留 Pixel Perfection heart/bubble、响应式状态布局、背包/聊天/采掘、capture、远环与水景；atlas、三栏布局、容量、fixture/goldens 与长期文档做双方语义组合。`hud-survival-feedback.png` 为分支已有第 15 张，最终同批重生成。
- `2026-08-23T03:11:33Z`：8 个文本/source conflict marker 已手工消除，自动合并的 main hunger test 调用已适配当前分支 resolved-slot health 签名；`go test ./internal/render/hud ./cmd/mornlea -run '^$' -count=1` EXIT 0，parser/compile 基线成立。七列 atlas RED：`go test ./internal/render/hud -run 'TestHotbar(TextureAtlas|ColumnUV)|TestHungerBar' -count=1` EXIT 1，核心失败为物品 offset `5`、期望 `7`；同时识别并重写 main 旧「20-quad hunger 与旧 health 同构」断言，使其不与最终 10-quad resolved health 契约冲突。最小 atlas GREEN 后同命令 EXIT 0；最终固定列为空/半/满心、空/满气泡、空/满鸡腿，物品 offset 7，逐像素 placement、二值 alpha、互异、确定性、UV 与奇数右半格断言通过。
- `2026-08-23T03:28:14Z`：三栏几何 RED：`go test ./internal/render/hud -run 'Test(Health|Oxygen|Hunger|Status|Responsive|InventorySlotAt)' -count=1` EXIT 1，旧 health `x=408`/oxygen `x=703` 与期望 `374.5`/`555.5` 及固定 `169/12/531` 组宽不符。以单一 `statusColumnOrigin` 最小组合三栏，health/oxygen/hunger 分列 0/1/2，hunger 右到左，open/closed 共用 Y 与响应式缩放；同命令 GREEN EXIT 0，并覆盖 full-oxygen 隐藏不回流、36 inventory cells 与 10 recipe rows 避让。
- `2026-08-23T03:28:14Z`：renderer/app 容量采用 omission mutant 建立真实 RED：临时省略 hunger append 后，`go test ./internal/render/hud ./cmd/mornlea -run 'Test(Hotbar|ChatOverlay|HUDHunger|ApplicationRenders)' -count=1` EXIT 1，分别命中 hunger 0/10、closed 76/96、app 10/30 与 10/32。恢复唯一 hunger append 并把 main 遗留的 17-quad health 断言校正为最终 10-quad resolved health 后，同命令 EXIT 0（HUD 0.962s、mornlea 1.166s）；`cmp -s AGENTS.md CLAUDE.md` EXIT 0；archcheck EXIT 0（2.396s）。最终固定 v19 为 267 quads、700 glyphs、glyph offset 13312、46912 bytes，closed/open 最大 96/265，instance 48 bytes 且 offsets 256-byte aligned；长期文档同步为 v24/schema v7/scenario v19、七 cell、三栏与 15 场景。
- `2026-08-23T03:28:14Z`：capture RED：扩展测试先引用 `captureHUDFixture.Hunger`，focused 命令编译失败并逐项报告字段缺失。最小实现只向现有 fixture/`network.PlayerState` 注入 Hunger，并固定 hotbar `20/full/full`、survival `5/third/9 + blocked mining 4/9`、inventory `5/third/9`；`go test ./cmd/mornlea -run 'Test(HUDCapture|CaptureSceneOrder|WaterUnderwater|FarHorizon)' -count=1` GREEN EXIT 0（0.711s）。测试覆盖 defer restore、非法 hunger 错误返回不变更状态、同一 app 二次 apply/restore、完整 predictor 指针/snapshot 与 mining 恢复，以及 15 项精确顺序。
- `2026-08-23T03:32:09Z`：focused precommit 全部通过。HUD/app race EXIT 0（2.054s/172.427s）；archcheck EXIT 0（2.781s）；scenario v19 + perfcheck 精确契约组 EXIT 0（HUD 0.510s、mornlea 0.805s、perfcheck 1.220s）；capture focused EXIT 0（0.353s）；`gofmt -l .` 无输出；docs `cmp`、OpenSpec strict 57/0、`git diff --check` 均 EXIT 0。此时 merge 仍未提交且正式 PNG 尚未从任何 parent 暂存。
- `2026-08-23T03:35:11Z`：`make visual-update VISUAL_OUT=build/visual-bedrock-survival-hud-main-integration-update` EXIT 0；由最终未提交 merge 的 Rust 1.97.1/Go capture 一次性重生成全部 15 张正式 golden，LOD on/off near-band control 通过。正式 golden 恰好 15 张且逐张与对应输出 `cmp` 相同、均为 640×360 RGB PNG；输出目录另含既有 guard 的 2 张 `far-horizon-*-control.png`，controller 裁决其为 diagnostic controls，不纳入正式 scene/golden 计数、不改机制。没有复制或选择任一 parent PNG。

### main authoritative-hunger integration 人工视觉验收（15 张正式 scene）

| 场景 | 实际检查结果 | 结论 |
|---|---|---|
| `terrain-noon` | 默认注水海岸、天空/云、近远景与 Pixel Perfection/fallback 材质均完整；无 HUD 外异常 | 接受 |
| `hud-hotbar-health` | 10 个满心、20 点满鸡腿，满氧气隐藏；三列固定槽组整体居中，hotbar/选中框正常 | 接受 |
| `hud-survival-feedback` | 生命 5 的两心半、约三分之一氧气、饥饿 9 的四鸡腿半清楚；磨损条和受阻采掘 4/9 同帧可辨且互不重叠 | 接受 |
| `avatar-nametag` | 远端玩家 body silhouette、Unicode name tag、遮挡与背景稳定 | 接受 |
| `inventory-crafting` | 三列状态缩放后位于 hotbar 下方；36 格背包与 10 行配方完整、无状态覆盖或错位 | 接受 |
| `debug-panel` | 中英文字形、行距、选中行背景和面板边缘稳定；可见内容无新增裁切/错位 | 接受 |
| `skylight-tunnel` | 天空光传播、隧道明暗纵深与方块边界稳定 | 接受 |
| `block-light-room` | 静态方块光中心、强度层次和墙体遮挡稳定 | 接受 |
| `materials-showcase` | 自然/加工/农业/水与 alpha-cutout 材质对齐；无七列 atlas offset 退化 | 接受 |
| `target-block-feedback` | 目标轮廓、`砖块` 名称、深度/遮挡正确，无 inventory-open 错误反馈 | 接受 |
| `oak-grove` | 确定性橡树、树叶 cutout、草地/岸线与远景稳定 | 接受 |
| `ai-companion` | 伙伴 body/name tag 与 Unicode 聊天输入/事件行分离，位置与遮挡正确 | 接受 |
| `water-surface-slope` | 斜水面角高、透明层次、岸线与水下边界无退化 | 接受 |
| `far-horizon` | LOD shell、海平面钳制、裙边和 near-band control 保留；场景为倒数第二 | 接受 |
| `water-underwater` | 水下 tint、可见半径、耗损氧气 HUD 与水面边界正常；场景为最后一项 | 接受 |

### 用户批准的 Task 1 视觉修订

- `2026-08-23T03:49:58Z`：用户检查第一批合并 capture 后拒绝 `531px` 三栏构图：满氧隐藏时，health/hunger 浮在快捷栏两侧之外，中间留下没有结构的空水平栏。此前 `2026-08-23T03:35:11Z` 的逐图“接受”仅是 implementer 对旧批准契约的检查，现由这次用户裁决明确取代；该批 PNG 不得作为最终 golden。
- Controller 记录用户已批准的新构图：health/hunger 各保留 `169` design px，分别精确对齐 `hotbarRowBounds` 左/右边缘；不再保留栏间 gap 或 `531px` 主状态组常量。depleted oxygen 与 hunger 共享右边缘，closed 向上、open 向下偏移恰好一个 `healthHeartSize + statusBarGap` design row；满/未确认氧气只省实例，两行垂直 sizing、mining/chat anchor 与 health/hunger 坐标均不折叠。open bottom reservation、`openHUDHeight` 与 `closedHUDHeight` 均纳入第二行，容量、状态语义、v19 数值、atlas 顺序与 15 场景顺序不变。
- 官方页面 `https://www.minecraft.net/en-us/article/health-minecraft` 只用于比较生命/饥饿对向与气泡外侧堆叠的构图关系；HUD 图标继续使用本项目原创程序化像素，禁止导入、描摹或复制 Mojang 像素。
- Controller 暂停 apply 并裁决 planning-only：仅修改五份既有 OpenSpec、tracked implementation plan 与本 ledger，不编辑 source/tests/goldens，不 stage/commit/push/archive/review；strict validation 与 diff check 后返回 `NEEDS_CONTEXT`，由 controller 检查 artifact diff 并显式恢复 `$openspec-apply-change`。当时 final visual-check 正在运行，已按新裁决用 interrupt 停止（EXIT 1），benchmark producer 尚未执行；merge 继续保持未提交。
- `2026-08-23T03:52:34Z`：planning-only 修订完成，实际 tracked artifact diff 恰好为五份既有 OpenSpec（`proposal.md`、两个 delta spec、`design.md`、`tasks.md`）、tracked implementation plan 与本 ledger；`tasks.md` 只改未勾选 6.4/6.5/6.8/6.10，历史已挣得 checkbox 与 6.1 未改。`openspec validate --all --strict --no-interactive` EXIT 0（57 passed, 0 failed），`git diff --check` EXIT 0；未 stage/commit/push/archive/review，未恢复 source/test/golden 编辑，merge 仍未提交。按 controller stop boundary 返回 `NEEDS_CONTEXT`，等待检查 artifact diff 后显式恢复 apply。
- `2026-08-23`：controller 检查 planning-only diff 后显式恢复 `$openspec-apply-change`；同一唯一 implementer `/root/hud_merge_impl` 重读 Task 1 brief 与五份 OpenSpec context，继续未提交 merge，仍不得派生子代理、自评审、push、archive 或执行 controller-owned 6.13 closeout。
- `2026-08-23`：修订布局真实 RED 在原 531px 实现上成立。focused 命令 EXIT 1，见证 health X `374.5` 而 hotbar left `408`、closed oxygen `(555.5,708)` 而目标 `(703,688)`、mining Y `680` 而完整栈目标 `660`、open oxygen Y `780` 而向外目标 `800`。最小 GREEN 删除 `statusColumnWidth`/`statusColumnGap`/`statusGroupWidth`/`statusColumnOrigin`，只增加共享 `statusBarBounds` 算术并复用 `hotbarRowBounds`；open/closed height、open bottom reservation、mining/chat anchor 永久纳入第二行。相同 focused 组、HUD 全包、HUD race（2.029s）、app health/chat/capture focused（1.363s）、docs cmp、旧三栏 grep 与 diff check 全部 EXIT 0。
- `2026-08-23`：重新运行第二批 `make visual-update VISUAL_OUT=build/visual-bedrock-survival-hud-main-integration-revised-update` EXIT 0；全部 15 张正式 golden 从修订二进制重新生成并逐张人工接受，未复用被拒绝的第一批。重点图：`hud-hotbar-health` 对齐 hotbar 左右边缘且无水平 oxygen 空栏；`hud-survival-feedback` 的 oxygen 沿 hunger 右边缘向上、mining 在完整栈上方；`inventory-crafting` 的 oxygen 向下且 36 格/10 配方无遮挡。官方页面只比构图，未下载/导入/描摹/复制 Mojang 像素。输出另有 2 张既有 far-horizon diagnostic control，guard 通过但它们不计入正式 golden。随后 `make visual-check VISUAL_OUT=build/visual-bedrock-survival-hud-main-integration-revised-check` EXIT 0，15 张均最大通道差 0、差异像素 0/230400；双阈值与尾序不变。
- `2026-08-23`：Task 6.11 提交前 focused gate 全部通过。HUD/app race EXIT 0（1.798s/182.176s）；archcheck EXIT 0（3.010s）；精确 scenario-v19/upload/perfcheck 组 EXIT 0（HUD 1.255s、app 2.250s、perfcheck 3.107s）；capture 顺序/恢复组 EXIT 0（0.819s）；`gofmt -l .` 空、docs cmp EXIT 0、OpenSpec strict 57/0、`git diff --check` EXIT 0。6.11 已据实勾选。
- `2026-08-23`：Task 6.12 重门禁只启动一次并完整 EXIT 0。`make rust`/`make rust-check` 通过（client 49、engine 160）；`go test ./... -race` 全包通过（app 208.925s、server 130.358s、storage 15.215s 等）；`go vet ./...` 无输出；benchmark producer 写入 `build/perf-bedrock-survival-hud-main-integration-v19.json`，身份为 scenario 19/memory/Apple M5 24GiB/Go 1.26.0，still 259.1033 FPS p95 4.133ms、flying 412.4912 FPS p95 6.520ms、multiplayer peak RSS 2,163,326,976 bytes。producer 只记录 multiplayer RSS ≥2GiB 与 flying p99 14.270ms ≥12ms 的性能 notice，不改变退出状态。最终 `make visual-check` 的 15 张正式场景均 0/230400 差异像素；strict 57/0、`gofmt -l .` 空、diff check EXIT 0。6.12 已据实勾选；6.13 保持未勾选并留给 controller。
- `2026-08-23`：tracked-only staging audit 通过。brief 的宽 marker regex 只命中既有 Pixel Perfection license 的三条长 `====` 分隔线，未发生 staging；改用精确 conflict marker 形态后无输出。`MERGE_HEAD` 仍为 exact main、HEAD 仍为 integration base；untracked 0、报告被 ignore、`build/`/`.superpowers/sdd/`/diagnostic controls 不在 index，正式 golden 恰好 15 张。暂存后 UU 与 `git ls-files -u` 均空，worktree-vs-index diff 空，cached diff check EXIT 0；main 自有两份 archived Markdown 只产生非失败的 EOF blank-line notice。6.13 仍未勾选。
