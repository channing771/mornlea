# Implementation Ledger

## Baseline

- Change: `pilot-godot-client-migration`
- Schema: `spec-driven`
- Baseline commit: `84f3e0e75dff6987e47cf2aa50f50d888e85eafb`
- Implementation start: 2026-09-16
- Controller: OpenAI Codex
- Execution shape: direct main-agent implementation. The first P0/P1 tasks are tightly coupled to the governance and architecture context already held by the controller; no material context-isolation benefit justifies a subagent.

## Ownership

The implementation is additive under `apps/mornlea-godot/`, `scripts/godot/`, `testdata/godot-pilot/`, the planned Go/Rust bridge packages, and explicitly named documentation/audit files. Pre-existing user changes in the following paths remain outside this change and must not be rewritten, staged, reverted, or used to authorize baseline updates:

```text
.superpowers/sdd/tasks/progress.md
openspec/specs/voxel-visual-presentation/spec.md
packages/client/render/block_outline.go
packages/client/render/block_outline_test.go
packages/client/render/name_tag.go
packages/client/render/name_tag_test.go
testdata/visual-golden/world/target-block-feedback.png
.boss-agent/
docs/superpowers/plans/2026-09-08-b02-bucket.md
docs/superpowers/plans/2026-09-11-sneak-double-tap-sprint.md
docs/superpowers/specs/2026-09-11-sneak-double-tap-sprint-design.md
```

The uncommitted `modernize-project-governance` implementation is an explicit prerequisite owned by its own change. Godot implementation may consume those rules but must not collapse the two change ledgers or scopes.

## Rulings

- Ruling: begin with P0 and follow the approved P0–P7 dependency order — the revised pilot has 89 tasks and hard No-Go points at dependency and lifecycle probes — do not create gameplay code before baseline, project-root, and native-binding prerequisites pass.
- Ruling: keep the first implementation round in the main agent — the work is small-to-medium, sequential, and coupled to current governance context — do not spend delegated context merely to parallelize it.
- Ruling: preserve the existing visual producer and tracked baselines — the worktree contains unrelated renderer and golden changes — Godot evidence remains untracked and no baseline-update command is authorized.
- Ruling: bind the pilot only to a machine-readable scenario v23 report — the two historical `perf-baseline*.json` files are actually v15 and v14 — generate a fresh current-client Memory report instead of relabeling or overwriting historical baselines.
- Ruling: after the user revised the pilot to make Python primary, remap the earlier pure-GDScript host work as superseded prototype evidence — preserve the stable pure-GDScript Bootstrap as task 2.4, but do not treat the earlier GDScript feature host and skeleton as completion of revised tasks 2.5–2.8.
- Ruling: stop the Python-primary implementation at task 2.3 — Py4Godot 4.7-alpha21 is the only current release artifact found with Godot 4.7 and macOS arm64 selectors, but it fails the required environment isolation and its editor integration emits runtime errors — do not accept a source fork, launcher-only environment workaround, GDScript feature fallback, or weaker gate without an explicit design revision.
- Ruling: resume only the P1 runtime frontier after the user's explicit 2026-09-16 approval — maintain a minimal, auditable Py4Godot derivative from the already pinned upstream revision; permit changes only to interpreter isolation, editor integration, packaging, and lifecycle correctness; keep all gameplay/data ownership and every qualification gate unchanged; do not begin P2 unless the derivative reaches Go.
- Ruling: accept `4.7-alpha21-mornlea.1` as a P1 Go for the current macOS Apple Silicon target only — the minimal loader/lifecycle patch passes the unchanged isolation, editor/headless, export, coexistence, and 100-cycle matrix — retain the patch deletion conditions and do not infer Windows/Linux qualification or any gameplay/data ownership from this result.
- Ruling: keep the task 2.5 Bootstrap probe pure and fail-closed — it may read project-local artifact paths and descriptor identities but may not instantiate either extension, import a Python feature, or start networking — defer successful dynamic class identity and Python host handoff to the later bridge/host tasks.
- Ruling: separate Godot Python development tooling from the production runtime — keep project dependencies empty, resolve Ruff/mypy only into the ignored `uv` development environment from the lock, execute checks with the exact embedded CPython interpreter, and type against project-owned stubs without importing generated add-on modules.
- Ruling: compose Python features through explicit Godot catalog, manifest, scene, and resource paths — the qualified isolated interpreter intentionally omits the project root from `sys.path`, and Py4Godot loads resource scripts as isolated modules — do not add directory scans, unrestricted dynamic imports, sibling implementation imports, or an ambient Python package root to make feature discovery work.
- Ruling: accept `4.7-alpha21-mornlea.2` as the current macOS Apple Silicon P1 runtime — the additional ordered patch repairs generic generated-wrapper ownership for dynamically loaded RefCounted resources and singleton accessors, exposes no Mornlea gameplay/data API, and passes the unchanged full qualification matrix — keep resource-lifetime repair in the binding derivative rather than adding host-level reference-count workarounds.
- Ruling: keep production feature code Python-led after the pure-GDScript Bootstrap — the five presentation features and three desktop adapters own explicit coarse boundaries, while internal components receive no manifests — reject any new production GDScript outside the two Bootstrap diagnostics and keep audio disabled until its later migration task explicitly enables it.
- Ruling: expose the provisional project bridge identity as static Godot-visible methods — the qualified Py4Godot `ClassDB.instantiate()` path returns an unusable generic object without an owned pointer — do not carry a Python-side native instance or patch around ownership in `bridge_host.py`; require a binding-boundary fix plus lifecycle qualification before a later data-plane task introduces one.
- Ruling: treat Rust extension stages, the Python host, feature teardown, and bridge release as one ordered lifecycle — validate Scene, optional Editor, and MainLoop transitions in Rust; start Python only at MainLoop; deactivate features and release the bridge before Rust stage teardown — convert panics and invalid transitions to observable failures and reject any process cycle with errors, script errors, or leak warnings.
- Ruling: keep the existing client registry and registered font files authoritative for Godot resources — invoke the production registry rather than reproducing procedural atlas logic, copy required license/provenance records, and bind the complete generated closure to source Git trees plus per-file and aggregate SHA-256 values — reject symbolic links, edits, missing outputs, and hand-authored files under `assets/generated/`.
- Ruling: execute task 2.14 under strict subagent-driven-development — the controller for this round is ZCode, a non-OpenAI provider, so `openspec/config.yaml` and `mornlea-implementation-orchestration` require independent implementation and review — a fresh native implementer built the failing audit guard and the three entry points, an independent reviewer verified spec compliance and empirically attacked the isolation guard, one fix round closed the two genuine coverage holes, and the controller owns integration, validation, ledger, and commits.
- Ruling: keep the Godot make gates leaf-only script wrappers — `godot-project-check`, `godot-python-check`, and `godot-smoke` only invoke the already-qualified scripts, with smoke pinned to `--iterations 100 --isolated-python`; `TestGodotIsOptionalForLegacyBuild` admits `scripts/godot` references only inside the three gate rules and rejects legacy probes through prerequisites, tab recipes, inline semicolon recipes, double-colon rules, parse-time `$(shell ...)` assignments, and CI coupling — accepted residuals are limited to deliberate indirection that splits the literal or hides it outside the audited files.
- Ruling: keep the CI `godot` job optional evidence during the pilot — it is intentionally absent from the final `test` job's `needs`, self-materializes ripgrep, the pinned Rust toolchain, Godot, the hardened Py4Godot runtime, and uv before the three make gates, and fails closed if the runner drifts from the pinned Apple clang 21.0.0 / macOS SDK 26.5 identity; promotion to a required release gate belongs to the cutover change.
- Routing record: model inherited host default on the native agent surface (no model enum exposed); effort default; backend native `general-purpose`; basis bounded three-file change with a deterministic validation oracle (focused audit test plus three make gates) and native lifecycle integration minimizing coordination cost; fallback fresh native agent with an expanded brief, or the Z Code bridge live worker at reasoning high after observable insufficiency.
- Ruling: isolate task 2.15 implementation in one native worker while the OpenAI Codex controller retains scope, integration, independent validation, ledger, and commit ownership — the mutation matrix spans the Go audit and shell validator but has a deterministic focused oracle, and the worker exclusively owns those implementation files during its turn.
- Routing record for task 2.15: model `gpt-5.6-terra`; effort `medium`; backend native; basis bounded cross-file test implementation with moderate repository context, reversible changes, and strong focused validation (`GodotDesktopOnly`, `GodotPythonBoundary`, and validator fixture modes); fallback `gpt-5.6-sol` at `high` only after observable insufficiency.
- Ruling: isolate task 3.1 in one native worker while the OpenAI Codex controller retains semantic review, dependency review, validation, ledger, and commit ownership — the new presentation package is file-isolated, but selecting stable zero values and renderer-independent domains requires discovery across existing camera, environment, HUD, target, and session code.
- Routing record for task 3.1: model `gpt-5.6-sol`; effort `high`; backend native; basis consequential cross-host value-contract design with moderate context breadth and a weaker oracle than a mechanical mutation gate, where the project router favors the eligible native flagship; fallback same model at `max` only after observable unresolved domain contradictions or failed independent review.
- Ruling: isolate task 3.2 implementation in one native worker while the OpenAI Codex controller retains contract review, integration, validation, ledger, and commit ownership — the two-file presentation addition is isolated, but its section identity, mesh payload, atlas, revision, capacity, and whole-batch semantics require bounded discovery across the current mesher, world-update, and bridge design.
- Routing record for task 3.2: model `gpt-5.6-terra`; effort `high`; backend native; basis moderate cross-package contract reasoning with reversible file-local changes and a strong focused race-test oracle, where the balanced native tier minimizes bootstrap and token cost without weakening validation; fallback `gpt-5.6-sol` at `high` only after observable domain contradictions, atomicity gaps, or failed controller review.
- Ruling: isolate task 3.3 in one native worker while the OpenAI Codex controller retains semantic-capacity review, integration, validation, ledger, and commit ownership — the new entity contract is file-local, but choosing the total remote-player capacity and separating protocol/mirror semantics from the private GPU instance requires bounded discovery across network, client, and render code.
- Routing record for task 3.3: model `gpt-5.6-terra`; effort `high`; backend native; basis moderate cross-package value-contract reasoning with reversible files and a strong focused race-test oracle; fallback `gpt-5.6-sol` at `high` only after unresolved capacity, tick-ordering, or atomicity contradictions.
- Ruling: isolate task 3.4 in one native worker while the OpenAI Codex controller retains corpus-contract review, integration, validation, ledger, and commit ownership — the test-only node is file-isolated, but producing a reusable parity oracle requires coordinated discovery across protocol states, packet IDs, binary codecs, validation layers, and bounded fixture semantics.
- Routing record for task 3.4: model `gpt-5.6-sol`; effort `high`; backend native; basis broad protocol context and a consequential future cross-runtime oracle justify the eligible native flagship despite deterministic replay validation; fallback the same model at `max` only after unresolved packet coverage, canonical-encoding, or rejection-layer defects.
- Ruling: isolate task 3.5 in one native worker while the OpenAI Codex controller retains characterization-scope review, integration, validation, ledger, and commit ownership — the test-only node crosses five existing app behavior chains and must distinguish stable runtime semantics from renderer and fixture accidents without changing production code.
- Routing record for task 3.5: model `gpt-5.6-sol`; effort `high`; backend native; basis broad Darwin app context, consequential future parity oracle, and weaker validation than a mechanical contract test justify the eligible native flagship; fallback the same model at `max` only after missing requested behavior or unstable summaries survive targeted review.
- Ruling: isolate task 4.1 in one native worker while the OpenAI Codex controller retains lifecycle API review, integration, validation, ledger, and commit ownership — the new package is file-isolated, while partial-construction cleanup and concurrent idempotent close benefit from an independent race-tested implementation trace.
- Routing record for task 4.1: model `gpt-5.6-terra`; effort `high`; backend native; basis moderate lifecycle/concurrency reasoning, reversible new files, and strong focused race-test and vet oracles make the balanced native tier sufficient; fallback `gpt-5.6-sol` at `high` only after observable close-order, ownership, or concurrency defects.
- Ruling: isolate task 4.2 in a fresh native worker while the OpenAI Codex controller retains transport-ownership review, integration, validation, ledger, and commit ownership — the change remains confined to runtime but must align three established packages' dial, login, receiver, cancellation, and close contracts without duplicating them.
- Routing record for task 4.2: model `gpt-5.6-terra`; effort `high`; backend native; basis moderate cross-package lifecycle reasoning, reversible runtime-only edits, and strong real-login, race, vet, and audit oracles make the balanced native tier sufficient; fallback `gpt-5.6-sol` at `high` only after unresolved ownership transfer, version-rejection, or terminal-error defects.
- Ruling: isolate task 4.3 in one native worker while the OpenAI Codex controller retains message-boundary review, integration, validation, ledger, and commit ownership — the implementation is runtime-local, but it composes twelve existing mirror families and must preserve the old app's platform-neutral routing semantics without importing UI, audio, cursor, renderer, or predictor ownership.
- Routing record for task 4.3: model `gpt-5.6-sol`; effort `high`; backend native; basis broad legacy-message context, consequential future host API, and a weaker semantic-parity oracle than a mechanical boundary test justify the eligible native flagship; fallback the same model at `max` only after unresolved mirror coverage, reset, atomicity, or transcript-parity defects.
- Ruling: isolate task 4.4 in one native worker while the OpenAI Codex controller retains semantic-input API review, transport/sequence ownership review, integration, validation, ledger, and commit ownership — the implementation stays runtime-local but crosses predictor fixed steps, authoritative state, collision mirrors, correction smoothing, sequence allocation, and endpoint borrowing.
- Routing record for task 4.4: model `gpt-5.6-sol`; effort `high`; backend native; basis broad predictor and legacy-input context, consequential future ABI contract, synchronous transport caveat, and exact direct-predictor parity tests justify the eligible native flagship; fallback the same model at `max` only after unresolved replay, suspension, correction, send-failure, or lifecycle contradictions.
- Ruling: isolate task 4.5 in one native worker while the OpenAI Codex controller retains camera/target ownership review, integration, validation, ledger, and commit ownership — the implementation is runtime-local, but it must reproduce first-/third-person pose, mirror occlusion, target-ray, F5-edge, projection, and reset semantics without importing device or render-matrix state.
- Routing record for task 4.5: model `gpt-5.6-terra`; effort `high`; backend native; basis bounded runtime-only camera math, reversible files, and strong existing client/app camera tests make the balanced native tier sufficient; fallback `gpt-5.6-sol` at `high` only after unresolved pose, occlusion, target, reset, or parity defects.
- Ruling: isolate task 4.6 in one native worker while the OpenAI Codex controller retains queue/revision API review, integration, validation, ledger, and commit ownership — composing the established mesher remains runtime-local but crosses worker lifecycle, stale-result rejection, bounded publication, reset epochs, per-section revisions, and terminal overflow behavior.
- Routing record for task 4.6: model `gpt-5.6-sol`; effort `high`; backend native; basis broad concurrency and backpressure context, consequential no-loss publication semantics, and exact race-tested Mesher/runtime oracles justify the eligible native flagship; fallback the same model at `max` only after unresolved worker-lifecycle, stale-result, overflow-atomicity, or revision defects.
- Ruling: adopt the orphaned task-4.7 frontier — the parallel OpenAI Codex controller's worker had produced only the untracked red test `packages/client/runtime/step_test.go` (2026-09-16 15:02) with no implementation, ruling, or evidence and then stayed idle for three hours; the ZCode controller adopted the frontier under the user's continue-development direction, treated the red test as the requirements source, and corrected two defects in it — a protocol-invalid `BlockChanges` revision pair that `BlockChanges.Validate` rejects before any desync path can run, and one gofmt misalignment.
- Ruling: place the step frame aggregate in `packages/client/presentation` as an immutable `FrameSnapshot` — it is a pure composition of presentation snapshots and the later client-core ABI frame family consumes semantic records there — while runtime keeps only step bookkeeping such as revision/epoch counters, remote interpolation samples, and the outbound worker.
- Ruling: accept the review-verified queue-full residuals rather than widening task scope into a mirror rollback — a dropped chunk resync leaves the mirror's desync/resync-in-flight latch set and converges only through `ResetMirrors` or `Close`, and a dropped prediction input aborts `Advance` mid-loop with a sequence gap that later steps heal through authoritative reconciliation; both recovery contracts are documented at the enqueue failure path.
- Routing record for task 4.7 (ZCode controller, strict SDD): model inherited host default on the native agent surface (no model enum exposed); effort default; backend native `general-purpose`; basis bounded runtime-local implementation with the adopted comprehensive red test as a strong oracle plus race/vet/audit gates; fallback fresh native agent with an expanded brief, or the Z Code bridge live worker at reasoning high, after observable insufficiency.
- Ruling: migrate the legacy app onto runtime through an adoption seam rather than duplicated ownership — `runtime.Adopt` takes the app-constructed mirrors, predictor, used-not-owned receiver/sender, and tick/sequence seeds, and `AdoptMeshing` transfers ready-queue/revision/epoch ownership of the existing host mesher, so both Memory and remote TCP keep one behavior path and the app re-adopts when capture paths legitimately swap session objects.
- Ruling: restore legacy forget semantics by exempting payload-less drops from publication capacity — a world-forget burst of any size applies completely (drop-only transitions consume no ready capacity; the upsert guard still preflights atomically), and `DrainMeshedSections` bounds only upserts so leading drops drain within one frame exactly as the legacy loop did; two overflow tests were rewritten to pin revision-exhaustion terminality because forget bursts no longer overflow.
- Ruling: accept the coarser container-open outcome granularity, the zero fallback of the runtime-routed sequence allocator when adoption cannot be ensured (phase-gated unreachable), and pointer-form message silence in the app outcome switch — the reviewer traced all cursor-recapture paths and reachable message forms and found no observable divergence.
- Ruling: the task-4.8 implementer stalled irrecoverably after its final file writes — with the reviewer's root cause and minimal fix in hand (a deterministic test-fixture defect where barrier-encoded absent neighbors made five of six upsert targets mesh empty), the controller executed the prescribed fixture repair directly and re-ran every gate first-hand instead of dispatching a fresh agent for a ten-line prescribed edit.
- Routing record for task 4.8 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the largest node so far (three incremental ownership loops across app and runtime) with the strongest oracle in the campaign (forty pinned app test files, characterization parity gates, and a recorded visual differential baseline); one implementation round, one review-driven fix round, and one controller-executed prescribed repair; fallback fresh native agent or Z Code bridge live worker at reasoning high, unused.
- Ruling: delimit the client C ABI for the boundary audit at the source level — runtime legitimately composes `packages/client/client` mirrors, so the audit discovers every exported top-level declaration of that package's cgo entry files as the ABI surface and rejects guarded references to it, dot-imports, `import "C"`, and raw `mornlea_client` tokens, with six liveness anchors failing loudly if the cgo surface moves.
- Ruling: accept two review-verified residuals in the boundary audit and defer their closure to task 11.1 — owner-side re-exports of cgo symbols in non-cgo files of `packages/client/client` are outside the discovered surface, and the guard scans the two top-level directories non-recursively, so future subpackages join only when 11.1 registers the sole allowed dependencies; the one-way edge table does not yet enumerate runtime/presentation, which 11.1 also owns.
- Ruling: the controller executed the reviewer-prescribed hardening directly — the OS family ban grew from exact `os`/`os/exec` matches to the whole `os/` import subtree, and the platform gate now pairs both guarded architectures with every guarded OS so arch-gated forks and filename suffixes are rejected on every host.
- Routing record for task 4.9 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a single-file audit addition with the package's own drift-probe machinery as the oracle; one implementation round, clean review, two controller-executed prescribed fixes; fallback unused.
- Ruling: make the client-core header the single ABI source of truth with independent both-side pins — `include/mornlea_client_core.h` defines identity, status order, `MCx1` magic tags, 8-byte-aligned fixed headers, limits, and the seven feature-family descriptors; Go derives constants through cgo and validates layout via `unsafe` facts, Rust mirrors constants and `#[repr(C)]` layouts while parsing the header text, and both sides assert both-direction define-set equality so any drift fails loudly; mirrored limits pin the frozen presentation/runtime constants and the four client-core-only bounds are labeled as pilot bounds.
- Ruling: route the Godot-visible bridge identity through the pinned ABI constants — `bridge.rs` no longer carries duplicate client-core major/minor literals and returns `abi::ABI_MAJOR/MINOR`, and the ambiguous-literal escape (Go base-0 octal vs Rust strict decimal on leading-zero defines) is closed by rejecting redundant leading zeros in the Rust parser.
- Routing record for task 5.1 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a definition-plus-cross-check node with the engine-ABI precedent (`mornlea_engine.h` + `nativeabi` + `ffi.rs`) as the pattern source and the new consistency tests as their own oracle; one implementation round, one prescribed fix round; fallback unused.
- Ruling (correction to the task-5.1 evidence record): the task-5.1 validation row records `cargo clippy -p mornlea_godot --all-targets -- -D warnings` as passing, but independent review during task 5.2 reconstructed the fcf17e6f tree and proved that command failed there with 63 `dead_code` errors — the 5.1 mirrors were consumed only by tests, which the lib target does not compile. The controller closeout for Rust-touching nodes now includes clippy; the 5.2 module-scoped allowances with removal conditions legitimately repair the break.
- Ruling: the registry's cross-language contract is the accept/reject predicate, not rejection-reason precedence — Go validates in a single input-order pass while Rust scans unknown families first, and both document that the difference is diagnostic-only; `record_limit` is the bounded batch capacity (byte bound for byte-payload families) with `record_bytes` zero marking deferred variable-length layouts, documented on both sides.
- Routing record for task 5.2 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a registry-and-negotiation node chained onto the 5.1 pins with both-language red-first coverage; one implementation round, clean review, two controller-executed prescribed comment fixes plus this ledger correction; fallback unused.
- Ruling: keep the session table producer-internal — the fixed eight-slot capacity, slot-plus-generation handle encoding, tombstone-idempotent destroy, and `StatusInvalidState` mappings for capacity and destroyed-handle status are producer bookkeeping, not wire vocabulary, so the frozen header and both pin suites stay untouched; request-array shape failures map to `StatusInvalidArgument`, family content failures to `StatusInputRejected`, and producer-wide identity mismatch to `StatusABIMismatch`.
- Ruling: accept the review-verified residuals — the invalid-state path's size-out-param no-write property is structurally guaranteed and canary-pinned on adjacent paths rather than re-canaried per path, and the panic-conversion seam is a package-level swap that is safe only under the package's sequential test discipline (documented at the seam, the swap site, and the reset helper).
- Routing record for task 5.3 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the first export-surface node with multi-defect validation-order proofs and race coverage as its own oracle; one implementation round, clean review with four nits (three one-sentence comment fixes applied by the controller, one accepted residual); fallback unused.
- Ruling: own connection establishment inside the session slot with one goroutine and a fixed lock order — `beginSessionConnect` spawns under the table mutex so destroy can never race the spawn, the goroutine takes only the session mutex (table→session order everywhere), disconnect cancels the context feeding `NewRemote`'s dial/login and joins the goroutine through a done channel, and the slipped-through-runtime self-close is provably disjoint from the disconnect close (exactly-once additionally backstopped by the runtime's own close-once). Phase words are an identity cast of `presentation.SessionPhase` kept as producer-side constants (`Login` never reported; terminal disconnect is the `StatusDisconnected` signal with no output write); the later Rust FFI task must pin these phase words cross-language — flagged so the header addition is not forgotten.
- Routing record for task 5.4 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the first async/lifecycle-critical node with runtime's 4.2 seam patterns as the fake-transport source and deterministic channel-based race tests as the oracle; one implementation round, approved review with one minor test gap and one comment nit, both closed in one fix round whose slip-through test is deterministic by construction (parks on context done rather than timing); fallback unused.
- Ruling: one input batch is one frame — the wire `MCN1` batch folds arrival-order records last-wins into exactly one `runtime.SemanticInput` submission, because `SubmitInput` overwrites the frame input wholesale and the legacy host resends the full held-state every frame; `Look` carries the absolute yaw/pitch pose (device deltas accumulate host-side, per the runtime contract) as float32 bits, and a wrong batch magic maps to `StatusABIMismatch` because the header's own status text defines magic as record identity rather than content.
- Ruling (task-text deferrals, review-verified): the input family ships no text records because `runtime.SemanticInput` has no text channel and inventing unconsumed vocabulary is forbidden — text capacity arrives as a reviewed compatible family addition when the runtime gains the channel; likewise the registry input descriptor keeps `record_bytes` 0 because the Rust mirror pins `(3,1,128,0)`, and the coordinated cross-language descriptor update is owned by the Rust FFI consumer task.
- Routing record for task 5.5 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a wire-layout and validation node whose oracle is runtime's own semantic validator plus a real-transport integration test; one implementation round, clean APPROVED review with no code findings (one closeout-process reminder, one no-action nit); fallback unused.
- Ruling: serialize the step export per session and retain the latest completed result — a dedicated step leaf lock acquired without the session mutex enforces the runtime's single-driver step contract at the ABI boundary (the concurrent test proves revision uniqueness, not just crash absence), and the retained frame/world result survives teardown so the later frame pull can report the terminal state after a disconnect; pulls read the latest completed step and a new step overwrites it.
- Ruling: map non-terminal step errors to `StatusInternal` — queue-full backpressure is producer state with a valid caller request, so the header's input-rejected promise of no consumed state is factually wrong there and no narrower frozen code exists; a future compatible status-code addition for retryable backpressure is the designated fix and must be considered by the error-precedence matrix task.
- Ruling (runtime-owned edges recorded for follow-up): extreme representable durations can wrap the predictor's residue accumulator and the entity interpolation window inside `packages/client/runtime` (pre-existing since the step task, bounded output, no panic) — the ABI delegates the elapsed domain to runtime unchanged; and a receiver-driven self-close racing an in-flight submission can classify that one call as `StatusInternal` instead of `StatusDisconnected` until the next observation (benign, inherited from the input family).
- Routing record for task 5.6 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a boundary node delegating to the already race-tested runtime step with per-session serialization and retention as the only new concurrency; one implementation round, APPROVED review with no code findings (one comment softening applied, three ledger-record obligations discharged here); fallback unused.
- Ruling: a world pull serves the latest retained batch at snapshot time and commits consumption only when the session generation is unchanged — a step landing mid-pull cannot have its fresh batch swallowed by an old pull's commit, the caller still receives one complete old-generation batch, and the new batch stays consumable; two concurrent consumes have exactly one winner under a family leaf lock never held with the session or step locks.
- Ruling: the world wire body is producer-side vocabulary under the frozen `MCW1` header — 32-byte operation records (kind/dimension/signed position/quad count/revision) followed by the packed-quad payload in presentation order, with the dimension codes pinned to 0/1, the maximal consumer allocation ceiling pinned at 4,325,408 bytes, and the no-batch outcome being `StatusOK` with size zero; terminal sessions still pull and consume their retained batch. The overlap ruling (size word inside the buffer span) is currently world-family-only — the consumer FFI task decides whether it generalizes to the identity family.
- Routing record for task 5.7 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis an encoding-plus-two-phase node over the already-frozen presentation batch value with generation-guarded consumption as the only new concurrency; one implementation round, APPROVED review with three nits (dimension literal pins and the allocation ceiling applied by the controller; overlap generalization deferred to the consumer task); fallback unused.
- Ruling: frame and status pulls are non-consuming — a frame is presentational state the host may read repeatedly between steps, so no commit bookkeeping exists and only a new step replaces the served snapshot; pulling after disconnect serves the retained terminal frame with `StatusOK`, and the no-frame outcome is `StatusOK` with size zero. The frame wire is a pure function of the snapshot and frozen constants (pinned by an independent literal-offset reference encoder), which is the optional-family stability proof. The terminal-cause classifier matches producer wrapper markers structurally where sentinels do not exist; the safe downgrade direction (unrecognized causes become internal) and its producer-side drift blind spot are documented at the classifier. Every registered block display name is pinned inside the wire's target-name budget so a long name cannot silently degrade the frame family.
- Routing record for task 5.8 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the last data-plane encode node over the frozen presentation frame value with no new concurrency (non-consuming pulls); one implementation round, APPROVED review with two minors and two nits, all four prescribed fixes applied by the controller (bound-attribution comments, block-name budget pin, classifier comment, registry-variant lookup); fallback unused.
- Ruling: the error-precedence matrix is the single cross-export precedence truth — combined-defect cells prove the documented order (identity before families before handle before state before shape before content, with the length bound judged before any buffer read), each cell asserts vocabulary, canary outputs, and recovery, and the three recorded conflations (queue-full backpressure folded into `StatusInternal`, the transient terminal-race misclassification, panic conversion) appear as explicitly labeled matrix rows; the fuzz corpus covers all eleven exports offline with per-iteration table resets and seam-restored sessions, and the Rust matrix is honestly scoped pre-FFI: an exhaustive status-bucket dispatch fail-closed to internal plus a ninety-nine-cell negotiation matrix serve as the binding task's contract baseline.
- Routing record for task 5.9 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a validation-only node whose oracle is the already-shipped exports plus the reviewer's own combined-defect probes; one implementation round, FINDINGS review with five prescribed test-level fixes (one real seed defect: the create seed packed the ABI identity halves in the wrong order so the success path was never seeded), all closed in one fix round; fuzz stayed at zero findings across millions of execs before and after; fallback unused.
- Ruling (task-5.10 pilot deviation, review-verified): the core build ships `libmornlea_client.dylib` beside the GDExtension, Go core, and engine dylib — a recorded deviation from design decision 8's distribution-unit enumeration, not a boundary violation: runtime, presentation, and the core reference no client-ABI symbol (the boundary audit's source-level delimitation holds; the full audit is green), and the link exists only because `packages/client/client`'s darwin cgo entry files sit in the same Go package as the mirrors runtime composes. Verified: no library initializers and zero production call paths into the linked `mornlea_client_*` symbols, but dlopening the core eagerly loads WebKit, AppKit, and Metal through that dylib — the P7 cold-start and RSS report must account for it. Recorded direction: split the darwin cgo entry files out of the mirrors package (or extract the mirrors into a cgo-free package) so the core drops the client-dylib link; this must land before any exported distribution and before the P7 report if the load cost is measurable.
- Ruling: the client-core build script owns the complete colocated unit honestly — one host-arch Go slice inside the established `macos-universal` tree (true universal reserved), the cgo-generated header deleted so the committed header stays the only contract, both engine libraries copied from the deployed build with `@loader_path` rpath surgery and ad-hoc re-signing per the established deploy discipline, and `--verify` rebuilding then proving all twelve exported symbols by `nm`, canonical-header presence, a real dlopen roundtrip from a scratch directory outside the repository with every expectation parsed from the committed header (BSD-sed-safe literal parsing), and dependency resolution through `@loader_path` with no non-relative rpath accepted.
- Routing record for task 5.10 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a packaging node over the finished exports with the established godot build-script conventions as the pattern and the script's own `--verify` as the oracle; one implementation round, FINDINGS review ruling the client-dylib link an acceptable-but-must-be-recorded deviation (recorded above) with one minor and two nits, all closed by the controller including a real BSD-sed incompatibility the first fix introduced; fallback unused.
- Ruling: confine every `unsafe` in `mornlea_godot` to the client-core FFI module — one `unsafe extern "C"` block declares all twelve producer exports with typed status/handle newtypes, and a recursive source-scan test strips comments and strings with a fail-closed lexer (panicking on every raw-string opener and char literals) so the only permitted `unsafe` outside the module is the pinned godot-rust `ExtensionLibrary` impl; symbol names, full signatures, status texts, and type widths are pinned both-direction against `exports.go` and the canonical header, and tests never reference the extern items as values so no dylib link is required before the bridge task.
- Routing record for task 6.1 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a declaration-and-pinning node over the finished Go producer with signature parity as the review's burden; one implementation round, FINDINGS review with clean signature fidelity and two lexer/scan hardening minors plus two nits, all closed in one empirically re-probed fix round; fallback unused.
- Ruling: resolve the client core at runtime beside the extension image, never at link time — `make rust` must build the workspace from a clean checkout where no producer dylib exists, so the extension carries zero undefined producer symbols and `dlopen`s `libmornlea_client_core.dylib` found via `dladdr` (`RTLD_NOW|RTLD_LOCAL`, vtable cached, deliberately never `dlclose`d), with every resolved signature pinned to the frozen extern declarations and an unavailable core failing closed as an internal status.
- Ruling (review-verified forward obligations): the exported-application layout does not yet place the core dylib where the resolver finds it — macOS exports move descriptor-referenced GDExtensions into `Contents/Frameworks` and pack unreferenced dylibs into the PCK, so before any exported distribution the descriptor/export pipeline must carry the core dylib as a filesystem resource and the export qualification scene must assert a live producer identity; additionally, the contract scene always closes before quitting, so task 6.3's host integration must quit once with an open session and prove clean teardown markers. Accepted residuals: off-thread bridge calls answer an internal status but still construct Godot core-type values on the calling thread (memory-safe, documented), and the content-level rejection of wrapped step arguments requires an online session (the offline contract now proves no panic or misroute).
- Routing record for task 6.2 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the campaign's heaviest integration node (Rust bridge surface + dynamic producer resolution + a new live gate + the hundred-cycle smoke) with the seam-tested cargo suite plus the real-process contract and smoke gates as the oracle; one implementation round, APPROVED review with two minors and three nits (one scene assertion applied by the controller, the rest recorded); fallback unused.
- Ruling: the host and every feature acquire the typed bridge node themselves through `get_node` plus the identity cast registration — the pinned Py4Godot runtime's receiver-side argument conversion only knows generated engine classes, so passing a project-class object across script-module calls strips the owned pointer (empirically confirmed by review against the runtime's cast machinery); `bind_host` therefore carries the bridge's scene path, a missing or wrong node fails closed at capability negotiation, and the retired static facade stays only as the coexistence-qualification subject.
- Ruling (forward obligation for the first enabling feature task): the production skeletons still declare the retired `bind_host(services)` object signature and every production manifest spells families in the word grammar, while the implemented table is keyed by numeric registry identifiers — enabling any manifest (task 6.5's session feature first) requires rewriting it to numeric family strings and its skeleton to the path contract; the divergence is inert today with all manifests disabled and nothing binds the skeleton signatures.
- Ruling: the open-session teardown obligation from task 6.2 is discharged as a repeatable gate — the `--host-open-session` mode drives the real host with a live session created against a denied address, quits without close, and asserts the drop-based close lands between the Python success marker and Rust main-loop deinitialization with no error or leak lines.
- Routing record for task 6.3 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a Python-host rewiring plus two audit disciplines with the real headless gates as the oracle; one implementation round, FINDINGS review with three minors and two nits — the checker mutation tests, the `from struct import pack` bypass closure in both scanners, the grammar-comment accuracy fix, and residue cleanup applied by the controller, the skeleton/manifest divergence recorded as the forward obligation; fallback unused.
- Ruling: every pull family routes through per-session reusable buffers — each family's backing starts empty, grows to exactly the declared required size at 8-byte word granularity, never beyond the frozen contract limit (an over-limit requirement on either the query or the retry fails closed as an internal status before any allocation), and never shrinks (worst case one maximal world backing per live bridge session); world and frame pulls track the served publication identity and refuse strictly older records while equal identities stay legal for every family, with identities reset when a producer session is created or closed; the identity fields are trusted without re-validating header magic because a producer success commits a complete frozen header by construction (documented trust boundary — the failure direction is fail-closed).
- Routing record for task 6.4 (ZCode controller, strict SDD): model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a pure-Rust buffer layer over the established two-phase driver with the scripted seam as its oracle; one implementation round, FINDINGS review with two comment-scale minors and two nits, all four applied by the controller (one of which first introduced and then corrected a wrong padding literal caught by the focused rerun); fallback unused.
- Ruling (task 6.5 adoption, four-point orphan check): the previous controller session left 6.5 in flight (last in-flight file mtime 13:27, adopted at 18:04, ~4.6 h idle with no ledger row, no checkbox, and untracked test assets) — the artifacts were adopted as the requirement source after empirical review, not trusted wholesale. Verified contract points: the manifest's numeric family strings `1@1.0`/`2@1.0`/`7@1.0` discharge the recorded 6.3 forward obligation (the implemented `FamilyTable` is keyed by `str(family)` from `feature_families_json`; the old symbolic names could never match); `dependencies` was emptied because the `platform.desktop.lifecycle` skeleton stays disabled and a required feature with a disabled dependency would hard-fail plan resolution; `reset_policy = "reset"` honestly matches the refresh-only `reset_feature`.
- Ruling (task 6.5 accepted residuals, review-prescribed): the two `feature_root.tscn` labels share the origin so they would overlap in a visible window — deferred until the feature becomes user-visible (headless checks read label text, so the deferral is invisible to every current gate); the Python-side phase/terminal-cause word literals (`PHASE_DISCONNECTED = 5`, the `CAUSE_DISPLAY` keys, and the check's `(0, 5, 1)` triple) are verified against the producer today but carry no cross-language pin, so a producer renumbering would surface as a fail-closed display or check failure rather than a named contract violation — a later task (6.6 catalog integration or the section-11 audits) should add a Go audit pin mirroring the `abi.rs` header-pin pattern. Forward obligation for the first stepping consumer: the Play phase is display-mapped but unreachable end to end because neither the session feature nor the host calls `session_step` yet, and only the dial terminal cause is exercised offline — when the world/player_view tasks (P4/P5) or the host own stepping, the online gates must cover the remaining phase and non-dial cause transitions, and reconnect-after-terminal deliberately requires an explicit `close_connection` first (no-fabrication behavior the connection-UI builder must preserve).
- Routing record for task 6.5 (ZCode controller, strict SDD, adoption): reviewer-only dispatch — the implementation was the orphan session's adopted work, so the strict-SDD review responsibility ran against the actual diff with the controller's green gate evidence supplied; model inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a bounded single-node review (Python feature scene + Rust typed decoder + bridge export + guards) with clear acceptance criteria and no model override exposed by the host; APPROVED verdict with zero blocking findings — two P2 process items (smoke evidence recorded below, partial staging enforced at commit) and four P3 nits, of which two were applied by the controller per the task-4.9 micro-edit precedent (the `status_decode` module-doc forward-compat wording and the `Control.pyi` stub-existence audit line) and two deferred by the ruling above; fallback unused.
- Ruling (task 6.6 review-prescription rejection, evidence-backed): the reviewer's suggested `activate:{id}:{epoch}` trace assertion was verified against the host and REJECTED — `feature_host.py` appends that entry BEFORE calling `activate_feature`, so it is emitted even when activation subsequently fails and is not a success record; sibling liveness stays proven by the `instantiate:` trace entry plus `active_count` and the exact disabled list. Recorded so a future pass does not re-apply the suggestion as an unverified "hardening".
- Ruling (task 6.6 accepted residuals, review-prescribed): the missing-family catalog carries a single manifest, so "MUST NOT partially enable dependents" is exercised through the generic plan-failure early return rather than a declared dependent feature — a dependent-manifest fixture variant is a cheap addition for the section-11.2 architecture gates if direct evidence is wanted; and "mirror unchanged" is proven through the no-session observables (family-table byte identity plus the typed status word `MORNLEA_CLIENT_STATUS_INVALID_STATE` with zeroed fields) because these fixtures legitimately never open a session — when P4/P5 features drive live sessions, the same before/after sampling pattern needs a live-mirror variant.
- Routing record for task 6.6 (ZCode controller, strict SDD): one implementer then one independent reviewer, both inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a bounded test-only node with two in-repo precedents as the pattern source (stub-mode contract check plus the real-bridge session-feature probe) and the new `--bridge-integration` gate as its own oracle; implementer completed in one round with self-run mutation probes proving both scenarios' assertions non-vacuous; reviewer APPROVED with three P3 nits — the bilingual README mode lists and the epoch-comment accuracy applied by the controller, the third prescription rejected by the ruling above; fallback unused.
- Ruling (task 7.1, review-verified decode judgments): the pilot's Rust-side packed-quad decode is pinned to the Go `mesh/quad.go`/`terrain.wgsl` truth with five verified judgment calls — the MCW1 world family carries one combined per-section payload so the water split is re-derived by `stream_of` (`Mat == LayerWater`, layer 28, the exact `SectionScheduler` predicate); the door layer stays opaque because Go's `isCutoutLayer` excludes it (cutout = leaves 12, glass 13, torch 59, the plant set); plant-face bits 13..19/55..62 are tolerated rather than rejected to keep the two consumers' acceptance domains identical to `UnpackQuad`; diagonal normals are stored unit-length (direction-preserving) because Godot needs well-formed attributes; and the daylight blend stays out of the decode because it is a pure function of the frame uniform — the Godot material completes it from the raw sky/block nibbles (formula pinned against `terrain.wgsl`). The decode module is pure safe Rust with zero `unsafe`, and its `#![allow(dead_code)]` is contracted for removal when task 7.2's worker consumes it.
- Ruling (task 7.1 accepted residuals): `check_world_batch` counts sections while the Go bound counts operations (upserts plus drops) — task 7.2's caller must pass the true operation count and the parameter renames to `operations`; the cutout-set mirror has no mechanical Go-side guard (unlike the plant set's assets test), so every future Go cutout layer that becomes a block material must be mirrored manually — drift misclassifies as opaque, a visual-quality failure; and the UV convention is deliberately wgpu's with the Godot-side flip owned by the 7.3/7.4 material tasks, never the worker.
- Routing record for task 7.1 (ZCode controller, strict SDD): one implementer then one independent reviewer, both inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis a bounded pure-compute module whose oracle is cross-language source-pinning against three in-repo truths, with the reviewer hand-recomputing face/UV/lighting tables; implementer honored test-first (20/29 red against stubs, and the parser caught a real off-by-one before implementation) and recorded five judgment calls; reviewer APPROVED with three P3 findings, all dispositioned by the rulings above with no code changes required; fallback unused.
- Ruling (task 7.2, review-verified worker design): the pilot's mesh prepare service is a single background `std::thread` over two byte-accounted queues — pending and completed each bounded at one maximal world batch of packed-quad bytes (`MAX_WORLD_BATCH_QUADS × 8` = 4 MiB, entries cost `max(len,1)×8`), exact-fit accepted, `Full` hands the payload back so a rejected submit never drops work; ownership moves at submit (`Vec<u64>` moved on `Queued`), enforcing the project's immutable-after-send rule in the type system; backpressure parks the worker (never the main thread) and every main-thread API is try-style; epochs filter at a single publication point (`epoch == current && epoch > retired_through`) with in-flight completing then discarding; panics are caught outside every lock as per-job `STATUS_PANIC` results with mutex-poisoning recovery documented; close is idempotent, discards pending and completed, joins bounded by one in-flight decode, and Drop equals close. Verified edge for 7.4: `cancel_pending(E)` retires through E permanently, so the bridge must guarantee strictly monotonic epochs and never cancel-then-resubmit under one epoch. The completed queue's retained value is expanded geometry (~25× packed bytes, worst case ~110 MiB), so 7.5's per-frame drain must keep it near empty; production observability counters and any finer error vocabulary are 7.4/7.5 additions.
- Routing record for task 7.2 (ZCode controller, strict SDD): one implementer then one independent reviewer, both inherited host default on the native agent surface; effort default; backend native `general-purpose`; basis the campaign's first concurrency-bearing node with the seam-tested decode as its oracle and the Go Mesher as vocabulary precedent; implementer delivered in one round with five repeat-stable gate runs; reviewer APPROVED with five P3 findings — three comment/assert-clarity fixes applied by the controller (bounded-time wording, the epoch-discard test's mechanism comment, one obfuscated assertion), the cfg-gate count corrected in this ledger (eight quad_decode items gated plus `axis_vec` deleted, each with an inline justification), and the panic-seam observation accepted as boundary-equivalent with no change; fallback unused.

- Ruling (task 7.3, controller-settled structure): the world feature manifest STAYS `enabled = false` until task 7.6's terrain gate proves rendering end to end (spec: unimplemented capabilities must not be advertised); the grammar moves to the numeric `5@1.0`, `dependencies` keep `("session",)`, and the audit implemented-map stays session-only. The terrain_near compartment is internal (no manifest of its own), anchored on a Node3D (one minimal typing stub plus one audit list line, following the Control.pyi precedent), and the three ShaderMaterials deliberately embed no texture — the raw `.rgba8` atlas is not a Godot texture format, so the Rust renderer of 7.4 builds the layered texture and assigns the `atlas` uniform. This shader layer owns the recorded 7.1 residuals: the Godot-side UV adaptation resolves to identity (documented with the single marked negation site), and the daylight lighting completion (`base`/`night_amb`/`night_expo` per the pinned `terrain.wgsl` formula) is encoded here; the sampler hint `filter_nearest_mipmap` is the honest coarsening of wgpu's mag-Nearest/min-Linear split, routed to the 7.6 visual gate.
- Ruling (task 7.3 review-loop evidence): the first review returned CHANGES_REQUIRED on a real P1 no static gate could catch — `bind_host` passed the manifest PATH into `json.loads`, so bind could never succeed (python-check is static, asset-check wraps the generator, audits are structural, and the disabled feature never instantiates); the fix round additionally caught a missing `MornleaClientBridge` identity-cast registration that raised `KeyError` on the POSITIVE bind path (the `smart_cast` dict lookup, mechanically confirmed by re-review). Both were closed with positive-and-negative probe evidence (real manifest binds empty; bogus path returns the exact stable error with no state stored), and the re-review APPROVED. Forward note for enablement: Godot's `FileAccess.get_file_as_string` prints an engine-side stderr line on a failed open beside the stable error string.
- Routing record for task 7.3 (ZCode controller, strict SDD): one implementer then one independent reviewer at the inherited host default; effort default; backend native `general-purpose`; basis a pattern-following Python/resource node over the session precedent with static gates as its oracle — the oracle gap (no runtime gate while the feature is disabled) is exactly what the review round and the implementer's scratch probe closed, which argues for keeping a bind-probe in later feature tasks until 7.6's gate exists; one CHANGES_REQUIRED round (R1 reused the implementer) plus one re-review, both findings-plus-one fixed; fallback unused.

- Ruling (task 7.4, review-verified design reading): design decision 5's "section identity is (dimension,x,y,z,revision)" is implemented as the coordinate keyed with per-entry revision tracking — the only implementable reading of atomic whole-section replace, since a revision-keyed map cannot find the section being replaced; revision policy exactly matches the Go truth (`Validate` refuses zero, `ValidateAfter` refuses `<=` for existing keys with no tombstone, so a batch-valid drop of an absent section is a successful renderer no-op, and empty payloads are rejected as "use a section drop"). The failure contract is publish-last: every surface is built before any live-state change, the instance registration is the single visibility step, a mid-submission failure frees only the partial NEW mesh and records no revision — so the same-revision retry after backend recovery succeeds, and task 7.5's batch loop may retry or discard freely but MUST NOT advance its own per-section revision bookkeeping on a renderer failure. Accepted residuals: the u32→i32 index clamp converges to rejection through the surface-count postcondition (no reachable corruption; decode bounds indices at 98,304); borrowed material/scenario RIDs carry no postcondition by documented contract, making 7.6's live-engine run the integration-wiring detector; `sections_in_dimension` is a linear walk that 7.5 should consume via a per-dimension `BTreeMap::range` prefix if it iterates per frame.
- Routing record for task 7.4 (ZCode controller, strict SDD): one implementer then one independent reviewer at the inherited host default; effort default; backend native `general-purpose`; basis a seam-architected table module whose oracle is the scripted backend's leak/double-free panics plus free-order history, following the bridge's scripted-session precedent; implementer delivered in one round (13 named-gate tests, full crate 120); reviewer APPROVED with four P3 findings, all dispositioned as recorded rulings with no code changes; forward obligation carried: the production `RenderingServer` backend is compile-verified only until 7.6's terrain gate executes it live, and the mesh_worker/terrain_resources `allow(dead_code)` removals land when the bridge session owns the pipeline; fallback unused.

- Ruling (task 7.5, review-verified budgets and view policy): the terrain frame stage uses whole-section budgets pinned to the replaced client's `SteadyFrameMeshWorkMax = 64` (uploads) with an equal drop budget (a drop is two RID frees), reclamation strictly before uploads each frame, and FIFO uploads; the view policy is horizontal Chebyshev on section X/Z with an inclusive boundary, no Y term and no hysteresis, matching both the server's streaming comparison and the old client's `DropOutside` band. REVIEW-CORRECTED EXACTNESS CLAIM: the Go core today declares the login MINIMUM (2) with view-distance configuration deferred, so the pinned keep-band of 33 (baseline 32+1) is strictly wider than the live stream radius — reclamation idles in production (safe direction, no churn, no loss), and the bridge integration MUST derive or assert the declared value before reclamation becomes load-bearing, because a future declaration above 32 would stream past the band. Failed upserts drop the expanded payload with no retry queue (bounded memory) and advance no revision bookkeeping, so the same-revision retry stays legal; metrics count packed bytes at accept, expanded bytes and worker-side prepare duration at drain (publication-side worker discards stay uncounted), and the whole in-frame render phase as upload duration; peaks survive reset as session high-water marks. The Node prohibition is a structural proof: one-quad and maximal 24,576-quad sections both hold exactly two table-owned RIDs.
- Ruling (task 7.5 accepted residuals): cross-dimension reclamation filtering is implemented but not yet exercised at stage level (renderer-level coverage exists) — the 7.6 integration tests should hold a non-camera-dimension section; the Vec-snapshot `sections_in_dimension` folds into the range iterator when its last test-only consumer turns production; `reset()` is queue-bounded but not frame-budgeted, so the bridge drives it from re-entry/teardown, never per frame; producer-driven `drop_section` calls are outside the drop budget, so the bridge bounds batch-driven drops per frame; the bridge calls `frame` exactly once per `_process`, owns the `Submit::Full` retry policy, and must map worker-spawn failure to a decodable ABI word; camera section derivation must match the server's floor-of-block-position rule or the inclusive boundary drifts by one section.
- Routing record for task 7.5 (ZCode controller, strict SDD): one implementer then one independent reviewer at the inherited host default; effort default; backend native `general-purpose`; basis a composition node over two seam-tested modules with named in-repo truths (SteadyFrameMeshWorkMax, subscription radius, tileChebyshev) as its derivation oracle; implementer delivered in one round (10 named-gate tests, full crate 130, two sibling-module integrations that resolved recorded 7.4 residuals); reviewer APPROVED with one P2 (the view-radius doc overclaim, corrected by the controller as a comment fix that states the conditional exactness and the bridge obligation) and five P3s — three doc/derive fixes applied by the controller (upload-duration scope, expanded-bytes scope, the invalid `Default` epoch derive dropped) and two recorded as residuals; fallback unused.

- Ruling (task 7.6, review-verified Go core deviations): two production changes in `mornlea-godot-core` were REQUIRED for the world family to ever publish and are approved as pilot-scoped — `openRemoteSession` now configures `pilotMeshOptions` (placeholder registry snapshot because the mesher consumes only layer indices/opacity, never generated pixels; `ReadyCapacity` equal to the legacy `MaxMeshReadyCapacity`; `AtlasRevision(1)` byte-identical to `AdoptMeshing`'s host-without-atlas pin; 2 workers as a deterministic pilot cost vs the legacy `NumCPU-2`), and `stepRuntime` now composes `AdvanceMeshes(meshBudget)` before `Step` because `Step` only drains already-scheduled results while the legacy app drove scheduling from its own frame loop — each half bounded by the caller's single mesh budget (per-call total at most twice it, docs corrected to say so), zero-budget steps degrade identically, and legacy app files are untouched. The step-export e2e composition is proven by the opt-in terrain gate, not by unit tests that replace the seam.
- Ruling (task 7.6 controller closeout gap, self-recorded): task 7.3 added `make godot-asset-check` without registering it in the audit's entrypoint recipes, breaking `godotMakefileScriptReferenceViolations` at HEAD — undetected because the 7.3 controller closeout ran only the focused audit set instead of the full package (the session-node precedent). Task 7.6 registered both `godot-asset-check` and `godot-terrain-check` with pinned recipes; lesson recorded: any node that adds or renames a make entry point must run the FULL audit package, not the focused set.
- Ruling (task 7.6 integration design, review-verified): the bridge owns the terrain pipeline — typed `terrain_attach` (materials loaded and validated Rust-side, atlas built from the manifest-validated `.rgba8` as a plain non-sRGB layered RGBA8 array with the 5 precomputed mips and assigned to the `atlas` uniforms), `terrain_ingest_world` (MCW1 decode fail-closed — magic/layout to ABI mismatch, shape to input rejected, zero-quad upserts rejected by kind after the fix round; producer drops bounded at 64/call; a `Submit::Full` replay queue byte-bounded for upserts and discipline-bounded for drops with newer-op supersession so a deferred upsert can never resurrect a dropped section), `terrain_frame` (exactly one stage frame, camera derived Rust-side by the server's floor rule, dimension following the newest ingested batch), and `terrain_sections_json` (the structural no-stale proof source). World is enabled and required; its Python stays lifecycle-only (attach on activate, exactly two typed calls per `_process`). The transcript helper synthesizes protocol-legal frames validated through `ValidateServerPacket` (the recorded corpus's LoginSuccess echoes a different pilot identity and its chunk frames carry unrelated content, so synthesis is the honest path); pacing is 1000 ms because the protocol has no consumption ack; the scenario lives in a subdirectory because the top-level corpus is pinned by a presentation test.
- Routing record for task 7.6 (ZCode controller, strict SDD): one implementer then one independent reviewer at the inherited host default; effort default; backend native `general-purpose`; basis the campaign's largest integration node (Go helper + core deviations + bridge surface + Python enablement + gate), oracle the new end-to-end gate plus the full battery; implementer needed one round plus one prescribed fix round (five P3 hardenings: frozen-ABI step docs, replay-queue bound precision, atlas-RID ownership totality on the spawn-failure arm, manifest layer-count derivation from the pixel file, zero-quad upsert rejection by kind with a decode test) — fix-round verification was a controller spot-check of the three behavioral sites plus a controller rerun of the terrain gate, proportionate to an APPROVED-with-P3 verdict per the 7.2/7.4/7.5 precedent; three findings recorded without action (delta-floor revision assertion would over-constrain against the mirror's sky-dirty volume; per-frame terrain error words visible only via stage timeout — a P5 diagnosability note; frame-budget vs wall-clock pacing sensitivity on fast CI hardware); fallback unused.
- Ruling (task 7.6 residuals for 7.7 and later): daylight uniform stays at the material default 1.0 (the environment task owns per-frame updates); view-distance reclamation stays inert (login-min declaration, recorded 7.5 conditional); the session-feature probe is now heavier (terrain attach runs during its catalog activation, building the atlas and spawning the worker each run); `Submit::Closed` mid-session stalls surface only as a stage timeout.

- Ruling (task 7.7, review-verified smoke design): the real-server terrain smoke starts `mornlea-server` from a temp root with `--listen 127.0.0.1:<derived-port> --world <temp>/world --seed 42 --config <temp>/absent-config.json` — the ABSENT config is deliberate isolation (missing file falls back to `config.Defaults()` without reading the user config dir, and zero companions take the early bootstrap path), so the repository's saves/worlds stay untouched; the loaded criterion is the transcript gate's initial-snapshot core (phase Play plus at least one live section) without the recorded-scenario revision/coordinate pins a live stream cannot promise; the error-word policy tolerates bounded pre-load transients and fails on ANY post-load terrain word plus leaving Play — closing the recorded 7.6 diagnosability residual for a real-server driver; 300 fixed frames equal five simulated seconds at the pilot step cadence; the driver's own step+ingest+frame calls run before the feature subtree's pair (root-first processing), the second ingest observing the empty no-batch outcome while the second frame re-serves the identical non-consuming snapshot and drives the stage once more — harmless by fixed per-drive budgets and exactly-once result consumption, and documented as an approximate doubling of the stage frame counter. Reaping is zombie-aware TERM→10s→KILL→per-PID wait→kill-0 plus pgrep over the unique temp root, verified by two forced-failure paths (bind-fail port 1; a failing Godot binary with the server live) with zero survivors.
- Ruling (pointer-defect node caught by the 7.7 gate, review-verified root cause): the intermittent (~1/5) c-shared fatal `invalid pointer found on stack` was a CALLER-side pointer-discipline defect — `ProducerCore::pull_via` passed `NonNull::<u8>::dangling()` (address 1) as `out` for every zero-capacity two-phase query, the Go export's closure kept that pointer-typed spill slot live, and Go 1.26's stack-copy scan fatals on pointer slots between 0 and `minLegalPointer` (4096) with no dereference — pure GC-timing intermittency, with the Go core innocent (no connection to the new mesh composition). The class fix routes null for empty buffers across every boundary input and output (`pull_via`, `connect_begin`, `submit_input`, and defensively `create_session`), preserving behavior because all four Go validators reject the empty length before the null check; scripted-vtable probes pin the raw pointer values for all four pull families and the input paths (pre-fix failures: 1 and 8 — the dangling addresses). Proof: 13/13 consecutive green smoke runs on the fixed tree plus the standing suite. The GOCACHE-staleness hypothesis was falsified analytically before spending the rebuild. The durable rule is promoted to the architecture skill: pointers crossing the Go FFI are null or genuinely valid, never sentinel addresses.
- Routing record for task 7.7 and the pointer-defect node (ZCode controller, strict SDD): one smoke implementer then one independent reviewer at the inherited host default (APPROVED with four P3s — the docstring precision fixes and the survivors-path log dump applied by the controller, port-collision fail-fast and EXIT-trap-only reaping recorded as precedent-consistent residuals); the controller's own gate rerun then caught the intermittent crash, and a dedicated defect node ran one investigator plus one independent reviewer at the inherited default — the investigator falsified the cheap cache hypothesis analytically from the traceback arguments, produced the cross-runtime root cause with file:line evidence on both sides, and delivered the one-site class fix with pre-failing pin tests; the reviewer APPROVED and surfaced the latent same-class input paths, closed in one R1 fix round extending the pins to all families and inputs; fallback unused throughout.
- Ruling (task 9.1 four-point orphan check): the task checkbox was open, the ledger contained no task-9.1 row, the worktree contained no untracked remote-player implementation, and no recent in-flight file belonged to this node; implementation therefore started fresh while the recorded user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan paths remained excluded.
- Ruling (task 9.1 execution): implement directly in the OpenAI Codex controller — the node crosses Go runtime publication, the Rust typed-frame decoder, and Python/Godot presentation, so it requires the project's OpenAI design gate, but the current session exposed no eligible native isolated worker surface and non-OpenAI external workers were ineligible; the controller retained test-first implementation, semantic review, validation, ledger, and commit ownership.
- Ruling (task 9.1 failure and capacity semantics): every successful ready-frame publication carries the current authoritative player tick even when the remote-player set is empty, so a final despawn cannot look stale and remain visible; more than seven tracked remote players fails frame assembly instead of silently truncating confirmed state. Rust validates the complete entity record set before exposing typed Godot dictionaries, and Python validates the complete snapshot plus the fixed seven-slot pool before any node mutation, limiting one apply to at most seven slot callbacks. The actors manifest remains disabled until the complete playable scenario in task 9.5 enables the feature honestly.
- Ruling (task 9.1 gate registration correction): the full optional-build audit exposed that the explicit Godot entrypoint allowlist had not been extended when the already-present input, camera, target, and disconnect leaf gates landed. Register all current leaf gates, including the new entity gate, and add their missing help entries without changing the legacy-target isolation rule; future Make entrypoint additions continue to require the full audit package.
- Routing record for task 9.1: direct OpenAI controller execution; basis a consequential cross-runtime boundary with moderate context breadth, strong focused Go/Rust/Python oracles, and no eligible native isolated-worker surface in the live host; no retry or model escalation occurred, and the controller review found and closed two oracle gaps (empty-snapshot tick preservation and reset callback accounting) before final validation.

## Validation Evidence

| Task | Worktree identity | Commands | Result |
|---|---|---|---|
| 1.1 | baseline plus uncommitted governance and user work | `openspec validate modernize-project-governance --type change --strict --no-interactive`; `openspec validate pilot-godot-client-migration --type change --strict --no-interactive` | Both changes passed strict validation; all four pilot artifacts are English and the governance prerequisite is implementation-complete. |
| 1.2 | same | `go test ./packages/audit -run TestBaselineVersionsMatchCode -count=1` | Passed; current governance documents and code-owned constants agree on protocol v44, engine ABI v11, client ABI v19, and benchmark scenario v23. |
| 1.3 | same | client package/Rust module/test inventory commands; `rg -n 'P0|P7|P14|protocol v44|engine ABI v11|client ABI v19' docs/notes/godot-client-pilot-baseline.md`; documentation manifest and plan-language audit | The English plan records all current client packages, representative tests, explicit pilot exclusions, tracked evidence counts, and the reversible P0–P14 mapping. |
| 1.4 | same | expected-red missing implementation and missing-manifest test runs; `go test ./packages/tools/perfcheck -run GodotPilotBaseline -count=1` | Strict decoding and completeness validation passed for commit, dirty-worktree state, Darwin/arm64, Apple M2/Metal 4, 2560×1440, view distance 32, scenes, timing/sample identity, v44/v11/v19/client-core v1/scenario v23, report checksums, and visual-source counts. Unknown fields, trailing JSON, mobile/Web platform values, duplicates, and incomplete fields are rejected. |
| 1.5 | same | `make visual-check`; `make bench-multiplayer`; expected-red `go test ./packages/tools/perfcheck -run GodotPilotBaselineSourceReportsMatchManifest -count=1`; full v23 Memory producer; `go test ./packages/tools/perfcheck -run GodotPilotBaseline -count=1` | Multiplayer microbenchmarks passed. The full producer emitted a complete scenario v23 report with hash `e588b40e...0339e`. The new source-report audit first exposed that the prior manifest incorrectly referenced v15/v14 reports, then passed after binding the current v23 report. Visual comparison ran without updates and reproducibly failed only `grass-closeup`, `mining-crack-early`, and `mining-crack-heavy`; all 28 other world scenes matched. This dirty-worktree variance is recorded, not accepted as a baseline change. |
| 1.6 | same | expected-red `go test ./packages/audit -run GodotResourceLicenses -count=1`; passing rerun of the same command | Added synchronized English/Chinese provenance manifests. The audit admits exactly Godot Engine, godot-rust, the engine-bundled default font, project-generated materials, project-owned shaders, and the project-owned bridge; an unapproved downloaded font mutation is rejected. godot-rust is source-probe-only until P1 records an exact compatible pin, and no mobile/Web/console or marketplace component is authorized. |
| 2.1 | same | official Godot release/archive lookup and GitHub release-asset metadata; expected-red and passing `go test ./packages/audit -run GodotDesktopOnlyToolchainPin -count=1`; `scripts/godot/fetch.sh --verify-only`; rejected `--target android-arm64` probe | Pinned official Godot Standard macOS universal SHA-256 `c58a24e...ba63e` and Standard export templates SHA-256 `f298490b...48011`. Verification resolves an external `/private/tmp` cache without downloading absent artifacts; normal mode verifies before atomic promotion. Only `darwin-universal` is accepted now, and the mobile-target probe failed as required. |
| 2.2 | same | upstream godot-rust release/docs and crates.io metadata; expected-red and passing `go test ./packages/audit -run GodotRustDependencyPin -count=1`; `rustup run 1.97.1 cargo test -p mornlea_godot`; `rustup run 1.97.1 cargo fmt --check`; `rustup run 1.97.1 cargo test -p mornlea_godot --locked`; provenance audit | Exactly pinned `godot` 0.5.5 with `api-4-7`; its declared Rust 1.94 minimum is below the project pin. The empty GDExtension compiled and tested on Rust 1.97.1, Cargo.lock added only the binding dependency chain, and the provenance pair now records the successful pin. Compatibility probe is Go. |
| 2.4 | same | expected-red and passing `go test ./packages/audit -run GodotProjectBootstrapIsPureAndStable -count=1`; `scripts/godot/godot.sh --headless --path apps/mornlea-godot --editor --quit` | Official Godot 4.7.2 imported the stable project root successfully without native artifacts. `project.godot` points only to the pure-GDScript Bootstrap; serialized bootstrap/setup scenes contain no native class, extension, or bin reference. The synchronized README pair fixes desktop-only ownership and extension points, local ignores exclude editor cache/native bins without excluding `.uid`, and the generated GDScript UID remains tracked. |
| Pre-revision P1 diagnostics prototype | same | expected-red then passing `scripts/godot/openable-smoke.sh --without-native`; `go test ./packages/audit -run 'GodotProjectBootstrapIsPureAndStable|GodotBootstrapDoesNotInitiateNetwork' -count=1`; headless editor import | This evidence predates the Python-primary revision. The static Bootstrap reports missing native bridge state and remains useful input for revised task 2.5, but it does not diagnose the new Python extension/interpreter requirements and therefore does not complete that task. |
| Pre-revision GDScript feature-host prototype | same | expected-red then passing `scripts/godot/feature-contract-check.sh` | This evidence predates the Python-primary revision. It demonstrated manifest and lifecycle semantics, but revised task 2.7 requires replacing the GDScript implementation with a Python host, so the task is no longer complete. |
| Pre-revision GDScript feature-skeleton prototype | same | expected-red then passing `scripts/godot/feature-contract-check.sh --extensibility-probe`; normal feature contract check; `go test ./packages/audit -run 'GodotDesktopOnlyFeatureSkeleton|GodotProjectBootstrapIsPureAndStable' -count=1` | This evidence predates the Python-primary revision. It remains architectural test input, but revised task 2.8 requires Python-led features and desktop adapters, so the task is no longer complete. |
| 2.3 | same | expected-red then passing `go test ./packages/audit -run 'GodotPythonRuntimePin|GodotResourceLicenses' -count=1`; exact release SHA-256 verification; Mach-O arm64 and dependency inspection; embedded CPython and license inspection; `scripts/godot/python-runtime-check.sh --qualify --offline`; direct poisoned-`PYTHONPATH` headless probe; headless editor import; current release/repository survey | Exactly pinned Py4Godot 4.7-alpha21, source revision `d8e17428deeb0428587349b663f6da26cd71ef3a`, artifact SHA-256 `7fa28db...bf3eb4c`, embedded CPython 3.14.4, and macOS arm64. The selected unit removes pip/ensurepip and runs with network denied and system executables absent. Qualification is No-Go: external `PYTHONPATH` appears in embedded `sys.path`; upstream initializes through `Py_InitializeEx(0)` and appends the current working directory instead of using isolated `PyConfig`; headless editor loading emitted six `PopupMenu` bounds errors and leaked two objects; headless script teardown leaked three objects and one resource. The alternative `maiself` and `ViynShade` Godot 4 Python extensions publish no release artifacts, while the legacy `touilleMan` release is from 2020 and targets the old engine generation. Export and lifecycle qualification are intentionally not waived or continued after the earlier isolation hard failure. Failed qualification cleans the generated add-on and cached extension list; the pure-GDScript project root then reopens cleanly in Godot 4.7.2. |
| 2.3a | same | expected-red then passing `go test ./packages/audit -run 'GodotPythonFork|GodotPythonIsolation' -count=1`; two byte-comparison loader builds; `scripts/godot/build-python-runtime.sh --verify --offline --target darwin-arm64 --cache-dir /tmp/mornlea-py4godot-cache`; `scripts/godot/python-runtime-check.sh --qualify --offline --target darwin-arm64 --cache-dir /tmp/mornlea-py4godot-cache`; clean editor probe; actual exported `.app` probe; `go test ./packages/audit -count=1` | Approved hardening reached P1 Go for macOS Apple Silicon. The build starts from source revision `d8e17428deeb0428587349b663f6da26cd71ef3a`, verifies source SHA-256 `e7a763fe...47fdd0`, ordered patch-series SHA-256 `9e3be26d...a71be`, loader SHA-256 `bb2e4f7f...b8ef4f`, and materialized artifact SHA-256 `b37fffb4...7e58`, and reproduces with Apple clang 21.0.0 plus macOS SDK 26.5. The isolated `PyConfig` rejects poisoned environment, user site, system tools, network, and current-directory imports; pip/ensurepip are absent. A clean Godot 4.7.2 editor, headless Python scene, `mornlea_godot` coexistence, 100 load/unload cycles, and a real offline-exported macOS application all passed without runtime errors. The single patch is confined to loader isolation, exported-layout resolution, deferred editor repair, and lifecycle cleanup and exposes no project gameplay/data API. The full architecture audit passed after registering the third Rust workspace member in the existing identity gate. |
| 2.5 | same | expected-red `go test ./packages/audit -run GodotBootstrapDiagnosesIncompleteDistribution -count=1`; expected-red `scripts/godot/openable-smoke.sh --without-native`; passing `scripts/godot/openable-smoke.sh --without-native`; passing `scripts/godot/openable-smoke.sh --without-python`; `go test ./packages/audit -run 'GodotProject|GodotBootstrap|DocumentationPair' -count=1`; clean headless editor import | Bootstrap now classifies missing Python extension, language library, bridge library, embedded interpreter, standard-library landmark, project extension descriptor, and project bridge binary; reports exact Godot/descriptor/desktop-target identity mismatches; and emits the complete offline Python plus native preparation command. Both smoke modes build isolated temporary project roots, suppress extension loading, mutate one descriptor identity, deny network access, and prove the setup path remains pure GDScript with `python=not-imported`. The complete materialized project reports `missing=none` and `mismatched=none`. English and Chinese project guides are synchronized at revision `2026-09-16.2`. |
| 2.6 | same | expected-red `go test ./packages/audit -run GodotPythonToolingIsLockedAndRuntimeFree -count=1`; expected-red `/usr/bin/python3 -m unittest scripts/godot/python_boundary_check_test.py`; generated `uv.lock` with embedded CPython 3.14.4; `scripts/godot/python-check.sh --locked`; `go test ./packages/audit -run 'GodotPythonTooling|GodotProject|DocumentationPair' -count=1` | Production dependencies are explicitly empty. The lock fixes Ruff 0.16.7, mypy 2.3.1, and their development-only closure while the ignored `.venv/` remains separate from the embedded runtime. Local `py4godot` stubs cover the current `gdclass`, `Node`, and `SceneTree` surface without loading generated add-on modules. The locked check passes Ruff format/lint, strict mypy over seven Python/stub sources, English-comment enforcement, and mutation-tested rejection of companion Agent imports, native ABI/dynamic libraries, network modules and Godot network classes, installers, process execution, unrestricted dynamic imports, and invalid syntax. The bilingual project guide is synchronized at revision `2026-09-16.3`. |
| 2.7 | same | expected-red Python contract scene before the host existed; expected-red dynamic-resource lifecycle run with 23 leaked Godot resources; `scripts/godot/build-python-runtime.sh --verify --offline`; `scripts/godot/feature-contract-check.sh`; `scripts/godot/feature-contract-check.sh --extensibility-probe`; `scripts/godot/python-check.sh --locked`; `scripts/godot/python-runtime-check.sh --qualify --offline`; `go test ./packages/audit -run 'GodotFeatureHost|GodotDesktopOnlyFeatureSkeleton|GodotPythonTooling|GodotPythonFork|GodotResourceLicenses|DocumentationPair' -count=1`; `go test ./packages/audit -count=1` | The production feature host is now Python-only and reads an explicit versioned Godot resource graph; the former GDScript host classes were removed. Serialized fixtures prove duplicate-ID and cycle rejection, required/optional protocol and bridge-family handling, required/optional activation failure, deterministic dependency order, typed bridge injection, and forward/reverse lifecycle order. The host uses no directory scan or project-root Python import. The ownership failure was fixed generically in ordered Py4Godot patch `0002`, producing audited derivative `4.7-alpha21-mornlea.2` with patch-series SHA-256 `2e4cf6a...c682ef` and artifact SHA-256 `aa050417...ffb0`; the unchanged offline qualification matrix, including 100 load/unload cycles and a real macOS export, passed without leak warnings. Ruff, strict mypy, boundary checks over 17 sources, the focused audit, and the full architecture audit passed. The bilingual project guide is synchronized at revision `2026-09-16.4`. |
| 2.8 | same | expected-red `go test ./packages/audit -run GodotPythonFeatureSkeletonsAndScriptOwnership -count=1` before Python skeletons existed; `scripts/godot/feature-contract-check.sh --extensibility-probe`; `scripts/godot/validate-project.sh --script-ownership`; `scripts/godot/python-check.sh --locked`; focused and full architecture audit | The five presentation roots and three desktop adapters now attach uniquely named, strictly typed Python scripts with explicit host-bind, activation, reset, and deactivation lifecycle boundaries. Each new unit documents its ownership and non-obvious exclusions in English. Audio fails closed and remains disabled in its manifest; no mobile, Web, or console directory exists. The audit admits manifests only for the eight coarse replaceable roots and mutation-tests rejection of production GDScript outside Bootstrap. The additive Python feature probe passes without modifying Bootstrap or `app_root.tscn`. |
| 2.9 | same | expected-red `go test ./packages/audit -run GodotBridgeHostIsIdentityOnlyAndDesktopBound -count=1`; runtime-red Python `ClassDB.instantiate()` probe with missing `_ptr` and one leaked object; `scripts/godot/build-extension.sh --verify`; `scripts/godot/python-runtime-check.sh --coexistence`; `scripts/godot/godot.sh --headless --path apps/mornlea-godot --quit-after 5`; both incomplete-distribution smoke modes | The existing desktop-only descriptor/build path now verifies both native registration and a typed Python facade. `MornleaClientBridge` exposes only static client-core ABI 1.0, Godot API 4.7, and godot-rust 0.5.5 identity methods; Python reaches them only through Godot `ClassDB`, with no dynamic-library or C-ABI access and no gameplay/data family. The isolated coexistence mode rejects errors and external Python. A fully prepared Bootstrap dynamically loads `app_root.tscn`, injects `BridgeHost` into the feature host, and reports `state=ready`; missing native or Python artifacts still stay on the diagnostics scene with `python=not-imported` and no network start. |
| 2.10 | same | expected-red `cargo test -p mornlea_godot lifecycle --locked` before the lifecycle module existed; expected-red `go test ./packages/audit -run GodotLifecycleSmokeContract -count=1` before the smoke runner existed; passing Rust lifecycle tests; passing one-cycle probe; `scripts/godot/smoke.sh --iterations 100`; locked Python checks and focused architecture audit | Rust now validates Scene → optional Editor → MainLoop initialization and exact reverse deinitialization, rejects duplicate/out-of-order transitions and non-4.7 identity, and converts caught panics into stable boundary failures. The bridge reports only identity and lifecycle readiness. The headless smoke materializes both pinned extensions once, then launches and exits the complete Bootstrap/Python host/Rust bridge 100 times; every cycle proved Python feature teardown and bridge release precede Rust MainLoop/Scene teardown, with no nonzero exit, Godot error, script error, or leaked-at-exit warning. |
| 2.11 | same | expected-red `go test ./packages/audit -run GodotAssetSync -count=1` before the synchronizer existed; `scripts/godot/sync-assets.sh`; `scripts/godot/sync-assets.sh --check`; repeated isolated generations; mutation probes for an extra file and modified atlas; focused and full architecture audit; comment-language gate; focused strict OpenSpec validation | The offline Go-backed synchronizer calls the production `assets.Registry` to emit one 16×16 RGBA8, five-mip, layer-major atlas without duplicating procedural texture logic. It copies the registered Noto Sans CJK font and its OFL/provenance files plus the Pastelcraft attribution/license/provenance closure. The deterministic manifest records both source Git trees, every input path/size/SHA-256, an aggregate input checksum, atlas layout/digest, and every generated output. Two independent generations were byte-identical; `--check` rejects missing, modified, symbolic-link, or hand-authored output. English/Chinese guides and provenance tables were synchronized at revision `2026-09-16.5`. |
| 2.12 | same | expected-red focused audit before full-project mode and `export_presets.cfg` existed; `scripts/godot/validate-project.sh`; compatibility `scripts/godot/validate-project.sh --script-ownership`; mutation probes for escaped links, absolute paths, resource traversal, autoloads, Git ignore drift, unselected features, unsupported export/GDExtension platforms, ambient Python search, installers, downloads, non-English comments, production GDScript, and Bootstrap network/process calls; required focused audit; `make comment-language-check`; headless Godot 4.7.2 editor preset parse; post-editor asset-sync closure check; full architecture audit; focused strict OpenSpec validation; project-skill validation and byte-equality check | The stable project root now has one macOS export preset whose resource closure excludes tests, development Python tooling, setup diagnostics, provenance, catalog-unselected features, and Linux/Windows bridge plus Py4Godot runtime families. The reusable validator keeps the prior ownership-only mode and rejects escaped or machine-local dependencies, generated-state/UID/import policy drift, implicit autoloads, ambient Python/library resolution, installer/network/process paths, non-English architecture comments, non-desktop presets/templates/selectors, and incomplete target-specific export exclusions. The deterministic asset synchronizer now owns the font import sidecar, including its stable UID and no-system-fallback policy, while ordinary extra files and all symlinks still fail. Windows and Linux remain permitted future desktop presets, but each must declare its own inverse native/runtime exclusions. |
| 2.13 | same | expected-red `go test ./packages/audit -run GodotLifecycleSmokeContract -count=1` after adding the isolated-runtime contract; `bash -n scripts/godot/smoke.sh`; one-cycle isolated probe; `scripts/godot/smoke.sh --iterations 100 --isolated-python`; focused Godot project audit | The smoke gate materializes the pinned native units once, then runs every process headlessly with network denied, an empty executable path, an isolated temporary home, and poisoned ambient Python home/path/startup/user-site settings. Each cycle requires exactly one host and Rust lifecycle marker in forward and strict reverse order, including paired optional Editor stages; rejects runtime, script, interpreter, feature, resource, and object leak diagnostics; scans temporary and macOS diagnostic roots for new Godot/Mornlea crash reports; and rejects any process retaining the cycle's unique pilot token. All 100 cycles exited cleanly without crash reports or leftover pilot processes. |
| 2.14 | same | expected-red then passing `go test ./packages/audit -run 'GodotIsOptionalForLegacyBuild\|GodotPythonIsolation' -count=1`; `make godot-project-check`; `make godot-python-check`; `make godot-smoke`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The Makefile exposes the three optional godot gates as `.PHONY` leaves with English help entries, and CI gains a non-gating `godot` job that materializes ripgrep, the pinned Rust toolchain, Godot, the hardened Py4Godot runtime, and uv before running the same three gates. The new audit guard pins exact recipes, help visibility, Makefile isolation through a whole-file `scripts/godot` invariant, and CI scoping, with eight mutation probes including double-colon rules, parse-time `$(shell ...)` assignments, and block-style `needs` lists; independent strict-SDD review drove one fix round that closed the two real guard holes. All three make gates and 100 isolated smoke cycles passed first-hand, and the full audit suite stayed green after promoting the optional-gate rule into both `mornlea-architecture` copies. |
| 2.15 | same | expected-red focused audit after adding the missing mutation cases; `bash -n scripts/godot/validate-project.sh`; `go test ./packages/audit -run 'GodotDesktopOnly\|GodotPythonBoundary' -count=1`; `scripts/godot/validate-project.sh --desktop-only-fixtures --python-isolation-fixtures`; normal project and script-ownership validation; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The project gate now mutation-tests Android, iOS, Web, and console presets and selectors; touch and sensor InputMap declarations; mobile lifecycle/platform trees; system Python path mutation; user-site resolution; and dynamic runtime installer calls. Fixture flags compose in one bounded command and synthesize a minimal valid source closure without copying generated native or embedded-Python artifacts. Controller review corrected the initial mutually exclusive CLI and 276 MB-per-mutation copy design and scoped InputMap detection to its declared section. Every mutation fails during source validation before build or packaging, while the real project and legacy script-ownership mode remain valid. |
| 3.1 | same | expected-red `go test ./packages/client/presentation -race -count=1` with undefined presentation types; passing rerun of the same command; `go vet ./packages/client/presentation`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added renderer-independent session, camera, environment, HUD, target, and stable error values. Zero and not-ready values cannot fabricate confirmed state or retain stale payloads; shared authoritative domains and finite camera projection bounds are validated; snapshots contain no mutable reference-bearing fields; and the package imports only the standard library and `packages/shared/core`. Controller review standardized the public `FOVY` name and added NaN/Inf domain cases before the closure gates passed. |
| 3.2 | same | expected-red focused race test with undefined world presentation APIs; `go test ./packages/client/presentation -run 'Section\|WorldBatch' -race -count=1`; full `go test ./packages/client/presentation -race -count=1`; `go vet ./packages/client/presentation`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added owned packed-quad payloads, signed dimension/section keys, nonzero presentation revisions, named atlas revisions, and immutable all-or-nothing world batches. Validation rejects unsupported dimensions, invalid heights and packed faces, duplicate/conflicting sections, stale epoch/atlas/section revisions, and empty or over-capacity input. Exact boundaries cover 24,576 quads per section, 4 MiB total mesh payload, and 4,096 operations; constructor failure returns the zero batch without mutating caller data. |
| 3.3 | same | expected-red focused race test with undefined entity presentation APIs; `go test ./packages/client/presentation -run Entity -race -count=1`; full `go test ./packages/client/presentation -race -count=1`; `go vet ./packages/client/presentation`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added value-only remote-player entity records and immutable all-or-nothing batches. The fixed total capacity is seven remote players, derived from the authoritative eight-player server/session limit rather than the private renderer's 96-byte instance or broader avatar budget. Validation rejects unknown kinds, invalid identities/dimensions, non-finite transforms, duplicate identities, overflow, and repeated or older server ticks; constructor failure publishes an empty batch. |
| 3.4 | same | expected-red focused test with an empty corpus directory; `go test ./packages/client/presentation -run TranscriptCorpus -count=1`; full `go test ./packages/client/presentation -race -count=1`; `go vet ./packages/client/presentation`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added five strict JSON corpus files with 16 canonical protocol-v44 frames covering login, snapshot, movement, authoritative correction, chunk update, remote-player lifecycle, weather/HUD state, disconnect, version mismatch, truncation, invalid enum, and capacity overflow. The test rejects unknown fields and trailing JSON, bounds file/frame/payload work, decodes with the real network codec, requires byte-identical canonical re-encoding for accepted frames, distinguishes codec from protocol rejection, and locks readable semantic summaries with per-file SHA-256 digests. |
| 3.5 | same | capture-then-freeze characterization pass; `go test ./packages/client/cmd/mornlea/app -run 'Characterization\|TranscriptParity' -race -count=1`; the same focused test at `-count=10`; full `go test ./packages/client/cmd/mornlea/app -race -count=1`; `go vet ./packages/client/cmd/mornlea/app`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added one deterministic bounded semantic transcript over the existing Darwin app: a two-message drain leaves the queued chunk unapplied, local input records exact protocol and predictor state, the final drain creates bounded mesh work, camera/remote-player/HUD/environment snapshots expose presentation semantics, and session reset clears every sampled session-owned value. The test reuses the single helper center and requires neither a GPU nor a foreground window. |
| 4.1 | same | expected-red focused race test with undefined runtime lifecycle APIs; expected-red safe-zero phase correction; `go test ./packages/client/runtime -run 'Lifecycle\|Close' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added a platform-independent runtime lifecycle skeleton with prevalidated ordered dependency factories, strict reverse cleanup on partial construction failure, joined cause/cleanup errors, concurrent idempotent close, and the existing presentation `SessionPhase` as the sole connection-phase domain. Construction publishes the safe `NotReady` zero and only close publishes terminal `Disconnected`; no TCP, receiver, mirror, server, device, renderer, or ABI dependency was introduced. |
| 4.2 | same | expected-red focused race test with undefined remote runtime APIs; expected-red terminal-plus-close error aggregation; `go test ./packages/client/runtime -run 'Remote\|Login\|Receiver' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added `NewRemote` with injected-or-production TCP dialing, the existing current-version login state machine, and the existing bounded receiver. Options are rejected before side effects, real v44 handshake rejection is exercised, login retains failed-stream ownership, successful endpoint ownership transfers only to the receiver, authoritative world seed is retained, and the successful phase starts at `Loading`. Receiver terminal errors atomically disconnect and preserve any close failure; close remains idempotent. |
| 4.3 | same | expected-red focused race test with undefined message and mirror APIs; controller-red rejected-outcome publication test; `go test ./packages/client/runtime -run 'Messages\|Mirror\|Reset' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/client -run 'Messages\|Mirror\|Reset' -race -count=1`; `go test ./packages/client/cmd/mornlea/app -run 'Connection\|Messages\|Inventory\|Chest\|Furnace' -race -count=1`; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Runtime now exclusively composes the world, inventory, crafting, chest, furnace, chat, item-drop, remote-player, companion, hostile, passive, and projectile mirrors. Bounded nonblocking drains produce ordered host-neutral outcomes, preserve unhandled `PlayerState` for the next extraction node, expose detached deterministic mirror views, keep platform effects outside runtime, and replace the world mirror plus reset every other mirror at session boundaries. Controller review prevented a rejected mirror update from being published as a partial drain result. |
| 4.4 | same | expected-red focused race test with undefined semantic-input and prediction APIs; controller-red negative-elapsed and connection-phase tests; `go test ./packages/client/runtime -run 'Predict\|Input\|Reconcile' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/client -run Predictor -race -count=1`; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Added copied semantic-input submission separated from explicit fixed-step advance, composed the existing predictor and mirror collision source, and retained the receiver-owned endpoint only as a non-owning sender with caller-controlled context. Direct-predictor parity covers fixed steps, correction smoothing, unconfirmed replay, history suspension, send failure/retry, readiness, reset, stale state, and retained snapshots. Runtime owns one monotonic sequence across player input and chunk resync, recreates all session state on session reset, and maps accepted readiness to `Play`/`Loading`; controller review rejected negative elapsed before state mutation. The bounded asynchronous outbound path required by nonblocking `Step` remains explicitly owned by task 4.7. |
| 4.5 | same | expected-red focused camera/target tests; controller-red persisted-mode validation and same-pose authoritative-reset suppression tests; `go test ./packages/client/runtime -run 'Camera\|Target' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/cmd/mornlea/app -run Camera -race -count=1`; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Runtime now owns validated local camera preference, projection scalars, semantic look deltas, first-/third-person pose derivation, mirror-backed third-person occlusion, and the six-block target ray from the predicted eye pose. Device edges enter only as an F5 level plus host blocking state; cursor capture and render matrices remain host-owned. Mode and projection persist across session resets while pose and target inputs clear; explicit modes reject invalid values atomically, pitch clamps to the legacy bound, unknown/desynced target paths hide feedback, and an authoritative reset suppresses exactly the next ready target presentation even when the corrected pose is unchanged. |
| 4.6 | same | expected-red focused race test with undefined mesh runtime APIs; controller review added nonblocking reset invalidation and atomic overflow preflight coverage; `go test ./packages/client/runtime -run 'Mesh\|Section\|Budget' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/client -run Mesher -race -count=1`; repeated stale-result race test; `go vet ./packages/client/runtime`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Runtime optionally owns the existing bounded `client.Mesher`, scheduling and draining it without changing the `mesh` → `nativeabi` engine-ABI-v11 path. A bounded coalescing ready queue publishes immutable world batches under operation and packed-byte limits; empty meshes become drops, and remesh/forget operations receive monotonic per-section revisions inside a resettable epoch. Full publication queues preserve existing worker/result backpressure, while overflow preflight leaves pending operations and revisions unchanged before terminating the session rather than losing a section update. Reset clears publications and revisions, increments the epoch, and invalidates tracked chunk generations without waiting for workers, so stale in-flight results cannot cross the session boundary. Unified step ordering remains owned by task 4.7. |
| 4.7 | same | adopted-red build failure, then `go test ./packages/client/runtime -run 'Step\|Bounded\|NoWallClock' -race -count=1`; full `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/presentation -race -count=1`; `go vet ./packages/client/runtime ./packages/client/presentation`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Runtime now exposes the explicit `Step(elapsed, messageBudget, meshBudget)` boundary: strict argument validation precedes any state mutation, one bounded drain enqueues numbered resyncs before prediction, the predictor advances only from the explicit elapsed argument with no wall clock, a budgeted world batch drains from the ready queue, and at most one immutable frame publishes per step with monotonic revisions inside the mesh epoch. A dedicated outbound worker owns every blocking send behind a queue sized so one maximal step never blocks or drops, and `Close` cancels the in-flight send while discarding queued messages. Receiver failure publishes exactly one terminal frame and rejects later steps. The new `presentation.FrameSnapshot` aggregate validates component domains and the disconnected⟺terminal-error coupling. Independent strict-SDD review verified the protocol-invalid orphan-test correction empirically, confirmed no wall-clock or queue-blocking paths, and drove two fix rounds: documented queue-full recovery contracts plus comment hygiene, and one backtick-identifier comment repair caught by the full audit suite. |
| 4.8 | same | pre-change baselines (full app race green 70.8s; `make visual-check` failing exactly grass-closeup 0.5586%, mining-crack-early 0.2691%, mining-crack-heavy 0.2500%); per-loop characterization parity `go test ./packages/client/cmd/mornlea/app -run 'Characterization\|TranscriptParity' -race -count=1` plus full app race after each of the three loops; final `go test ./packages/client/cmd/mornlea/app -race -count=1` (65.2s green); `go test ./packages/client/runtime -race -count=1`; `go test ./packages/client/presentation -race -count=1`; focused warp/mass-forget/capacity tests; full `go test ./packages/audit -count=1`; `make visual-check` differential identical to baseline; strict change validation | The legacy Darwin app now consumes runtime for all three frame paths: message draining routes through `DrainMessages` with every UI/audio effect driven from `MessageOutcome`, interactive input routes through `SubmitInput`/`AdvancePrediction`, and the game-phase mesh loop routes through `AdvanceMeshes`/`DrainMeshedSections` into the unchanged scheduler upload path. The new adoption seam (`Adopt`, `AdoptMeshing`, `NextSequence`, `DrainMeshedSections`) hands host-constructed session state to runtime without forking Memory/TCP behavior; empty-mesh drops now carry connectivity after the first visual run exposed a visibility BFS dead-end; and review caught a warp-scale ForgetChunks regression that the 4096 ready-capacity preflight converted into session termination — drop-only transitions are now capacity-exempt with the upsert guard intact. The visual differential stayed byte-identical to the recorded dirty-worktree baseline. A stalled implementer left one unfinished capacity-accounting test whose fixture the controller repaired per the reviewer's diagnosis; the pre-existing non-race fast-mode epoch-test flake was reconstructed on HEAD and is recorded as unrelated. |
| 4.9 | same | `go test ./packages/audit -run 'ClientRuntime\|GodotBoundary' -count=1` with all 19 drift subtests verbose-green; full `go test ./packages/audit -count=1`; `go test ./packages/client/runtime ./packages/client/presentation -race -count=1`; `go vet ./packages/audit`; `make comment-language-check`; strict change validation | One new audit file prohibits runtime/presentation production files from importing any server package (source plus `go list -deps` closure), anything Godot, the client C ABI surface (dynamically discovered as the exported declarations of `packages/client/client`'s cgo entry files, with six liveness anchors), platform forks (MatchFile across three OSes and both architectures, rejecting build tags and filename suffixes), WebGPU/Vulkan/GLFW bindings, and device I/O families including the whole `os/` subtree. Nineteen drift subtests cover ABI selector/dot-import/token escapes, blank imports, every forbidden family, transitive closures, and platform forks with positive controls. Independent review approved after probing bypasses empirically; the two accepted residuals (owner-side cgo re-exports, non-recursive scan) are deferred to task 11.1 registration. |
| 5.1 | same | expected-red cross-check runs (undefined Go constants; unreadable header in Rust); `go test ./packages/client/cmd/mornlea-godot-core -run 'ABI\|Layout\|Identity' -count=1`; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot abi --locked` and the full crate test; `rustup run 1.97.1 cargo clippy -p mornlea_godot --all-targets -- -D warnings`; `cargo fmt --check`; gofmt/vet on the new package; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Client-core ABI v1 is now defined in one header: major/minor identity with explicit bump rules, a ten-entry frozen status order with the two-phase `INSUFFICIENT_CAPACITY` signal, `MCx1` magic tags whose generation digit is test-tied to the major, eight fixed 8-byte-aligned headers with reserved-must-zero contracts, limits mirroring the frozen presentation/runtime constants plus four labeled pilot bounds, and seven feature-family descriptors. Go derives every constant through cgo and pins layout with `unsafe` facts; Rust mirrors constants and `#[repr(C)]` layouts while parsing the header; both sides assert both-direction define-set equality, ambiguous leading-zero literals are rejected, and the Godot-visible bridge identity now flows from the pinned constants. Independent review probed parser escapes, verified the layout triple-check across C/Go/Rust, and confirmed forward compatibility for the remaining family tasks before the prescribed fix round. The recorded clippy pass was later corrected: see the task-5.2 correction ruling. |
| 5.2 | same | expected-red registry and negotiation runs on both languages; `go test ./packages/client/cmd/mornlea-godot-core -run 'Feature\|Compatibility\|Registry' -count=1` (13 tests); full package test; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot feature_negotiation --locked` (8 tests) and the full crate test (19); `rustup run 1.97.1 cargo clippy -p mornlea_godot --all-targets -- -D warnings`; `cargo fmt --check`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The sole client-core registry now reports data-plane availability for the seven pilot families, built exclusively from cgo header constants with a validation injection seam rejecting unknown families, zero versions, duplicates, and missing required families, two-phase descriptor enumeration rehearsing the capacity protocol, and negotiation that accepts a compatible-or-older minor, rejects any major mismatch, and rejects unknown or too-new families; the Rust mirror carries identical accept/reject semantics over `PILOT_FAMILIES`. Literal pins on both sides chain header→constants→registry so any ID, version, or limit change without a header bump fails loudly. The round also repaired the fcf17e6f clippy break with module-scoped dead-code allowances carrying removal conditions, and review verified the Go/Rust predicate equivalence plus the pin-chain integrity. |
| 5.3 | same | expected-red handle/exports build failure; `go test ./packages/client/cmd/mornlea-godot-core -run 'Identity\|Lifecycle\|Panic' -race -count=1` (plus `-count=3` flake check); full package `-race`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Create/destroy/status-identity exports now sit on a fixed eight-slot, mutex-guarded, zero-allocation session table with slot-plus-generation handles that never alias across reuse, tombstone-idempotent destroy, and documented `StatusInvalidState` mappings for capacity and destroyed-handle status. Multi-defect tests prove the validation order — ABI identity before family negotiation before out-handle pointer, handle lifecycle before pointer arguments — and the oversized-count case proves shape checks precede the array read. The identity status export is two-phase (192 bytes: `MCI1` header plus the pinned seven-descriptor table), writes nothing on any non-OK path except the required-size signal, and converts injected panics to `StatusPanic` through a guarded scratch-then-copy encode. Independent review verified the encoding wraparound analysis, the sequential-test safety of the panic seam, and byte-level layout fidelity before three one-sentence comment fixes. |
| 5.4 | same | expected-red connect-exports build failure; `go test ./packages/client/cmd/mornlea-godot-core -run 'Connect\|Disconnect\|Cancel' -race -count=1` and `-count=3`; full package `-race`; `go test ./packages/client/runtime -race -count=1` (untouched); gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Asynchronous connection exports now drive `runtime.NewRemote` from one establishment goroutine per session: begin spawns under the table mutex (no destroy race), the goroutine takes only the session mutex, disconnect cancels the dial/login context and joins through a done channel, and the slipped-through-runtime self-close is provably disjoint from the disconnect close. Poll reports the identity-cast `presentation.SessionPhase` words while live (`Login` never reported, `Play` delegated to the runtime) and returns `StatusDisconnected` with no output write as the terminal signal, recording the terminal cause (real v44 handshake rejection included) for the later status family. Repeated begin in any begun phase rejects with `StatusInvalidState`; address validation follows the shape-before-content order with the header's 8-byte record-buffer alignment. Review proved the spawn window, publication happens-before, and exactly-once close, then one fix round added a by-construction deterministic slip-through test and corrected the poll-cost comment. |
| 5.5 | same | expected-red input-exports build failure; `go test ./packages/client/cmd/mornlea-godot-core -run Input -race -count=1` (12 tests); full package `-race`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The input family now has a wire contract and a submission export: a fixed `MCN1` header plus ≤128 sixteen-byte records folds arrival-order last-wins into exactly one `runtime.SemanticInput` per frame, with the seven action enums mapped one-to-one to runtime fields, absolute yaw/pitch poses as float32 bits, axes clamped to −1/0/1, and every boolean carried explicitly. Validation follows the established order — handle, session state, length bound judged before any read, pointer shape, then content on an owned copy — with whole-batch atomicity proven by a seam that counts zero submissions across twelve invalid-middle-record defect classes while a later valid batch still submits once, and a real-transport integration test exercises real acceptance and real semantic rejection. Text capacity and the fixed record byte size are deferred as recorded rulings; review verified the absolute-pose and magic-to-`StatusABIMismatch` mappings against the runtime contract and the header's own status text. |
| 5.6 | same | expected-red step-exports build failure; `go test ./packages/client/cmd/mornlea-godot-core -run Step -race -count=1` (8 tests, plus `-count=5`/`-count=10` stability); full package `-race`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The step export validates handle, session state, exact 24-byte shape, then content on an owned copy — identity magic before budgets, elapsed bounded exactly by u64→int64 representability, budgets capped at the runtime-pinned 4096, zero elapsed and zero budgets valid — then makes exactly one serialized `runtime.Step` call and retains the latest completed frame/world result for the later pull exports, surviving teardown so the terminal frame stays pullable. Receiver failure retains the terminal frame, claims the session terminal transition, and rejects later steps with `StatusDisconnected`; a parked outbound sender proves the export returns while the send blocks; queue-full backpressure maps to `StatusInternal` per the recorded ruling. Review verified the lock-order and serialization proofs, the elapsed overflow analysis, and the status mapping before one comment softening. |
| 5.7 | same | expected-red world-exports build failure; `go test ./packages/client/cmd/mornlea-godot-core -run WorldBatch -race -count=1` (15 tests, `-count=3` stable); full package `-race`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Two-phase world draining is live: a zero-capacity pull queries the exact encoded size without consuming, an exact-capacity pull encodes into owned scratch and commits consumption in one copy guarded by the session generation (a mid-pull step cannot have its fresh batch swallowed; concurrent consumes have exactly one winner), and insufficient capacity reports the required size with zero writes. The `MCW1` wire body carries 32-byte operation records (upserts then drops in presentation order, dimension codes pinned 0/1, per-operation revisions) plus the packed-quad payload, with the 4,325,408-byte maximal ceiling pinned for the consumer's allocation. No-batch is `StatusOK` with size zero; terminal sessions still consume their retained batch; stale queried sizes are never honored against a newer step's batch. Review verified the snapshot/commit race semantics, wire fidelity against the presentation value, and that the step-generation edit preserves the step family's behavior. |
| 5.8 | same | expected-red frame/status build failure; `go test ./packages/client/cmd/mornlea-godot-core -run 'FrameSnapshot\|Metrics\|Capacity\|OptionalFamily' -race -count=1` (plus `-count=3`); full package `-race`; gofmt/vet; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | Frame and status pulls complete the data plane: the `MCF1` wire carries the frozen header plus fixed camera/HUD/environment/entity-batch-tick/entity/target/phase-error records and the UTF-8 target-name tail (max 576 bytes, entity count capped at the presentation limit of seven), encoding exactly what the retained snapshot holds — HUD-not-ready serializes readiness zero with no fabricated values and the terminal frame served after disconnect carries phase Disconnected with the error word. Pulls are non-consuming and repeated pulls are byte-identical; the status family reports phase, terminal cause (six-way classifier with the safe internal downgrade), steps completed, and messages processed. The optional-family proof pins v1 bytes through an independent literal-offset reference encoder and a bumped-version registry variant. Review verified field-by-field fidelity, terminal-snapshot faithfulness, and the classifier's drift characteristics before four prescribed fixes (bound-attribution comments, the block-display-name budget pin, the classifier comment, and the registry-variant lookup). |
| 5.9 | same | expected-red missing matrix/fuzz symbols; `go test ./packages/client/cmd/mornlea-godot-core -run 'Matrix\|Precedence\|FuzzClientCoreABI' -count=1`; `go test ./packages/client/cmd/mornlea-godot-core -fuzz FuzzClientCoreABI -fuzztime 30s` (2.44M execs) and a post-fix 15s rerun (1.17M execs), both zero findings with no crasher corpus; full package `-race`; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot abi_matrix --locked` (4 tests) and the full crate (23); clippy/fmt; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation | The error-precedence matrix now covers every export with combined-defect cells (141 subtests) proving the documented order — including alias-versus-capacity-signal, shape-versus-identity, and content-versus-connection-state pairs — each asserting status vocabulary, untouched canary outputs, and recovery, with the three recorded conflations present as labeled rows. The fuzz corpus drives all eleven exports offline through selector-gated paths that now genuinely reach the create-success, world-write, and frame-retention deep paths, asserts two-phase idempotency in-body, and resets the session table per iteration. The Rust matrix adds an exhaustive fail-closed status-bucket dispatch with obligation strings and a ninety-nine-cell negotiation matrix as the binding task's contract baseline. Review probes found five test-level defects (one real: a seed packed the ABI identity halves backwards), all fixed in one round with the fuzzer still at zero findings. |
| 5.10 | same | expected-red missing script; `scripts/godot/build-core.sh --verify` end-to-end (exit 0, symbol/header/dependency/identity lines reported) rerun by controller after review fixes; `otool -L` on the built core showing both `@rpath` project dependencies; focused `go test ./packages/audit -run GodotBuildCore -count=1` and the full audit; package race; `bash -n`; `make comment-language-check`; strict change validation; bin tree untracked | The client core now builds as a c-shared library into the established `macos-universal` bin tree with debug/release profiles, host-arch-bounded targets, and fail-closed prerequisites: the engine and client dylibs are colocated from the deployed build, cgo-embedded build-tree rpaths are replaced with `@loader_path`, and the artifact is ad-hoc re-signed. Verification proves all twelve exported symbols, the canonical header (generated one deleted), a real dlopen roundtrip from outside the repository with expectations parsed from the committed header, and dependency resolution through `@loader_path` rejecting every non-relative rpath. Review ruled the discovered `libmornlea_client.dylib` link an acceptable recorded deviation (ledger ruling above) and drove one minor plus two nit fixes, including a BSD-sed incompatibility caught and corrected during the controller's own fix pass. |
| 6.1 | same | expected-red zero matched tests; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot client_core --locked` (7 tests) and the full crate (30); `cargo clippy -p mornlea_godot --all-targets --locked -- -D warnings`; `cargo fmt --check`; full `go test ./packages/audit -count=1`; strict change validation | The handwritten client-core FFI module declares all twelve producer exports in one `unsafe extern "C"` block with typed status/handle newtypes and width/alignment const-asserts, every declaration's full signature pinned against the module's own source and every symbol name pinned both-direction against `exports.go`; the status-text table covers every defined status with header-verbatim strings and fails closed outside the vocabulary. All `unsafe` in the crate is confined to this module by a recursive source-scan test whose fail-closed lexer panics on every raw-string opener (both hiding routes empirically re-probed after the fix) and permits only the pinned godot-rust `ExtensionLibrary` impl elsewhere. Review verified signature fidelity char-by-char before the hardening round; tests deliberately reference no extern item as a value so the crate stays dylib-free until the bridge task. |
| 6.2 | same | expected-red unresolved-import build failure; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot bridge_lifecycle --locked` (18 tests) and the full crate (49, confinement scan included); clippy/fmt; `scripts/godot/build-core.sh --verify`; `scripts/godot/python-runtime-check.sh --bridge-contract` (rerun by the controller after a scene-assertion addition); `scripts/godot/smoke.sh --iterations 100 --isolated-python` on the final tree; full `go test ./packages/audit -count=1`; `make comment-language-check`; `python-check.sh --locked`; `validate-project.sh` | The bridge is a scene-held Node instance with typed Godot-visible methods for create/connect/poll/submit/step/close and the four two-phase pulls returning copied packed-byte records — Python never receives a pointer or a buffer it must free, off-thread calls answer an internal status without panicking, and destruction is idempotent end to end (producer tombstone plus handle take, Drop closes before the call table drops, fitting the recorded teardown order). The producer resolves at runtime beside the extension with every vtable signature pinned to the frozen declarations; py4godot's public cast registration bridges the scene wrapper without any new patch. The new `--bridge-contract` gate runs the real core through a headless scene under network denial, byte-comparing the identity record and family table, exercising offline rejections, async connect-cancel, double close, and re-create; the hundred-cycle smoke passes with markers byte-unchanged. Review verified the retry-loop bound, missing-dylib loudness, and the export-placement forward obligations now recorded as rulings. |
| 6.3 | same | expected-red discipline-test and open-session-scene failures; `scripts/godot/godot.sh --headless --path apps/mornlea-godot --quit-after 5` (markers plus an all-disabled catalog as designed); the open-session-quit run (clean exit, drop-close before Rust deinit); `scripts/godot/python-check.sh --locked`; `go test ./packages/audit -run 'GodotScriptsDoNotImplementProtocol\|GodotHostIsFeatureAgnostic\|GodotPythonBoundary' -count=1` (12 probes) and the full audit; `validate-project.sh`; `--bridge-contract`; the new `--host-open-session` mode; `feature-contract-check.sh` plus the extensibility probe; a 5-iteration isolated smoke sanity; `make comment-language-check`; strict change validation | The Python feature host now binds the real native bridge: `app_root` places a `MornleaClientBridge` node, verifies its typed identity statics, and hands the host the node path; the host and each feature acquire the typed object themselves through `get_node` plus the registered identity cast, because the pinned runtime cannot marshal project-class objects across script modules (review-verified against the cast machinery); capability negotiation reads the bridge's typed family table with a documented numeric-identifier grammar. Two new audit disciplines enforce the boundary — production scripts carry no protocol markers or wire-record codecs (magics, hex words, byte prefixes, `struct` codecs including the direct-import and `Struct` forms, both scanners aligned) and host files name no gameplay bridge method or concrete feature — with twelve executed mutation probes, and the Python boundary checker gained the same path-scoped rules with mutation-tested rejection. The open-session teardown obligation is a repeatable gate; the retired static facade survives only as the coexistence subject; review confirmed the re-pinned audit tests are strictly stronger and recorded the skeleton/manifest divergence as the enabling-feature forward obligation. |
| 6.4 | same | expected-red unresolved-import build failure; `cd packages/engine && rustup run 1.97.1 cargo test -p mornlea_godot pull_buffers --locked` (11 tests) and the full crate (60, confinement scan and all 6.2 bridge tests unchanged); `cargo clippy -p mornlea_godot --all-targets --locked -- -D warnings`; `cargo fmt --check`; full `go test ./packages/audit -count=1`; strict change validation | All four pull families now route through per-session reusable buffers: empty-until-first-pull, growth to exactly the declared requirement at word granularity, hard fail-closed on any requirement beyond the frozen family limit (query and retry paths), no shrink, and steady-state reuse proven by grow counters plus seam-recorded backing-address stability across pulls. World and frame pulls track the served publication identity and refuse strictly older records (equal identities legal for all families; identities reset on session create/close), so stale-epoch bytes can never re-serve as fresh, and repeated empty batches never leak the previous record. The maximal 4,325,408-byte world batch round-trips without regrowth; mid-two-phase failures and failed retries leave buffer and served state untouched. Review verified the ceil8 discipline against the producer's capacity semantics, the epoch monotonicity on the Go side, and the two-phase preservation before four comment-scale fixes. |
| 6.5 | same (adopted in-flight node; see the adoption ruling) | `cd packages/engine && cargo test -p mornlea_godot --locked` (66 tests, adopted tree) and again after the review micro-fixes; `cargo clippy -p mornlea_godot --locked --all-targets`; `cargo fmt -p mornlea_godot --check`; `make godot-python-check` (7 unit tests, Ruff format/lint, mypy over 32 sources); `scripts/godot/python-runtime-check.sh --session-feature --offline` (new end-to-end probe); `make godot-smoke` (100 iterations, isolated Python, on the enabled-catalog tree); focused Godot audit set; full `go test ./packages/audit -count=1`; `make godot-project-check`; `make comment-language-check`; `bash -n` on the script; strict change validation | The first real Python feature scene is live: `features/session` owns the pilot session lifecycle display as a Control scene with phase/error labels, never dialing without an explicit `request_connect`, mapping the whole phase vocabulary honestly with stable English error lines keyed by the typed terminal-cause classification and the internal downgrade for drift or unknown causes. Record decoding is Rust-owned (design decision 4): the new `status_decode` module fail-closes every structural violation before any read, skips unknown appended kinds as a decode-layer guarantee, and the bridge's `session_status_typed` serves the typed dictionary with zeroed fields on every error word. The manifest discharges the 6.3 numeric-family obligation (`1@1.0`/`2@1.0`/`7@1.0`), the feature is required and enabled with refresh-only reset, and the `--session-feature` gate drives the real catalog activation, Connecting-to-terminal transition, latched stable dial error, host reset, clean close, and deactivation offline. Independent review APPROVED with no blocking findings; residuals recorded as rulings above. |
| 6.6 | same | expected-red missing mode/scene (usage exit, unknown flag rejected); implementer run then controller rerun of `scripts/godot/feature-contract-check.sh --bridge-integration` (exit 0, `Python bridge integration checks passed.`, zero ERROR/SCRIPT ERROR lines) on the final tree after the review micro-fixes; default-mode and `--extensibility-probe` regressions (both exit 0, markers present); `bash -n`; `make godot-python-check` (35 sources); `make godot-project-check`; full `go test ./packages/audit -count=1`; `make comment-language-check`; strict change validation; implementer mutation probes (family 99 made available; optional failure made to succeed — each produced the expected failure and exit 1, then restored) | Catalog integration now runs against the real native bridge: a required feature demanding unavailable family `99@1.0` fails plan validation with a bridge-family diagnostic before any scene instantiation (zero `instantiate:` trace entries, `active_count` 0), and an optional feature failing at activation is disabled alone while its required sibling stays live (`instantiate:` entry, `active_count` 1, exact disabled list). The mirror-untouched proof samples the real bridge before and after every scenario and after final deactivation: the `feature_families_json` table is byte-identical and `session_status_typed` keeps reporting `MORNLEA_CLIENT_STATUS_INVALID_STATE` (6) with all fields zeroed, so no failed or partial activation ever touched producer state. New fixtures bind the real bridge surface (`feature_families_json` via `has_method`, no stub-only `bridge_identity`), the script mode mirrors the `--session-feature` precedent (silenced build verifies, marker requirement, error scan), and the bilingual README documents all three modes. Independent review APPROVED; one prescription rejected with host-source evidence (recorded ruling), two applied, residuals recorded. |
| 7.1 | same | expected-red 20/29 tests against stub bodies before implementation; implementer run then controller rerun of `cd packages/engine && cargo test -p mornlea_godot quad_decode --locked` (29 passed, 0.42s); `cargo test -p mornlea_godot --locked` (95 passed, unsafe-confinement scan included); `cargo clippy -p mornlea_godot --locked --all-targets -- -D warnings`; `cargo fmt -p mornlea_godot --check` | The pilot now owns an independent CPU decode/expansion of the 8-byte packed quad in `mornlea_godot`: a fail-closed typed decode (bit 63, plant-material-on-axis-face, and plant-reserved-bit rejections exactly mirroring `UnpackQuad`'s three panics), per-face expansion into section-local positions/unit normals/wgpu-convention UVs/shade (face shade × AO factor × hemi plus raw sky/block nibbles), class classification (opaque/cutout/water with the water split re-derived from the combined MCW1 payload), and section/batch decoding within the frozen limits — every constant, set, and formula pinned by source-parsing tests against `mesh/quad.go`, `assets/blocks.go`, `presentation/world.go`, `terrain.wgsl`, `water.wgsl`, `cull.wgsl`, and the render winding order; the reviewer hand-recomputed face/UV/lighting tables, the corner-height lifts, and the iota ladder before approving. Five decode judgments and the residuals (operations-vs-sections rename at 7.2, cutout-set manual-sync, UV-flip ownership at the material tasks) are recorded as rulings. |
| 7.2 | same | expected-red missing worker module (unresolved import); implementer run (5× stable) then controller rerun after review fixes of `cd packages/engine && cargo test -p mornlea_godot mesh_worker --locked` (12 passed); `cargo test -p mornlea_godot --locked` (107 passed); `cargo build -p mornlea_godot --locked` (zero warnings after the quad_decode allow removal); clippy `-D warnings`; fmt --check | The bounded CPU mesh-prepare worker is live: a single background thread consumes section upserts (epoch + `(dimension,x,y,z,revision)` + moved packed payload) through a byte-accounted pending queue (4 MiB, one maximal batch), decodes and expands on the worker thread via `quad_decode` (`check_world_batch` now takes the true operation count — the 7.1 residual discharged), and publishes owned per-surface vertex/index arrays into a byte-accounted completed queue; `Full`/`Closed` hand payloads back, results-full parks the worker (never the main thread), `cancel_pending` retires an epoch permanently, superseded-epoch results are discarded at the single publication point, panics become per-job `STATUS_PANIC` with the worker kept serving, and close joins after at most one in-flight decode with Drop equal to close. quad_decode's `#![allow(dead_code)]` is removed with eight test-only items cfg-gated and dead `axis_vec` deleted. The reviewer traced every interleaving (cancel-vs-publish, sweep-vs-in-flight, close-vs-park) before approving; three clarity fixes applied by the controller. |
| 7.3 | same | implementer run then controller rerun (post-fix) of `make godot-asset-check` (new target wrapping `sync-assets.sh --check`), `make godot-python-check` (37 sources, locked ruff/mypy/boundary), `make godot-project-check`, `go test ./packages/audit -run 'GodotResourceLicenses|GodotPythonTooling|GodotPythonBoundary' -count=1`, and the headless open (order keeps session only, world not instantiated, zero errors); implementer's transient bind probes (positive: bind returns empty against the real manifest; negative: bogus path returns the stable missing-manifest error) with byte-identical restore verified | The Python side of near terrain is structurally complete: `features/world/terrain_near` holds the Node3D compartment scene, the pinned-configuration component (path accessors only, no state or per-frame work), and three surface-class shader+material pairs pinning nearest-within-level mipmapped filtering (the recorded coarsening), the manual pow(2.2) decode with ACES output, cutout alpha discard at 0.5, water alpha/depth behavior, and the full daylight formula — every parameter commented with its wgpu source of truth. `world_feature.py` gained the full lifecycle with bounded bind-time validation (bridge identity, shader token pins, atlas-manifest identity via one bounded file read — schema/format/16x16/5 mips/storage/path plus pixels existence — with sha256 deliberately left to asset-check and the 7.4 Rust loader) and the identity-cast registration; the manifest grammar is numeric `5@1.0`, still disabled for 7.6. `make godot-asset-check` joins the optional gate block. The review loop caught two runtime-only defects (manifest path parsed as text; missing identity cast) before any gate could, both closed with probe evidence and re-review approval. |
| 7.4 | same | implementer run then controller rerun of `cd packages/engine && cargo test -p mornlea_godot terrain_resources --locked` (13 passed); full crate (120 passed); clippy `-D warnings`; fmt --check | The TerrainRenderer RID table is live behind a four-method `RenderBackend` seam: coordinate-keyed `BTreeMap` with per-entry revisions matching the Go validation truth, whole-section upsert that builds every surface before touching live state and publishes through a single instance-registration step, mid-failure rollback that frees only the partial new mesh and keeps the old section exactly intact with no revision recorded (same-revision retry proven), whole-section drop and reset with asserted free order, and the 7.3 shader contract encoded exactly (CUSTOM0 RGBA float shade/sky/block/layer via the api-4-7 bitfield encoding verified against the vendored gdextension JSON, section origin on the instance transform). The scripted backend's double-free/foreign-free/use-after-free panics make every test a leak oracle; the production backend is main-thread-guarded like the bridge and reaches its first live execution at 7.6. Reviewer traced every failure interleaving before approving; four P3s recorded as rulings. |
| 7.5 | same | implementer run then controller rerun (post-fix) of `cd packages/engine && cargo test -p mornlea_godot terrain_budget --locked` (10 passed); full crate (130 passed); clippy `-D warnings`; fmt --check | The per-frame terrain stage is live: fixed 64-section upload and drop budgets with reclamation-first frame order (drop priority proven by event order), out-of-view reclamation under the drop budget using horizontal Chebyshev at the baseline-derived 33-section keep-band (exactness claim corrected to conditional — today's minimum-declaring core makes the band safely wide; the bridge derives or asserts the declared value before reclamation is load-bearing), session reset that cancels, drains, clears, and advances epochs strictly monotonically, honest metrics (packed at accept, expanded and worker-measured prepare time at drain, whole-frame render-phase upload duration, live/peak RID and section counts with session-lifetime peaks), and the structural Node prohibition (two table-owned RIDs per section regardless of block count). Failed upserts drop the expanded payload with no retry queue and no revision advance; the completed-queue ~25x RAM bound stays near-empty under the per-frame drain. Reviewer verified every derivation against the named truths before approving. |
| 7.6 | same | implementer run then controller rerun (post-fix) of `make godot-terrain-check` (exit 0, no-stale marker, clean two-round helper exit); full crate `cargo test -p mornlea_godot --locked` (137 passed) + clippy `-D warnings` + fmt; helper `go build`/`go vet`/tests + full `mornlea-godot-core` suite + presentation corpus; `make godot-python-check` (39 sources); `make godot-project-check`; `make godot-asset-check`; full `go test ./packages/audit -count=1`; regressions: `--bridge-integration`, `--session-feature --offline`, headless open (order [session, world]); `make godot-smoke` (100 iterations, world enabled); `make comment-language-check` | The Godot pilot now renders terrain end to end from transcripts: a deterministic protocol-faithful helper serves a two-round scenario (snapshot rev 1, delta rev 2 with cross-section content, forget, disconnect/reset, re-entry at fresh rev 1), the bridge owns the full pipeline behind four typed main-thread-guarded methods with the atlas built and assigned Rust-side, the world feature is enabled and required with lifecycle-only Python, and the structural summary proves no stale section at every stage (empty inventory after forget AND after close with resets>=1/epoch>=2; re-entry repopulates at revision 1). The Go core gained its two review-verified pilot deviations (mesher options + AdvanceMeshes composition), the broken-at-HEAD asset-check entrypoint registration was repaired, and the first live execution of the production RenderingServer backend happened inside the gate. Review APPROVED (deepest trace on the Go deviations and the no-stale proof); five P3 hardenings applied in one fix round; three recorded. |
| 7.7 | same | implementer run (4x incl. forced-failure reaping evidence), controller reruns (2x — the second caught the crash), defect node 10/10 green, fix-round 3/3 green, final controller rerun green with zero surviving processes; `bash -n`; `make godot-python-check` (40 sources); `make godot-project-check`; `make godot-terrain-check` regression | The real-TCP dedicated-server terrain smoke is live: a temp-rooted `mornlea-server` (absent-config isolation, seed 42, derived port), the pilot logging in and waiting on the transcript gate's loaded-criterion core, 300 fixed frames with post-load strict terrain error words (closing the 7.6 diagnosability residual), a clean feature-owned close with the no-stale reset assertions, and zombie-aware trap reaping proven by two forced-failure paths. The gate immediately earned its keep: the controller's rerun exposed an intermittent c-shared GC fatal whose root cause was the Rust caller passing dangling sentinel pointers for empty FFI buffers — fixed as a class across all boundary paths with scripted-vtable pointer pins, 13/13 consecutive green smokes, and the durable rule promoted to the architecture skill. Review APPROVED with doc-precision and diagnosability fixes applied; two precedent-consistent residuals recorded. |
| 9.1 | same, with every recorded user-owned path excluded | Initial red evidence: Rust lacked typed entity fields, the Python remote-player component did not exist, the eighth Go remote was silently truncated, the tightened despawn test observed tick 0 instead of 7, and the reset test retained one callback unit. Final gates: `make godot-entity-check` (2 Rust decode tests and 5 Python pool tests); `go test ./packages/client/runtime -run RemotePlayer -race -count=1`; `make godot-python-check` (7 boundary tests, Ruff format/lint, mypy over 45 sources); `make godot-project-check`; `cargo fmt --all -- --check`; `cargo test -p mornlea_godot --locked` (150 tests); `cargo clippy -p mornlea_godot --all-targets --locked -- -D warnings`; full `go test ./packages/audit -count=1`; `make comment-language-check`; `git diff --check`; strict change validation | Remote-player presentation now flows through one authority-preserving chain: Go publishes deterministic interpolated semantic entities and rejects overflow, Rust decodes and validates the whole MCF1 entity set into typed Godot values, and Python maps those values onto a fixed seven-instance capsule pool with stable identity, despawn, epoch/reset, backward-order, duplicate, incomplete-pool, and callback-budget guards. The tightened oracle found and fixed a real end-to-end despawn defect: an empty entity batch had discarded its current server tick, so Python correctly rejected the last-player removal as stale. The optional-gate registry now covers all current Godot leaf gates while preserving legacy build isolation. |

## Round Retrospective

- Architecture skill: no change. The failed candidate is dependency-specific evidence, not a stable cross-task Mornlea architecture rule; the existing rule that Python must be isolated remains correct.
- Model router: no change. No delegation was used because the qualification work was sequential and already bounded in the main agent context.

### Task 2.3a

- Architecture skill: updated both project-owned copies with the verified macOS export packaging closure. Future Godot Python work must account for exported GDExtension placement and must materialize the selected embedded runtime and Python sources as filesystem resources instead of relying on PCK, ambient, or current-directory lookup.
- Model router: no change. Direct main-agent execution remained the lowest-token option because the approved hardening and qualification formed one tightly coupled runtime context; no delegated isolation boundary was warranted.

### Task 2.5

- Architecture skill: no change. The verified behavior directly instantiates the existing stable-root and pure-bootstrap rules and does not add a new cross-task placement or dependency decision.
- Model router: no change. The implementation was small, sequential, and already contained in the main agent's Godot bootstrap context; delegation would have increased context-transfer cost without isolating material complexity.

### Task 2.6

- Architecture skill: updated both project-owned copies with the verified separation between the embedded production runtime and the locked development-only Python environment. Both copies passed the skill validator and byte-equality check.
- Model router: no change. Toolchain locking and boundary checks were a focused sequential task with low context-transfer value, so direct execution minimized token use without reducing validation quality.

### Task 2.7

- Architecture skill: updated both project-owned copies with the verified explicit-resource composition rule for isolated Godot Python, the requirement that generated-wrapper ownership defects be fixed and qualified at the Py4Godot boundary rather than worked around in host or feature code, and the user-required rule that new architecture/boundary units include concise English comments at non-obvious ownership, lifecycle, compatibility, and failure decisions. Both copies passed the skill validator and byte-equality check.
- Model router: no change. The Python host, serialized resource graph, and binding-lifecycle defect formed one tightly coupled debugging context already resident in the main agent; delegation would have duplicated that context without isolating a material independent concern.
- Git checkpoint: created `codex/godot-client-migration` from the original implementation baseline and adopted scoped, focused-gate commits before later migration nodes. User-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.

### Task 2.8

- Architecture skill: no change. The verified Python-led coarse-feature layout, desktop-only platform boundary, disabled-audio posture, and English boundary-comment requirement are already stable project rules; this task instantiates them without discovering a new cross-task rule.
- Model router: no change. The eight small skeletons, their scenes, and the ownership audit form one tightly coupled low-context node, so direct main-agent execution avoids delegated context and token overhead.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add Python feature skeletons` commit after its focused and closure gates pass; concurrent governance, routing, renderer, golden, canonical-spec, and historical-plan work remains excluded from staging.

### Task 2.9

- Architecture skill: updated both synchronized project-owned copies with the verified prohibition on Python `ClassDB.instantiate()` for project GDExtension classes in the current Py4Godot derivative. Identity-only calls remain static through Godot; a future Python-held native instance requires a binding repair and lifecycle qualification. Both copies passed the skill validator and byte-equality check.
- Model router: no change. The facade, native identity surface, prepared Bootstrap handoff, and coexistence probe shared one narrow lifecycle/debugging context, so direct main-agent execution avoided token-heavy transfer without reducing independent runtime evidence.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add typed bridge host` commit after closure gates pass; all concurrent governance, routing, renderer, golden, canonical-spec, and historical-plan work remains excluded from staging.

### Task 2.10

- Architecture skill: updated both synchronized project-owned copies with the verified cross-runtime lifecycle order and the requirement to convert Rust panics/invalid transitions into stable failures. Both copies passed the skill validator and byte-equality check.
- Model router: no change. Rust state-machine tests and the headless lifecycle runner were a focused ownership boundary already resident in the main-agent context; delegation would duplicate the exact runtime ordering context and consume additional tokens.
- Git checkpoint: the node is isolated for a scoped `feat(godot): enforce host lifecycle order` commit after closure gates pass; unrelated concurrent changes remain excluded from staging.

### Task 2.11

- Architecture skill: updated both synchronized project-owned copies with the verified asset-authority and generated-closure rule. Future Godot resource work must derive from the production registry and registered fonts, retain licenses/provenance, bind outputs to input revisions and checksums, and reject manual drift. Both copies passed the skill validator and byte-equality check.
- Model router: no change. The registry generator, manifest, provenance, and mutation audit formed one focused deterministic-build context; direct main-agent execution avoided context-transfer token cost, and no isolation boundary justified delegation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add deterministic asset sync` commit after focused and closure gates pass; unrelated concurrent governance, routing, renderer, golden, canonical-spec, supervisor, and historical-plan changes remain excluded from staging.

### Task 2.12

- Architecture skill: updated both synchronized project-owned copies with the verified source/export resource-closure rule. Future desktop presets must add target-specific exclusion sets while preserving common rejection of escaped paths, implicit autoload state, ignored UID/import sidecars, development/test/diagnostic/provenance content, unselected features, and non-target native/Python libraries. Both copies passed the skill validator and byte-equality check.
- Model router: no change. The validator, export preset, and mutation audit share one compact project-closure context, so direct main-agent execution remained the lowest-token implementation shape and no context-isolation boundary justified delegation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): enforce project export closure` commit after focused, comment-language, full architecture, and strict OpenSpec gates pass; unrelated concurrent governance, routing, renderer, golden, canonical-spec, supervisor, and historical-plan changes remain excluded from staging.

### Task 2.13

- Architecture skill: no change. The isolated, headless, repeated cross-runtime lifecycle gate directly exercises the existing lifecycle-order, embedded-Python isolation, and desktop-only rules without discovering a new placement or dependency boundary.
- Model router: no change. Extending the existing lifecycle runner was a compact, tightly coupled shell-and-audit node already resident in the main agent; delegation would have duplicated runtime context and increased token use without isolating a material concern.
- Git checkpoint: the node is isolated for a scoped `test(godot): harden isolated lifecycle smoke` commit after the 100-cycle gate, focused architecture audit, and strict OpenSpec validation pass; unrelated concurrent governance, routing, renderer, golden, canonical-spec, supervisor, and historical-plan changes remain excluded from staging.

### Task 2.14

- Architecture skill: updated both synchronized project-owned copies with the verified optional-gate rule — Godot build gates are leaf entry points, legacy make entries never reference `scripts/godot` or a `godot-*` target, and the CI Godot job stays outside required pipeline gates until an approved cutover change promotes it. Both copies passed the full audit suite and the byte-equality check.
- Model router: no change. One native `general-purpose` implementer at the inherited host default completed the bounded three-file change with one review-driven fix round; the independent reviewer catching the guard's coverage holes is the designed strict-SDD control rather than a routing insufficiency, and no stable cross-task routing rule emerged.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add optional make and ci gates` commit after the focused and closure gates pass; unrelated concurrent governance, routing, renderer, golden, canonical-spec, supervisor, and historical-plan changes remain excluded from staging.

### Task 2.15

- Architecture skill: no change. The completed mutation matrix directly proves the existing desktop-only source/export closure and isolated-Python rules without adding a new ownership, placement, dependency, or lifecycle decision.
- Model router: no change. Native `gpt-5.6-terra` at medium effort was sufficient for the bounded Go-and-shell test node; one controller feedback round corrected CLI composition, fixture cost, and false-positive scope before the worker's focused gates passed, and the controller's full audit found no further implementation defect.
- Git checkpoint: the node is isolated for a scoped `test(godot): enforce desktop-only mutation gates` commit after focused mutation gates, normal project validation, the full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 3.1

- Architecture skill: no change. The pure value package instantiates the existing renderer-independent snapshot, safe-zero, immutable-publication, and server-authority rules without adding a new durable ownership, lifecycle, placement, or dependency decision.
- Model router: no change. Native `gpt-5.6-sol` at high effort was appropriate for the cross-host public value contract and weaker design oracle; the worker completed the test-first implementation without escalation, and controller review was limited to public naming and additional finite-domain coverage.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add presentation value types` commit after the focused race gate, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 3.2

- Architecture skill: no change. The bounded immutable batch, atomic failure, and renderer-independent value rules already cover this node; the approved eight-byte packed quad remains a terrain input specific to the existing design rather than a new cross-task architecture rule.
- Model router: no change. Native `gpt-5.6-terra` at high effort was sufficient for the bounded presentation contract. One worker feedback round added the aggregate mesh cap and packed-input edge cases, while controller review tightened exact operation boundaries and stale-drop coverage without escalation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add bounded world batches` commit after the focused and full presentation race gates, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 3.3

- Architecture skill: no change. The entity contract directly instantiates existing server-authority, renderer-independent value, bounded-batch, immutable-publication, and atomic-failure rules; the seven-remote-player limit is a current protocol/server fact, not a new cross-task rule.
- Model router: no change. Native `gpt-5.6-terra` at high effort completed the bounded cross-package contract without retry or escalation, and controller review confirmed the capacity source and semantic/GPU separation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add semantic entity batches` commit after the focused and full presentation race gates, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; concurrent task 3.5 work and unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 3.4

- Architecture skill: no change. The corpus instantiates existing version-lock, bounded-input, deterministic-artifact, explicit-rejection, and real-codec reuse rules without creating a new runtime ownership or dependency boundary.
- Model router: no change. Native `gpt-5.6-sol` at high effort completed the broad protocol-corpus design without retry or escalation; deterministic codec replay, stable digests, focused tests, and controller review supplied sufficient validation.
- Git checkpoint: the node is isolated for a scoped `test(godot): add protocol transcript corpus` commit after focused replay, full presentation race, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 3.5

- Architecture skill: no change. The characterization transcript records existing app behavior for later parity without changing ownership, lifecycle, authority, or renderer boundaries.
- Model router: no change. Native `gpt-5.6-sol` at high effort was appropriate for the broad Darwin app context and weaker capture-then-freeze oracle; the bounded test-only task passed focused repetition, full race, vet, and controller review without retry or escalation.
- Git checkpoint: the node is isolated for a scoped `test(godot): characterize legacy client runtime` commit after focused repetition, full app race, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; concurrent task 3.4 corpus work and unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.1

- Architecture skill: no change. The lifecycle skeleton directly instantiates existing reverse-order cleanup, safe-zero presentation, platform-independent ownership, and idempotent-close rules without adding a new cross-task boundary.
- Model router: no change. Native `gpt-5.6-terra` at high effort was sufficient for the bounded lifecycle/concurrency node; one controller review round corrected the initial terminal start phase and source-comment identifier formatting before all race, vet, audit, and comment gates passed.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): add client lifecycle skeleton` commit after focused/full runtime race, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.2

- Architecture skill: no change. The remote constructor directly reuses the existing TCP, login, protocol validation, and bounded receiver ownership rules; the private logged-in endpoint seam preserves later local assembly without creating a new cross-task boundary.
- Model router: no change. Native `gpt-5.6-terra` at high effort completed the runtime-only composition without escalation; controller review added real v44 rejection, full identity prevalidation, and joined terminal/close errors before all focused and closure gates passed.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): compose remote client sessions` commit after focused/full runtime race, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.3

- Architecture skill: no change. The private mirror aggregate, copied host-facing state, bounded nonblocking drain, atomic failure, and platform-neutral outcome directly instantiate existing server-authority, immutable-publication, budget, and host/runtime ownership rules without adding a new cross-task decision.
- Model router: no change. Native `gpt-5.6-sol` at high effort was appropriate for the broad legacy message chain and weaker parity oracle; the worker completed the twelve-family composition without escalation, and controller review added one atomic-publication regression test and fix before all focused and closure gates passed.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): compose authoritative mirrors` commit after focused/full runtime race, old client/app parity gates, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.4

- Architecture skill: no change. The semantic-input copy boundary, reuse of the existing predictor/collision source, single session sequence, borrowed sender/receiver close ownership, explicit elapsed time, and readiness-driven phases directly instantiate existing authority, lifecycle, and host/runtime boundaries without adding a new durable rule.
- Model router: no change. Native `gpt-5.6-sol` at high effort was appropriate for the broad predictor and input chain; the worker incorporated controller feedback to separate submission from advance, retain correction-smoothed presentation state, and unify resync ordering without escalation, while controller review added negative-time and lifecycle-phase closure tests.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): compose client prediction` commit after focused/full runtime race, direct predictor parity, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.5

- Architecture skill: no change. The runtime-owned semantic camera/target result, host-owned device and matrix state, mirror-backed occlusion, persistent local preference, and one-presentation reset suppression directly instantiate existing presentation and ownership rules without adding a new durable cross-task decision.
- Model router: no change. Native `gpt-5.6-terra` at high effort was sufficient for the bounded camera-math node; the worker completed the test-first implementation without escalation, and controller review added persisted-mode validation plus same-pose reset suppression before all closure gates passed.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): add camera presentation` commit after focused/full runtime race, legacy app camera parity, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.6

- Architecture skill: no change. Optional runtime ownership of the established bounded Mesher, nonblocking reset invalidation, immutable bounded world batches, terminal no-loss overflow, and the existing `mesh`/`nativeabi` path directly instantiate current worker, ABI, hot-path, and publication rules without adding a new durable cross-task decision.
- Model router: no change. Native `gpt-5.6-sol` at high effort was appropriate for the concurrency-sensitive worker/queue node; controller review found and corrected the initial Reset wait and partial overflow-transition risks, after which focused repetition and all closure gates passed without escalation.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): compose mesh pipeline` commit after focused/full runtime race, direct Mesher race, repeated stale-result validation, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.7

- Architecture skill: updated both synchronized project-owned copies with the verified frame-record placement rule — the per-step client frame aggregate is an immutable semantic value in `packages/client/presentation`, and every host or client-core ABI frame family consumes that record instead of growing a second host-private or renderer-specific frame encoding. Both copies passed the byte-equality check.
- Model router: no change. One native `general-purpose` implementer at the inherited host default completed the adopted-red-test implementation across two review-driven fix rounds (queue-full recovery documentation plus comment hygiene, then one backtick-identifier repair surfaced by the full audit suite); the comprehensive pre-existing red test was the decisive oracle, and no stable cross-task routing rule emerged.
- Git checkpoint: the node is isolated for a scoped `feat(runtime): add explicit bounded step` commit after focused/full runtime race, presentation race, vet, full architecture audit, comment-language validation, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.8

- Architecture skill: updated both synchronized project-owned copies with the verified host-adoption rule — a host with already-constructed session state consumes `packages/client/runtime` through the adoption seam instead of duplicating mirror, predictor, or sequence ownership; both Memory and remote paths keep one behavior source. Both copies passed the byte-equality check.
- Model router: no change. The inherited-default native worker was credible for the largest node because the oracle (pinned app tests, per-loop characterization gates, recorded visual differential) carried the risk; the stall was an infrastructure failure, not a capability failure, and the prescribed fixture repair was cheaper as a direct controller edit than any re-route.
- Git checkpoint: the node is isolated for a scoped `refactor(app): consume runtime for frame paths` commit after the full gate battery; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 4.9

- Architecture skill: no change. The six prohibitions instantiate already-recorded rules (server authority isolation, no Go GPU imports, platform-independent semantics packages) rather than discovering a new placement or dependency decision; enforcement now lives in the audit itself.
- Model router: no change. A fresh native worker at the inherited default delivered the single-file audit with self-caught pre-gate fixes and a clean independent review; the two reviewer-prescribed hardening edits were cheaper as direct controller changes than a re-dispatch.
- Git checkpoint: the node is isolated for a scoped `test(audit): guard client runtime boundaries` commit after the focused drift-probe gate, full audit, race, comment-language, and strict OpenSpec gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.1

- Architecture skill: updated both synchronized project-owned copies with the verified client-core ABI source-of-truth rule — the header at `packages/client/cmd/mornlea-godot-core/include/mornlea_client_core.h` is canonical, Go derives constants through cgo, Rust mirrors constants and layouts while parsing the same header, and both sides enforce both-direction define-set equality so silent drift is impossible. Both copies passed the byte-equality check.
- Model router: no change. The engine-ABI precedent carried the pattern; one implementation round plus one prescribed fix round at the inherited default was proportionate to a definition-and-cross-check node.
- Git checkpoint: the node is isolated for a scoped `feat(godot): define client core abi v1` commit after the focused Go/Rust gates, full-crate Rust tests, clippy, fmt, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.2

- Architecture skill: no change. The registry instantiates the already-recorded one-way product-assembly direction and the 5.1 header source-of-truth rule; the accept/reject contract is pinned in code and tests rather than being a new cross-task placement decision.
- Model router: no change. The inherited-default worker chained the registry onto the 5.1 pins with both-language red-first coverage; review was clean and the two prescribed fixes were comment-level. Controller lesson recorded: Rust-touching closeouts must include clippy — the 5.1 record overclaimed it and 5.2's worker caught the break.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add feature family registry` commit after the focused Go/Rust gates, full-crate tests, clippy, fmt, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.3

- Architecture skill: no change. The handle table, validation order, two-phase identity export, and panic guard directly instantiate the already-recorded versioned-bridge, failure-atomicity, and 5.1 header rules; the producer-internal rulings are recorded in the change ledger rather than as cross-task architecture rules.
- Model router: no change. The first export-surface node completed in one round with a clean review at the inherited default; the multi-defect order proofs were specified in the brief and the worker delivered them without escalation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add identity lifecycle exports` commit after the focused race gate, full-package race, gofmt/vet, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.4

- Architecture skill: no change. The establishment-goroutine ownership, fixed lock order, cancel-and-join teardown, and phase-signal mapping instantiate the existing lifecycle, immutability, and failure-atomicity rules; the one durable forward obligation (pinning the producer-side phase words cross-language when the Rust FFI lands) is recorded in the ruling above where task 6.1 will find it.
- Model router: no change. The inherited-default worker delivered the first async node using runtime's existing seams without any runtime modification; the reviewer's one test-gap prescription came back deterministic by construction.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add async connection exports` commit after the focused race gates, full-package race, untouched-runtime verification, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.5

- Architecture skill: no change. The one-batch-one-frame folding, absolute-pose encoding, and whole-batch atomicity instantiate the existing frame-boundary and failure-atomicity rules; the two deferrals (text capacity, fixed record bytes) are recorded as rulings where the consumer task will find them.
- Model router: no change. The inherited-default worker mapped the wire onto runtime's own validator without runtime changes and produced a clean APPROVED with no code findings.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add input submission export` commit after the focused race gate, full-package race, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.6

- Architecture skill: no change. Per-session step serialization with result retention instantiates the existing single-driver frame contract and failure-atomicity rules at the ABI boundary; the three durable obligations (backpressure status-code addition, runtime wrap edges, transient terminal misclassification) are recorded as rulings where the error-matrix and runtime follow-ups will find them.
- Model router: no change. The inherited-default worker delegated the step semantics to the already-tested runtime surface and added only bounded concurrency; review APPROVED without code findings.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add bounded step export` commit after the focused race gate with repeated stability runs, full-package race, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.7

- Architecture skill: no change. The generation-guarded consumption commit and producer-side wire vocabulary under a frozen header instantiate the existing two-phase and failure-atomicity rules; the one open generalization (size-word overlap discipline across families) is recorded in the ruling for the consumer FFI task.
- Model router: no change. The inherited-default worker encoded the already-frozen presentation value with one new leaf lock; review APPROVED with nits only.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add world batch pull export` commit after the focused race gate, full-package race, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.8

- Architecture skill: no change. Non-consuming presentational pulls, snapshot purity as the forward-compatibility proof, and the safe-downgrade classifier instantiate the existing frame-record, two-phase, and failure-atomicity rules; the classifier's producer-side drift blind spot is documented where a sentinel-introduction refactor will find it.
- Model router: no change. The inherited-default worker encoded the frozen frame value with no new concurrency and delivered a review whose four findings were all comment/test-level prescriptions.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add frame and status pull exports` commit after the focused race gate, full-package race, full audit, comment-language, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.9

- Architecture skill: no change. The precedence matrix and fuzz corpus enforce the already-recorded validation-order and failure-atomicity rules; the Rust matrix is the contract baseline for the existing versioned-bridge rules rather than a new placement decision.
- Model router: no change. A validation-only node at the inherited default; the one real defect found anywhere was a fuzz-seed packing bug caught by review, not by capability strain.
- Git checkpoint: the node is isolated for a scoped `test(godot): add abi error matrix and fuzz corpus` commit after the matrix, fuzz (30s + 15s, zero findings), full-package race, Rust matrix, clippy/fmt, full audit, comment-language, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 5.10

- Architecture skill: updated both synchronized project-owned copies with the verified distribution-unit deviation rule — the pilot client core colocates `libmornlea_client.dylib` beside the GDExtension, Go core, and engine dylib purely because the darwin cgo entry files share the mirrors' Go package; no production symbol crosses that link, the split that removes it must precede any exported distribution, and per-platform pilot reports must account for the eager framework loads it drags in. Both copies passed the byte-equality check.
- Model router: no change. The packaging node reused the established godot build-script conventions; the review's findings were process records plus small hardening, and the one incompatibility surfaced during controller fixes (BSD sed alternation) argues for controller-run end-to-end verification of script edits, which this closeout performed.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add client core build script` commit after the end-to-end verify run, focused and full audit, package race, comment-language, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 6.1

- Architecture skill: updated both synchronized project-owned copies with the verified unsafe-confinement rule — every `unsafe` in `mornlea_godot` lives in the client-core FFI module (one extern block plus its fail-closed source-scan enforcement), so later bridge and buffer work must extend that module rather than opening unsafe anywhere else. Both copies passed the byte-equality check.
- Model router: no change. The declaration-and-pinning node at the inherited default produced clean signature fidelity on first review; the hardening round was two empirically verified lexer fixes, not capability strain.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add client core ffi module` commit after the focused and full crate tests, clippy, fmt, full audit, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 6.2

- Architecture skill: updated both synchronized project-owned copies with the verified runtime-resolution rule — the extension resolves the client core beside its own image at load time (never at link time, because the Rust workspace must build from a clean checkout), pins every resolved signature to the frozen declarations, and fails closed when the dylib is absent; before any exported distribution the export pipeline must carry the core dylib as a filesystem resource. Both copies passed the byte-equality check.
- Model router: no change. The heaviest integration node to date completed in one implementation round at the inherited default, with the review APPROVED on first pass; the deep uncertainties (producer coupling, Python instance acquisition) were resolved by the worker using documented in-repo patterns rather than escalation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add bridge session lifecycle` commit after the cargo, contract, and hundred-cycle smoke gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 6.3

- Architecture skill: updated both synchronized project-owned copies with the verified host-injection rule — Python host and features acquire the typed bridge node themselves via `get_node` plus the registered identity cast because the pinned Py4Godot runtime cannot marshal project-class objects across script-module calls, host bindings carry scene paths, and host files never name gameplay bridge methods or concrete features (enforced by two audit disciplines plus path-scoped checker rules). Both copies passed the byte-equality check.
- Model router: no change. The rewiring and discipline tests at the inherited default needed one review round whose findings were checker-test gaps and one lexical bypass — both mechanical, closed by the controller; the marshal-limitation root cause was confirmed by review against the runtime source rather than requiring escalation.
- Git checkpoint: the node is isolated for a scoped `feat(godot): inject bridge into feature host` commit after the headless bootstrap, python-check, focused and full audit, project validation, contract and open-session gates, and strict OpenSpec validation pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 6.4

- Architecture skill: no change. The per-family reusable buffers, growth ceiling at the frozen contract limit, and publication-identity refusal instantiate the existing two-phase, failure-atomicity, and bounded-hot-path rules; the per-session worst-case retention bound and the unvalidated-header trust boundary are documented at the owning code and recorded in the ruling.
- Model router: no change. A pure-Rust buffer layer over the established driver completed in one round with comment-scale review findings; the one literal the controller mis-fixed during cleanup was caught by the focused rerun, which is the gate system working as designed.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add reusable pull buffers` commit after the focused and full crate tests, clippy, fmt, full audit, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes remain excluded from staging.

### Task 6.5

- Architecture skill: updated both synchronized project-owned copies with the verified first-feature enablement rule — a Python feature manifests the implemented subset of the numeric family registry (`family@major.minor` keyed by the bridge's numeric identifiers), declares no dependency on a still-disabled skeleton, and pins its display to typed bridge views whose record decoding stays Rust-side, so enabling a feature never silently loosens the manifest grammar or the typed-values-only boundary. Both copies passed the byte-equality check.
- Model router: no change. The adoption node needed one reviewer dispatch at the inherited host default (no override surface exposed); the orphan implementation was coherent enough that review returned APPROVED with process items and nits, and the two code-level prescriptions were comment/test-list edits cheaper as direct controller changes than a re-dispatch, matching the task-4.9 precedent.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add session feature scene` commit after the full crate tests, clippy, fmt, python-check, the new session-feature probe, the hundred-cycle smoke on the enabled-catalog tree, full audit, project validation, comment-language, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.

### Task 6.6

- Architecture skill: no change. The catalog integration probe instantiates the already-recorded rules (plan validation before instantiation, numeric family negotiation, optional-failure isolation, typed-views-only observables) rather than discovering a new placement or dependency decision; the two residuals are recorded as rulings where the section-11 gates and the P4/P5 session tasks will find them.
- Model router: no change. The test-only pattern-following node completed in one implementer round at the inherited default with self-run mutation probes strengthening the oracle, and the reviewer's APPROVED needed only micro-edits plus one evidence-backed rejection — no routing strain observable at any step.
- Git checkpoint: the node is isolated for a scoped `test(godot): add bridge catalog integration` commit after the controller-rerun bridge-integration probe, both mode regressions, python-check, project validation, full audit, comment-language, and strict OpenSpec validation gates pass; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.

### Task 7.1

- Architecture skill: no change. The decode module instantiates the already-recorded Rust-owns-decode and cross-language-pin rules (the header source-of-truth pattern generalized to the packed-quad vocabulary); the deliberate convention splits (wgpu UV ownership, daylight at the material layer) are recorded in the module doc and the rulings rather than as new placement rules — P4's later tasks may promote the layering once it survives the worker and renderer.
- Model router: no change. A bounded pure-compute node with a self-contained oracle completed in one implementer round at the inherited default; the reviewer's hand-recomputation confirmed fidelity without escalation, and all three findings were disposition-level rather than capability-strain signals.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add packed quad decode` commit after the controller-rerun named gate, the full crate suite, clippy, and fmt pass; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.

### Task 7.2

- Architecture skill: no change. The worker instantiates the already-recorded bounded-hot-path, immutable-after-send, panic-to-boundary-failure, and two-phase rules at a new seam; the queue-bound derivation and the epoch/cancel contract edge are recorded in the ruling where the 7.4/7.5 consumers will find them rather than as new cross-task placement rules.
- Model router: no change. The first concurrency-bearing node completed in one implementer round at the inherited default with five repeat-stable gate runs; the reviewer's five findings were comment-level, three closed by controller micro-edits — no capability strain observable.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add mesh prepare worker` commit after the controller-rerun named gate, full crate, zero-warning build, clippy, and fmt pass; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.


### Task 7.3

- Architecture skill: no change. The compartment structure, disabled-until-proven enablement, typed-bind acquisition, and pinned-resource-without-texture decisions instantiate the already-recorded feature-ownership, numeric-family, and typed-boundary rules; the shader-layer ownership of the UV/daylight residuals is recorded in the module and the ruling where 7.4/7.6 will consume it.
- Model router: no change. The pattern-following node exposed an oracle gap (static gates cannot exercise a disabled feature's runtime bind) rather than a capability gap; the review round and scratch probe closed it at the inherited default, and the routing lesson (keep a bind-probe until the 7.6 gate exists) is recorded in the routing record rather than the router skill because it is task-scoped, not model-selection knowledge.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add terrain near feature structure` commit after the controller-rerun asset/python/project/audit gates and the headless open pass on the post-fix tree; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.


### Task 7.4

- Architecture skill: no change. The publish-last atomicity, borrowed-resource ownership, and revision monotonicity instantiate the already-recorded failure-atomicity and versioned-bridge rules at the renderer seam; the design-wording interpretation and the 7.5/7.6 handoffs (range-based enumeration, retry bookkeeping, live-engine verification) are recorded in the ruling where their consumers will find them.
- Model router: no change. A seam-architected module over two in-crate precedents completed in one round at the inherited default; the reviewer's findings were all disposition-level, showing no capability strain at any step.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add terrain rid table` commit after the controller-rerun named gate, full crate, clippy, and fmt pass; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.


### Task 7.5

- Architecture skill: no change. The budget unit, drop-priority frame order, conditional view-band exactness, and metrics placement instantiate the already-recorded bounded-hot-path and failure-atomicity rules; the bridge-facing obligations (declared-view assertion, once-per-process frame, drop bounding, spawn-failure mapping) are recorded in the ruling where the 7.6 integration will find them.
- Model router: no change. The composition node over two seam-tested modules completed in one round at the inherited default; the reviewer's one P2 was a doc-accuracy correction the controller applied directly, with no capability strain anywhere in the loop.
- Git checkpoint: the node is isolated for a scoped `feat(godot): add terrain frame budgets` commit after the controller-rerun named gate, full crate, clippy, and fmt pass on the post-fix tree; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.


### Task 7.6

- Architecture skill: no change for the pipeline shape — the bridge-owned terrain pipeline, lifecycle-only Python, transcript-driven gates, and the two Go core deviations all instantiate already-recorded rules (typed bridge views, Rust-owns-bulk, bounded budgets, pilot-scoped core seams); the one durable process rule (make-entrypoint changes require the FULL audit package) is recorded as a ledger ruling because it is a closeout-discipline lesson, not an architecture placement decision.
- Model router: no change. The largest node of the campaign completed with one implementer round plus one prescribed fix round at the inherited default; the reviewer's deepest-trace findings were P3 hardenings, and the controller spot-check sufficed for fix-round verification — no escalation signal at any step.
- Git checkpoint: the node is isolated for a scoped `feat(godot): drive terrain check from transcripts` commit after the controller-rerun terrain gate, Python/project/asset gates, full audit, smoke, and comment-language pass on the post-fix tree; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.


### Task 7.7

- Architecture skill: updated both synchronized project-owned copies with the verified FFI pointer-discipline rule — pointers crossing the Go client-core boundary are null or genuinely valid buffers, because cgo holds C pointer arguments in pointer-typed stack slots that Go's stack-copy scan checks against `minLegalPointer` (4096) without dereferencing, so sentinel or dangling-aligned addresses are intermittent fatals, and null is the only placeholder the boundary universally tolerates. Both copies passed the byte-equality check.
- Model router: no change. The smoke node and the defect investigation both completed at the inherited default with no escalation; the investigator's analytical falsification of the cheap hypothesis before spending the rebuild is the proportionate-cost behavior the router already prescribes, not a new rule.
- Git checkpoint: the defect fix is isolated for a scoped `fix(godot): pass null for empty ffi buffers` commit and the smoke node for a scoped `feat(godot): add real server terrain smoke` commit after the final controller reruns of both terrain gates pass with zero surviving processes; unrelated user-owned renderer, golden, canonical-spec, progress, and historical-plan changes remain excluded from staging.
### Tasks 8.1-8.2

- Architecture skill: no change. Desktop semantic action names, unsupported-action fail-closed behavior, and cursor/focus baseline handling instantiate the existing desktop-only and typed input-family boundaries.
- Model router: no change. The adapter and contract were a tightly coupled, low-context implementation completed directly.
- Validation: `cargo test -p mornlea_godot input_map --locked`, `make godot-input-check`, Python compilation, and `git diff --check` passed. The locked `make godot-python-check` and runtime cursor test remain environment/consumer gates for the parent round.

### Tasks 8.3-8.6

- Implementation: completed the typed Camera3D mapping, resize/aspect and phase/readiness guards, target block/name/outline feedback, fixed-input movement parity, authoritative correction parity, protocol/network/receiver-overflow handling, client-requested shutdown, old-epoch discard, and reverse-order resource release. Python now submits semantic desktop intent and consumes one Rust-decoded typed frame dictionary; it does not encode or parse `MCN1`/`MCF1` wire bytes.
- Validation: `make godot-camera-check`, `make godot-target-check`, `make godot-disconnect-check`, `make godot-input-check`, `go test ./packages/client/runtime -race -count=1`, `go test ./packages/client/cmd/mornlea-godot-core -race -count=1`, `go test ./packages/client/runtime -run GodotMovementParity -race -count=1`, `go test ./packages/client/runtime -run Target -race -count=1`, `go test ./packages/client/runtime -run 'Disconnect|Overflow|Shutdown' -race -count=1`, `cargo fmt --check`, `cargo test -p mornlea_godot --locked`, and `cargo test -p mornlea_godot camera_mapping --locked` passed. Python boundary, Ruff, mypy, and format checks also passed.
- Routing: the earlier isolated 8.1-8.2 worker used the live native `gpt-5.6-terra` configuration at medium effort for a tightly coupled input adapter/contract task. Its output was adopted after controller review and focused gate reruns; no retry or escalation was needed. The current 8.3-8.6 integration was completed by the controller because it was tightly coupled to concurrent bridge/frame work and had a strong local validation oracle.
- Agent-output synthesis: the delegated input work established the semantic action inventory, fail-closed unsupported-action policy, cursor capture/release, focus-loss neutral input, and mouse-baseline reset. Controller integration extended that boundary so Rust owns wire encoding/decoding and Python receives only typed values, eliminating a duplicate Python codec and preserving one-frame revision consistency across camera and target components.
- Architecture skill: updated both synchronized project-owned copies with the verified Rust-owned wire codec and one-typed-frame-per-host-tick rule; both files remain byte-identical.
- Model router: no change. The existing copies already contain live capability filtering, high-level OpenAI gating, validation-oracle weighting, escalation limits, and round-end review guidance. This round produced no reusable routing rule beyond the task-scoped observation that tightly coupled low-context input work is cheaper to keep direct or minimally delegated.
- Git checkpoint: the P5 frontier is recorded in the active worktree; a scoped commit remains deferred because the worktree contains overlapping user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan changes that must stay excluded from staging.

### Task 9.1

- Implementation: added the fixed seven-slot Godot remote-player pool and actor lifecycle wiring, Rust-owned entity record decoding and typed dictionary export, Go runtime regression coverage for spawn/state interpolation/despawn/duplicates/order/overflow/reset, and the dedicated entity gate. Whole snapshots fail before Python node mutation; empty snapshots retain the current tick so despawn is observable; session reset clears both pooled identity and callback accounting. The full clippy gate also closed two narrow P5 debts: the standalone `input_map` contract module is compiled only for tests, and the two fixed-signature Godot semantic-input methods explicitly document their unavoidable argument-count lint exception.
- Architecture skill: no change. This node instantiates the existing server-authority, renderer-independent typed-value, Rust-owned wire decode, bounded-main-thread work, failure-atomicity, and one-frame observation rules; the seven-player limit and current pool scene are pilot-specific facts rather than durable cross-task architecture.
- Model router: no change. Direct execution was forced by the eligible-surface constraint and completed without retry or escalation; the two additional red-first fixes came from controller review of the validation oracle, not from a reusable model-selection failure.
- Git checkpoint: the node is ready for a scoped `feat(godot): add remote player presentation` commit after all focused and full node gates pass; partial staging excludes every recorded user-owned renderer, golden, canonical-spec, progress, cache, and historical-plan path.

### Tasks 9.2–9.5 execution round

- Frontier check (2026-09-18 17:52 Asia/Shanghai): task checkboxes and ledger stop at 9.1; no untracked implementation belongs to 9.2–9.5; world/UI file mtimes precede this round. No orphan implementation is adopted. All pre-existing renderer, golden, spec, progress, cache, and historical-plan changes remain user-owned.
- Orchestration: mixed execution. Fresh native Grok46 workers own HUD and disabled-capability reservations in separate detached worktrees; the controller owns the shared bridge, environment mapping, gates, and full real-server integration. These multi-file presentation/registry tasks repay isolation overhead; shared frame and lifecycle integration remain tightly coupled controller work. Neither worker edits the primary checkout.
- Routing: native `grok-4.6` / `xhigh` via the host's fixed Grok46 role, without a model override. Ordinary bounded implementation under controller-owned design, reversible consequences, focused deterministic tests, and native lifecycle integration make this route sufficient. The local time is inside the Z Code disabled window; no quota value is assumed. No fallback or escalation has occurred.
- Environment boundary ruling: Go's existing render functions remain the only day/night, seasonal warp, and weather-color implementation. A separately negotiated environment projection family carries bounded derived daylight/sky values tied to the retained frame identity; the frozen frame-v1 bytes and existing consumers remain unchanged. Python maps these validated values onto Godot resources only. This is an additive pilot ABI extension within the approved family-evolution design.
- Runtime publication defect (task 9.5): a late session could add an already-ready chunk to its per-session wanted set without changing the global union, so `TickResult.Ready` never woke snapshot publication for that session. The red test observed `late-session Ready = []`; the minimal fix emits `Ready` when a session newly wants a globally ready chunk and sorts/compacts the global fan-out list. Stable subscriptions emit no repeated notification, while the existing publication layer filters by `SessionWantsChunk` and suppresses snapshots already sent to older sessions.
- Runtime validation: `go test ./packages/server/sim/runtime -run TestLateSessionPublishesAlreadyReadySharedChunk -race -count=1`; full `go test ./packages/server/sim/runtime -race -count=1`; `go test ./packages/server/server -run 'RemotePlayer|Publication|Snapshot' -race -count=1`; `go vet ./packages/server/sim/contract ./packages/server/sim/runtime`; `make comment-language-check`; focused `git diff --check`; and strict change validation all pass.
- Real-server evidence after the runtime fix: the 5-second playable run changed the Godot terminal observation from `target=False entities=0` to `target=True entities=1`; the helper simultaneously reached Play, observed the grass-block target, and observed its remote peer. The run still failed because the current scene exceeded its bounded wall-time and its remote sticky flag remained false despite the typed frame containing one entity, isolating the remaining work to the task-9.5 oracle rather than server publication.
- Routing record for the task-9.5 oracle hardening: model `gpt-5.6-sol`; effort `high`; backend native; basis a cross-component real-TCP/lifecycle/presentation acceptance path with a weak integration oracle, long tool horizon, and reversible edits isolated from the controller-owned server fix; fallback is controller integration plus focused gates and a real short smoke. The replacement worker was explicitly started through the tool-enabled native worker surface after the user stopped the prior tool-less dispatch.

### Tasks 9.2–9.4

- Validation: `make godot-environment-check` passed 3 tests; `go test ./packages/client/render -run 'Daylight|Weather|Season' -count=1` passed; `make godot-hud-check` passed 5 tests; `go test ./packages/client/runtime -run HUD -race -count=1` exited successfully but reported no matching Go tests; and `make godot-capability-check` accepted all 14 disabled coarse capabilities. The shared `make godot-python-check` gate had already passed on this implementation baseline and remains a required regression gate before the P6 checkpoint.
- Architecture skill: no change. Confirmed-only environment/HUD mappings and disabled capability reservations instantiate the existing typed-view, server-authority, feature-manifest, and no-empty-support rules without adding a reusable placement or lifecycle decision.
- Model router: no change. The implementation and review route exposed no retry, escalation, context-transfer, or validation-quality rule reusable beyond this task group.

### Task 9.5 closeout

- Implementation: completed the minimum real-server playable loop over login, loading, movement, look, terrain upload and update, target feedback, remote-player observation, environment, HUD, and clean disconnect. The smoke helper uses one fixed Godot/helper UUID pair, the first real session epoch, typed-view UUID validation, a bounded movement observation followed by neutral input, exact log allowlisting, and process-group teardown so a long run cannot silently pass through an identity, stale-input, or cleanup failure.
- Runtime fix: the red late-session publication test exposed that a session subscribing to an already-ready shared chunk could change its wanted set without changing the global union, leaving that session asleep with no `TickResult.Ready`. The minimal server fix publishes one wake-up for newly wanted ready chunks, keeps stable subscriptions quiet, and retains publication-side filtering so another session's snapshot is never reused as an implicit publication.
- Formal real-server evidence: `scripts/godot/playable-smoke.sh --duration 300s` passed with `duration=300.000s`, `samples=1179`, and `movement=1 look=1 terrain=1 terrain_update=1 target=1 remote=1 environment=1 hud=1 teardown=1`; the wrapper reported `Godot playable smoke verified: 300s real-server minimum loop and clean teardown.` A controller rerun of `scripts/godot/playable-smoke.sh --duration 5s` also passed with all ten observable flags set and clean teardown.
- Closeout gates: `go test ./packages/client/cmd/mornlea-godot-smoke -count=1`; `go test ./packages/audit -run 'TestGodotPlayable(SmokeOracleContracts|LogRejectsEveryUnapprovedError)' -count=1`; `go test ./packages/client/render -run 'Daylight|Weather|Season' -count=1`; `go test ./packages/server/sim/runtime -run TestLateSessionPublishesAlreadyReadySharedChunk -race -count=1`; all required `make godot-{input,camera,target,environment,hud,entity,capability,python,project}-check` gates; shell syntax checks; and `git diff --check` passed.
- Audit correction: the first full `go test ./packages/audit -count=1` run rejected the fourteen valid reserved capability manifests because the script-ownership test only loaded the product activation catalog. The audit now also reads paths from `catalog/capability_registry.tres`; the complete audit rerun passed. This preserves the separate disabled-capability ownership boundary instead of weakening the ordinary-component rule.
- Architecture skill: updated both synchronized project-owned copies with the verified rule that global chunk readiness and per-session publication readiness are distinct; a newly subscribing session receives one publication wake-up even when the global wanted union is unchanged. Both copies remain byte-identical.
- Model router: no change. The final review used the live native `gpt-5.6-sol` at high effort in an isolated read-only worktree; the task had a strong deterministic validation oracle after the real-server run, and no reusable routing rule, retry, or escalation was observed.
- Git checkpoint: 9.x is complete at the OpenSpec frontier. The next checkpoint must stage only confirmed 9.2–9.5 files and the related architecture/ledger/task updates; it must exclude the pre-existing renderer, golden, canonical-spec, progress, cache, historical-plan, and `.boss-agent` changes. Work stops here and does not enter section 10.x.

### Routing retrospective after the 9.x closeout

- User-observed failure: while a delegated worker was still executing, the controller repeatedly searched, reread, and re-planned the same node. This duplicated tool work and consumed context without improving the worker's result.
- Model router: updated both synchronized project-owned copies with a controller/worker operating contract. The controller now owns decomposition, boundaries, integration, final validation, and handoff; an eligible bounded node defaults to a Grok worker; after dispatch the controller enters cursor-aware `WAIT` and must not repeat the worker's searches, reads, implementation, or tests until a wake condition or terminal handoff.
- The new delegate-first threshold raises Grok frequency for multi-file nodes, three-or-more-call tool horizons, independent review/discovery, or traces that would bloat controller context. Tiny deterministic edits, inseparable integration, and final cross-node validation remain controller-owned. The two-worker ceiling, high-level OpenAI design gate, isolated ownership, and final controller accountability are unchanged.
- Validation: both skill copies are byte-identical; `uv run --with pyyaml --no-project python /Users/chen/.codex/skills/.system/skill-creator/scripts/quick_validate.py` passed for each copy; and `git diff --check` passed. No runtime or protocol behavior changed.
