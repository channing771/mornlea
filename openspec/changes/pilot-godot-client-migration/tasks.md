## 1. P0: Freeze the current `main` baseline and migration inventory

- [x] 1.1 Complete or merge the provider, English OpenSpec, `*.md`/`*.zh.md`, and visual-routing rules from `modernize-project-governance` that affect this change. Translate all four active artifacts in this change into English without changing requirement or task semantics before implementation continues. Verify with `openspec validate modernize-project-governance --type change --strict --no-interactive` and `openspec validate pilot-godot-client-migration --type change --strict --no-interactive`.
- [x] 1.2 Confirm protocol v44, engine ABI v11, client ABI v19, and benchmark scenario v23 in `docs/architecture.md`/`docs/architecture.zh.md`, root `AGENTS.md`, and the `packages/audit` version-baseline tests. Correct any remaining v10/v14 document identity without changing runtime behavior. Verify with `go test ./packages/audit -run TestBaselineVersionsMatchCode -count=1`.
- [x] 1.3 Record the current client feature inventory, representative packages, existing test entry points, explicit non-migrated items, and the P0–P14 mapping in the English plan `docs/notes/godot-client-pilot-baseline.md`. Verify with `rg -n 'P0|P7|P14|protocol v44|engine ABI v11|client ABI v19' docs/notes/godot-client-pilot-baseline.md`.
- [x] 1.4 Define commit, platform, GPU, resolution, view-distance, scene, warmup, sample, protocol/ABI, and benchmark identity fields in `testdata/godot-pilot/baseline-manifest.json`. In `packages/tools/perfcheck`, write a failing test before implementing strict completeness validation. Verify with `go test ./packages/tools/perfcheck -run GodotPilotBaseline -count=1`.
- [x] 1.5 Generate or reference unchanged world/UI visual goldens and benchmark v23 reports from the current Rust client. Record commands, output paths, and run identity in the English baseline document without updating any golden. Verify with `make visual-check` and `make bench-multiplayer`.
- [x] 1.6 Create the Godot, GDExtension binding, font, material, shader, and plugin provenance/version/license/distribution-obligation table in `apps/mornlea-godot/assets/provenance/THIRD_PARTY.md`. Initially permit only Godot MIT, godot-rust, and existing project-owned or procedurally generated resources. Verify with `go test ./packages/audit -run GodotResourceLicenses -count=1`.

## 2. P1: Establish the stable Python-primary Godot host and pin both GDExtensions

- [x] 2.1 Add `scripts/godot/version.env` and `scripts/godot/fetch.sh`, pinning Godot `4.7.2-stable` Standard and desktop export templates. The pilot registers only the current macOS desktop download URL/SHA-256; later additions are limited to Windows/Linux desktop. The script downloads to a cache outside the repository and rejects checksum mismatches and every mobile/Web/console target. Verify with `scripts/godot/fetch.sh --verify-only`.
- [x] 2.2 Add a minimal `mornlea_godot` workspace crate to `packages/engine/Cargo.toml`, then probe and exactly pin a `godot` crate release compatible with Rust 1.97.1 and Godot 4.7.2. If the minimal build fails, stop the change and record No-Go. From `packages/engine`, verify with `cargo test -p mornlea_godot --locked`.
- [x] 2.3 Treat Py4Godot `4.7-alpha21` as the first candidate, then add a failing compatibility/provenance probe and exactly pin the qualifying plugin artifact, source revision, embedded CPython version, macOS Apple Silicon target, SHA-256, license, and distribution obligations in `scripts/godot/python-version.env` and `assets/provenance/THIRD_PARTY.md`. Prove editor/headless script loading, exported-project startup, and coexistence with Godot 4.7.2 plus `mornlea_godot` while system Python, `PYTHONPATH`, user site-packages, network access, and runtime `pip` are unavailable. If no exact artifact passes, record Python-primary No-Go and stop before P2. Verify with `scripts/godot/python-runtime-check.sh --qualify --offline`. Outcome: Python-primary No-Go; the exact candidate fails import-path isolation before the remaining matrix can qualify.
- [x] 2.3a Under the user's explicit hardening authorization, add an ordered minimal patch stack and locked build-input manifest under `scripts/godot/py4godot/`, plus a reproducible external-cache source/build/materialization script. Start from source revision `d8e17428deeb0428587349b663f6da26cd71ef3a`; replace legacy interpreter initialization with isolated `PyConfig`, remove ambient/current-working-directory import paths, exclude runtime installers, and repair only the editor/lifecycle defects required by the qualification matrix. Every patch MUST document the upstream defect and deletion condition, the series and output artifact MUST have verified digests, and the fork MUST expose no Mornlea gameplay/data API. First add failing audit and poisoned-environment tests; then prove Godot 4.7.2 editor/headless loading, exported-project startup, `mornlea_godot` coexistence, and 100 load/unload cycles offline on macOS Apple Silicon. If the derivative fails or requires broader changes, record final Python-primary No-Go and stop before P2. Verify with `scripts/godot/build-python-runtime.sh --verify --offline`, `scripts/godot/python-runtime-check.sh --qualify --offline`, and `go test ./packages/audit -run 'GodotPythonFork|GodotPythonIsolation' -count=1`.
- [x] 2.4 Create `apps/mornlea-godot/project.godot`, `app/bootstrap/bootstrap.tscn`, pure-GDScript Bootstrap, `setup_required.tscn`, English `README.md`, Chinese `README.zh.md`, and project-scoped ignore rules. The permanent main scene MUST NOT serialize a reference to Python or a native class. Verify with `scripts/godot/godot.sh --headless --path apps/mornlea-godot --editor --quit`.
- [x] 2.5 In a clean state without either `addons/py4godot` runtime artifacts or `addons/mornlea_bridge/bin`, first write failing headless scene assertions, then extend the pure-GDScript Bootstrap to diagnose missing Python/plugin/interpreter and project-bridge artifacts, target triple, identity mismatch, and the preparation command. Confirm that it neither imports Python features nor initiates a network connection. Verify with `scripts/godot/openable-smoke.sh --without-native` and `scripts/godot/openable-smoke.sh --without-python`.
- [x] 2.6 Add `apps/mornlea-godot/pyproject.toml`, `uv.lock`, local Py4Godot typing stubs or adapters, and `scripts/godot/python-check.sh`. Development-only tools are locked with `uv`; production runtime dependencies are empty unless separately approved. Enforce Python typing, Ruff format/lint, English comments, no `packages/agent` import, no direct C/engine ABI loading, no runtime installer/network calls, and no unrestricted dynamic import. Verify with `scripts/godot/python-check.sh --locked`.
- [x] 2.7 In `apps/mornlea-godot/config` and Python `app/host`, define coarse-grained `feature.tres` manifests, an explicit `feature_catalog.tres`, a versioned host protocol, and the validate/instantiate/bind/activate/reset/deactivate lifecycle. Replace the existing GDScript feature host rather than maintaining two implementations. Use Python stub features first to cover dependency cycles, duplicate IDs, incompatible versions, required/optional failure, deterministic order, and typed bridge injection. Verify with `scripts/godot/feature-contract-check.sh` and `scripts/godot/python-check.sh --locked`.
- [x] 2.8 Establish Python-led `features/{session,player_view,world,actors,ui}` and `platform/desktop/{input,lifecycle,audio}` skeletons. Audio is disabled in the pilot, and no mobile/Web/console directory is created. Only independently enableable, disableable, or replaceable features own manifests; ordinary components are not registered. Prove that adding a synthetic optional Python feature requires no Bootstrap or `app_root.tscn` change and that production GDScript outside the bootstrap allowlist fails validation. Verify with `scripts/godot/feature-contract-check.sh --extensibility-probe` and `scripts/godot/validate-project.sh --script-ownership`.
- [x] 2.9 Add `scripts/godot/build-extension.sh`. In `apps/mornlea-godot/addons/mornlea_bridge/mornlea_bridge.gdextension`, define only macOS/Windows/Linux desktop platform/architecture/profile mappings and a thin Python `bridge_host.py` facade over Godot-visible typed methods. The pilot initially builds the current macOS target into the project-local directory and registers only a `MornleaClientBridge` that exposes ABI/Godot identity, with no gameplay. Verify with `scripts/godot/build-extension.sh --verify`, `scripts/godot/python-runtime-check.sh --coexistence`, and `scripts/godot/godot.sh --headless --path apps/mornlea-godot --quit-after 5`.
- [x] 2.10 In `mornlea_godot`, first write failing tests for registration, initialization levels, repeated load/unload, wrong engine version, and Rust-panic conversion, then implement the minimum lifecycle. Include 100-cycle tests that load and tear down the Python runtime, Python host, Rust bridge, and Godot resources in the declared order. From `packages/engine`, verify with `cargo test -p mornlea_godot lifecycle --locked` and `scripts/godot/smoke.sh --iterations 100`.
- [x] 2.11 Add `scripts/godot/sync-assets.sh` to generate `apps/mornlea-godot/assets/generated` and a manifest with input revision/checksum from `packages/client/assets` and registered fonts. Identical input MUST be byte-stable, and the generated directory MUST reject hand-authored source files. Verify with `scripts/godot/sync-assets.sh --check`.
- [x] 2.12 Add `scripts/godot/validate-project.sh` and `packages/audit` guards that reject `res://../`, absolute development-machine paths, symbolic links escaping the project root, system Python/library search, runtime `pip` or downloads, an unignored `.godot/`, ignored `.uid` files, unregistered autoloads, production GDScript outside Bootstrap, non-English comments in new Godot/Python architecture code, and export presets that package tests, development Python tooling, diagnostics, provenance, non-selected features, or non-target-platform libraries. Permit only macOS/Windows/Linux desktop presets; mobile/Web/console presets or template dependencies MUST fail. Verify with `scripts/godot/validate-project.sh`, `make comment-language-check`, and `go test ./packages/audit -run 'GodotProjectRoot|GodotResourceClosure|GodotUIDPolicy|GodotExportClosure|GodotDesktopOnly|GodotPythonBoundary' -count=1`.
- [x] 2.13 Add `scripts/godot/smoke.sh` to repeatedly start and exit the minimal Python host 100 times without a foreground window or external Python environment, checking nonzero exits, crash logs, leftover pilot processes, interpreter/feature-lifecycle leaks, and unload order. Verify with `scripts/godot/smoke.sh --iterations 100 --isolated-python`.
- [x] 2.14 Add explicit optional `godot-project-check`, `godot-python-check`, and `godot-smoke` entry points to the Makefile and `.github/workflows/ci.yml`. Existing `make build`, `make test`, `mornlea`, and `make companion-agent-check` MUST NOT probe or share the Godot Python runtime when those entry points are not invoked. Verify with `make godot-project-check`, `make godot-python-check`, `make godot-smoke`, and `go test ./packages/audit -run 'GodotIsOptionalForLegacyBuild|GodotPythonIsolation' -count=1`.
- [x] 2.15 Add desktop-only mutation tests that inject Android, iOS, Web, and console export presets, touch/mobile-sensor input actions, mobile lifecycle scripts, non-desktop GDExtension selectors, system Python paths, user site-package dependencies, and runtime installer calls, proving that each is rejected before build or packaging. Verify with `go test ./packages/audit -run 'GodotDesktopOnly|GodotPythonBoundary' -count=1` and `scripts/godot/validate-project.sh --desktop-only-fixtures --python-isolation-fixtures`.

## 3. P2a: Establish platform-independent presentation values and transcripts

- [x] 3.1 Add `packages/client/presentation`. First test zero values, domains, and immutability for `SessionPhase`, `CameraSnapshot`, `EnvironmentSnapshot`, `HUDSnapshot`, `TargetSnapshot`, and error state, then implement pure value types. Verify with `go test ./packages/client/presentation -race -count=1`.
- [x] 3.2 In `packages/client/presentation`, define dimension/section/revision-bearing `SectionMeshUpsert`, `SectionDrop`, and atlas revision. Test signed coordinates, stale revisions, capacity, and whole-batch failure before implementing validators. Verify with `go test ./packages/client/presentation -run 'Section|WorldBatch' -race -count=1`.
- [x] 3.3 In `packages/client/presentation`, define semantic remote-player entity records with a fixed total capacity. Do not reuse the current 96-byte GPU instance as the public model. Test invalid kinds, duplicate identities, out-of-order ticks, and overflow before implementation. Verify with `go test ./packages/client/presentation -run Entity -race -count=1`.
- [x] 3.4 Under `testdata/godot-pilot/transcripts/`, create valid login/snapshot/movement/correction/chunk-update/remote-player/weather/HUD/disconnect transcripts, plus version-error, truncated, invalid-enum, and overflow transcripts. Verify with `go test ./packages/client/presentation -run TranscriptCorpus -count=1`.
- [x] 3.5 In `packages/client/cmd/mornlea/app`, add characterization tests for current message draining, session reset, input prediction, mesh enqueue, and basic presentation snapshots. Produce stable summaries for later runtime parity. Verify with `go test ./packages/client/cmd/mornlea/app -run 'Characterization|TranscriptParity' -race -count=1`.

## 4. P2b: Extract the platform-independent Go client runtime

- [x] 4.1 Add `packages/client/runtime` options, dependency-injection interfaces, connection phases, and idempotent `Close`. Test reverse-order release after partial construction failure and repeated close before implementation. Verify with `go test ./packages/client/runtime -run 'Lifecycle|Close' -race -count=1`.
- [x] 4.2 Compose remote TCP dialing, current-protocol login, and receiver ownership into `runtime`, retaining the `packages/shared/network` implementation. Test version rejection, dial/login failure, receiver errors, and resource-close order before implementation. Verify with `go test ./packages/client/runtime -run 'Remote|Login|Receiver' -race -count=1`.
- [x] 4.3 Compose `Mirror`, inventory/crafting/chest/furnace/chat, and entity mirrors into `runtime`. Migrate platform-independent message routes from `app/app_messages.go` one class at a time and compare them with transcripts. Verify with `go test ./packages/client/runtime -run 'Messages|Mirror|Reset' -race -count=1` and `go test ./packages/client/cmd/mornlea/app -run 'Connection|Messages|Inventory|Chest|Furnace' -race -count=1`.
- [x] 4.4 Compose `Predictor`, input sequence numbers, fixed steps, authoritative correction, and unconfirmed-input replay into `runtime`. Godot and the old app may submit only semantic input. Write identical input/state parity tests before wiring. Verify with `go test ./packages/client/runtime -run 'Predict|Input|Reconcile' -race -count=1` and `go test ./packages/client/client -run Predictor -race -count=1`.
- [ ] 4.5 Extract camera mode, first-/third-person camera semantics, and target-raycast input as runtime/presentation output; device cursor behavior and matrix writes remain in the host. Test yaw/pitch, occlusion, reset, and F5 state before implementation. Verify with `go test ./packages/client/runtime -run 'Camera|Target' -race -count=1` and `go test ./packages/client/cmd/mornlea/app -run Camera -race -count=1`.
- [ ] 4.6 Connect mesher scheduling/draining, the ready queue, backpressure, and section revision to runtime, continuing to call engine ABI v11 through `packages/client/mesh` and `packages/shared/nativeabi`. Test stale results, overflow, reset, and budgets before implementation. Verify with `go test ./packages/client/runtime -run 'Mesh|Section|Budget' -race -count=1` and `go test ./packages/client/client -run Mesher -race -count=1`.
- [ ] 4.7 Implement explicit `Step(elapsed,messageBudget,meshBudget)` in runtime. Each step publishes at most one `FrameSnapshot` and budgeted `WorldBatch`, MUST NOT read an implicit wall clock, and MUST NOT block on network. Verify with `go test ./packages/client/runtime -run 'Step|Bounded|NoWallClock' -race -count=1`.
- [ ] 4.8 Incrementally make the old `app.Frame` remote-message, prediction, and mesh paths consume runtime, retaining the current renderer/window/UI/audio and local Host assembly. Run characterization parity after each ownership loop moves. Verify with `go test ./packages/client/cmd/mornlea/app -race -count=1` and `make visual-check`.
- [ ] 4.9 Add source/dependency audits in `packages/audit` that prohibit runtime/presentation from importing server, Godot, the client C ABI, Darwin, WebGPU, or device I/O. Verify with `go test ./packages/audit -run 'ClientRuntime|GodotBoundary' -count=1`.

## 5. P3a: Define and implement client-core ABI v1

- [ ] 5.1 Define ABI major/minor identity, status codes, magic/layout, fixed headers, reserved fields, alignment, maximum batches, two-phase capacity contracts, and feature-family descriptors in `packages/client/cmd/mornlea-godot-core/include/mornlea_client_core.h`. First add failing cross-Go/Rust constant and layout consistency tests. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run 'ABI|Layout|Identity' -count=1` and, from `packages/engine`, `cargo test -p mornlea_godot abi --locked`.
- [ ] 5.2 Establish the sole client-core feature-family registry. Register stable IDs, versions, and limits for pilot lifecycle, connection, input, step, world, frame, and status/metrics. Cover unknown families, duplicate IDs, missing required families, compatible minor and incompatible major versions, and prohibit changing an existing layout without a version bump. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run 'Feature|Compatibility|Registry' -count=1` and, from `packages/engine`, `cargo test -p mornlea_godot feature_negotiation --locked`.
- [ ] 5.3 Implement create/destroy/status identity exports with a fixed-capacity handle table. Validate ABI/required families before handle/pointer/content, convert panics to stable status, and do not write output on failure. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run 'Identity|Lifecycle|Panic' -race -count=1`.
- [ ] 5.4 Implement asynchronous `connect_begin`, connection phase/error polling, and disconnect exports without waiting for DNS/TCP/login on the calling thread. Cover repeated connect, cancellation, version rejection, and close races. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run 'Connect|Disconnect|Cancel' -race -count=1`.
- [ ] 5.5 Implement InputBatch encoding/decoding and `submit_input`, with fixed per-frame event count, text capacity, finite mouse values, and action enums. First cover wrong ABI, nil/length/overlap, NaN/Inf, trailing bytes, and whole-batch rejection for an invalid middle record. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run Input -race -count=1`.
- [ ] 5.6 Implement the `step` export, validating elapsed time and all three budgets before calling runtime's bounded Step. Cover zero size, over-budget, closed state, receiver failure, and absence of synchronous network waits. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run Step -race -count=1`.
- [ ] 5.7 Implement two-phase WorldBatch draining. Capacity queries MUST NOT consume; consumption commits only after a successful exact write. Cover required size, insufficient capacity, overlapping output, old epoch, and repeated queries. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run WorldBatch -race -count=1`.
- [ ] 5.8 Implement two-phase pilot FrameSnapshot reads and metrics/status reads, using semantic records rather than the old GPU TLV. Cover entity capacity, HUD-not-ready, disconnected terminal state, and unchanged buffers on failure. Prove that adding an unknown optional family does not change v1 frame decoding. Verify with `go test ./packages/client/cmd/mornlea-godot-core -run 'FrameSnapshot|Metrics|Capacity|OptionalFamily' -race -count=1`.
- [ ] 5.9 Create one error-precedence matrix and fuzz corpus for every versioned ABI export. Rust bindings MUST NOT panic or retain partial Godot state on an error status. Verify with `go test ./packages/client/cmd/mornlea-godot-core -fuzz FuzzClientCoreABI -fuzztime 30s` and, from `packages/engine`, `cargo test -p mornlea_godot abi_matrix --locked`.
- [ ] 5.10 Add `scripts/godot/build-core.sh` to build with `go build -buildmode=c-shared` into `apps/mornlea-godot/addons/mornlea_bridge/bin/<target>/<profile>/`. Verify exported symbols, header, family identity, and engine dynamic-library dependencies without writing system directories. Verify with `scripts/godot/build-core.sh --verify`.

## 6. P3b: Connect the Rust GDExtension to the Python Godot host

- [ ] 6.1 In `mornlea_godot`, create a handwritten or generated client-core Rust FFI module. Keep all unsafe code inside that module and validate Rust/Go constants, sizes, alignment, and status text for each export. From `packages/engine`, verify with `cargo test -p mornlea_godot client_core --locked`.
- [ ] 6.2 Implement `MornleaClientBridge` identity/features/create/connect/poll/submit/step/close with typed Godot-visible values that Python can consume without direct FFI or retained native buffers. Godot objects are called only on the main thread, and repeated destruction is safe. Verify with `cargo test -p mornlea_godot bridge_lifecycle --locked` from `packages/engine`, `scripts/godot/python-runtime-check.sh --bridge-contract`, and `scripts/godot/smoke.sh --iterations 100 --isolated-python`.
- [ ] 6.3 Inject the real bridge into Python `feature_host.py` under `apps/mornlea-godot/app/host`. It only orchestrates SceneTree phases, budgets, and feature lifecycles and MUST NOT parse production packets, retain gameplay/native-buffer state, call C symbols, or branch on concrete features. Verify with `scripts/godot/godot.sh --headless --path apps/mornlea-godot --quit-after 5`, `scripts/godot/python-check.sh --locked`, and `go test ./packages/audit -run 'GodotScriptsDoNotImplementProtocol|GodotHostIsFeatureAgnostic|GodotPythonBoundary' -count=1`.
- [ ] 6.4 Implement reusable Rust buffers and two-phase world/frame pulls. On insufficient capacity, grow to the declared required size but never beyond the contract limit. Cover repeated empty batches, maximum batches, invalid batches, and discarding old epochs. From `packages/engine`, verify with `cargo test -p mornlea_godot pull_buffers --locked`.
- [ ] 6.5 Implement the first real Python feature scene/manifest in `apps/mornlea-godot/features/session`, displaying Connecting/Login/Loading/Play/Disconnected and stable errors without automatically falling back to the old renderer or a GDScript host. Verify with `make godot-python-check` and `make godot-smoke`.
- [ ] 6.6 Add catalog integration tests: a missing required bridge family fails before world creation, while optional-feature initialization failure disables only that feature and leaves other features and the mirror unchanged. Verify with `scripts/godot/feature-contract-check.sh --bridge-integration`.

## 7. P4: Migrate near-ring terrain presentation

- [ ] 7.1 In `mornlea_godot`, add independent decoding/expansion tests for the existing 8-byte packed quad. Cover all six faces, UVs, atlas layers, lighting, opaque/cutout/water classes, maximum sections, and invalid bits. From `packages/engine`, verify with `cargo test -p mornlea_godot quad_decode --locked`.
- [ ] 7.2 Implement a bounded Rust CPU mesh-prepare worker. Copied input batches become immutable and output owns its vertex/index arrays. Cover full queue, cancellation, old epochs, panic, and close-wait behavior. From `packages/engine`, verify with `cargo test -p mornlea_godot mesh_worker --locked`.
- [ ] 7.3 Create Python-led internal scenes/components plus materials/shaders under `apps/mornlea-godot/features/world/terrain_near`, assembled by the coarse world manifest. Python controls bounded lifecycle and configuration only; Rust owns packed-quad expansion, buffers, RIDs, and bulk resource submission. Consume only current atlas pixels in `assets/generated` after manifest validation. Pin nearest filtering, mipmaps, color space, cutout alpha, and water-transparency parameters. Verify with `make godot-python-check`, `make godot-asset-check`, and `go test ./packages/audit -run GodotResourceLicenses -count=1`.
- [ ] 7.4 Implement the `TerrainRenderer` RenderingServer RID table, keyed by dimension/X/Y/Z/revision, with whole-section upsert/drop and preservation of old resources on failure. From `packages/engine`, verify with `cargo test -p mornlea_godot terrain_resources --locked`.
- [ ] 7.5 Implement fixed per-frame terrain upload/drop budgets, out-of-view-distance reclamation, and session reset; prohibit one Node per block. Record packed/expanded bytes, prepare/upload duration, RID count, and peak count. From `packages/engine`, verify with `cargo test -p mornlea_godot terrain_budget --locked`.
- [ ] 7.6 Drive a Godot headless scene from transcripts through initial snapshot, chunk deltas, forget/reset, and world re-entry. Screenshots or structural summaries MUST prove that no stale section remains. Verify with `make godot-terrain-check`.
- [ ] 7.7 Add a real-TCP dedicated-server terrain smoke test: start the existing `mornlea-server`, log the pilot in, wait for the same loaded criterion, run a fixed number of frames, exit, and reliably reap every child process. Verify with `scripts/godot/terrain-smoke.sh`.

## 8. P5: Connect remote input, prediction, camera, and target feedback

- [ ] 8.1 In Godot InputMap, Python `platform/desktop/input`, and the Rust bridge contract, implement desktop keyboard/mouse semantic capture for forward/back/left/right, jump, sprint, sneak, primary, secondary, hotbar, F5, Esc, and mouse delta. Disable unimplemented actions and do not send guessed commands; add no touch, mobile sensor, or virtual joystick. Verify with `cargo test -p mornlea_godot input_map --locked` from `packages/engine`, `make godot-python-check`, and `make godot-input-check`.
- [ ] 8.2 Implement cursor capture/release, neutral input on focus loss, and mouse-baseline refresh in the Python desktop adapter so the first frame after recapture does not jump the view. Verify with `make godot-python-check`, `make godot-input-check`, and `go test ./packages/client/runtime -run 'Cursor|NeutralInput' -race -count=1`.
- [ ] 8.3 Under `features/player_view/camera`, implement one typed Python mapping from runtime's predicted and correctable CameraSnapshot to Godot Camera3D, while keeping reusable coordinate validation values in Rust/Go. Centrally test coordinates, yaw/pitch, FOV, near/far, resize aspect, and bounded callback cost. Verify with `make godot-camera-check`, `make godot-python-check`, and, from `packages/engine`, `cargo test -p mornlea_godot camera_mapping --locked`.
- [ ] 8.4 Under Python `features/player_view/target_feedback`, connect the current block target, localized name, and outline presentation. The outline respects terrain depth testing, disables depth writes, and hides in the same frame during UI/not-ready/disconnected/reset states. Verify with `make godot-python-check`, `make godot-target-check`, and `go test ./packages/client/runtime -run Target -race -count=1`.
- [ ] 8.5 Run fixed-input and authoritative-correction transcripts, comparing the old app and Godot runtime command sequence, sequence numbers, predicted positions, and terminal state. Verify with `go test ./packages/client/runtime -run GodotMovementParity -race -count=1`.
- [ ] 8.6 Cover server close, network error, protocol rejection, receiver overflow, and client-requested exit. Confirm terminal-state UI, old-epoch discard, and reverse-order resource release. Verify with `make godot-disconnect-check` and `go test ./packages/client/runtime -run 'Disconnect|Overflow|Shutdown' -race -count=1`.

## 9. P6: Complete minimum entities, environment, and HUD

- [ ] 9.1 Under Python `features/actors/remote_player`, connect remote-player spawn/state/despawn and interpolation semantics. Godot instances consume only platform-independent EntitySnapshot values through typed bridge views. Cover duplicate spawn, out-of-order ticks, despawn, capacity, session reset, and Python callback budget. Verify with `make godot-python-check`, `make godot-entity-check`, and `go test ./packages/client/runtime -run RemotePlayer -race -count=1`.
- [ ] 9.2 Under Python `features/world/environment`, implement basic day/night, sky-color, and clear/rain/thunderstorm parameter mappings. Input comes only from confirmed world time, day phase, season, and weather. Verify with `make godot-python-check`, `make godot-environment-check`, and `go test ./packages/client/render -run 'Daylight|Weather|Season' -count=1`.
- [ ] 9.3 Under Python `features/ui/hud`, add a minimum Control HUD that displays health, hunger, oxygen, and connection state after ready, and persistently labels the build as a limited Godot pilot. Unconfirmed values MUST NOT appear as fabricated defaults. Verify with `make godot-python-check`, `make godot-hud-check`, and `go test ./packages/client/runtime -run HUD -race -count=1`.
- [ ] 9.4 In the feature/ABI registry, assign stable but disabled identities and expected family dependencies for menus, containers, chat, audio, LOD, viewmodel, companions, hostile mobs, passive cows, projectiles, drops, name tags, particles, and full weather effects. Hide or disable unimplemented entry points, prohibit manifests for ordinary leaf components, and prohibit empty implementations from masquerading as support. Verify with `make godot-capability-check`.
- [ ] 9.5 Complete a minimum playable real-server scenario that continuously runs login, loading, movement, look, terrain updates, targeting, remote players, environment, HUD, and disconnect. Verify with `scripts/godot/playable-smoke.sh --duration 300s`.

## 10. P7: Establish the dual-client visual and performance decision

- [ ] 10.1 Under `apps/mornlea-godot/tests` and in `scripts/godot/capture.sh`, create Python-driven fixed-camera capture without a foreground window. `godot-visual-evidence` writes scenario/commit/Godot/Py4Godot/CPython/platform/GPU/catalog/feature-family identity and images to `build/visual/godot-pilot/<run-id>/` and MUST NOT write tracked goldens. Verify with `make godot-python-check`, `make godot-visual-evidence`, and `go test ./packages/audit -run VisualBaselineRouting -count=1`.
- [ ] 10.2 Add a Godot pilot report schema to `packages/tools/perfcheck`, recording CPU/GPU frame P50/P95/P99, Python host/apply duration and allocation pressure, cold start/loading, section preparation/upload, packed/expanded bytes, RSS, RID/mesh counts, input-to-presentation latency, and queue/overflow data. Verify with `go test ./packages/tools/perfcheck -run GodotPilotReport -count=1`.
- [ ] 10.3 Add `scripts/godot/visual-compare.sh` and `benchmark.sh`. The former only reads same-semantics tracked baselines and produces difference/classification reports; the latter reuses benchmark scenario v23 seed, resolution, view distance, warmup, sampling window, and work budgets. Neither may overwrite baselines; fields that cannot be compared MUST be marked non-comparable rather than set to zero. Verify with `make godot-visual-compare` and `make godot-benchmark`.
- [ ] 10.4 In `docs/notes/godot-client-pilot-report.md`, record the old Rust client and Godot pilot side by side: visual evidence, performance values including Python host cost, feature coverage, difference classification, exact Py4Godot/CPython identity, and complete run identities. Verify with `go test ./packages/tools/perfcheck -run GodotPilotReportCompleteness -count=1`.
- [ ] 10.5 Conduct specification, architecture, Python-runtime, performance, and resource-authorization final reviews for P0–P7. Mark every hard condition as Go, No-Go, or Evidence Missing. Any unqualified Python/runtime artifact, dependence on system Python or runtime installation, unbounded Python hot path, data loss, authority fork, missing report identity, or lifecycle crash MUST result in No-Go. Verify with `rg -n '^Decision: (GO|NO-GO)$' docs/notes/godot-client-pilot-report.md`.
- [ ] 10.6 If the decision is Go, only split P8–P14 into candidate OpenSpec changes and do not switch the default entry point. If it is No-Go, remove pilot runtime/build entry points while preserving the report and archived evidence. Verify with `openspec validate --all --strict --no-interactive`.

## 11. Architecture, documentation, and rollback gates

- [ ] 11.1 In `packages/audit/dependency_test.go`, feature-boundary tests, and source guards, register the sole allowed dependencies for `runtime`, `presentation`, `mornlea_godot`, Py4Godot, and `apps/mornlea-godot`. Reject server→Godot, Python/Godot feature/platform→protocol/TCP/mirror/client-core C ABI/engine ABI/`packages/agent`, `mornlea_godot`→engine ABI, Go→Godot/Python/GPU, and production GDScript outside Bootstrap. Verify with `go test ./packages/audit -count=1`.
- [ ] 11.2 Create architecture gates for the Python feature catalog that reject duplicate IDs, dependency cycles, unregistered features, manifests on ordinary leaf components, cross-feature private scene paths, unrestricted dynamic imports, shared mutable autoloads, unknown budget classes, and missing required families. Use a synthetic Python feature to prove additive catalog extension without Bootstrap/app-root changes. Verify with `go test ./packages/audit -run 'GodotCapability|GodotFeatureBoundary|GodotPythonBoundary' -count=1` and `scripts/godot/feature-contract-check.sh --extensibility-probe`.
- [ ] 11.3 Add default-path regression tests proving that existing `mornlea`, Memory, local/remote assembly, capture, benchmark, and client ABI v19 do not load or probe Godot. Verify with `go test ./packages/client/cmd/mornlea/... -run 'Godot|Legacy|Default' -race -count=1` and, from `packages/engine`, `cargo test -p mornlea_client --locked`.
- [ ] 11.4 Record the stable Godot project root, Python-primary/GDScript-bootstrap language ownership, Py4Godot isolation and replaceability, microkernel/feature ownership, client-core ABI family evolution, optional build, P8–P14 extension points, and No-Go rollback in English canonical `docs/architecture.md`, `docs/README.md`, and `docs/notes/go-rust-division.md`, synchronized `.zh.md` counterparts, root guidance, and the English `AGENTS.md` adjacent to `apps/`. Do not present the pilot as current default production behavior. Verify with `go test ./packages/audit -run 'Architecture|Documentation|Baseline' -count=1`.
- [ ] 11.5 In `scripts/godot/rollback-check.sh`, verify that disabling or removing Godot artifacts leaves existing build/test/visual entry points independent of Godot and that pilot failure does not rewrite saves or default configuration. If P7 is Go, later features remain independently reversible without deleting the project root. Verify with `scripts/godot/rollback-check.sh`.
- [ ] 11.6 Inspect provenance and licenses for added binaries, Python interpreter/standard-library files, Python packages, textures, fonts, shaders, and plugins, confirming no Mojang assets, unrecorded downloads, runtime installers, or system-path dynamic libraries. Verify with `go test ./packages/audit -run 'GodotResourceLicenses|ReleaseUnit' -count=1`.
- [ ] 11.7 Audit `apps/mornlea-godot` for resource, Python-runtime, version-control, and desktop-export closure: the sole `project.godot` is at the stable root; all runtime references stay within `res://`; `.godot/` and generated native/Python `bin/` are ignored or reproducibly materialized; script/shader `.uid` files are not ignored; `assets/generated` and release manifests are reproducible; production PCKs exclude tests, development Python tooling, diagnostics, provenance, non-selected features, and non-target desktop libraries; and the repository contains no mobile/Web/console preset or adapter. Verify with `make godot-project-check` and `make godot-python-check`.

## 12. Formatting and complete validation

- [ ] 12.1 Format every added or modified Go file with `gofmt`, run `cargo fmt --check` for the Rust crate, run locked Ruff format/lint and mypy checks for first-party Godot Python, enforce English comments for all new Go/Rust/C/Python/GDScript/JavaScript/TypeScript code, and run formatting/integrity checks for Godot text resources, `.uid` files, and deterministic asset imports. Verify with `make fmt`, `cargo fmt --check` from `packages/engine`, `make godot-python-check`, `make comment-language-check`, `make godot-asset-check`, and `make godot-project-check`.
- [ ] 12.2 Build the Rust numerical kernel, old client, project Godot GDExtension, and pinned Py4Godot/CPython runtime; assemble the offline pilot distribution unit in the current macOS desktop directory; and verify engine ABI v11, client ABI v19, client-core ABI v1, Python host contract v1, feature families, feature catalog, both extension identities, interpreter identity, and desktop target identity. Verify with `make rust`, `make godot-build`, `scripts/godot/python-runtime-check.sh --exported --offline`, and `go test ./packages/audit -run 'TestBaselineVersionsMatchCode|GodotDesktopOnly|GodotPythonIsolation' -count=1`.
- [ ] 12.3 Run focused race tests for the client core and commands. Verify with `go test ./packages/client/runtime ./packages/client/presentation ./packages/client/client ./packages/client/render ./packages/client/cmd/mornlea-godot-core ./packages/client/cmd/mornlea/... -race -count=1`.
- [ ] 12.4 Run Rust/Python/Godot-specific gates. Verify with `make rust-check`, `make godot-python-check`, `make godot-project-check`, `make godot-check`, `make godot-smoke`, `make godot-visual-evidence`, `make godot-visual-compare`, and `make godot-benchmark`.
- [ ] 12.5 Run architecture, six-module vet, and regular development gates. Verify with `go test ./packages/audit -count=1` and `make dev-check`.
- [ ] 12.6 Run the full six-module race, existing visual gate, and frontend gate to prove the old client remains available. Verify with `make test-race`, `make visual-check`, and `make frontend-check`.
- [ ] 12.7 Run full strict OpenSpec validation and reconcile proposal/spec/design/tasks, the P7 decision, and actual implementation before checking this item. Verify with `openspec validate --all --strict --no-interactive`.
