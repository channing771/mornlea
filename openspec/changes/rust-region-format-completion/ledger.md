# Rust region format completion ledger

## 2026-09-23 — planning and parallel boundary

**Baseline and user intent:** current checkout HEAD `0f5ff747`; the user requested one task that can execute alongside `rust-protocol-completion` using Superpowers. This is a planning-only round. The pre-existing modified root `AGENTS.md` and untracked `.cursor/agents/` and `.cursor/rules/` are unrelated and untouched.

**Brainstorming ruling:** three candidate paths were considered. F2 server and F3 client core were rejected because their plans explicitly wait for completed F1 protocol and storage acceptance. A full F1 storage successor was deferred because its corpus and oracle closure would share the protocol task's `packages/tools/cmd/runtime-oracle/` and `testdata/runtime-migration/` ownership. The Rust region-format boundary was selected: it is a real storage-format defect, directly supports the final Rust server, has existing Go oracle tests, and can be implemented and reviewed entirely in `mornlea_storage` while protocol work continues.

**Controller-owned design:** fixed 1,024-entry boxed bank representation; checked owned-vector constructor; v1 bytes and recovery behavior unchanged; bank validation before destination capacity; direct caller-buffer encoding with untouched output on failure; explicit output-capacity error. The implementation packet fixes signatures, limits, test inputs, expected Go CRC32C values, file ownership, dependent gates and rollback. No worker is asked to choose shared ownership, data model, error precedence, corpus policy or format version.

**Isolation:** only `packages/engine/crates/mornlea_storage/src/{region,error,lib}.rs`, the region test section of `tests/runtime_contract.rs`, and that crate's `AGENTS.md` are implementation-editable. This change's OpenSpec files are controller-owned. The protocol task's crate, narrow domain changes, Go oracle and shared runtime corpus are excluded. New implementation must use an isolated worktree; no editing agents share a checkout. Workspace consumer search found `RegionBank` in the storage crate's public re-export and integration tests, with no external literal construction. `packages/audit/identity_test.go` checks workspace identity, not the edited source text. No generated or embedded artifact derives from the editable source set.

**Readiness self-review:** [spec requirement fixed v1 format](specs/rust-runtime-foundation/spec.md) maps to node 1.1 and the existing decoder/layout regression in 1.2. Atomic caller-buffer requirement maps to 1.2; recovery requirement maps to 1.2's existing corruption and selection table. Nodes 1.1 → 1.2 → 2.1 form an acyclic serial path; each has one deliverable, exact editable/read-only files, public signatures, red/green cases, focused commands, review and scoped rollback. `tasks.md` is the only status source. The potentially missed 1,025-entry, invalid-before-short, destination-tail, zero-generation standby and divergent-tie cases are explicitly assigned. Directory guidance is updated at closeout; no new important directory with independent ownership is created.

**Planning evidence:** Go's current `region.EncodeSuperblock` and `region.EncodeRegionBank`, executed from a temporary program outside the repository for key `{dimension:-3,x:-1,z:2}` and a generation-1 bank with slot 0 at sector 15, one sector, payload length 0, revision 1, yielded superblock CRC32C `ef554c52` and bank CRC32C `24d271a5`. These constants are independent of the Rust CRC implementation. `openspec validate rust-region-format-completion --strict --no-interactive` passed. `openspec validate --all --strict --no-interactive` passed 128 items with 0 failures. Baseline `go test ./packages/server/storage/region -count=1` passed; baseline `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_storage --test runtime_contract region_ --locked` passed 10 region tests. A six-file Markdown scan found zero broken relative links, trailing whitespace or missing newlines. Runtime implementation tests and closeout gates have not run for this planning-only change.

**Architecture skill:** no change. The fixed v1 region contract is already a storage-specific decision; no new stable cross-task rule has been verified by implementation.

## 2026-09-23 — node 1.1 fixed-shape bank

**Routing and baseline:** cherry-picked plan-only `cdf8676a` onto clean `0f5ff747`, producing `b0b9c8a2`. The accepted plan and current sources agree. The two behavior nodes share `region.rs` serially, so this isolated executor handles their tightly coupled edits directly. Project OpenSpec artifacts are the sole status and ledger store; no parallel `.superpowers/sdd` store is created. Consumer search found only the storage crate re-export and its integration tests, with no derived or protocol-owned consumer.

**Red:** a temporary `region_bank_short_slot_vector_must_not_encode` test failed as intended: the baseline encoder returned `Ok` for 1,023 entries. After replacing it with the final constructor tests, compilation failed with `E0599` because `try_from_entries` did not exist. Both failures preceded production edits.

**Green and review:** `Bank.entries` is a heap-backed fixed array; the constructor checks cardinality, takes vector ownership without cloning entries and validates before returning. Decode fills a fixed bank. `rustup run 1.97.1 cargo test --manifest-path packages/engine/Cargo.toml -p mornlea_storage --test runtime_contract region_ --locked` passed 12/12; the whole storage crate suite passed 19 unit, 65 integration and 0 doc tests. The planned focused Go test passed. `make rust` completed successfully before Go tests. Storage crate format check and `git diff --check` passed. Diff review found no byte-layout or selection change, no edits outside owned source/tests and this change directory, and no new dependency or generated consumer. The zero-generation standby and divergent-tie cases remain covered by existing tests.

**Architecture skill:** no change. Fixed bank cardinality is a format-specific contract already stated in this change; no new cross-task placement or lifecycle rule emerged. Rollback remains a scoped revert of this node's Rust source, tests and status evidence.

## 2026-09-23 — node 1.2 atomic caller-buffer output

**Baseline and red:** node 1.1 was accepted and committed as `20eb6287`. The added region canary, capacity, error-order, exact/tail and Go-reference tests failed to compile first: `E0432` for the two absent exports and `E0599` for the absent `OutputTooSmall` variant. No implementation file had changed when that red result was captured.

**Green:** `encode_superblock_into` and `encode_region_bank_into` write only the exact caller prefix after complete preflight. Invalid bank validation precedes capacity; short and invalid calls leave all canaries intact. Owned-array encoders delegate to the new operations. The Rust region filter passed 15/15 and crate `--lib` passed 19/19. The whole storage suite passed 19 unit and 68 integration tests, the Go region suite passed, storage formatting and `git diff --check` passed. Independent Go v1 reference checksums matched: superblock `0xef554c52` and bank `0x24d271a5` for signed key `(-3,-1,2)` and the planned generation-1 extent.

**Review ruling:** the direct buffer path creates no bank-sized temporary; it scans at most 1,024 entries and has no fallible operation after its first write. Exact-size and seven-byte-longer outputs preserve their specified prefix and tail, and current corruption/selection tests still pass. The source-only correction to the region module's tie comment now reflects the existing divergent-tie rejection. No protocol/domain/oracle/corpus, workspace manifest, lockfile or Go production file changed. Rollback can revert this scoped encoder/error/export/test commit while keeping node 1.1.

**Architecture skill:** no change. The atomic buffer rule is already recorded in this storage format change and does not establish a new cross-task architecture rule.

## 2026-09-23 — node 2.1 stage acceptance

**Baseline and scope:** behavior was committed as `20eb6287` and `699d4cd4`. The crate guide now names the fixed bank, checked constructor, direct caller-buffer API, error order and selection policy. Source search still finds no new external Rust bank consumer or derived artifact; the complete diff from the plan commit contains only the four allowed storage source/test files and this change's OpenSpec files, with the crate guide as the closeout edit. No protocol/domain/oracle/corpus, Go production storage, workspace manifest, lockfile, format version or live save changed. The existing crate guide owns this boundary; no new directory guide is warranted.

**Gate results:** 15 region tests were discovered by `-- --list`. `git diff --check` and workspace `cargo fmt --all --check` passed. `make rust-check` passed workspace format, warning-denying Clippy and all Rust workspace tests. `make dev-check` passed Go format, vet and short tests for all six modules plus Rust format, Clippy and workspace tests. `make test-race` passed full race tests across all six Go modules after its Rust release build. `go test ./packages/audit -count=1` passed. `openspec validate --all --strict --no-interactive` passed 128 items with zero failures. The exact storage `runtime_contract` target passed 68/68 and Go region tests passed. No manual foreground-window acceptance run was invoked.

**Whole-change review:** checked the delta spec against the fixed representation, constructor rejection, v1 geometry and CRC, destination failure atomicity and recovery table. Existing Rust corruption cases cover resealed reserved/padding bytes, offset before sector 15, overflow, overlap, EOF, future versions and trailing records; the new tests cover wrong cardinality, invalid-before-short precedence, exact/long output and independent Go checksums. The 1024-slot scan and range sort remain bounded, and direct encoding uses no bank-sized scratch. No unresolved finding or acceptance gate remains. A scoped revert of these three implementation commits restores the prior Rust API without migrating save data.

**Architecture skill:** no change. This work verified the already documented Rust-storage ownership and v1 region rules but discovered no new stable cross-task rule to promote.
