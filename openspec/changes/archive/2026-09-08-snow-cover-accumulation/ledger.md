# snow-cover-accumulation ledger

- 基线 SHA：`5b88c4ef`（main 045465c4 重定基后）。
- Ruling: 4 档雪层呈现高度 2/16..5/16（第 k 档 `block_top_raw`=k，即 raw 1..4）而非 1/16 步进或 raw 2..5 — raw 0 为满格哨兵、8 档视觉差异过细；raw 2..5 在 mesher 公式 (raw+1)/16 下会实渲 3..6/16 与 spec 矛盾 — 档间 1/16 差与 4 档上限在可辨性与方块预算间最优。
- Ruling: 减速放 Go 共享物理落足采样而非 Rust step — Rust 只见碰撞盒不见方块 ID — 权威与客户端预测走同一 `physics.Step` 路径自动一致、零 ABI 变更。
- Ruling: 雪层不可被玩家放置（`PlaceableBlockAtFace` 拒绝） — 雪层是机制产生的装饰层，开放放置引入建造语义与放置面校验 — 超出本 change 范围。
- Ruling: 踢雪粒子复用 precip 实例流与 256 总上限（超限舍弃踢雪尘保天气粒子） — 不新增帧 TLV tag/client ABI 变更 — 粒子语义从属「降水的地面交互」。
- Ruling: 四项踩雪交互（脚印/音效/减速/粒子）与 4 档厚度、每季 3 游戏日均经用户显式选择（本会话 AskUserQuestion）。
- 版本：全部不变（协议 v37、metadata v4、engine ABI v10、client ABI v18、区块 schema v9、scenario v22）；Rust 零改动。

## 任务进度

- 4.2 收尾门禁（控制会话执行，HEAD fce6beef）：`gofmt -l packages/` 无输出；`go test ./packages/audit -count=1` ok（含门禁修复提交 `fce6beef`：三处注释反引号断行拆名改正 + 伙伴寻路阻挡表按 `IsSnowLayer` 谓词放行零碰撞雪层，oracle 测试自愈）；`openspec validate --all --strict --no-interactive` 101 项全过；`make dev-check` 退出码 0；`make test-race` 退出码 0（45 包 ok 零失败）；`make visual-check` 28/28 零差异（snow-cover 首录 + 旧 27 景零字节改动）。协议 v37、metadata v4、engine ABI v10、client ABI v18、区块 schema v9、scenario v22 全部不变；Rust 零改动。Ruling: 伙伴寻路阻挡表补雪层放行属 4.2 门禁修复 — oracle 测试「可通过 ⇔ 零碰撞体」逐编号对齐是既有守护 — 雪层与植物同语义（零碰撞/无后续状态/不可支撑），照 `IsPlant` 谓词先例。
- 4.1 snow-cover 场景与遗留加固：提交 `8013d219`（场景+加固）+ `6334502f`（golden 首录）。验证：`go test ./packages/client/cmd/mornlea/capture -race -count=1` ok（22.7s）、runtime Ownership|Snow ok、`make visual-check` 28 景全绿（旧 27 景零差异，git 层证明零字节改动）。评审 APPROVE：冬季相位数学逐项独立复算成立（yearPhase 0.875 无量化误差、arc 9454、offset 22727、effPhase=6000 精确整除、冬中海平面雨温 −6.44℃ 全雪）、三处清单守护同步、golden 首录提交拆分干净（代码与 PNG 分离）、ownership stage 三处一致且守护测试具 mask 位/map 项双探测器、wiring 测试经完整 Step 且夹具钉冬至晴天使脚印为唯一写入（突变证红）。观察项（不阻断）：雨天灰化把冬季 tint 压到 8bit 量化阈下，天空冷色由 CPU 侧断言承接——设计内后果记录备考。
- 3.3 踩雪音效与踢雪粒子：提交 `eeb68a27`（基线 e2c1df10）。验证：`go test ./packages/client/... -race -count=1` 12 包全 ok（app 70.8s）。评审 APPROVE：xorshift32 纯整数噪声合成与既有 cue 纪律同构（跨平台逐位一致）、cueCount 快照守护迁移正当、触发全本地（一帧一次 BlockIDAt 只读、三处重置完备、tracker 语义镜像 3.1）、雨天满槽整组让位/晴天并入 8 粒/逐字节确定性/零分配全锁定、capture 抓帧路径事件恒空。Ruling: 「晴天非雪面不触发」按雪面门控解读（无天气门控） — 天气门控会使「超限舍弃踢雪尘」条款永远不可触发成死条款、Scenario 停止条件只钉雪面 — 雪层是持久方块状态、晴天残雪照常踩响符合机制语义；spec 措辞已按评审建议改为「非雪面 MUST NOT 触发（晴天残雪照常；满槽让位）」。Ruling: 音效步频门控 0.8 与脚印 0.6 刻意不锁步 — 脚印逐格削雪、音效按步频出声。
- 3.2 厚雪减速：提交 `e2c1df10`（基线 f1f2349e）。验证：`go test ./packages/shared/physics -race -count=1` ok、`./packages/client/client -run 'Predict|Collision|Step'` ok、`./packages/server/sim/... -race -count=1` 全 ok。评审 APPROVE：编码链逐环核实（缩放落在按值 `Tunables.WalkSpeed` 快照、sweep bounds 与 ABI 编码同源消费、Rust 从头字段派生目标、疾跑倍率乘在缩放后两侧同步 ×0.7）、窄接口照 FluidSource 先例逐形对应、镜像侧三分支守卫与既有方法同构、Tunables 两侧同源 `ActiveTunables()` 无新分叉源；40 tick 预测器 vs 权威形态全 State 逐位一致测试锁定。Ruling: 附带效应——被动牛/夜行者/同伴经同一 `dimensionCollisionSource` 同减速 — spec 措辞「实体」泛指（对照脚印 Requirement 明确列举的写法差异）— 零额外代码即覆盖。
- 3.1 踩雪脚印：提交 `f1f2349e`（基线 d14494f5）。验证：`go test ./packages/server/sim/... -race -count=1` 全 ok、`TestSnowFootprint -count=3` 确定性复跑。评审 APPROVE：直线行走 tick 级独立推演（步速 0.215/tick、采样间距 0.645 格鸽笼保证每格恰一采样、格记忆挡同格二次削减）与 Scenario「路径恰降一档无削穿」逐点吻合；脚部格采样与雪层零碰撞几何一致（刻意区分于吃草/踩踏的支撑格语义）；结算入口在 SettleTramples 后满足区块写者契约且先于随机 tick 抽样；瞬态完备（不落盘/不进哈希/不进协议、beginReset 三路径覆盖、邻域回滚不误触）。Ruling: 累计器取「采样即清零 + 落足格记忆」而非「保留余数」 — 纯清零下直线行走约半数格被连续采样窗口削穿两档违反 Scenario — 格记忆使每格恰削一次且静止/同格踱步更强保证。遗留两条可选加固（ownership stage 表补 SettleSnowFootprints、runtime 端到端 wiring 测试）并入 4.x 收尾。

- 1.1 雪层方块族：提交 `4a375c6c` + raw 修正 `3bacdedb` + 容量满守护 `2701aeef`。验证：core/physics/assets/mesh 四包 `-race` 全 ok（mesh 25s 经真实 Rust mesher）、entity Mining/Drop/SnowLayer ok。评审 REQUEST_CHANGES→修复后 APPROVE：数值（ID 85..88 追加零扰动、raw 第 k 档=k 呈现 (k+1)/16）、属性（透明不遮光/零碰撞/不可放置 fail-closed/无掉落清块）、材质层（160/161 末位追加、人物层循环上界改固定 48 的必要性核实、不可材质包覆盖）全部逐点吻合；唯一必须项「容量满也成功清块」守护测试（照短草先例 fillMiningDrops 满仓）已按评审指定修法落地。
- 2.1 积雪与消融：提交 `aeb9d0ad` + 列顶门补强 `d14494f5`（基线 2701aeef）。验证：`go test ./packages/server/sim/... -race -count=1` 全 ok、作物成本 benchmark 等式门禁全绿（barren reads 14520/14400 cells ≤2× 契约）。评审 APPROVE：判定序逐条吻合（含 `HighestOpaque` 实为最高非空气格的语义处理——新雪落位列顶==本格恰等价露天、已有雪层不套 heightmap 否则升档永拒）、tick 起点快照 vs 尾部发布快照的区分必要且正确（环境判定须与被推进世界同一时刻）、`sampleCells` 复用不破作物等式、9+2 测试经命中 tick 预解+气候手算锚把全部负向用例升级为守护绿。Ruling: 接线走 `EnvironmentConfig` 三字段气候束扩展 — 该结构本就是 tick 内固定参数快照、零值无降水零写入 — 既有构造零改动。Ruling: 残留雪层在地面变非白名单后不再被本机制消融 — 该状态无法由机制自身产生（需玩家锄地干预）、补救已有（徒手采掘/踩碎） — 为边角态付热路径读成本不成比例，记为已知取舍。Ruling: 升档子分支补列顶门（评审建议采纳） — design「升档（同 1）」应继承露天条件 — 顶盖下只消不长有守护测试。
