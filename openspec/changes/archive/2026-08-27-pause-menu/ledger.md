# Ledger

## 执行记录段

| 日期 | 阶段 | 结论 | 验证 |
| --- | --- | --- | --- |
| 2026-08-27 | 0 认领与批准 | **Ruling（用户）:** ① 控制会话核实 A-02（活跃 zcode 会话 5 分钟内有产出）与 B-17（opencode-implementer 已交设计+计划）均不可抢、其余排队行依赖未满足后，用户选定 D-02 晋升认领，语义边界取「单机暂停最小闭环」：权威 tick 冻结仅作用单机嵌入服，TCP 远程会话不宣称暂停；② 菜单条目两项方案：「返回游戏 / 退回主菜单」，设置入口顺延。批准来源为控制会话内问答显式选择（两轮）。 | 积压表晋升+认领 docs-only 提交 `b10bbfe7` 快进推送 origin/main；worktree `feat/D-02-pause-menu` 基于 origin/main 创建 |
| 2026-08-27 | 1 契约产物 | 创建 `pause-menu` 全套产物：proposal/design/tasks/ledger + `egui-tool-ui` delta（三条 ADDED Requirements：Esc 开合覆盖层含远程不宣称暂停、权威门冻结可恢复、退回主菜单拆解会话）。关键取舍四条记录于 design.md（服务端 tick 门优于客户端停发/cancel ctx；原子布尔无锁；TCP 同页不同标注；页面级布局版本沿既有先例）。独占文件集见 proposal「Impact」。 | `openspec validate --all --strict --no-interactive` 68 passed |
| 2026-08-27 | 2 契约补钉 | **Ruling:** 核实主菜单动作表占用 1..7 后钉死跨语言契约：动作编号「返回游戏」=8、「退回主菜单」=9，Esc≡8 沿设置页先例；页面布局版本常量新增 `UI_PAUSE_LAYOUT_VERSION=4`，既有 1/2/3 与各自下线格式不动。Task2 建 Rust 侧常量并测试钉住，Task3 建 Go 侧同值常量消费；两者不得各自另定数字。 | grep 确认 8/9 未被任何菜单/UI 动作常量占用 |
| 2026-08-27 | 3 Task1 权威暂停门实现 | TDD 红绿完成：`internal/server/pause_test.go` 五个测试（冻结多调度周期、重复 Pause 幂等、Resume 从冻结值续接增量、同种子孪生世界逐 tick 对照暂停段不改变续跑结果、RunTicks 调度层观察者证实暂停窗口整个 tick 不存在）先红（Pause/Resume 未定义编译失败）后绿。门落点：`Server.paused atomic.Bool` + 导出 `Pause()`/`Resume()`（幂等）；短路安放两处共享同一原子位——`step()` 编排最前（observer 登记之前，覆盖包括显式 `Step` 在内的全部推进点，保证暂停期任何调用方无法推进世界时间）与 `RunTicks` ticker 到期的前置读（免 `stepMu` 争用）。门不持锁、热路径零分配，消息通路与 worker 不受影响。 | `go test ./internal/server -race -count=1` ok（204s 全量含新增）；`go test ./internal/archcheck -count=1` ok；`gofmt -l internal/server` 无输出 |
| 2026-08-27 | 3 Task1 R1 清理轮（SPEC/QUALITY 双评审 PASS 后四条 nit） | ① 测试改名 `TestPauseFreezesWorldTimeAcrossExplicitSteps` 名实相符（走显式推进点；调度循环性质由 RunTicks 观察者测试覆盖）；② `Pause` doc 补并发边界——与在途 tick 并发时已开始的一个 step 会跑完本轮编排，其后周期才被短路（门不持锁固有语义）；③ 「整个 tick 不存在 / worker 与消息通路存活」表述收敛至 `paused` 字段注释单一落点，`step`/`RunTicks` 注释与 `Pause` doc 仅留局部落点理由；④ 种子常量按实际用途拆分：孪生世界改用局部常量 `twinWorldSeed`，调度器测试取独立任意种子。行为逻辑零改动。 | `gofmt -l internal/server` 无输出；`go test ./internal/server -race -count=1` ok（202s） |
| 2026-08-27 | 3 Task2 Rust 暂停页实现 | TDD 红绿完成：先落 `ui/pause_abi_tests.rs`（契约数字钉值：layout=4、「返回游戏」≡Escape 动作=8、「退回主菜单」动作=9；v4 两布尔字段精确解码；未知布局/非法布尔/截断/尾随拒绝）与 `ui/pause_render_tests.rs`（两固定按钮各恰好绘制一次且单机无注明行、远程注明行两分支、Escape 与「返回游戏」产生同一 typed action、「退回主菜单」按钮产生退回动作、隐藏帧零布局），红灯为缺失符号编译失败（20 处）后转绿。实现沿 settings/debug 先例：`UI_PAUSE_LAYOUT_VERSION=4`+`UI_ACTION_PAUSE_BACK=8`+`UI_ACTION_PAUSE_QUIT_TO_MENU=9` 常量族；`UiPauseFrame{visible,remote}` 与 `UiFrame::Pause` variant；layout v4 线格式=版本号+两个 u32 布尔（无变长字段，严格尾随拒绝）；绘制复用 `menu_button_layout` 几何与主菜单标题锚位，半透明遮罩，「远程世界不会暂停，服务端仍在推进」注明行仅远程形态呈现于末按钮下方；布尔汇总 `PauseActions` 保证每帧每类 action 至多一个事件，单帧上界常量 2。既有主菜单 1/设置页 2/调试面板 3 版本常量与线格式零改动，client ABI 保持 v9；Go 编码侧按 design「关键取舍」第 5 条由接线任务建立同值 `menuAction*` 常量后消费，本任务 Go 侧零改动。顺手机械补齐 `menu_frame` 夹具与新 variant 的穷尽匹配 panic 臂（`test_support.rs`、`menu_render_tests.rs` 各一处，行为零变化）。 | `cd engine && cargo test -p mornlea_client --locked` ok（167 passed / 0 failed，含新增 8 个暂停页测试）；rustfmt --check 触碰文件全部干净 |
| 2026-08-27 | 3 Task2 R2 收尾轮（SPEC/QUALITY 双 PASS 后一条 nit） | 暂停页呈现测试补标题呈现完备性断言：本地分支同帧绘制文本须含「已暂停」，与两按钮、注明行两分支构成完整可判定面。行为代码零改动；「已暂停」之外的 draw_pause 标题锚位与 draw_menu 字面重复按控制会话裁决顺延，不在本轮处理。tasks.md 2.3 注记同步补一句说明。 | rustfmt --check 触碰文件干净；`cd engine && cargo test -p mornlea_client --locked` ok（167 passed / 0 failed） |
| 2026-08-27 | 3 Task3 相位机接线——阻塞上报（未进入红绿循环） | TDD 前置核查发现单点集成缺口，未写任何生产/测试代码即停：Task 1 的 `Pause()`/`Resume()` 落在 `*server.Server`，而交互单机（StartAtMenu 延迟装配与直接本地装配）的权威世界只在 `*server.Host` 私有字段 `world` 里；穷举 `*Host` 导出方法面（Run/AcceptStream/Shutdown/Stats/RunAtInputBoundary 及登录内部方法）确认没有任何公共途径取回内部 Server 或转发暂停门；`application.server` 仅 benchmark 分支赋值；`NewWorld` 在配置伙伴时 panic 强制用 `NewHost`（伙伴为常态配置，不可替换构造）。故「本地形态真实调用门」（design 关键取舍第 1 条已否决客户端停发伪暂停）在不动 internal/server 的前提下无合规解，超出本任务边界约束。待裁决的最小修复：①授权给 `(*Host)` 增 `Pause()`/`Resume()` 直通（约 10 行+直通幂等小测试），cmd/mornlea 侧经 `type applicationPauseGate interface{ Pause(); Resume() }` 对装配返回的 host 做类型断言捕获可选门，接口签名零改动、远程形态 nil 安全跳过并照常下发 remote 标志；②或另派微任务交付该直通后继续。相位常量（动作 8/9 同值钉定）、Esc 栈档位序、防重入哨兵、退回主菜单复用 closeClientSession+releaseWorldConnection 同序拆链（含 clientCloseOnce 只耗一次需并用世界连接释放避免二次进入泄漏）、layout v4 段编码（版本 4+两 u32 布尔）、-connect 形态防御性拒绝本地再装配等全套实现与测试映射已定稿，放行即可立即 TDD。 | 生产/测试代码零改动；worktree 停在 f1b9b8c4 干净态；本行仅为可追溯性记录 |
| 2026-08-27 | 3 Task3 裁决与产物修订（控制会话） | **Ruling:** 采纳阻塞上报建议①：授权同一实现者在 Task3 内以 TDD 先行补全 `(*server.Host).Pause()/Resume()` 幂等直通转发（内持 Server 同一门），作为该门的唯一宿主暴露面；cmd/mornlea 侧以可选接口断言捕获，远程形态 nil 安全跳过并照常下发 remote=true。依据：internal/server 本就在本 change 独占文件集（proposal「Impact」），缺口属契约暴露面遗漏而非范围扩张；客户端停发伪暂停已被设计否决，无更小合规解。已按「先改产物再编码」同步修订 proposal「Impact」、design 架构落点表与受影响文件、tasks.md 新增 3.0 前置条目。 | 产物修订提交见后续 commit；验证命令同 tasks.md 3.0/3.3 所列 |
| 2026-08-27 | 3 Task3 裁决后继续（3.0–3.3 全部完成） | TDD 红绿完成全部接线。**3.0**：`pause_test.go` 追加两测试先行（宿主 `Pause()` 冻结世界直至 `Resume()` 从冻结值续接；置位与恢复双向幂等），红灯为 `host.Pause undefined` 六处编译失败；`internal/server/host.go` 增直通两方法转发内持 `*Server` 同一门，注释声明门唯一宿主暴露面。**3.1/3.2**：`cmd/mornlea/app_pause_test.go` 七测试先行（开侧释放光标+门恰一次+段 12 字节；Esc 边沿与重复返回动作只恢复一次且跨周期重新武装；未知/错相位 id 表面安全；退回拆链序=Resume→会话态复位→Host Shutdown→字段复位→二次装配全新镜像与门；远程形态零门调用 remote=1 且本地对照 =0；-connect 防御拒绝落错误行不 panic；非暂停相位无暂停段）红灯为缺失符号编译失败，后随实现转绿。实现：`menuPhasePaused`（枚举尾追加，零值语义不变）；动作常量 8/9 同值钉定 Rust 契约；新增 `app_pause.go`——可选能力接口 `applicationPauseGate` 与断言提取器、恢复防重入哨兵、layout v4 段编码（版本 4+visible 恒真+remote 位）、退回主菜单先解冻再复用拆链（自 `closeClientSession` 提取 `resetSessionOwnedState`，`clientCloseOnce` 不再阻碍跨周期重复拆链，端点与 Host 生命周期交 `releaseWorldConnection`）；`interactive.go` Esc 栈五档位最终序：聊天>背包>面板>暂停(Esc≡返回动作 8)>默认档打开暂停层（同时释放光标）；游戏循环内暂停相位独占 drain typed UI 事件并整体静音输入（中性移动/抑制点击重捕与快捷栏/Enter 不新开聊天/F3 抑制且面板 drain 语义原样保留）；`runInteractive` 改外层相位循环支持游戏→菜单往返；装配点类型断言捕获门（构造本地/benchmark 分支 + startWorld）；startWorld 增 -connect 防御拒绝与会话新鲜化（镜像/预测器/区块表/序号复位、`clientSessionClosed` 清除）。引擎/engine/ 零触碰，wire 协议零变更。 | **红灯证据**：两轮缺失符号编译失败（`host.Pause undefined` 六处；cmd 侧 `connectionTestHost.Resume` 等）；**绿灯证据**：`go test ./internal/server -race -count=1` ok 211s、`go test ./cmd/mornlea -race -count=1` ok 274s、定点 TestPause*/TestStartWorldRejects/TestHostPausePassthrough 全绿、`gofmt -l cmd/mornlea internal/server` 无输出、`go vet ./cmd/mornlea ./internal/server` 干净、`go test ./internal/archcheck -count=1` ok |
| 2026-08-27 | 3 Task3 R 清理轮（SPEC/QUALITY 双 PASS 后五项） | ① design.md 关键取舍撞号修复：golden 条由重复的「5.」重排为「6.」，序列恢复唯一（第 5 条继续指向跨语言契约，既有引用不漂移）；② delta spec「退回主菜单安全拆解会话」Requirement 补半句远程边界（再进 SHALL 错误行优雅拒绝）并新增 Scenario「远程会话退回后再进入被优雅拒绝」，与既有 `TestStartWorldRejectsRemoteConnectForm` 锁定行为对应；③ `app_pause_test.go` 两处补强——暂停段版本字正向断言 offset[0:4]==4（跨语言互钉数字的 Go 半边显式化）+ 两条暂停期输入静音行为锁（Enter 不新开聊天、F3 不切换面板；复用 `newChatLoopApplication` 四帧按键脚本，「终态相位=游戏」兼作违规判别器并附恢复后对照帧防假绿）；④ tasks.md 3.2 追加执行注记一句；⑤ 本行记录。过程中发现并排除一起环境级假警报：mute 锁首跑触发 Rust `INVALID_ARGUMENT`，探针（临时测试，已删）证明 v4 段字节本身合法——根因是本 worktree 链接的 `engine/target/release` dylib（19:48）早于 Task2 提交（20:31），`make rust` 重建后端到端渲染通过；佐证 clean checkout 先 `make rust` 的既有规程，非契约缺陷、零代码改动。 | 探针对比：基线帧过/带段帧败→重建 dylib 后同探针双帧过；`gofmt -l cmd/mornlea internal/server` 无输出；`go vet ./cmd/mornlea ./internal/server` 干净；`go test ./cmd/mornlea -race -count=1` 全绿（含新增 3 断言/2 测试）；`openspec validate --all --strict --no-interactive` 通过 |
| 2026-08-27 | 4 收尾门禁 | 全部门禁串行执行（无并发重型命令），1-3 组既有勾选核对无误。**`make visual-check` EXIT=2 裁决（控制会话，采纳方案 A）**：预期视觉不变的本 change 断言成立且证据充分——detach 到 merge-base `b10bbfe7`（零本 change 提交）重建该点 dylib 后跑 capture，失败集与本分支 HEAD **逐项一致**（同 10 场景、同差异像素数、同最大通道差、同首差像素坐标），9 对失败场景实拍图 `cmp` **逐字节相同（9 identical / 0 differing）**，main-menu/settings-menu/debug-panel 等 0.0000% 像素差——本 change 对画面增量精确为零，「暂停页不出现在任何 capture 场景」成立。目标失败归因**预存陈旧基线**：`workbench-crafting.png` golden 从未入库（`capture.go` 注明由批次集成任务在 scenario 迁移时统一生成，A-06/A-07 取消后无人再生，origin/main 亦缺失）+ 9 张陈旧 golden（容器/背包 UI 版式演进差异与世界渲染微抖动，实拍内容无暂停页痕迹）。处置：不放宽阈值、不运行 `--update-golden`、不在本分支补预存 golden（避免与在途 golden 职责行冲突）。 | 门禁数值：`gofmt -l .` 无输出；`go vet ./...` 干净；`go test ./... -race -count=1` 26 包全 ok、0 FAIL，总墙钟 4m55s（最长 `cmd/mornlea` 293.658s、`internal/server` 223.400s）；`make rust` 成功（增量 0.29s，dylib 与分支源码一致）；`make rust-check` 成功（`cargo fmt --check` 与 clippy `-D warnings` 干净；mornlea_client 167 passed / 0 failed、mornlea_engine 166 passed / 0 failed）；visual-check 对照实验全数据——HEAD 失败 10 场景：terrain-noon 10px/0.0043%、avatar-nametag 10px/0.0043%（两者首差均 (423,123)）、inventory-crafting 84182px/36.5373%（首差 (219,16)）、chest-container 112382px/48.7769%（(138,16)）、furnace-container 74738px/32.4384%（(138,71)）、oak-grove 56px/0.0243%、ai-companion 39px/0.0169%、far-horizon 32px/0.0139%、water-underwater 4px/0.0017%、workbench-crafting golden 缺失；merge-base 同轮失败数值逐项相同，其余 10 场景通过（菜单/HUD/面板类 0.0000% 像素差）；`openspec validate --all --strict --no-interactive` 68 passed, 0 failed（留痕编辑后复核同值）。**移交清单（非本 change 范围，留 golden 职责行/用户裁决）**：① `workbench-crafting.png` golden 补生成；② 9 张陈旧 golden 再生：terrain-noon、avatar-nametag、inventory-crafting、chest-container、furnace-container、oak-grove、ai-companion、far-horizon、water-underwater。 |
| 2026-08-27 | 5 终审修复轮 R1 | **Blocking B1（终审裁定，证据链六环核实）**：宿主 winit `poll` 同一次泵既更新 Go 键位快照、又把 `UiEvent::Key{Escape,pressed}` 压入 UI 事件队列——Go 帧 N 检测 Esc 边沿打开暂停层，帧尾 `uiSegment()` 下发暂停段，同帧 Rust `take_ui_events()` 取到该 Escape 事件与暂停段一起喂 egui，`draw_pause` 的 `actions.back |= key_pressed(Key::Escape)` 合成动作 8，Go 帧 N+1 drain 到 8 立即 `closePauseOverlay`；净效果为覆盖层只活一帧、世界冻结约一 tick 后自动解冻。Go 侧测试用 fake renderer 不回放 winit 事件、Rust 测试单侧，双测皆盲故前轮未暴露。**修复（裁决采纳：删除 Rust 侧 Escape 合成，Go 成为 Esc 唯一裁决者）**：`draw_pause` 删合成行并改写相邻注释（设置页 Esc≡back 先例仅适用于菜单相位，暂停层属游戏相位不可照搬）；`pause_render_tests.rs` 的 `pause_escape_emits_same_back_action_as_return_button` 反转为 `pause_frame_ignores_escape_key_events`（暂停帧+Escape 键事件⇒零动作；「返回游戏」⇒8、「退回主菜单」⇒9 既有断言保持）；`pause_abi_tests.rs` 钉值注释去 `(≡Escape)`。Go 侧行为零改动（暂停档 Esc 边沿关闭由既有 `TestPauseTransitionMatrixEscOrButtonResumesOnce` 覆盖），仅同步陈旧契约表述——`app_menu.go` 动作常量注释与 `interactive.go` 两处「Rust 合成」措辞；delta spec 核对无「Rust 合成」类字样，Esc 开合行为表述不变（裁决者在 Go）。design.md 关键取舍第 5 条与测试矩阵、tasks.md 2.1 尾注同步修订。**顺带清偿三 nit**：N1 `app_pause_test.go` 补 `menuActionPauseBack==8`/`menuActionPauseQuitToMenu==9` 钉值断言（与版本字断言同处）；N3 proposal「Impact」代码清单补 `app.go`、`app_lifecycle.go`、`app_startup.go`（Task3 接线实触文件）；N4 执行记录表两处空行切断合并回单表（全部行内容保留）。**N2**（285dc4b0 英文提交标题）：已确认，不重写历史。 | 红灯：反转测试先以「暂停帧不得把 Escape 键事件合成为动作」失败，复现 B1 语义；绿灯：`cd engine && cargo test -p mornlea_client --locked` 167 passed / 0 failed（rustfmt --check 干净）；`gofmt -l .` 无输出；`go test ./cmd/mornlea -race -count=1` 全绿；`openspec validate --all --strict --no-interactive` 通过 |
| 2026-08-27 | 5 终审复审（B1 限范围） | **复审 PASS，blocking 清零**。五点核实摘要：① `draw_pause` 已无任何 Escape→back 合成路径，动作 8 仅由「返回游戏」按钮产生；② 呈现测试语义反转成立且红灯先行（`pause_frame_ignores_escape_key_events` 暂停帧+Escape 键事件⇒零动作断言，按钮⇒8/9 既有断言原位保持）；③ Go 侧行为零改动，Esc 键位栈暂停档裁决路径完好（`TestPauseTransitionMatrixEscOrButtonResumesOnce` 与静音族行为锁全绿）；④ 两侧契约注释与产物表述同步无残留——`app_menu.go` 常量注释、`interactive.go` 两处、`pause_abi_tests.rs` 钉值注释、design 关键取舍第 5 条与测试矩阵、tasks 2.1 尾注、delta spec 核对无「Rust 合成」字样；⑤ 修复轮验证门禁四连全绿（cargo 167/0、gofmt、`go test ./cmd/mornlea -race`、openspec 68/0）。**残留 nit 一条**：`app_pause.go` `pauseState` doc 仍写「Esc 边沿与 Rust 回传的返回动作（同一物理按键的两条上游通路）」——与修复后事实不符，已由本日收口轮以纯注释修正清偿（改为「Esc 键位边沿与『返回游戏』按钮点击可能同帧相继到达」，哨兵去重必要性说明保留，零行为）。 | 收口终轮验证（注释轮不重跑全量）：`gofmt -l cmd/mornlea` 无输出；`go vet ./cmd/mornlea` 干净；`go test ./cmd/mornlea -run 'TestPause' -count=1` 全绿 |
| 2026-08-27 | 5 归档 | `openspec archive pause-menu -y`（CLI 1.7.0）完成：delta 三条 ADDED Requirement 原样沉淀进 `openspec/specs/egui-tool-ui/spec.md`（+3/~0/−0，含修复后「Esc 开合由 Go 键位栈裁决」口径），归档目录 `openspec/changes/archive/2026-08-27-pause-menu/`（沿既有日期前缀命名）。E-14 先例担忧的工具怪癖（对已存在主规格忽略 delta Purpose、Scenario 标题异议）未触发——本 delta 无 Purpose 段、工具零异议，无需直编。基线事实行落 `docs/notes/progress.md` 基线演进段；根 AGENTS.md 版本矩阵无变化不触碰。 | 归档后核对：`openspec/changes/` 下无活跃 pause-menu 目录；`egui-tool-ui` 主规格含三条新 Requirement（Esc 暂停覆盖层、权威暂停门、退回主菜单拆链）；`openspec validate --all --strict --no-interactive` 全过 |

### 版本矩阵（基线 `b10bbfe7`）

全部不变：协议 v27、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` schema v4、engine ABI v7、client ABI v9、benchmark scenario v19。唯一结构变化为 UI 下行布局段内部版本 3→4（仓库自有契约，沿 D-03 先例）；capture 场景表与视觉 golden 零变化。
