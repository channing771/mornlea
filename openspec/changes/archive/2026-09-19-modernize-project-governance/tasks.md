## 1. Freeze inventories and ownership

- [x] 1.1 Regenerate the exact current inventory of first-party comment-language violations, canonical OpenSpec language debt, current/historical plan language, bilingual-document candidates, stale version claims, and Hook-policy claims; record commands, counts, exclusions, current HEAD, and pre-existing dirty paths in the change ledger without modifying user-owned files. Verify with `git status --short`, the inventory commands recorded in the ledger, and `openspec validate modernize-project-governance --type change --strict --no-interactive`.
- [x] 1.2 Define the current-document classification in `docs/documentation-manifest.json`, covering every maintained explanatory/architectural document and explicitly classifying plans, historical evidence, machine governance, licenses, and generated files; begin with a failing audit test for unclassified current Markdown. Verify with `go test ./packages/audit -run 'DocumentationManifest|DocumentationClassification' -count=1`.
- [x] 1.3 Record an ownership matrix that separates this governance change, `pilot-godot-client-migration`, and unrelated in-flight work; reject overlapping edits to the current modified render/golden/spec/plan files unless their owner explicitly transfers them. Verify the matrix paths against `git status --short` and record the ruling in the ledger.

## 2. Establish provider-aware machine governance

- [x] 2.1 Update root `AGENTS.md` in English so verified ChatGPT/Codex OpenAI controllers have standing project authorization to choose direct or delegated execution and review depth without a separate per-task user request, while non-OpenAI or unknown providers retain strict SDD; preserve OpenSpec, TDD, ledger, validation, ownership, safety, and external-action authorization requirements. Verify with `go test ./packages/audit -run 'ProviderAwareOrchestration|BaselineVersionsMatchCode' -count=1`.
- [x] 2.2 Update applicable scoped `AGENTS.md` files, especially `docs/AGENTS.md` and `packages/engine/AGENTS.md`, to remove Chinese-comment requirements and point to the root provider-aware policy without duplicating it. Verify with `go test ./packages/audit -run 'AgentGuidance|CodeCommentLanguagePolicy' -count=1`.
- [x] 2.3 Translate `openspec/config.yaml` into English and change proposal/spec/design/tasks guidance to English-only; replace the unconditional SDD apply guidance with the provider-aware policy while retaining test-first work, artifact reconciliation, validation, ledger, and archive requirements. Verify with `openspec instructions proposal --change modernize-project-governance --json`, `openspec instructions apply --change modernize-project-governance --json`, and `go test ./packages/audit -run 'BaselineVersionsMatchCode|ProviderAwareOrchestration' -count=1`.
- [x] 2.4 Create `.codex/skills/mornlea-implementation-orchestration/SKILL.md` with standing OpenAI delegation authorization, the two execution modes, explicit user prohibition handling, and the higher-priority runtime-policy boundary; keep it concise and do not patch cached third-party skills. Verify with the skill-creator `quick_validate.py` and a source check that the skill contains no model-name substring heuristics.
- [x] 2.5 Create the mirrored `.claude/skills/mornlea-implementation-orchestration/SKILL.md` with identical policy content, then add a focused audit that requires both project copies to remain synchronized. Verify both with `quick_validate.py` and `go test ./packages/audit -run ProjectOrchestrationSkillsMatch -count=1`.
- [x] 2.6 Update the project-owned Codex and Claude OpenSpec apply entry points to route implementation orchestration through the new project skill without changing CLI-controlled blocked/ready/all-done semantics. Verify with a focused text/fixture test and `openspec instructions apply --change modernize-project-governance --json`.
- [x] 2.7 Establish the initial bounded-delegation policy in root guidance and both `mornlea-implementation-orchestration` skill copies, including the two-subagent ceiling, live `adaptive-model-router` discovery, lowest-sufficient selection, and one-step escalation after observable insufficiency. The later isolation-first refinement in task 2.13 supersedes the initial main-agent preference while retaining these cost and safety bounds. Verify with `go test ./packages/audit -run 'ProviderAwareOrchestration|DelegationBudget|AdaptiveModelRouting' -count=1` plus both skill validators.
- [x] 2.8 Create synchronized `.codex/skills/mornlea-architecture/SKILL.md` and `.claude/skills/mornlea-architecture/SKILL.md` as concise decision aids covering durable cross-task ownership, dependency, lifecycle, desktop-platform, visual-routing, and documentation conventions with pointers to authoritative sources. Verify both with `quick_validate.py`, byte comparison, and `go test ./packages/audit -run ProjectArchitectureSkillsMatch -count=1`.
- [x] 2.9 Add the round-end architecture retrospective to the orchestration skill and current development-process guidance: promote only verified, reusable decision rules; otherwise record `Architecture skill: no change` in the ledger. Add a focused audit that rejects missing promotion criteria or unsynchronized skill copies. Verify with `go test ./packages/audit -run 'ArchitectureSkillRetrospective|ProjectArchitectureSkillsMatch' -count=1`.
- [x] 2.10 Create synchronized project-owned `.codex/skills/adaptive-model-router/` and `.claude/skills/adaptive-model-router/` packages with live capability discovery, the OpenAI ceiling, multi-axis routing, coequal code-quality/token-efficiency goals, and evidence-based escalation. Verify both packages with `quick_validate.py`, recursive synchronization checks, and `go test ./packages/audit -run 'ProjectAdaptiveModelRouter' -count=1`.
- [x] 2.11 Integrate the project router into root guidance, both orchestration skills, and the bilingual development-process pair. Require a round-end over-routing/under-routing retrospective and update the project router only for verified reusable improvements; otherwise record `Model router: no change`. Verify with the project-router audit, documentation pair/semantic checks, and strict OpenSpec validation.
- [x] 2.12 Add synchronized `adaptive-model-router/scripts/zcode-agent.mjs` resources as a credential-safe external-agent bridge for the installed Z Code `GLM-5.3` route, with read-only `probe`, fresh `run`, resumable `send`, machine-readable output, and secret redaction. Start with fixture-backed failing tests and verify with the skill-local focused Node suite plus a real read-only probe.
- [x] 2.13 Change root guidance, OpenSpec apply guidance, and both orchestration skills from main-agent-first to isolation-first routing. Prefer fresh/minimal contexts for independently briefable discovery, multi-file reasoning, specialized review, and long traces while keeping tiny or tightly coupled work direct. Verify focused mutation tests with `go test ./packages/audit -run 'ProjectOrchestrationPrefersContextIsolation|DelegationBudgetAndAdaptiveModelRouting' -count=1`.
- [x] 2.14 Extend both model-router skill copies with native-versus-external backend selection, exact Z Code probing and session follow-up, startup-cost accounting, and editing ownership rules. Keep the copies byte-identical and verify with both skill validators and `go test ./packages/audit -run 'ProjectAdaptiveModelRouter|ProjectRouterDefinesZCodeBridge' -count=1`.
- [x] 2.15 Add the directory-scoped guidance rule to root `AGENTS.md`, `openspec/config.yaml`, `docs/agents-md-style.md` and its Chinese counterpart, both `mornlea-architecture` skill copies, and both `mornlea-implementation-orchestration` skill copies. Add an audit assertion for the important-directory definition, exact `AGENTS.md` naming, required guide content, same-change updates, and parent inheritance; do not generate guides for directories without independent invariants. Verify with `go test ./packages/audit -run 'DirectoryScopedGuidance|ProjectArchitectureSkillsMatch|ProjectOrchestrationSkillsMatch' -count=1`, `cmp -s`, the skill validator, and `git diff --check`.

## 3. Adopt English canonical and Chinese counterpart documentation

- [x] 3.1 Add document front-matter parsing and pair validation in `packages/audit`: require English `*.md`, Chinese `*.zh.md`, reciprocal counterpart paths, matching `doc_id`/`doc_revision`, valid classification, and no newly introduced `*.en.md`. Start with failing tests for missing, duplicate, mismatched, and unclassified pairs. Verify with `go test ./packages/audit -run 'DocumentationPair|DocumentationManifest' -count=1`.
- [x] 3.2 Extend document validation to compare fenced commands, referenced repository paths, protocol/schema/ABI/benchmark claims, and local links across each pair; require all current version claims to match code-owned sources. Verify with mutation fixtures and `go test ./packages/audit -run 'DocumentationSemanticClaims|CurrentDocumentationVersions|DocumentationLinks' -count=1`.
- [x] 3.3 Migrate the root documents to `README.md` English canonical plus `README.zh.md` Chinese, remove `README.en.md`, update every repository-owned link, and correct all stale engine ABI v10/client ABI v14 statements to v11/v19. Verify with `go test ./packages/audit -run 'MornleaCurrentIdentity|CurrentDocumentationVersions|DocumentationPair' -count=1` and a repository link check.
- [x] 3.4 Migrate `docs/README.md` and `docs/architecture.md` to English canonical documents with `docs/README.zh.md` and `docs/architecture.zh.md`; add the approved future `apps/mornlea-godot/` location without presenting unimplemented Godot runtime behavior as current fact. Verify with documentation-pair, link, version, and architecture audits.
- [x] 3.5 Migrate `docs/development-process.md` and `docs/openspec.md` to English canonical plus `.zh.md` counterparts; remove installed-Hook claims and document provider-aware orchestration consistently. Verify with `node --test scripts/agent-hooks/guard.test.mjs`, `go test ./packages/audit -run 'ProviderAwareOrchestration|HookDocumentation|DocumentationPair' -count=1`, and link checks.
- [x] 3.6 Migrate `docs/test-organization.md` and `docs/agents-md-style.md` to English canonical plus `.zh.md` counterparts, changing all source-comment requirements to English while preserving test placement, helper, naming, and no-task-ID rules. Verify with `go test ./packages/audit -run 'CodeCommentLanguagePolicy|DocumentationPair' -count=1`.
- [x] 3.7 Classify unchanged pre-policy player/configuration/compatibility/server/limitations/texture-pack explanatory documents as `legacy` rather than bulk-translating them. Require any future edit to migrate the document atomically to English `*.md` plus Chinese `*.zh.md`, preserving commands and localized examples. Verify with manifest, legacy-change, pair, link, and command-claim audits.
- [x] 3.8 Classify the remaining unchanged pre-policy technical notes selected by the manifest as `legacy`; keep completed governance-critical pairs synchronized and historical reports/task ledgers classified as history. Verify that every governed Markdown path has exactly one classification and every changed legacy fixture is rejected until pair migration.

## 4. Migrate active and canonical planning language

- [x] 4.1 Translate every active OpenSpec artifact other than user-owned in-flight files to English while preserving requirement names, scenario coverage, task checkboxes, identifiers, and semantics; `pilot-godot-client-migration` is updated through its own change workflow. Verify with `openspec validate --all --strict --no-interactive` and the active-artifact language audit.
- [x] 4.2 Add a language classifier for active/canonical OpenSpec prose and new/substantively revised project plans, with narrow exclusions for localized literals, code fences, identifiers, and historical paths. Start with failing fixtures and verify with `go test ./packages/audit -run 'OpenSpecLanguage|PlanLanguage' -count=1`.
- [x] 4.3 Generate a canonical-spec language debt inventory for `openspec/specs/` and enforce that new or modified canonical specs are English and total debt cannot increase; archived changes and historical plans must remain outside the scan. Verify with `go test ./packages/audit -run 'OpenSpecLanguageDebt|HistoricalPlanningExemption' -count=1`.
- [x] 4.4 Grandfather unchanged pre-policy authoritative gameplay/storage/network specification prose through the fixed language inventory; reject any new debt, increase, or non-English replacement without requiring bulk translation in this change.
- [x] 4.5 Apply the same grandfather/non-growth rule to unchanged client presentation, UI, audio, visual, and tool specifications; require all new or substantively revised prose to use English and keep optional decreases explicit and validated.
- [x] 4.6 Apply the same grandfather/non-growth rule to unchanged engine, architecture, repository, performance, and Agent specifications. Retain the inventory without a zero-debt deadline; removal or whole-corpus translation requires a separately approved future decision. Verify with `go test ./packages/audit -run 'OpenSpecLanguage|OpenSpecLanguageDebt|OpenSpecLanguageGrandfather' -count=1` and strict OpenSpec validation.

## 5. Enforce and migrate English source comments

- [x] 5.1 Add comment-token scanners and mutation fixtures for first-party Go, Rust, C, GDScript, JavaScript, and TypeScript; exclude only recorded generated/vendor/license paths and prove that localized strings are not scanned. Verify with `go test ./packages/audit -run 'CodeCommentLanguage|CommentScanner' -count=1`.
- [x] 5.2 Generate `testdata/audit/english-comment-migration.json` as a grandfather inventory with per-file counts and digests, and implement a non-increasing gate that rejects new files with non-English comments, changed non-English comments, count increases, and baseline growth while allowing unchanged legacy comments to remain. Verify with mutation tests and `go test ./packages/audit -run 'EnglishCommentMigration|CodeCommentLanguage' -count=1`.
- [x] 5.3 Integrate the changed-comment check into `scripts/agents/gates.sh`, Makefile, and CI without scanning unrelated historical documentation or launching expensive runtime tests. Verify with focused script tests, `GATES_SKIP_RACE=1 scripts/agents/gates.sh`, and CI configuration checks.
- [x] 5.4 Reconcile the policy with the approved grandfather rule: remove comment-translation follow-up changes, retain existing debt without a zero-debt deadline, and require every new architecture file plus every new or rewritten comment to use English. Verify the Godot plan invokes `make comment-language-check`, the baseline cannot grow, and unchanged legacy comments remain accepted.

## 6. Keep visual baselines renderer-neutral

- [x] 6.1 Update the mirrored `.codex/skills/visual-baseline/SKILL.md` and `.claude/skills/visual-baseline/SKILL.md` in English: route by UI/world/cross-tick semantics, treat Godot pilot output as untracked evidence, forbid renderer-specific golden classes, and require explicit producer handoff. Verify skill synchronization and run `quick_validate.py` for both copies.
- [x] 6.2 Migrate `testdata/visual-golden/README.md` to English canonical and add `README.zh.md`; preserve the current three classes, scene/fixture registries, counts, thresholds-by-source references, and explicit update discipline. Verify file counts/registries and documentation-pair checks without changing any PNG or GIF.
- [x] 6.3 Migrate `docs/notes/visual-verification.md` to English canonical plus `.zh.md`, reconcile it with the current Hook state and semantic routing, and keep comparison thresholds sourced from code rather than duplicated into skills. Verify with documentation-pair/link checks and focused capture tests.
- [x] 6.4 Add audit tests that reject `testdata/visual-golden/godot/`, require pilot evidence outside tracked goldens, and require a handoff record before a producer change can update an existing semantic class. Verify with `go test ./packages/audit -run 'VisualBaselineRouting|VisualProducerOwnership' -count=1`.
- [x] 6.5 Confirm that this governance change modifies no tracked PNG/GIF and does not relax thresholds; run `git diff --stat -- testdata/visual-golden` and the existing non-update visual checks. Record the user-owned PNG and six renderer differences as external baseline findings without changing evidence or thresholds.

## 7. Reconcile the Godot migration plan

- [x] 7.1 Update `pilot-godot-client-migration` so `features/` replaces the overly fine-grained `modules/` layout; define coarse `session`, `player_view`, `world`, `actors`, and `ui` ownership, keep `app/` feature-agnostic, and restrict `platform/desktop` to macOS/Windows/Linux with no mobile, Web, or console adapters or export presets. Verify its proposal/spec/design/tasks remain coherent.
- [x] 7.2 Make the Godot feature catalog consume bridge feature-family availability in one direction, reserve manifests for independently replaceable features, and add export exclusions for tests, diagnostics, provenance, and non-selected features. Verify with the Godot project's planned architecture/audit tasks.
- [x] 7.3 Replace Godot tracked-golden terminology with `godot-visual-evidence` and `godot-visual-compare`; write pilot output under `build/visual/godot-pilot/<run-id>/`, keep tracked baselines unchanged, and defer producer handoff to later feature changes. Verify the change never defines `testdata/visual-golden/godot` as an output while still preserving `make visual-check` for the current client; references that explicitly reject that path remain valid guardrail text.
- [x] 7.4 Translate all four Godot change artifacts to English and update explanatory-document tasks to produce English `*.md` plus Chinese `*.zh.md`. Verify with `openspec validate pilot-godot-client-migration --type change --strict --no-interactive`.

## 8. Final validation and rollout

- [x] 8.1 Run formatters for all changed Go/Rust/script files and validate both new project skills; verify with `make fmt`, `cd packages/engine && cargo fmt --check`, and both `quick_validate.py` commands.
- [x] 8.2 Run focused governance and audit suites, Hook implementation tests, document link/pair checks, OpenSpec/plan language checks, comment ratchets, and visual-routing checks. Verify with the exact focused commands recorded in the ledger.
- [x] 8.3 Run Rust and six-module development gates: `make rust-check`, `make dev-check`, and `go test ./packages/audit -count=1`. Rust and audit must pass; record the reproducible non-race `TestWarpParityMemoryVsTCP` failure as an external pre-existing server finding rather than modifying server behavior in this governance change.
- [x] 8.4 Run the six-module full race gate and preserve the existing visual/frontend baselines: `make test-race`, `make visual-check`, and `make frontend-check`. Require race/frontend success and record user-owned visual differences without updating PNG/GIF or thresholds.
- [x] 8.5 Run `openspec validate --all --strict --no-interactive`, confirm both changes and every completed bilingual pair are coherent, confirm all unchanged pre-policy documents/specifications are explicitly grandfathered, and record their non-growth inventories.
- [x] 8.6 Perform the final implementation-round architecture retrospective: update both `mornlea-architecture` skill copies only for newly verified cross-task conventions, or record `Architecture skill: no change` with evidence in the ledger; then rerun both skill validators and synchronization audits.
