## Task 1: 登录 tick 等待预算同源化

- [x] 1.1 按 TDD 先新建 `internal/server/login_wait_budget_test.go`，覆盖初始满足零推进、预算内精确推进、永不满足恰好耗尽 3000 tick 且保留动态诊断；用 `go test ./internal/server -run TestIntegrationLoginTickBudget -count=1` 记录预期编译 RED。随后在既有 helper 中加入 `integrationLoginTickBudget` 与只负责活性的登录等待函数，并迁移以下十二个手动 tick 登录循环：`farming_loop_e2e_test.go`、`hunger_loop_e2e_test.go`、`farming_integration_test.go`、`till_soil_integration_test.go`、`material_processing_integration_test.go`、`eating_parity_test.go`、`transport_parity_integration_test.go`（两处）、`fluid_transport_parity_test.go`、`block_light_integration_test.go`、`placement_success_integration_test.go`、`player_melee_parity_test.go`。保持消息解析、镜像应用和业务断言原样，不触碰已有 `context`/墙钟/有限次数等待或生产代码。完成后运行 `gofmt`、上述 helper 定点测试、受影响登录/Memory-TCP 用例、`go test ./internal/server -race -count=1`、`go test ./internal/archcheck -count=1`、`go vet ./...`、`go test ./... -race`、`test -z "$(gofmt -l .)"` 与 `openspec validate --all --strict --no-interactive`；把 RED/GREEN、完整迁移清单和门禁摘要写入 `ledger.md`，全部通过后才勾选本项。
