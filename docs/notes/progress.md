# 项目实现进度

本文件记录已交付里程碑、当前基线与下一步方向。可观察行为一律以代码、测试与 `openspec/specs/` 主规格为准；`docs/superpowers/` 中的设计文档写了某项能力，不代表代码已经实现。

## 里程碑

已完成的 M3 多人里程碑支持最多八名玩家通过局域网专用服务端同步移动、角色和 Unicode 昵称；M4A–M4D 依次增加权威快捷栏、持久掉落物、36 格背包与固定石砖配方；M4E 新增煤矿、铁矿、共享权威熔炉、铁锭与铁块资源链；M4F 新增服务端权威的按住采掘、石镐、铁镐与五条固定配方；M4G 新增由服务端权威推进并持久化的 24000 tick 昼夜与直射天空光，并把世界 metadata 升级到 v2；M4H 新增权威单件原地丢弃；M4I 新增消费权威世界时间的天体天空呈现；M4J 为石镐和铁镐加入权威耐久、损坏形态及跨 TCP/存档的耐久保真；M4K 新增由区块拥有的固定容量共享箱子（每区块 16 个、每箱 27 格），把熔炉与箱子的查看生命周期收敛为共用实现，统一容器界面扩展为 `0..62`，并把线上协议升级到 v12、区块存档升级到 schema v6；M4L 新增由服务端唯一权威、跨重连与重启保真的生命值（满值 20），实现摔落伤害、未受伤自动回复与死亡时的背包掉落和重生结算，并把线上协议升级到 v13、玩家存档升级到 schema v5；M4M 增加客户端从权威方块镜像派生的 `0..15` 传播天空光；M4N 增加客户端派生的静态方块光和发光块，并把线上协议升级到 v14、区块存档升级到 schema v7；随后的常见块状材料批以协议 v15、玩家 schema v6、区块 schema v8 交付 14 种常见块状材料、缺失玩家一次性材料包、世界坐标 terrain UV、玻璃/树叶单 pass alpha cutout 与无窗口材料展示；此后的自然材料与建造批次继续交付沙子/砾石/黏土/雪块自然生成、确定性橡树、材料加工闭环（橡木板配方与三条熔炼映射）、目标方块轮廓与中文名反馈、发光方块固定配方和程序化方块云；M4O 完成全仓职责导向代码组织。

## 基线演进

M5A（m5a-companion-entity-chat）基于 M4Q 的 Mornlea 项目身份和 M4P 固定 Rust 1.97.1 `mornlea_engine` cdylib，交付最多四个可配置、服务端权威且保持 idle 的具名伙伴；伙伴独立于八名玩家容量，使用协议 v16 和独立 `companions.ai` schema v1，active 与 inactive 身体记录合计最多 64 条。客户端通过统一 Avatar/NameTag pass 呈现伙伴，并提供有界 Unicode 聊天输入与 HUD；`@伙伴名 指令` 只在权威 tick 边界确认大小写精确的寻址事实。无窗口视觉场景新增唯一末场景 `ai-companion`，benchmark producer 升到 scenario v16，M2 v15/M5 v14 基线保持不变。

`rust-engine-collision-raycast-ci-stability` 的 rename 阶段把 Rust 动态库从 `mornlea_mesh` 原子改名为 `mornlea_engine`；现有 mesh ABI v1、`mornlea_mesh_section`、layout 与 status `0..9` 保持不变。

`rust-engine-collision-raycast-ci-stability` 的 collision 阶段让 `mornlea_engine` 同时成为 client prediction 与 authoritative simulation 共用的 collision resolver 唯一生产实现；Go 保留 physics state/input/tunable 与 checked snapshot 编码，旧 Go resolver 只作测试 oracle。无图形专服的 Linux amd64 canonical bundle 由 `make build-linux-server` 原生构建，binary 通过 `$ORIGIN` 加载同目录 `libmornlea_engine.so`；二者是不可跨版本混装的 release unit。

`rust-engine-collision-raycast-ci-stability` 的 raycast 阶段让 `mornlea_engine` 同时成为方块射线 DDA 的唯一生产实现；Rust 以 caller-owned 64-record cursor batch 生成候选格，Go `core.RaycastBlocks` 仍保留输入校验、单次归一化、惰性 callback、首个 error identity 和 `RayHit.Point`。旧 Go DDA 只作测试 oracle，生产无 fallback。

`rust-engine-physics-step` 让 `mornlea_engine` 同时成为物理 tick 积分的唯一生产实现；Go `physics.Step` 保留输入校验、tunables 快照、yaw 三角、位移凸包 sweep bounds 与 prism 编码，旧 Go 积分只作测试 oracle，生产无 fallback。

`rust-engine-worldgen` 让 `mornlea_engine` 同时成为世界生成（高度图地形、地表分层、矿石、橡树）的唯一生产实现；engine ABI 升到 v3，新增 `mornlea_worldgen_chunk` 整区块 dense 生成与 `mornlea_worldgen_probe` 64-record 单点查询。Go `worldgen` 保留 seed→perm 表播种（`math/rand` 语义）、材料表编码与 `world.Chunk` 回写，旧 Go 噪声/地形/矿石/橡树实现只作测试 oracle，生产无 fallback；同种子世界与迁移前逐位一致，差分门禁全平台启用。

R1(rust-client-window) 把 darwin 客户端的窗口与事件循环交给新的 Rust `mornlea_client` cdylib(winit,client ABI v1)独占生产;Go `internal/client` 保留 `Window` 领域 API、快照解码与帧内缓存,每帧一次 FFI 输入快照取代逐键轮询,`go-gl/glfw` 依赖已移除。client 库与 engine 库 ABI 互不耦合,Linux 专服不链接 client 库。这是"GPU 与窗口迁 Rust"路线图(R1 窗口→R2 渲染核心→R3 HUD 呈现)的第一步。

R2a(rust-client-render-terrain)在 `mornlea_client` 内交付纯离屏的 Rust wgpu 世界渲染器(terrain/sky/云/GPU culling/HiZ,client ABI v2),WGSL 与 Go 渲染器单源共享;oak grove 双后端对照门禁以零像素差异通过(最大通道差 0)。生产渲染仍由 Go `internal/render` 执行,golden 基线不变;渲染切换与实体/文本迁移属后续 R2b/R2c。

R2b(rust-client-render-entities)把实体与文本 pass(avatar、掉落物、目标方块轮廓、名牌 billboard、伤害红边、HUD 家族、调试面板)补齐到 Rust 离屏渲染器:frame payload v2 以 TLV pass 段单次 FFI 过境全部帧数据,字形图集(R8 增量矩形)与 HUD 图集字节与 Go 同源(client ABI v3)。完整帧双后端对照(地形+实体+名牌+HUD+面板)以零像素差异通过;生产渲染仍由 Go 执行,golden 不变。剩余 R2c:surface 接线、入口切换与删除 Go 渲染栈。

R2c(rust-client-render-cutover)完成生产切换:client ABI 升到 v4(`render_create_windowed` 把 winit 窗口接为 wgpu surface、`render_resize`、acquire 失败返回 SKIPPED 跳帧),`cmd/mornlea` 窗口与离屏帧循环均改为装配 `client.RenderFrame` 单次 FFI;随后删除 `internal/gfx` 全包、`oliverbestmann/webgpu` 依赖与 render/hud 的 GPU 半部,Go 保留布局、编码、字形 worker 与 `SectionScheduler` 上传调度等 CPU 半部,WGSL 移入 `mornlea_client` crate。全部 capture golden 字节不变,archcheck 改为全仓禁止 Go WebGPU 绑定;双后端对照测试随 Go 渲染器退役,golden 成为长期视觉门禁。

M5B（m5b-companion-planning-fifo）交付第一个可玩的 AI-native 闭环：寻址成功的指令进入每伙伴持久 FIFO（16 条、严格按序、满员 `QueueFull` 同步拒绝），OpenAI-compatible Planner 在 worker 上把有界不可变观察快照转换为严格 JSON `go_to` 计划（30 秒超时不重试、64 KiB 上限、M5B 只接受 go_to 步骤），确定性整数 A*（水平 ±16/垂直 ±4 窗口、4096 节点预算、固定邻居序，归属裁决留 Go）与路径点重验驱动 Task Runner 经 `sim.CompanionAction`（玩家命令后按 ID 字节序、统一物理积分、复用 Rust 出口）移动伙伴；任务状态机 Queued→Planning→Validating→Running→各终态的每次迁移广播 ChatEvent，deadline 用持久化 WorldTimeTicks（1..60 分钟、缺省 10）。协议升到 v17（ChatEvent 追加任务生命周期 kind 与 TaskFailReason 16..19、Rejected 追加 QueueFull，wire 形状与长度上限不变）；`companions.ai` 升到 schema v2（active 记录追加当前任务与最多 16 条 FIFO，文件上界 350,208 bytes，v1 只读迁移，Planning/Validating 关服按 Queued 恢复，Running 精确恢复且不盲走）；`ai` 组新增 endpoint/model/apiKeyEnv/taskTimeoutMinutes（config version 保持 1，https 或 loopback http，密钥只从环境变量解析、不入配置日志存档）。benchmark scenario 保持 v16（固定工作负载未变，场景版本独立于协议演进），全部视觉 golden 字节不变。

M5C（m5c-companion-interactions）交付跟随与世界交互：计划步骤扩展为交付全集 `go_to`/`follow`/`mine`/`place`（follow 只能最后一步、目标须为快照在线玩家，mine 目标须窗口内单一掉落非容器，place 方块须注册表内且伙伴持有）；持续跟随以 4 格水平距离为界、deadline 豁免、目标离线以 `TaskFailWorldChanged` 失败，`@名称 停止`（trim 后精确）是唯一绕过 FIFO 的控制指令并产生第五终态 `Stopped` 与 `TaskStopped` 广播；伙伴采掘复用玩家 `miningRule` 计时与 raycast 距离语义（进度经 TickResult 发布），完成 tick 三方原子（方块/耐久/产物直入背包，容量前验失败不结算）；放置由 Runner 先验交互距离后单 tick 原子扣料写入（复用玩家放置校验与容器槽位预留）；`CompanionAction` 扩展为 Move/MineHold/MineRelease/Place 判别载荷，采掘状态上移 `actorState` 共享。协议升到 v18（`TaskStopped`/`NotFollowing`/`TaskFailInventoryFull` 枚举，wire 形状不变）；`companions.ai` 升到 schema v3（步骤按 kind 变长 13/15/17 bytes，文件上界 430,080，v2/v1 只读迁移，follow 零 deadline 双侧校验）；客户端补三个新值的稳定中文事实行。Memory/TCP 多人共同指挥 parity 锁定。benchmark scenario 保持 v16，全部视觉 golden 字节不变。

M5D（m5d-companion-persona-dialogue）交付性格与近期记忆：`ai.companions[].persona` 可选内联人设（≤4,096 bytes）或配置同目录 `personas/<canonical 名称>.txt` 外部文件（内联优先、双源告警、损坏降级空人设不阻启动，`Definition.Persona` 保留磁盘镜像、`ResolvedPersona`（`json:"-"`）承载生效值保证 Save 不吸收外部文件/不清除越界原文）；Dialogue worker 与 Planner 共用 endpoint/model 配置但提示与输入完全隔离（人设 + 最近摘要 + 事实节点 + 极小环境摘要），台词触发节点确定性导出（进入 Running 一次、按计划长度均匀选择 ≤6 个步骤完成节点、终态一次——末个选中步骤的完成折入终止节点，每任务 ≤8 次；follow 仅开始/首次到达跟随距离/终止三节点），与 Planner 共享全服 4 模型槽（Planner tick 边界重试、Dialogue 无槽即跳过不排队）、每伙伴 1 在途、结果携带世代过时即丢、模型失败只跳过台词绝不改任务状态；终态响应捎带 ≤2 KiB 最近对话摘要（不额外调模型、失败保留旧摘要、只喂后续 Dialogue 绝不进 Planner）。协议升到 v19（`CompanionSpeech` kind 携带伙伴身份与 ≤256 bytes 台词、广播全部在线玩家，任务事件仍不携带模型自由文本，wire 形状不变）；`companions.ai` 升到 schema v4（每记录可选摘要区 2-byte 前缀 + ≤2,048 bytes，文件上界 438,280，v3/v2/v1 只读迁移，inactive 不存摘要，解码新增「payload 读空」门禁封死变长区错位残留）；客户端以 `伙伴名：台词原文` 一行呈现（唯一显示模型文本的位置，32 rune 同源截断）。M5A–M5D 阶段总验收：persona 文件→config.Load→请求体全链、混合计划台词序列、schema v4 原始字节、跨重启摘要复用与双传输 parity 均有集成锁定，全程无前台窗口。benchmark scenario 保持 v16，全部视觉 golden 字节不变。

历史[设计](../superpowers/specs/2026-08-13-ai-native-companions-design.md)与[实施计划](../superpowers/plans/2026-08-13-m5a-companion-entity-chat.md)仅作决策背景。

M5E（m5e-backlog-hardening）清偿 M5B/M5C/M5D 终审遗留欠账（39 项，无协议/存档/wire/golden 变化）：考古重建 M5B/M5C 丢失的 29 条 minor 全部执行（过期注释、魔法数出处、死防御删除、跨测试 helper 去重、常量同源化——聊天/指令/台词上限统一引用 `companion.MaxPlanCommandBytes`/`MaxDialogueLineBytes`、wire 上限常量按字节构成推导注释化、schema 白名单前瞻口径显式 V4 字面、`[32]`/穷举界等字面重复收敛）；唯一行为变化是 `planWireStep` 契约收紧——显式 JSON null 视为字段出现，专属外字段携带 null 与非法值同以非法计划拒绝（delta spec 落 companion-planner）；新增双实现交叉锁 `buildPlanPlaceBlocks`↔`BlockDrop` 全集往返测试与 `core.ItemIDMax` 哨兵穷举守护（含 atlas 穷举界统一）；M5D 延期五项落地（ResolvedPersona tag 反射锁、Dialogue 错误哨兵按请求/响应拆分、ChatEvent 无效 UTF-8 wire 突变矩阵、chat 呈现 29/32 rune 精确断言、阶段验收 parity 投影复用）；负载 flake 治理：TestScenarioV7 在 race 构建下 skip（50ms 实时门禁在 race 开销下测机器负载，非 race 原样）、47 处共享等待 helper 的 Gosched 热轮询改 sleep 退避（16 文件同型治理）、TimedOut 子测试改为确定性等待伙伴激活（消除终态派发被出生扫描守卫永久跳过的 60s 假超时）；编排防御加固：dispatchPlanning 的 issuers 失配检查前移 BeginHead 之前并以「pending 非空而 issuers 空」为准确条件（空闲态零误报日志）；流程修复：backlog 沉淀纪律入 proposal「延期与放弃」节，ledger 不再做唯一载体。协议保持 v19、schema 保持 v4、benchmark scenario 保持 v16，全部视觉 golden 字节不变。

`authoritative-fluid`（M4 系列基础玩法补齐第一批）为世界引入服务端权威的水。此前 `worldgen` 的 `SEA_LEVEL` 只是噪声偏移、海平面以下全留空气，所有低洼地形都是干盆地。本批只交付**权威侧**：`internal/core` 追加 8 个稳定流体编号（`WaterSourceID` + `WaterLevel1..7ID`，1 最强），流体不进物品表、不可放置、不提供碰撞体、非不透明；新包 `internal/fluid` 以显式待更新队列驱动流动（单区块 98,304 格，全扫描不可行），处理前按 `(dueTick, ChunkKey, y, z, x)` **全序排序**（绝不遍历 map），受 `FluidUpdatesPerTick`（默认 512）硬约束、超额顺延不丢弃，同 tick 冲突写入取流体等级最小者且与处理次序无关；队列**不持久化**，重启靠边界重扫恢复——该决策的依据「平衡态是重扫的不动点」被提升为规格场景与门禁关口，并在评审中证明：支撑关系良基故无自持环，`level(c) = 1 + min(水平邻居 level)` 归纳即得**不动点唯一**。`sim.Engine.Step` 新增 `advanceFluids` 阶段，只推进活动兴趣范围，变更经既有 `recordChange` 汇入同一批 `pendingChunkChanges`，**不新增任何协议消息类型**；重扫入队另有 `FluidRescanCellsPerTick`（默认 65536）预算并跨 tick 分摊，内部水源可证为不动点故海洋重扫从 O(体积) 降到 O(表面)（最坏单 tick 244 ms → 2.19 ms）。worldgen 注水在 Rust 内与高度图、分层、矿石、树木同批完成，`Materials` 13→14，门控放 **Go 侧**（关闭时 `water` 字段传 air 编号、Rust 侧零分支），故关闭态生成路径与本批之前**逐位一致**；材料表互异性校验为 `(air, water)` 开且仅开这一对豁免。协议升到 v20（唯一变化是方块 ID 集合扩展，wire 形状与长度上限不变）、区块存档升到 schema v9（v8 **恒等迁移**——旧区块里的空腔可能是玩家挖的地下室，盲目注水会淹掉建筑）、engine ABI 升到 v4（worldgen header layout 1→2 作带内第二道混装防线）。新增配置项 `fluidEnabled`（**默认关闭**，benchmark 与 capture 路径显式解耦不随配置漂移）。benchmark scenario 保持 v16，全部视觉 capture golden **逐字节不变**（11 个场景，最大通道差 0）。流体的呈现与生存（斜水面几何、半透明 water pass、水下光衰减、浸没物理、溺水与氧气）属后续变更 `fluid-presentation-survival`；该变更必须承接的五项待办（含 Rust `MAX_REGISTRY_ENTRIES` 扩容、列顶与天空光、raycast 目标判定、出生点选取、伙伴寻路对流体的豁免）记录在归档的 `design.md` 专节里。

`fluid-presentation-survival`（M4 系列基础玩法补齐第二批）让水**可以被打开**，并把 `fluidEnabled` 默认值从 `false` 翻为 `true`。呈现侧：Rust `mornlea_engine` 的 registry 上限 27 → 35、条目 16 → 18 bytes（新增每方块 `fluid_height` 与 `light_attenuation`，等级→高度的映射留在 Go 一处），mesher 用**全整数**角高度（`h_raw = 14 - level`，上方为流体取 15，角高度是相邻四格流体 `h_raw` 的向下取整平均）把水面插值成连续斜面，水面按 1×1 出面不贪心合并、角高度打进 `w`/`h` 释放的 8 bit，**quad 实例仍是 8 字节**；`mornlea_client` 新增**唯一一个**额外半透明 water pass 与 `water.wgsl`（alpha blend、深度测试开、深度写关、排在 terrain 之后 HiZ build 之前、按区段距离由远及近、不接 GPU culling、一次性固定容量实例池，预热后零每帧动态资源），流体获得独立水材质层与程序化纹理；天空光 BFS 每步扣减改为按方块查 `light_attenuation` 表，水透光但每格额外衰减 1、竖直向下不再无损，方块光模型不动（水与玻璃一样阻断）。生存侧：新增权威浸没标志 `BodyInFluid`（AABB 与任意流体格相交）与 `EyeInFluid`（眼睛所在格是流体），服务端与客户端预测共用同一纯函数；`physics.Input` 扩两个 bool、`StepInput` 头版本 v1 → v2，Rust `step.rs` 在身体浸没时切换重力/垂直终端速度/水平阻力，`Jump` 改为持续上浮，摔落峰值重置留在 Go 侧 `internal/sim`；新增权威氧气（`core.MaxOxygenTicks = 300`，眼睛浸没每 tick −1，归零后按 `DrownDamageIntervalTicks`（默认 20）走既有伤害入口扣血并重置回血计时，出水立即回满，**不入存档**），HUD 在既有 hotbar 布局与 pass 内加氧气条（仅未满时出现、零新 pipeline），相机在流体格时叠水色 tint 并压低远处可见度（判定复用同一个 `EyeInFluid`，没有第二套）。同时清偿 `authoritative-fluid` 遗留的五项待办：交互射线的 solid 谓词收敛为唯一的 `core.InteractionTarget`（空气与流体都不是目标），玩家采掘/放置/开容器、伙伴采掘的视线遮挡与客户端瞄准高亮共用它，水下可正常瞄准、采掘、开箱；放置把流体格视作可覆盖空位、落点是水下那格并让相邻流体重新入队；出生点按干燥程度三档阶梯选取（身体不浸没 > 身体浸没但眼睛不浸没 > 完全浸没），只要存在可站立落脚点就绝不停在待出生状态；伙伴寻路对流体的豁免**保留**，退出条件改写为指名的两条。性能：`internal/fluid` 的 `Queue.Advance` 从「每 tick 全量遍历并排序 `pending`」改为索引最小堆取批，单 tick 成本与队列规模解耦——修复前瀑布场景（单玩家、25 区块、普通玩法）最坏单 tick 实测 107.38 ms，是 50 ms 预算的 2.15 倍；修复后队列峰值那一 tick（501,587 项）从 57.81 ms 降到 661.8 µs，整 tick 最慢从 123.81 ms 降到 9.00 ms（其中流体段 4.43 ms），F1 的四条不变量（确定性全序、预算不改变平衡态、同 tick 冲突取最小等级、入队顺序无关）逐条未改且仍绿。协议升到 v21（`PlayerState` 追加 `Oxygen`，wire 形状与长度上限不变）、engine ABI 与 client ABI 同升到 v5、benchmark scenario 升到 v17（唯一显式迁移 `16:17`，历史 `15:16` 退为归档证据；benchmark 的注水门控仍被钉死为关闭，被测世界与 v16 逐格一致，v17 只反映被测进程自身的变化）。玩家 schema v6、区块 schema v9、世界 metadata v2、`companions.ai` schema v4 **均不变**。视觉门禁先在 `fluidEnabled=false` 下验证 11 个 capture golden 逐字节不变，再翻开开关重新生成，并新增 `water-surface-slope` 与 `water-underwater` 两个水景场景（共 13 个）。

`authoritative-farming`（M4 系列基础玩法补齐第三批）交付生存循环的**产出端**：可耕作的土地与一种作物。生存侧此前只有摔落、溺水、伤害与死亡结算，没有任何可消耗与恢复的资源；先做农业而不是先做饥饿，是因为顺序反过来会造出一段「饿了没东西吃、只能等死」的不可玩窗口。`internal/core` 追加 **10 个稳定方块编号**（`FarmlandDryID` / `FarmlandWetID` 与 `WheatStage0..7ID`）与 **4 个物品编号**（`ItemStoneHoe` / `ItemIronHoe` 及其损坏形态、`ItemWheatSeeds`、`ItemWheat`）；沿用流体「每阶段一个编号」的模式，因此**区块 schema 不变**——阶段本就存在方块数组里，采掘表、碰撞表、mesh registry 快照与方块变更广播天然覆盖。mesh registry 条目上限 **35 → 48**（Rust `MAX_REGISTRY_ENTRIES`、Go `nativeMaxRegistryEntries` 与 `maxNativeInputBytes` 三处同批；`input.rs` 的 `BLOCKS_BYTES` 里那个 27 是 3×3×3 邻域区段数，与条目上限只是数字撞了，未动），并逐处修正全仓以 `WaterLevel7ID` 为界的哨兵与循环上界。三个农业动作里只有**翻地**需要新命令：种植复用放置、收获复用采掘，协议因此只升到 **v22**（新增 `TillSoil` 命令 kind 与三条只追加不重排的 `RejectReason`）。翻地要求目标是泥土或草、正上方为空气、手持锄头且在触及距离内，成功后目标格变耕地并扣**恰好一点**锄头耐久——这是全仓第一个「不移除方块却扣耐久」的动作，`tool-durability` 的判据因此从「方块是否被移除」改写为「工具是否完成了一次有效作用」，四条拒绝路径一点耐久都不掉。生长走**随机 tick** 而非显式待更新队列（流体向平衡态收敛、漏一次更新就永远错，作物按时间推进、漏抽一次只是长得慢）：每权威 tick 按 `(ChunkKey, sectionY)` 全序枚举活动兴趣范围内的已加载 section，每 section 用 `splitmix64(worldSeed, tick, 维度, 区块坐标, 区段索引, i) mod 4096` 抽 `RandomTicksPerSection`（默认 3）格，纯整数、无全局 RNG、无浮点、绝不遍历 map；抽中的作物在**露天**（读既有列顶高度图，非空气即遮挡）且**下方是湿耕地**时按 `CropGrowthChancePercent`（默认 50）推进一阶段，默认配置下七次推进期望约 16 分钟成熟。耕地干湿在被抽中时重判（水平切比雪夫距离 ≤ 4、同层或上一层存在流体即湿），双向转换、干耕地不退回泥土；挂随机 tick 而不挂 `recordChange` 是为了有界——后者在水源被移除时的扇出没有上界。掉落固定不随机：成熟作物掉 1 小麦 + 2 种子、未成熟掉 1 种子、耕地掉 1 泥土，「误挖不亏种子」使循环不会死；作物采掘时长是 1 tick（0 是既有「不可采掘」哨兵），耕地 5 tick。**伙伴被三处防御清单显式拒绝**种地与收获——原以为「成熟小麦是多掉落 ⇒ 伙伴天然被拒」，但 `core.BlockDrop` 只支持单一产物、多掉落在编号层面表达不出，巧合性安全不成立。呈现侧新增通用能力 `plant-visual-presentation`：植物出 4 片交叉斜面 quad（两条对角线 × 正反两面），`face` 字段用 6/7 表对角线、借植物 quad 恒为 1 的 `w`/`h` 位表正背，`face ∈ {6,7} ⟺ material ∈ 植物区间` 在 Go 解包与 Rust 发射端**双向**强制，**quad 位布局零变更（bit 63 仍空闲）、零新绘制阶段**，走既有 terrain pass 的 alpha cutout；出正背两片而不是两片单面，是因为 `cull.wgsl` 在 compute 阶段按 quad 法线判可见，单面会在某些视角被整片剔除。植物的光照取正上方相邻格（交叉面在方块内部，没有相邻空气面可采），且与玻璃/树叶同类**不额外衰减**天空光。碰撞侧作物零碰撞体、耕地高 15/16——耕地是全仓第一个非满立方体碰撞体，`BlockCollisionBoxes` 的六个 Go 消费者里有五个按 `boxes.Count` 逐盒读盒顶、只有 `sim/spawn.go` 的 `findSpawnInColumn` 仍假定「有碰撞体即 `y+1`」（后果是出生在耕地上时留 1/16 空隙，已接受）。缺失玩家一次性材料包在 14 格材料**紧随其后**补一格 64 颗种子，使玩家在草丛存在之前也能起步；HUD 合成面板补石锄与铁锄两行（`hud.inventoryRecipeIDs` 此前写死 8 行，锄头在 UI 里不可达，整条闭环在客户端不可达）。benchmark scenario 升到 **v18**（唯一显式迁移 `17:18`，历史 `16:17` 退为归档证据）：benchmark 的固定输入与被测世界一格未动（仍七名远端玩家、零伙伴、不注水、不含农业方块），升版记录的是**被测进程本身**的三处变化——mesh registry 条目上限 35 → 48 且实际烘焙 35 → 45，使每次 mesh 调用的 FFI 输入从 910 涨到 1170 bytes；合成面板 8 → 10 行使 Hotbar HUD 固定上传布局移动（quad 容量 238 → 247、glyph offset 11776 → 12288、总容量 45376 → 45888 bytes、空聊天帧每帧实际写入 11776 → 12288 bytes），这正是主规格判定 v15 → v16 时用的同一条条文；权威 tick 新增 `advanceCrops` 阶段，满编 200 区块实测约 114 µs/tick（20 TPS 预算的 0.23%），单 tick 触及格数恒等于「区块数 × 24 区段 × 抽样数」、与世界里有多少株作物无关（0 株、256 株、51200 株三档实测 cells/op 完全相等）。区块 schema v9、玩家 schema v6、世界 metadata v2、`companions.ai` schema v4、engine/client ABI v5 **均不变**。视觉门禁 13 个 capture golden 里只有 `hud-hotbar-health` 重生成（6 个新物品编号（含两个损坏形态）使 HUD 图集列数 32 → 38、宽度 512 → 608，`column×16/width` 的 float32 结果随分母改变，石头与石砖缩略图采样点亚像素漂移 265 px / 0.115%）；重生成前先做过像素级确认——实拍缩略图与 `registry.LayerRGBA` 逐层比对后最接近的仍是石头层与石砖层，纹素内部像素逐字节相等，列 UV 的 texel 边界与整数列边界相差约 4e-6 纹素，不是邻层。

`fix-hoe-harvest-durability` 补齐玩家 `advanceMining` 的完成分叉：复用 `core.IsCrop` × `core.TillingTool`，作物 × 完好锄头是唯一不耗耐久的采掘组合；其他工具收获作物、锄头破坏非作物仍各耗恰好一点耐久。掉落规则、翻地、疲劳、拒绝路径与伙伴采掘均不变；协议、存档 schema、ABI、benchmark scenario、配置与视觉 golden 均不变。

`rust-engine-lod-shell` 交付「种子即真相」的远环 LOD：近环（权威同步 + 精确 section mesh）完全不动，远环由客户端本地确定性生成纯地表壳 mesh，以距离雾掩盖精度接缝，把可视距离从 `viewDistance`（默认 32 chunk）扩展到 `lodFarMultiplier` 倍（默认 3），网络、存档与服务端模拟零改动（同步半径外的人工修改不反映到远环，属接受的正确性边界）。协议升到 v23——`LoginSuccess` 追加 `WorldSeed uint64`（wire 上紧跟 PlayerID，种子即真相的前提是种子到达客户端；v22 为 `authoritative-farming` 交付的翻地命令段，本变更经变基重编排在其后），单机与 TCP 远程共用同一条登录链路取得种子，Linux 专服不下发种子、零影响。engine ABI 升到 v6——新增 `mornlea_lod_shell` 无状态纯函数批量导出（变基重编：该段在旧基线原编号 v4，main 的 fluid 系列占用 v4/v5 后顺延），输入与 `mornlea_worldgen_chunk` 同格式的 perm 播种 header、tile 原点与列合并步长（`lodStep` 合法集 {2,4,8}，默认 4），内部复用 worldgen 高度与表层材质选择，步长窗内列高取 max（保守遮挡）、同材质等高顶面贪心合并、高度断差生成侧裙 quad（接缝在构造上不可能裂开），输出带世界坐标 terrain UV 与斜坡着色权重的壳 quad 流；海平面壳语义（Ruling 22）：海平面以下的窗口顶面钳到海平面并取水材质、水下窗不发裙边、陆海交界按钳制后高度由陆侧发裙，`fluidEnabled=false` 时逐位回到干盆地呈现。client ABI 升到 v7——v6 新增 `render_upload_lod_tile`/`render_drop_lod_tile` 与远环独立 pipeline（大尺寸世界坐标 quad，帧序为天空 → 远环 → 近环 terrain → 实体/HUD，远环写深度，tile 表容量 MAX_LOD_TILES=131072 对最大合法配置 64×8 的全方环 66049 留约 2 倍余量），v7 新增雾参数化 setter（雾色与昼夜 tint 同源，锚点为 0.5×far 起雾、0.75×far 全雾，默认几何 32×3 精确落在 768/1152 block）。Go 侧新建 `internal/lod`（Scheduler 语义镜像 SectionScheduler：pending 覆盖、由近到远冲刷、独立 4 MiB 帧预算且生成与上传共用、壳生成在独立 worker goroutine），带状入队（Ruling 19）：内半径 = floor(viewDistance/4)+1 使壳的最小覆盖块严格在近 mesh 覆盖半径之外、与近 mesh 零重叠（壳步长窗高系统性高于精细地表，入近环会 poke 出地表），`DropOutside` 内外双侧释放保证移动下零重叠仍成立。capture 视觉门禁既有 golden 因远景入画与注水地形重新生成（变化仅限远景带与水景，近处像素不变由双侧保护壳上下界的近带断言锁定），新增 `far-horizon` 场景排在 `water-underwater` 之前（共 14 个），抓帧收敛判据并入远环 LOD Busy。配置项 `lodEnabled` 默认开启；benchmark 观察者路径显式关闭远环（不建 Scheduler、不消费种子），scenario 保持 v18 不迁移（v18 为 `authoritative-farming` 的迁移段，本变更不触碰固定工作负载），LOD 专项数值只记录于 `docs/notes/lod-perf-notes.md` 不作门禁。

`mornlea-texture-pack` 把图形客户端的产品默认材质改为程序化注册表叠加内嵌 [Pixel Perfection 子集](https://github.com/minetest-texture-packs/Pixel-Perfection)，未映射 layer 仍以程序化像素作最终回退；可选配置 `texturePackPath` 只在启动时从本地目录按固定 39 名读取 16×16 PNG，经有界验证后全成全败地覆盖已知 layer。benchmark 与 capture 固定忽略本地覆盖，无图形专用服务端不加载客户端资产；macOS 客户端发布物字节复制许可证、署名和逐文件 provenance。内嵌子集固定于 upstream commit `7935d064fc6f993d1b5038ed5ec17a615600cf0a`，依 [CC BY-SA 4.0](https://creativecommons.org/licenses/by-sa/4.0/) 分发并保留作者与贡献者署名。该材质里程碑未推进远环 LOD 合入后的协议 v23、engine ABI v6、client ABI v7 与 benchmark scenario v18。

m5e-deferred-clearing 清偿 M5E 归档「延期与放弃」8 项递延中的 6 项冷区项（递延 4/5 分别归属 capture 与 network 的活跃流领地、显式再递延）：planner 必填字段（follow 的 player_id、place 的 block）显式 null 的拒绝分支（兼 nil 解引用防 panic 屏障）与 `formatChatEvent` 未知 kind 的「未知事件」兜底分别获得直接锁用例并附变异验证；系统提示头段「水平/垂直格数」改为引用快照环境摘要窗口常量（提示词字节逐位不变，由字节锁测试固化），客户端逐帧文本输入缓冲与 `companion.MaxPlanCommandBytes` 同源；阶段验收夹具的忽略返回值改为携 `*testing.T` 的显式断言；`plannerWorker` 与同构 `dialogueWorker` 的共享模型槽令牌释放前移到模型调用返回之后、结果发送之前——令牌语义（在途模型调用许可，cap=4）不变，消除「结果已入队而名额未还」的 ns 级调度窗口，令 M5E CI 修复的两段式等待成为严格 happens-before 确定。零 wire 可观察行为变化，协议 v23、玩家/区块/世界/伙伴存档 schema、engine/client ABI、benchmark scenario 与全部视觉 golden 不动。

`authoritative-hunger`(M4 系列基础玩法补齐第四批)交付生存循环的**消耗端**:农业给了产出,但在此之前吃下去的东西没有任何去处,生命值只会因摔落、溺水与伤害下降,回血也没有代价。本批为每个玩家引入三层权威状态——饥饿值(`0..20`)、饱和度(千分位、不超过饥饿值×1000)与疲劳值(千分位),全部定点整数,推进不含浮点:唯一的浮点入口是 `swimExhaustionMilli` 把物理体的水平位移换算成千分位疲劳,**向下取整、绝不四舍五入**(物理步每 tick 都有极小抖动,四舍五入会让原地泡在水里的玩家凭空积累疲劳),取整只做这一次、结果立刻回到整数域。疲劳来源是**五项固定表**且刻意不做 tunable(它们之间的比例关系就是玩法本身,逐个可调只会造出互相矛盾且没有验收标准的组合):起跳 50、身体浸没时每水平移动一格 10、采掘完成 5、翻地完成 5、自然回血每点生命 6000;五个判定点全部只挂在**玩家的成功路径**上,被拒绝或中断的采掘与翻地一点疲劳都不涨。`applyExhaustion` 每累积满 `ExhaustionThresholdMilli`(默认 4000)就减去一个阈值并扣一点饱和度,饱和度为零改扣一点饥饿值、饥饿值为零不再扣;回血一次就加 6000 > 阈值,故它必须是**循环**而不是单次判断(只减一次阈值会让回血的代价凭空少掉三分之一),且一次跨阈值最多消耗**一种**资源——残余不足一点的饱和度被清零后本次结算即止,让「饱和度耗尽」成为可观察的分界点而不是瞬间穿过的中间态。自然回血由 `RegenHungerThreshold`(默认 18)门控,饥饿值低于它时既有回血计时照推但不产生回复(不冻结计时,与参考实现同);饥饿值归零后每 `StarvationDamageIntervalTicks`(默认 80,即 4 秒)经**既有 `applyDamage` 入口**扣 1 血——走同一入口的可观察后果是回血计时被清零——并在生命值到 1 时停止且冻结计时(照推的话一吃饱就会立刻结算一次积压伤害),饥饿**不致死**(困难难度饿死记遗留)。进食是与采掘同形的持续输入状态机:`PlayerInput.Eating` 为真、选中格在食物表内且饥饿未满时逐 tick 推进,开始时记录 `(slot, item)`(否则第 31 tick 切格就会「吃 A 扣 B」),`EatingTicks`(默认 32)到时在单一 tick 内原子结算(扣 1 件、饥饿加固定值钳到 20、饱和加固定值钳到饥饿值×1000);松手、切换栏位、同格换物、受伤与死亡都清零进度且**不扣料**,不新增任何 `RejectReason`(持续输入没有命令级拒绝,与采掘一致)。食物表只有面包(饥饿 5、饱和 6000),由固定配方 11(3 小麦 → 1 面包)合成,`internal/core` 追加 `ItemBread`(堆叠 64、不可放置),既有配方 1..10 不位移。三层状态随**玩家 schema v6 → v7** 全部持久化(不持久化疲劳会让「重登即清疲劳」成为零成本操作),v6 存档按 `Hunger=20 / Saturation=5000 / Exhaustion=0` 迁移、v5→v6→v7 链仍可读、v8+ 按 `ErrFutureVersion` 拒;死亡重生回同一初值,而掉出世界/卡住的 `beginReset` **不**回满(否则即成免费进食途径)。**协议 v23 → v24** 只上线两个字段(`PlayerInput.Eating` 与 `PlayerState.Hunger`,后者 `Validate` 拒 >20),饱和度与疲劳值不上线——HUD 只需要饥饿值,少一个 wire 字段就少一份 golden/fuzz/对照。呈现侧在既有 hotbar pass 与 HUD 图集内新增右下镜像生命条的 10 格鸡腿饥饿条(鸡腿两列与爱心同处同法程序化绘制、半格粒度、满时仍显示——它是常态资源,氧气条「未满才出现」是因为它是异常态、只画权威确认值、零新绘制管线);这 20 个 quad 使 Hotbar HUD 固定上传布局移动(quad 容量 247 → 267、glyph offset 12288 → 13312、总容量 45888 → 46912 bytes、空聊天帧每帧实际写入 12288 → 13312 bytes),**benchmark scenario 因此升到 v19**(唯一显式迁移 `18:19`,历史 `17:18` 退为归档证据;固定工作负载仍是七名远端玩家、零伙伴、不注水,v19 只反映被测进程自身的变化)。伙伴不接饥饿:`companionState` 没有三层状态、疲劳判定点不在伙伴路径、也不进食,由 AST 名字守卫、运行时三层状态守卫与进食守卫三条测试成对锁定(名字守卫看不见「伙伴间接改到某个 `playerState`」,运行时守卫看不见还没有夹具驱动到的路径,两条不得只留一条)。区块 schema v9、世界 metadata v2、`companions.ai` schema v4、engine ABI v6、client ABI v7 **均不变**。视觉门禁 14 个 capture golden 全部重生成:共有差异是饥饿条矩形 x∈[464,630] y∈[337,350] 各 1120 px,`hud-hotbar-health` 另有 167 px 落在快捷栏物品图标条带(`ItemBread` 使 `ItemIDMax` +1、HUD 物品图集变宽,归一化 UV 亚像素移动),地形/天空/水/远环/实体/文本零差异。执行期的遗留与简化清单(含「打开容器界面时仍可继续进食」与「手持小麦仍发 `PlaceBlock`」)记录在归档的 `design.md` 里。

`bedrock-survival-hud` 在 authoritative-hunger 的协议 v24、玩家 schema v7 与 benchmark scenario v19 基线上交付最终原创程序化生存 HUD：关闭容器时是居中九格快捷栏、外扩高对比外框加强调色内框的双层选中轮廓、权威数量与工具耐久，采掘轨道用移动末端标记与固定警示缺口在去色后仍能区分可采/不可采。HUD 图集在物品列前固定为空/半/满心、空/满气泡、空/满鸡腿七个程序化 cell；已确认生命与耗损氧气分别解析为十个无背景的单实例心形槽与气泡槽，已确认饥饿解析为十个常驻空槽加最多十个填充、奇数末格只显示右半，未确认值完全隐藏。health 与 hunger 各以 169px 设计宽度锚定快捷栏左右边缘，depleted oxygen 沿 hunger 右边缘向外堆叠；满氧隐藏只省实例，完整两行几何永久保留；关闭态氧气在饥饿上方，打开态在其下方；两行状态栈避让既有 36 个交互格与十行配方，聊天面板与字形共用统一有界缩放，在极窄正尺寸 framebuffer 中也保持界内且不与状态栈相交。固定上传契约为 267 quad、700 glyph、13312-byte glyph offset、46912-byte 总容量和 48-byte instance，关闭/打开合法最坏组合分别为 96/265，仍复用同一个 HUD pass 且预热后零每帧动态资源。本批交付时无窗口 capture 清单固定 15 个场景，尾序为 `water-surface-slope`、倒数第二 `far-horizon`、唯一末场景 `water-underwater`；随后 `container-ui-visual-alignment` 扩展为 17 个正式场景。全部 golden 在 Pixel Perfection + hunger 合并二进制上重生成并逐图验收。协议 v24、玩家 schema v7、benchmark scenario v19、engine ABI v6、client ABI v7、区块 schema v9、世界 metadata v2 与 `companions.ai` schema v4 为最终版本身份。

`container-ui-visual-alignment` 统一三种既有容器打开态的原创程序化像素界面：背包/合成、熔炉与箱子共用固定 atlas 的凹槽、标题和来源轮廓，每个 overlay 只新增一个零 glyph 标题 quad，panel 只向上扩 20px header，原有栏位、hit-test、十条固定配方和两次点击整堆权威移动语义逐项不变。熔炉复用原有四个进度 bar quad：火焰填充按确认 burn progress 自下向上、箭头填充按确认 smelt progress 自左向右同步裁剪实例尺寸与 UV。打开态最大从 265 到 266 quad，仍在固定 267 quad、700 glyph、13312-byte glyph offset、46912-byte 总容量、48-byte instance 与 256-byte 对齐的 HUD pass 内，未引入动态 GPU 资源。无窗口 capture 在 `inventory-crafting` 后依次加入 `chest-container` 与 `furnace-container`，正式场景为 17 张，尾序仍为 `water-surface-slope`、`far-horizon`、`water-underwater`；全部 golden 重生成并逐图验收。协议 v24、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` schema v4、benchmark scenario v19、engine ABI v6、client ABI v7 和配置格式均保持不变。

`stabilize-companion-dialogue-outcome-test` 修复三项伙伴台词 outcome 测试把异步 HTTP worker 当作固定 tick 边界的偶发问题：测试改为等待 outcome 已入既有 `dialogueResults`，再推进恰好一个 tick，不增加 sleep、重试或产品并发语义。`ci-retry-isolation` 将 macOS 原生构建、质量门禁、三片 race 与 integration 分离；下游严格校验同 SHA native artifact，最终 `test` 对失败、取消和 skipped fail-closed，并支持 GitHub failed-only rerun。两个远端偶发根因分别是跨接收者 transcript 的无语义采集交错，以及 complete tick 前 progress dialogue outcome 尚未就绪；前者只按 `(EventID, recipient)` 规范化跨流比较，后者等待 outcome-ready 边界，均未改变产品行为。PR #65、#67、#68 分别以 merge `4437670cc3110d33351dd1eccdbcadeb96606555`、`c1f205f1f7d7a8cd4aa71c5bf501aa5910cd07d5`、`296efae6f6ff6ca79288fa6bbf99ac7395398a64` 合入；PR #68 最终 run `32639020586` 的 8 项均成功。

`authoritative-player-melee` 把既有 `Mining` 输入的 primary action 扩展为服务端权威玩家近战，协议升为 v25：同维 active 玩家按 3 格射线最近命中，等距按 `SessionID`，固体遮挡、流体穿透；每次命中经既有伤害入口结算 2 点伤害，目标有 10 tick 冷却。同 tick 意图先冻结再结算，因此对打双方都能提交意图；命中者仅在该 tick 抑制采掘，下一 tick 恢复原路径，Memory/TCP 共用该裁决。PR #66 以 merge `ee3a8eba7257a0b507dae7a622de80397a815784` 合入，最终 run `32640869549` 的 8 项均成功，整分支 SPEC/QUALITY 终审通过。

`darwin-local-audio-feedback` 在不增加第三方依赖或二进制音频资产的前提下，用预分配 `AudioQueue` 与无声降级提供 `audioVolume` 控制的本地确认音效；采掘、放置、进食和受伤只在确认边界触发。为使放置 cue 有绝对确认事实，协议升为 v26 并新增仅回发发起会话的 `PlaceBlockSucceeded(sequence)`；客户端只消费递增序号，重复、旧序号、拒绝、无关状态与其他会话均静音。capture、benchmark 与专服不请求设备。PR #69 以 merge `f039377ab2e6f8248d971610eb265242f1a226dc` 合入，最终 run `32641082086` 的 8 项均成功，整分支 SPEC/QUALITY/远端终审通过；真机扬声器四 cue 与 `audioVolume=0.25/0` 试听仍保留为人工验收。

`fix-grass-block-texture` 修复草方块侧面显示异常：内嵌默认材质 `grass_side.png` 直接复制的 Minetest overlay（只有顶部约 5 行草缘完全不透明，row5 起 alpha 递减、row8 后全为 0），而 terrain 着色器对 alpha<0.5 的片段判 discard 且 `LayerGrassSide` 是不透明层（不在 `isCutoutLayer` 集合），于是草方块侧面下半部被整段丢弃、出现看穿/破洞，mip 降采样后远处整块侧面消失。修复以直通 alpha source-over 把草缘合成到 `dirt.png` 之上生成完全不透明的 16×16 合成图（`scripts/composite_grass_side.go` 可复现、运行两次逐字节一致），`PROVENANCE.json` 的 grass_side 条目新增 `derived` 字段记录派生来源，`ATTRIBUTION.md` 注明派生例外；新增 `TestOpaqueLayersAreFullyOpaque` 守卫锁定非 cutout 层必须全图不透明、cutout 层 alpha 必须为 0 或 255。渲染管线、layer 分类、`applyPack` 替换语义、协议/存档/engine ABI/client ABI/benchmark scenario 均保持不变，部分可看到草方块侧面的视觉 golden 重生成。

`fix-grass-block-texture` 第二轮修复侧脸纹理朝向 bug：上一轮把 `grass_side.png` 合成为不透明图后，草方块侧面仍有朝向错误，控制会话复核根因是 `terrain.wgsl` 的 `face_uv` 对侧面采样的轴约定错误——wgpu 纹理坐标 v=0 是图像顶行，而 `face_uv` 用 `v = world.y` 让图像顶行（草缘）落在方块底部（上下颠倒），±X 面更把 `u = world.y` 当纵向、整张纹理旋转 90°；同一 bug 影响所有带方向性的侧面材质（草侧、熔炉正面、箱子锁扣、原木 ±X 树皮、雪侧）与小麦等植物交叉斜面（植物分支 `uv = (world.x, world.y)` 同样上下颠倒）。修复在 shader 侧统一把纵向分量取 `-world.y`（`terrain.wgsl` 的 `face_uv` 三轴、植物分支，以及 `lod.wgsl` 与 `water.wgsl` 的 `face_uv` 同源同步），采样器地址模式为 Repeat、负 v 正确环绕，世界坐标 UV 相位仍由世界坐标决定、连续性与规格不变；纹理数据按「图像顶行=方块顶部」创作、不动。新增离屏语义守护测试 `engine/crates/mornlea_client/src/render/side_tests.rs`（按 `plant_tests.rs` 范式，单层「上半红/下半绿」atlas，用例覆盖 +Z、+X、植物交叉斜面与 −Z），以「上半红主导 / 下半绿主导」判据直接读出「纹理顶行在方块顶部」，并以空场景守卫计数口径；对旧公式做变异检查确认 ±Z/植物会得到 `g_up=128`、+X 得到 `r_up=64,g_up=64` 的红绿相等分界，即测试确有牙齿。渲染管线、layer 分类、mesh 编码、协议/存档/engine ABI/client ABI/benchmark scenario 均不变，全部 17 张视觉 golden 因侧面方向修正而重生成。

`window-title-and-sizing` 优化图形客户端窗口标题与初始行为：窗口标题从 `Mornlea — M3C multiplayer world` 改为纯产品名 `Mornlea`（`M3C` 是内部里程碑代号，泄露到用户可见标题），集中为 `applicationWindowTitle` 常量并同步测试断言；初始逻辑尺寸从 2560×1440 改为 1280×720（16:9）——Retina 2x 下物理分辨率恰好仍为 2560×1440（既有的交互渲染上限不变），1x 屏/小屏则不再创建超出显示器的巨窗；winit 窗口属性新增最小尺寸 640×360（与无窗口 capture 一致，防止 HUD 与交互几何被拖出界），并在创建后做超屏钳制：物理尺寸超过显示器尺寸 90%（winit 0.30 `MonitorHandle` 不暴露工作区，用全尺寸乘 `DISPLAY_CLAMP_RATIO=0.9` 近似）时按最小缩放比、保持宽高比缩小（`clamp_to_work_area` 纯函数，经 `request_inner_size` 生效）；`fitFramebuffer` 语义从「无条件对齐物理 2560×1440」改为**只缩不放大**——帧缓冲超过目标才收缩、屏幕放不下就保持创建尺寸，杜绝把窗口撑出屏幕，目标分辨率集中为 `interactiveFramebufferWidth/Height` 常量。新增守卫：`cmd/mornlea/app_window_fit_test.go` 三个属性测试（不放大、超屏收缩、目标已满足不重复 Resize）与 Rust 端 `window.rs::tests` 四个 `clamp_to_work_area` 纯函数单测。client ABI v7、协议 v26、engine ABI v6、benchmark scenario v19 与全部视觉 golden 均不变。

`fix-window-startup-flash-quit` 修复启动闪退(与窗口优化无关的既有竞态):登录成功后服务端立即按 `SnapshotChunks`(默认 64 条/tick)推送初始快照,视距 32 时约 4489 条 `ChunkSnapshot`,而窗口/渲染器初始化期间客户端没有任何消费方;旧的 256 条接收缓冲会在构造完成前溢出,把「启动慢」误判为 `consumer too slow` 并关闭会话——进程表现为窗口一闪即退(冷启动含着色器编译时必现,机器负载波动时偶发)。修复把接收容量提升为 `applicationReceiverCapacity = 8192`(≈ 全量快照 + 一分钟每 tick 状态的余量;运行期 fail-fast 语义不变,8192 条 ≈ 128 个权威 tick 的积压上界,内存上界约 8 MiB)。`cmd/mornlea` 与 `internal/client` 的接收语义、协议、存档与 golden 均不变。

`dev-test-speedup` 为开发迭代提速:为两个最慢包(实测 `cmd/mornlea` 244 秒、`internal/server` 122 秒,合计占全量时长 82%)中 23 个单测耗时 ≥1.5s 的重型测试(capture golden 场景、benchmark 场景、长 tick 集成/容量测试)增加 `testing.Short()` 跳过分支(每处顶部三行内联),`go test ./... -short` 由约 430 秒降到约 37 秒;新增 `make dev-check` 一键快检(gofmt 检查 + `go vet ./...` + `go test ./... -short` + Rust `fmt --check`/`clippy -D warnings`/单测),完整门禁(`make test-race`、`make visual-check`、CI 不带 `-short`)保持全量,短模式不做任何正确性放宽;新增 `docs/notes/test-quickstart.md` 记录「改动域→定点命令」映射与缓存/`-count=1` 使用纪律。顺带修复 `TestChatCommandAddressesExactConfiguredCompanionAtTickBoundary` 的既有时序易碎性:AI 未配置环境下首条命令的任务失败事件(异步 `TaskFail(PlannerUnavailable)`)到达 tick 不固定,负载高时会与「@阿 前缀拒绝」混进同一 tick 致精确事件数断言偶发失败——测试改为先推进 tick 等到该事件落地再发第二条命令,全模式与短模式均确定通过(全量基线 A/B:无跳过两次通过、短模式两次失败后修复)。


`add-egui-main-menu` 交付 egui 工具型 UI 技术栈（选型见 `docs/superpowers/specs/2026-08-23-egui-tool-ui-selection-design.md`：egui，弃 iced/Slint/RAUI/bevy_ui/Go GUI/webview）的第一个竖切——**进入游戏主菜单**，并把 `mornlea_client` 升到 client ABI v8。依赖线实测（crates.io，2026-08-23）：`egui-wgpu` 0.36.1 声明 `wgpu ^30`，仅 0.35.x 声明 `wgpu ^29`，按「绝不单侧升级 wgpu」采用 **egui 0.35.0 + egui-wgpu 0.35.0**（egui `default_fonts` 关闭、不引入 `egui-winit`：winit 事件在 Rust 侧手工翻译为 `egui::RawInput`）。客户端行为：交互式启动停留在主菜单（标题「Mornlea」、四按钮列（进入/退出可用、多人/设置禁用）、版本行、红色错误行），打开存档、启动本地权威服务端、登录与远环播种全部延迟到点击「进入游戏」之后（`cmd/mornlea` 新增 `StartAtMenu` 相位机，复用既有装配链路）；`-connect`/benchmark/capture 跳过菜单；菜单期光标不捕获、游戏输入不生效（`runMenuPhase` 输入隔离经间谍窗口测试锁定）；装配失败在菜单内显示错误且可重试/退出。client ABI v8：新增 `render_upload_ui_font`（一次性上传 OFL Noto Sans CJK SC，复用既有 embed/provenance，无新增二进制资产）与 `render_drain_ui_events`（事件数与状态码经 `out_count` 分离——Ruling 7；首版把计数当返回值与 WINDOW=3 撞码）两出口；帧输入 TLV 新增 UI 段（tag 9，UTF-8 文本段豁免 4 字节对齐——Ruling 8，真实菜单 142 字节非对齐由 `main-menu` capture 回圆）。egui 以一条附加 render pass 挂进既有渲染器（窗口/离屏同构，排 HUD/debug 之后），`FrameInput.ui_segment` 为空时 `take_ui_events()` 丢弃、零 GPU 参与。capture 视觉门禁新增 `main-menu` 场景（插在 `water-surface-slope` 之后、`far-horizon` 之前；`far-horizon` 仍倒数第二、`water-underwater` 仍最后），正式场景 17→18，既有 16 张 golden 逐字节不变（egui pass 只在有 UI 段的帧运行）。benchmark scenario 保持 v19（无固定上传布局变化；benchmark 帧无 UI 段）；协议 v26、engine ABI v6、各存档 schema、配置格式不变。CI 同步修复：`scripts/composite_grass_side.go`（`package main` 材质工具，前置提交引入）使 `go list ./...` 包含 `mornlea/scripts` 而分片矩阵未覆盖——`quality` 的「Race 分片全集自检」恒红，已把 scripts 并入 internal-rest 分片与自检联合集（含一处 YAML 双引号引语句法修正）。

`fix-player-flush-stall`（规划行 E-07，hunger 遗留 9）修复服务端玩家存档 `Flush` 的恒脏自旋：旧实现的重派去重用 `(playerID, revision)` 精确键，而成功完成后 `dirty = !matchesSave(save)`，恒脏态（快照在每次保存完成前又被更新）下每轮以 `save(persisted + 1)` 铸出新 revision、精确键永不命中，Flush 无界重派、只能靠 ctx 取消终止，关服路径可能挂起。修复（方案 A′，飞书两轮确认后批准）把重派记账换成按 `core.PlayerID` 的双类名额 `playerFlushSlots`：继承 in-flight 在 Flush 开始即预占 retry 类，冻结重试派发只占 retry 类（成功后仍可追派最新快照），fresh 派发同时占用两类（其失败冻结的同 revision retry 不得在同次 Flush 重派），每玩家每次 Flush 至多 2 次派发、循环有界终止；继承等待屏障仍按 `(playerID, revision)` 精确身份匹配。退出时仍有已加载脏玩家且本次 Flush 未记录其失败 → 以哨兵 `errPlayerFlushStalled` 计入 failures（键 revision 取 `persisted + 1`）并带错误返回，不静默掩盖残余脏；脏状态、冻结 retry 与最新快照完整保留给下次 Flush（关服路径对 Flush 失败本就保留 runtime 并重试）。旧实现 `TrySubmit` 被拒时携脏静默成功的路径同样改为上报失速（有意收紧，生产不可达）。10 条既有 Flush 测试零改动全绿，新增 `player_flush_stall_test.go` 两条钉住测试（恒脏有界终止 + 失败/失速双玩家混合上报）；既有错误文本逐字不变。新 OpenSpec capability `player-persistence` 随本 change 归档建立。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 均不变。

`fix-spawn-support-top`（规划行 E-10，farming 遗留 14）修复出生扫描把任意碰撞方块顶面固定解释为 `y+1` 的空隙：`findSpawnInColumn` 现在枚举候选方块的全部碰撞盒，按真实盒顶从高到低复用 `playerBoundsAreFree` 与 `playerSupport`，只接受身体无碰撞且底面完整支撑的候选；较高盒不成立时继续同格较低盒与列内较低方块。玩家与伙伴仍共用出生路径，登录恢复和 grounded safe 点继续使用同一完整支撑语义；真实耕地的出生、恢复与 safe 点均锁定脚底 `y=15/16`。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与配置格式均不变。

`crop-random-drop-count`（规划行 B-10，farming 遗留 10）推翻 farming 首版「掉落固定不随机」决策（design.md D9），把成熟小麦收获改为哈希驱动数量：两类产物各做一次独立抽取、各自落在 `[1,3]`，由 `(worldSeed, 权威 tick, 维度, 方块坐标)` 的 splitmix64 纯整数哈希确定——同输入重放逐件相同，无进程级随机源、零浮点零分配；yield 流用独立 salt 与生长抽样流解耦（同构于既有 `cropGrowthRollSalt` 论证）。种子下限 1 使「误挖不亏种子」的耕种循环保底升格为规格条款；未成熟固定 1 种子、耕地 1 泥土与 `core.BlockDrop` 单产物表形状不变。server e2e 的「净赚」自持断言回归规格本义「不亏」。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。PR #81 以 merge `dd429667` 合入，run `32856760888` 的 8 项全绿；整分支 SPEC/QUALITY/终审通过（Task 1 经一轮注释数学修正）。

`server-test-wait-budget`（规划行 E-11，hunger 遗留 11）把 `internal/server` 十二处「测试主动推进权威 tick + 登录就绪」循环收敛到共享 helper `waitIntegrationLoginReady`：固定预算 `integrationLoginTickBudget = 3000`，已满足时零推进、预算内成功、耗尽时以场景标签 + 预算 + 调用方诊断精确失败——取代此前登录永不收敛时只能等包级全局超时的挂起形态；farming/hunger 两份内联预算一并同源化，`Recv(ctx)`、墙钟 deadline 与业务阶段等待按边界保留。纯测试基础设施（`skip_specs: true`），生产代码、协议、存档与 golden 均不变；主题测试含「第 3000 次推进后成立仍成功」的变异杀伤边界用例。PR #80 以 merge `345f60776566a5bf37159dadcae6736f38380a68` 合入，SPEC/QUALITY 各经一轮 R1 修复后通过。

`hud-atlas-texel-stable-uv`（规划行 D-05，farming 遗留 18）把 HUD 图集列 UV 改为对称亚纹素收进 1/256 纹素：图集宽度随 `core.ItemIDMax` 扩列时的 float32 重归一化不再能把任何采样点推过列边界（适用域 `W ≤ 2^15` 纹素内解码界距边界恒 ≥ 1/512），同时消除列边界 tie-break 的实现定义行为——追加物品不再使既有图标亚像素漂移或串味（farming 期间 `hud-hotbar-health` 实测 0.115% 的那类像素）。`hotbarTextureUV` 签名不变、消费方与 Rust/client ABI 零改动；四条性质测试（裕度下界、区间不侵入、跨宽度解码集合一致、偏差上界 <1/64）经变异矩阵验证各自杀伤。两景心形区 golden（各 +81px 贴边界样本，即下次扩列本来就会翻转的同一批）经用户裁决外科手术式再生，其余 16 景零触碰。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与其余场景 golden 均不变；`survival-hud-presentation` 主规格新增「HUD 图集列采样稳定性」要求。PR #82 以 merge `7494ac68` 合入，CI 8/8 全绿；SPEC 首轮 FAIL 经 R3/R4 清偿后终审 PASS，QUALITY PASS。

`eating-container-interrupt`（规划行 B-31，hunger 遗留 10）补齐进食的第五条中断判据：会话打开容器界面（`session.viewContainer`）或视野尚未就绪（`!session.hasView`）即清零进度且不扣料——此前手持食物对准箱子按住「使用」时，开箱的同刻进食照常推进并结算。判据由 `internal/sim/player.go` 的 `advanceEating` 调用点以单个 `suspended` 参数求值并入既有中断分支，与 `stepMiningProgress` 的中断形态同源（`eatingState` 不持有 session 反引用）；两条新契约随之立规：中断优先于同 tick 的结算（恰在结算 tick 开箱也不扣料不回饱），关箱后输入仍按住则从第 1 tick 重新推进（「中断」与「根本没开始」观察上同一件事）。客户端开箱时的进食输入抑制列为非目标（权威中断已达成可观察结果）。三组引擎级用例（开箱中断与关箱重启、结算 tick 优先序、挂起持续保持的容器/视野双来源子用例）经双向变异（调用点恒传 `false`、删除 `suspended` 判据）各杀全部 4 用例。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。
 PR #83 以 merge `e1c725e8` 合入，CI 8/8 首跑全绿；SPEC/QUALITY 双评审、整分支终审与双向变异验证均 PASS。
`farmland-trample`（规划行 B-05，farming 遗留 4）把 farming 遗留的「踩踏不破坏耕地」清偿为：玩家从空中落地的边沿（`!wasOnGround → OnGround`，与摔落伤害同一次判定）把碰撞盒水平覆盖（至多 2×2 列、内域相交贴边不算）的耕地（干/湿）踩回泥土，其上作物按与采掘完全相同的确定性掉落规则结算后移除（成熟走 `cropYieldRolls` 双产物、未成熟 1 种子，同一株同一权威 tick 被踩掉与被挖掉数量逐件相同），耕地转泥土本身不掉落；掉落容量不足（`PrepareDropBatch` 预演失败）时整格保持原样绝不部分结算，踩踏只在边沿触发一次、持续站立不重复、跳起再落重新判定，多玩家同格结算幂等。落地边沿在物理阶段而区块写者必须位于 `reconcileSubscriptions` 之后（阶段顺序契约），故拆为「`player.go` 边沿一行收集 + `Engine` 暂存字段 + `advanceCrops` 首部一行结算」三处最小受控重叠（控制会话裁决，全部纯追加）；`cropYieldRolls` 的 tick 取值点与 `completeMining` 同一读取路径，收集序按 `SessionID` 升序确定。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。PR #85 以 merge `1a6120e0` 合入，CI 8/8 全绿（run `32874791885`）；三个 Task 经独立 SPEC/QUALITY 双评审 PASS、整分支终审 PASS（零 blocking）。

`settings-menu`（规划行 D-01）把主菜单的「设置」入口从禁用占位扩展为首版可持久化设置页，只包含总音量、材质包目录与 `640x360`/`960x540`/`1280x720` 三个固定 16:9 窗口预设。Go 持有 committed/draft：取消恢复已保存值，dirty 返回或 Escape 不静默丢弃；保存按“读取变化且非空的材质候选并完整校验（不应用像素） → 单次读取并完整校验最新配置、只 patch 三项 raw JSON 且保留其他原文 → rename 原子提交 → 更新 committed → 应用当前进程音量与窗口（Rust 按当前 NSWindow 所在 `NSScreen.visibleFrame` 的逻辑 point 与 style/chrome 钳制 content 并重定位 outer frame，Go 再守物理 framebuffer 上限）”执行；rename 前失败均保留草稿且不改变磁盘或运行态，rename 后目录同步异常则显式标记已提交、应用新运行态并显示持久性警告。生效材质只在启动时装载，当前 atlas 不热替换，候选通过后从下次启动加载；配置 v1 新增可选 `windowSize`（缺省 `1280x720`），`texturePackPath` 统一为最多 1024 个 UTF-8 字节且不得含 CR/LF。`mornlea_client` 升到 client ABI v9：下行保留 layout v1 主菜单并新增 layout v2 设置页，上行改为有版本、有类型、有长度且固定 64 条上限的结构化事件批，容量不足时整批不写、不计数、不出队。capture 在 `main-menu` 后加入 `settings-menu`，正式场景 18→19，`far-horizon` 仍倒数第二、`water-underwater` 仍最后；只更新启用设置按钮的 `main-menu.png` 并新增 `settings-menu.png`，其余既有 golden 逐字节不变。benchmark scenario v19、协议 v26、engine ABI v6、区块 schema v9、世界 metadata v2、玩家 schema v7 与 `companions.ai` schema v4 均不变。
`attack-exhaustion`（规划行 B-13 攻击半边，hunger 遗留 6）把成功近战命中接入疲劳固定表第六行：一次权威近战命中使攻击者累积固定 100 千分位疲劳（参考实现攻击疲劳 0.1 × 1000，与既有五行同一换算口径），判定点在 `advancePlayerMelee` 的意图冻结分叉——落空、固体遮挡与目标受击冷却免疫窗口内的输入不形成命中、不累积；意图冻结即收费，同 tick 被反击致死的攻击者仍支付已成功的命中代价，死亡结算的 `resetHunger` 抹平濒死者读数故该语义以结构保证而非观察型测试锁定。疲劳来源固定表保持刻意不做 tunable；协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19、视觉 golden 与客户端均不变（饱和度与疲劳值不上线）。冲刺疲劳半边依赖 B-30 协议升版，另行认领。

`drop-go-test-oracles`（规划行 E-04，rust-engine-go-rules §15 oracle 删除条款）删除三切片 test-only 旧 Go 实现副本——physics 的 `oracleStep`/`oracleResolveCollision` 积分与碰撞镜像、core raycast 的 `oracleRaycastBlocks` DDA 副本、worldgen 自包含 pointwise oracle（含 dense 差分门禁与点差分 fuzz）；`internal/mesh` 切片因 A-02 正在演进 Rust mesher 刻意延迟。门禁以等价网补位不净放宽：physics 新增 13 条位级 golden 向量钉住跨平台逐位一致契约（评审经变异自查与 Rust 侧复核）；raycast 性质 fuzz 补「命中点在命中格单位立方内」「进入面法线与方向点积为负」两条几何不变量并配确定性孪生（注释归因经评审者 Rust 变异实验校准，序锁归属如实指向既有 XYZ 平局序测试）；worldgen 幸存性质测试改写为生产黑盒双出口对照（chunk dense vs 单点 probe），树冠几何/跨界树一致性/树高区间全部保留。基线文档两句失效 oracle 从句随删同步（AGENTS.md/CLAUDE.md 逐字节一致、config.yaml、engine worldgen.rs 模块文档）。生产逻辑零改动（生产文件触碰仅 archcheck 强制的注释级修订）。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变；归档主规格三处「Go 测试 oracle」措辞卫生记入延期清单待 spec-hygiene change。PR #87 以 merge `c2aed88c` 合入，run `32885347564` 的 8 项首跑全绿；整分支终审 PASS（回退性验证四提交 revert 后终树与基线逐字节一致），Task 1 经 R1+R2、Task 2 经 R1 修复循环。
`companion-mine-containers`（规划行 C-01，M5C mine 首版显式留给后续设计的容器语义）解除伙伴 `mine` 对箱子与熔炉的拒绝，交付批量「全或无」结算（方案 A，需求方在「全或无」与「对齐玩家先例」间裁决）：完成 tick 在伙伴背包副本上按固定序（本体在前、容器槽位序）逐堆 `AddStack` 预演「容器本体 + 全部内容物」（箱子 ≤1+27 堆、熔炉 ≤1+3 堆），任一堆放不下该 tick 整体不结算——方块、容器内容物、耐久、背包全部不变，采掘进度保持满格（「就绪但无容量」的稳定可观察状态），Task Runner 观察到饱和即以既有 `TaskFailInventoryFull` 失败（v18 枚举复用、wire 零变更）；预演通过则同一权威 tick 内方块改空气、容器槽停用（generation 保留）、耐久扣减、背包提交。sim 与 Runner 经单一导出纯函数 `CompanionMineContainerStaging` 共享同一产物集合构造与预演（「没有第二套规则」从单件推广到批量）；计划侧 `planMineableBlock` 与模拟侧 `companionMineableBlock` 同步放开，农业十编号显式拒绝原样保留（C-11 另行裁决），Planner 提示词与解码拒绝文案同步。多掉落目标集合显式枚举 {箱子, 熔炉}，不引入编号层泛化判据，`core.BlockDrop` 单产物表形状不变。协议 v26、全部存档 schema（`companions.ai` v4）、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。全量门禁 `gates.sh` 六项 PASS（全量 race 28 包、`cmd/mornlea` 333.8s）；整分支 SPEC/QUALITY 终审通过（Task 2 经一轮 R1 注释修复，首轮聚合 race 失败取证为并行会话同机负载 flake、隔离复跑全绿）。
`eating-progress-hud`（规划行 B-14，hunger 遗留 2 的呈现半边）交付进食进度条的客户端预测呈现：手持食物、光标捕获、按住使用且权威确认饥饿未满时，两行状态栈上方出现与采掘条同锚点、同轨道几何的进度条（固定轨道 + 比例填充、最多 2 quad），满程约 1.6 秒。三层实现：`internal/client` 的 `EatingProgressTracker`（整毫秒按 `physics.FixedDelta` 累积、分母与权威 `EatingTicks` 默认值同源的 32、满格钳制、时钟倒退零增量、复位四源含数量变化——权威结算吃掉一件即「从零开始下一件」的客户端镜像）；`internal/render/hud` 的 `EatingOverlay`/`appendEatingBar`（互斥判定在函数内且保守取向——采掘反馈激活或其退化态均让位）；`cmd/mornlea` 三个经控制会话裁决的装配点位（字段/构造/复位，B-05 先例同形）。输入位派生与 `interactive.go` 的 Eating 位同源（光标捕获 + 次键 + 手持食物 + 权威饥饿未满），justCaptured 单帧偏差与受伤/死亡中断不镜像记为已知简化；无头路径（capture/benchmark）window 为 nil 使输入位恒假，属结构不变式。执行期修正两处：change 产物「20 ms/tick」撰写错误（权威 tick 是 50 ms，实现以 `physics.FixedDelta` 为准并回改产物）；评审 NIT-4 升级为行为修正——饥饿已满不呈现进度条，对齐权威侧「饥饿已满不推进」。协议 v26、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` schema v4、engine ABI v6、client ABI v8、benchmark scenario v19、267 quad/700 glyph/13312-byte glyph offset/46912 bytes 固定上传容量与全部 capture golden 均不变。
 PR #89 以 merge `bb65a28e` 合入，CI 8/8 全绿；SPEC/QUALITY 双评审、整分支终审与三向变异验证均 PASS（NIT-4 升级为饥饿门控行为修正）。
`flood-destroys-crops`（规划行 B-07，farming 遗留 1）补上流体与农业的交互：`fluid.Replaceable` 把作物格对流动水放行，冲毁结算挂在 `sim.fluidWorld.SetBlock` 唯一写入汇聚点——成熟按 `cropYieldRolls` 与采掘/踩踏共享同一哈希流、未成熟 1 种子，掉落槽满时拒绝破坏并按既有延迟重试（零数据丢失）；作物→水单调故收敛论证保持成立，水源不动点捷径经同一谓词自动收紧。双源同 tick 冲突合并、恰好一次结算与 Memory/TCP parity（开录前空转对齐绝对 tick，产量哈希四输入逐项相同）均有直接测试落点。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。PR #90 以 merge `eeb08525` 合入，CI 8/8 首跑全绿；SDD 三任务 SPEC/QUALITY 双评审、整分支终审与两轮 scoped 复审均 PASS。

`farmland-mesh-top-sink`（规划行 D-07，farming 遗留 13）把耕地可见几何收敛到既有碰撞边界：方块 registry 条目追加 1 字节 `block_top_raw`（0=满格哨兵、1..=14 为 4-bit 顶面高度原值、15 拒绝、与 `fluid_height` 互斥），干/湿耕地填 14 使全部可见面的上缘呈现在 (14+1)/16 = 15/16，与 `farmlandCollisionHeight` 一致；mesher 走常量角高度路径（复用水面 quad 角高度位通道、仿 `fluid_corners` 形状但数值取 registry 常量、不贪心合并、quad 仍 8 字节），engine ABI 随条目布局 18→19 字节升到 v7。客户端解码半边同期补齐：terrain.wgsl 以 material 区间（29..30，与 Go 层枚举三方手工同步、双侧钉子互焊）判别短方块并按 water.wgsl 同源公式抬升顶点，cull.wgsl 经核实本就不读尺寸位、以注释固化「误差有界且方向单一（只漏画不多画）」论证并用源码扫描钉机械锁定。materials-showcase 夹具追加干/湿耕地两列（近相机落位使 1px 下沉可辨并与草缘形成台阶对照），单景 golden 显式再生；主规格新增 `short-block-presentation` 能力并修订 `visual-verification` 夹具契约（19 景真值表）。分支与演进后的 main（135 提交、6 个已合 PR）变基合入：engine ABI 无碰撞、client ABI 已由 D-01 升至 v9 本行未触；集成树全量 race 唯一失败为既有负载 flake（分诊记录在案）。PR #91 以 merge `410fc135` 合入，CI 8/8 首跑全绿；Task 1/1b 各经一轮 R1 修复循环（quad 注释归属证伪、cull 论证方向反转均由评审拦截）、Task 2 双评审一次通过。
`fluid-audio-cue`（规划行 B-32，fluid-presentation 非目标清偿）把本地确认音频扩到第六个边界：权威确认位置上 `physics.SubmersionFlags` 求出的身体浸没标志出现 false→true 上升沿时播放恰好一次 `CueWaterSplash`（既有方波下滑合成器新条目，约 91ms，零新增合成与播放管线）；持续浸泡、出水、首观测即水中、会话 Reset 与未就绪清基线后均不响，未加载区块按干燥求值并按字面记账（持续浸没跨区块到达边界的至多一次补响是接受的已知边界）。判定收在 `localAudioFeedback` 的 has+值基线模式（缺席位参与判定防 Reset 清零误判），接线在权威 `PlayerState` 应用点复用 predictor 同款 `MirrorCollisionSource`。协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变；capture/benchmark/专服 playCue=nil 逐位不变。
`spec-oracle-wording-hygiene`（规划行 E-14，drop-go-test-oracles 延期项②）把三份 rust-engine 主规格的失真验收措辞更正为 E-04 实际落地的门禁：physics-step 的「与 Go 测试 oracle 逐位一致」改为 13 条冻结位级 golden 向量、collision-raycast 改为几何不变量性质 fuzz 加确定性孪生、worldgen 改为黄金摘要加生产黑盒双出口对照；全部 MODIFIED requirement 名称与 Scenario 正文经程序化比对与现存测试网逐条核实（含「任意种子」收敛为冻结语料的反过度声称）。勘察发现 openspec 1.7.0 对已存在主规格忽略 delta Purpose 且 MODIFIED 漂移守卫不支持 Scenario 标题改名，2 句 Purpose 与 3 个标题共 5 处残留经控制会话裁决按 design D2 在归档阶段直编（被否方案：REMOVED+ADDED 同名重建会触发工具硬冲突）。`rust-engine-mesh` 的两处 oracle 措辞在 mesh oracle 实存期间保持原样，待 A 批次合流后随 mesh 切片更正。纯文档变更：零代码、零测试、零 golden 触碰。

`instant-farmland-moisture`（规划行 B-09）把耕地干湿从随机 tick 扫描改为确定性的事件驱动权威阶段：`fluidWorld.SetBlock` 的真实流体 membership 变化、成功玩家放置以非流体覆盖流体和成功翻地生产候选，fluid→moisture→crop 阶段顺序让无旧积压的单格变化在同一 tick 生效。候选检查与方块读取分别受每 tick 65,536 次硬上限约束，FIFO 消费前缀以 O(1) slice rebase 回收；队列保持瞬态，active Ready 区块首次进入、重启后进入或重入时由完整高度 halo 重扫按预算恢复。随机作物阶段不再扫描湿润邻域，读取成本锁定为每样本最多两次；集成 `origin/main` 后的 `make rust`、`internal/sim` race、archcheck、全仓 race、vet、五轮 crop benchmark、guarded fluid performance 与 OpenSpec strict 67/67 均通过。当前版本身份为协议 v26、区块 schema v9、玩家 schema v7、世界 metadata v2、`companions.ai` schema v4、engine ABI v7、client ABI v9 与 benchmark scenario v19。

`dry-farmland-revert`（规划行 B-06，farming 遗留 5）补上干耕地废弃退化：干+正上方为空气（无作物/方块覆盖）时经与作物同源的随机 tick 抽样以 30% 固定概率原子退回泥土（`FarmlandDryID→DirtID`），湿/有作物覆盖永不退，零掉落；复用 `advanceCrops` 有界性，每样本至多多一次上方读取（`block_reads` 上界 1×→2×），与踩踏正交。`openspec/changes/archive/2026-08-27-dry-farmland-revert` 归档并 sync 进 `authoritative-farming` 主规格（`openspec validate --all --strict` 67 passed），`go test ./... -short` 全绿；协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。

`companion-idle-dialogue`（规划行 C-08）让完全空闲的伙伴（queue 无 current 且 pending 为空）按 FNV-1a(`ID‖LE(seed)`) 确定导出的 1200..2400 tick 闭区间机会，向仍在线且位于水平 16 格内的最近真实任务发令者说一句台词：新增零载荷 `DialogueNodeIdle`（HTTP kind `"idle"`，追加在枚举末尾不动既有数值），`dispatchIdleDialogues` 接在 `dispatchPlanning` 后按 `orderedIDs` 有界扫描，模加法与半区间比较覆盖 tick 回绕；server-only `restored` 标记使恢复合成身份永不成为受众，idle 复用共享四模型槽与单在途纪律、不检查不递增任务八次 Dialogue 预算。`applyDialogueOutcome` 在结果时重验空队列、同一真实 issuer（playerID+name）、active 身体与在线+距离资格，有效结果复用 `applyDialogueEffect` 广播 `CompanionSpeech`（非 terminal 不写 summary，模型失败不补发不重排期）；idle 在途时任务启动不取消也不发第二请求，迟到结果静默丢弃。`openspec/changes/archive/2026-08-27-companion-idle-dialogue` 归档并 sync 进 `companion-dialogue` 主规格（strict 67/67）；PR #97 CI 8/8 首跑全绿后合并，全量 race、archcheck、vet、rust-check 与 gates.sh 均通过；协议 v26、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。

`bone-meal`（规划行 B-03，farming 遗留 3）补上骨粉催熟最小闭环：`ItemBoneMeal`（`ItemIDMax` 前 append-only，堆叠 64）+ `BoneMeal` 命令（`Play/C→S` ID 14，`Yaw/Pitch` 载荷与 `TillSoil` 同形，`ProtocolVersion 26→27`）对 `WheatStage0..6` 原子推进 1 阶（`Stage7` 拒绝），消耗 1，拒绝零消耗。`executeBoneMeal` 校验序与翻地同源（`PlayerActive → 射线有效命中 → 区块就绪 → 未成熟作物 → 手持骨粉`），`SetBlock(下一阶段)+recordChange+Consume` 在 `reconcileSubscriptions` 之后的 `interactions` 阶段原子提交；客户端 `placeBlock` 在翻地分支后插入骨粉分支（`IsCrop && ItemBoneMeal` 时发 `BoneMeal`）。`openspec/changes/archive/2026-08-27-bone-meal` 归档并 sync 进 `authoritative-farming` 主规格（`openspec validate --all --strict` 67 passed），`go test ./... -short` 全绿；`AGENTS.md`/`openspec/config.yaml` 版本矩阵同步至协议 v27，其余存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。

`literal-single-source`（规划行 E-12，m5e 归档递延 4、5 清偿）把三处 `[32]network.ChatEvent{}` 裸容量字面同源到 `client.ChatEventCapacity`，并为 ChatCommand 文本上限新增包级常量 `chatCommandTextMaxBytes = companion.MaxPlanCommandBytes`，编码端、解码端与 payload 超限错误文案三点收敛同一来源（`%d` 推导输出经常量链演算与原静态串逐字节等价）；锁测试的独立 1024/1025/1026 字面刻意保持不同源以充当漂移守卫。零行为变化（skip_specs）：协议 v27、全部存档 schema、engine/client ABI、benchmark scenario v19 与视觉 golden 均不变。原认领失联后的转移裁决、双评审 PASS、整分支终审与两条 NIT 顺延均登记于 change ledger。落点为 PR #99（merge `6add2597`），CI run `33045444253` 的 8 项首跑全绿。

`more-crops`（规划行 B-01，farming 遗留 2）在小麦 8 阶段闭环之外追加马铃薯与胡萝卜各 8 阶段闭环（`PotatoStage0..7`/`CarrotStage0..7` 合计 16 方块，与 `WorkbenchID` 同批追加后共 27 个方块于 `WaterLevel7ID` 之后；`ItemPotato/Carrot/PoisonousPotato` 紧接 `ItemBoneMeal`/`ItemWorkbench` 之后且堆叠 64，食物值 `1/600`、`3/3600`、`2/1200` 千分位，毒土豆不可放置），`IsCrop` 扩展为 `Wheat||Potato||Carrot` 三区间并集，`growCrop` 按类型分派但规则同 `wet&&sky→+1`、封顶 `Stage7`、`+1` 依赖连续编号；成熟掉落 `CarrotStage7` 产 `1..4` 胡萝卜、`PotatoStage7` 产 `1..4` 马铃薯且附独立 `poisonRoll` 2%（`hash%50==0`）额外毒土豆，未成熟各产 `1` 自身以不亏种，均走原子容量语义复用既有 `pendingChunkChanges`；种植复用 `PlaceBlock`、骨粉/踩踏/水冲沿 `IsCrop` 自动覆盖零改，资产侧按小麦同形各新增两套 `cross` 植物与纯色占位纹理（`LayerWheat0..LayerCarrot7` 连续 24 层 `31..54` 后追加工作台 3 层，`PlantMaterialLast` 38→54，`nativeMaxRegistryEntries` 48→64 容纳 58 条，`cross` 几何与 `downsampleCutout` 复用，不新增 model tag 规避 A-02 容量冲突），`storage` 仅测试覆盖 round-trip；全部推进与掉落为 `(worldSeed,tick,dimension,pos)` 纯整数 `splitmix64` 确定性链，无 `math/rand`、无 map 遍历、枚举顺序全序固定，相同输入重放逐格/逐件一致。`openspec/changes/archive/2026-08-27-more-crops/` 归档并 sync 进 `authoritative-farming` 主规格（`openspec validate --all --strict` 67 passed），`go test ./... -short` 全绿，协议 v27、区块 schema v9、玩家 schema v7、世界 metadata v2、`companions.ai` schema v4、engine ABI v7、client ABI v9、benchmark scenario v19 与视觉 golden 均不变。

`egui-debug-panel`（规划行 D-03）与 `authoritative-grid-crafting`（A-01）已合入 `main`（见 `origin/main` 基线），本分支变基后保留其 `progress` 条目与 `backlog` 状态。

`pause-menu`（规划行 D-02）交付单机暂停最小闭环：`internal/server` 新增原子权威暂停门（幂等 `Pause()`/`Resume()`，置位时整个权威 tick 不被调度执行——世界时间、随机 tick、作物、流体、实体与持久化调度全部停走，恢复后同种子确定性续跑），单机交互路径的权威世界由 `server.Host` 私有持有，故经 `Host` 直通两方法暴露为该门的唯一宿主面；`cmd/mornlea` 新增 `menuPhasePaused` 相位机与 Esc 栈暂停档位（优先级居聊天/背包/egui 面板之后），本地形态开层恰好调用一次门、TCP 远程形态零服务端调用仅呈现「远程世界不会暂停」注明行，「退回主菜单」复用既有会话拆链后「进入游戏」可二次装配全新会话；`mornlea_client` 新增 egui 暂停页，页面级布局版本 4（主菜单 1/设置 2/调试 3 不动），动作「返回游戏」=8、「退回主菜单」=9 与 Go `menuAction*` 常量跨语言同值互钉，Esc 开合由 Go 键位栈唯一裁决、Rust 侧不合成 Escape 动作——终审 B1 曾因宿主 winit 泵同帧回声（同一帧既更新键位快照又把 Escape 入队 UI 键事件）致覆盖层开层即闭，修复链（删除 Rust 合成→复审 PASS→哨兵注释收口）见归档 ledger「5」段各行。`openspec/changes/archive/2026-08-27-pause-menu` 归档并 sync 三条 ADDED Requirement 进 `egui-tool-ui` 主规格。协议 v27、区块 schema v9、玩家 schema v7、世界 metadata v2、`companions.ai` schema v4、engine ABI v7、client ABI v9、benchmark scenario v19 均不变；capture 场景表与全部视觉 golden 零变化（暂停页不出现在任何 capture 场景，经与 merge-base `b10bbfe7` 的对照实验逐项证实）。

`placeable-torches`（规划行 A-02，从当前 main 重做）交付五向火把闭环：落地+四向墙面方块、`ItemTorch`、`RecipeTorch`（煤上棍下出 4，2×2 个人网格可摆）、命中实心支撑面的权威放置（底面/非实心/流体/未加载/玩家占位拒绝且不扣物品）、支撑被任何权威方块变化移除时对本 tick 已变位置的固定六邻居有界复核（同批写空气+火把掉落，掉落容量不足整体保留待下次自愈）、零碰撞/非不透明/发光 14 与 `core.BlockEmission`/`BlockLightAttenuation` 全仓唯一光源表（assets 转调收敛）、伙伴 mine/place 防御清单拒绝火把且寻路可通过表与 collision oracle 逐块一致。mesh registry entry 19→20 bytes（model@19）与上限 64→80（Task 1 收口点提前以防跨任务红态），model tag 0=默认/1..5=火把/6=床保留拒绝，`emit_torch` 落地 4 交叉斜面与墙面 3 斜板贴帽几何（quad 打包断言由双向放宽为单向并与 Rust 同口径）；engine ABI v7→v8 三处同步，terrain.wgsl 角高度解码门控扩 torch_material(=59)。程序化 `LayerTorch` 纹理与 torch-night 封闭暗室场景（表内第 12 位）自带 golden。集成期 main 合入门批次（方块 62..70/物品 43/配方 14/纹理层 55..58）与 A-02 原编号撞车，经独立集成任务重订为方块 71..75/`ItemTorch`=44/`RecipeTorch`=15/`LayerTorch`=59，B-12 的协议 v29 zero flag 与 quad 位布局核实无冲突，F-05 再生后的 20 张基线并入后 21 场景 compare 全零差异；openspec 1.7 MODIFIED 漂移守卫不支持 Scenario 改名的约束在归档时按 E-14 先例处理。四任务组+整分支终审全部双评审 PASS；11 包 race、全量 race、rust-check、gates.sh 与 PR #107 CI 8/8 全绿。协议 v29、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` v4、client ABI v9、scenario v19 均不变，engine ABI v8 与 golden 21 张为唯一基线变更。

`split-client-subpackages`（需求方直发，无规划行）把 `cmd/mornlea` 单一 `package main`（89 个 Go 文件、约 2.4 万行）拆为薄 main（`main.go`/`options.go` 装配入口）加 `app/`（`app.Application` 主体与输入/UI/帧/生命周期）、`capture/`（视觉 golden 与 21 张 PNG 资产随迁，`captureGoldenDir` 同步）、`benchmark/`（性能场景 v19 与八会话探针）三个功能域子包：跨包访问经消费端接口（capture `SceneApplication` 54 方法、benchmark `BenchmarkApplication` 15 方法，`*app.Application` 隐式实现），加载等待函数族与 drain 常量下沉 `app/app_load.go` 单一实现；依赖方向（main → 三子包、capture/benchmark → app、禁止反向与互依）由 archcheck 新增平台无关 AST 断言强制（`go list` 在 GOOS=linux 下对 darwin-only 包不可用，弃用）；每包保留唯一 helper 中心，`go test -list` 四包并集与迁移前基线逐名零差异（384 Test + 1 Benchmark），golden 21 张纯 rename 逐字节不变（visual-check 21 场景全零差异），dev-check、全量 race 31 包、rust-check、openspec strict 全绿；文档按 harness 模式分层（总纲目录树+依赖方向+同步政策，子包文档不变量+精确路径+钉死回归测试名，三份薄 CLAUDE.md 入 `claudeImportDocs` 登记门禁）；CI 的「50ms 服务端探针门禁」与「M3C v6」入口随包迁移（评审捕获的空跑回归）。`openspec/changes/archive/2026-08-28-split-client-subpackages` 归档并 sync 四条 ADDED Requirement 进 `repository-code-organization` 主规格（72 passed）。协议 v29、玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` schema v4、engine ABI v8、client ABI v9、benchmark scenario v19 与 golden 内容均不变；收益为 `go test ./cmd/mornlea/app` 不再为 capture/benchmark 重型测试付费，重型域可单独定点。

`authoritative-hostile-nightwalker`（规划行 A-04）交付服务端权威夜行者最小闭环：稳定非零 ID 由世界种子、世界时间与候选坐标确定性派生，受全服 64 只与任一 active 玩家水平 48 格内 8 只双上限约束、集合按 ID 升序维护；显示相位 `13000..23000` 且候选格局部区块光 ≤7 时从锚点玩家（按 `WorldTimeTicks % 会话数` 选锚）水平 `24..48` 窗口确定性生成，双格空气+实心支撑+非流体+chunk 完整加载逐项校验、每 tick 至多验证一个候选；追逐最近 active 玩家并经既有权威伤害入口结算 3 点近战（重置回血计时、致死走既有同 tick 死亡结算），白昼露天按共享冷却周期灼烧 1 点伤害、远离全部玩家水平超 64 格累计 despawn，死亡掉落腐肉（食物 `4/0` 千分位、无状态效果、不进 BlockDrop 表）。存档 `hostile_mobs.bin` schema v1：固定头+至多 64 条固定记录、CRC 与逐项合法性校验损坏整拒、Memory/Disk 同构（缺失文件视为空集合、写入失败保留旧文件、重启恢复全部记录且损坏文件不能以空集合覆盖）；协议 v30 新增三类 S→C `HostileSpawn`/`HostileState`/`HostileDespawn`（按会话视野订阅发布，客户端 latest-wins 镜像+插值呈现、绝不预测），avatar 容量扩到 75 且 client ABI 升 v10 旧动态库早期拒绝。`openspec/changes/archive/2026-08-28-authoritative-hostile-nightwalker` 归档并 sync 进 `authoritative-hostile-nightwalker`/`hostile-mob-persistence`/`hostile-mob-protocol` 三个新建主规格与 `authoritative-health`/`authoritative-hunger`/`rust-client-render-entities`/`visual-verification` 四处既有主规格（strict 76/76）；`hostile-mob` 无窗口场景入场（`ai-companion` 之后），golden 22 张口径。PR #111 以 merge `1688e4e1` 合入，CI 8/8 全绿；协议 v30、client ABI v10、`hostile_mobs` v1 为版本变更，玩家 schema v7、区块 schema v9、世界 metadata v2、`companions.ai` v4、engine ABI v8、benchmark scenario v19 均不变。

`authoritative-bed-sleep`（规划行 A-05）交付床与睡眠闭环：床为床尾/床头×4 朝向共 8 形态的双格方块，放置要求床头同处已加载区块、两格空气且各自实心支撑，原子写入两格并恰好消耗 1 个床物品（跨区块/支撑不足/床头被占据整单拒绝不扣物品），采掘任一半清空整床并掉落恰好 1 个、支撑失效整床移除，注册 9/16 半高碰撞体且射线可分别命中床头床尾；显示相位 `13000..23000` 对床右键入睡并把个人重生点设为床尾格（白昼拒绝、移动输入或受伤取消入睡但保留重生点），全员入睡跳夜只推进显示相位——设置 `DayPhaseOffset` 使显示相位跳到周期起点并清除全部入睡状态，绝对 `WorldTimeTicks` 与作物/流体/掉落寿命节奏不变、不依赖或查询敌怪状态；死亡重生时延迟校验重生点对应两格仍为同一张床则回床尾满血，否则回落世界出生锚点并清除失效记录（区块未就绪保留记录、重生不停摆，各玩家重生点互不影响）。床配方入 `authoritative-grid-crafting`（顶排 3 小麦+下排 3 橡木木板，与门形状互不误配）；玩家 schema v7→v8 持久化个人重生点（v7 旧档迁移为无重生点、行为不变），世界 metadata v2→v3 持久化 `DayPhaseOffset`（v1/v2 旧档迁移偏移 0、显示相位行为不变），两类迁移均读入即升级且不阻塞权威 tick；协议 v31 `PlayerState` 尾部追加 `DayPhaseOffset` u16。`openspec/changes/archive/2026-08-28-authoritative-bed-sleep` 归档并 sync 进 `authoritative-bed-sleep` 新建主规格与 `authoritative-daylight`/`authoritative-health`/`authoritative-grid-crafting`/`visual-verification` 四处既有主规格（strict 76/76）；`bed-night` 无窗口夜景场景入场（`torch-night` 之后），golden 23 张口径。PR #112 以 merge `6c49b1ed` 合入，CI 8/8 全绿；协议 v31、玩家 schema v8、世界 metadata v3 为版本变更，区块 schema v9、`companions.ai` v4、`hostile_mobs` v1、engine ABI v8、client ABI v10、benchmark scenario v19 均不变。

`split-storage-subpackages`（需求方直发，无规划行）把 `internal/storage` 单一 `package storage`（65 个 Go 文件、约 1.86 万行、234 个测试入口）拆为薄根包（Store 接口、`DiskStore`/`MemoryStore`、world metadata、backup 与别名再导出）加五个域子包：`region/`（通用 region 容器原语与 `RegionKey`/`RegionFor`）、`chunk/`（chunk 编解码与逐版本迁移 + region 记录层 `region.go`——实施核实其为 chunk 记录层而非通用容器，随 chunk 域走，作废初稿的「无 chunk 类型门面」设计）、`player/`、`companion/`、`hostile/`（各自 codec、迁移、类型与版本化 bin fixture），另设 `storagedef/` 叶子承载跨域哨兵 `ErrCorrupt`/`ErrFutureVersion` 防根包↔子包循环。消费面经别名再导出零改动：全部既有 `storage.X` 符号引用以同名同类型身份解析，错误哨兵为同一错误值（`errors.Is` 身份不变），消费方仅 `internal/server/hunger_persistence_test.go` 一处 fixture 相对路径随 `testdata` 迁移更新（delta spec 据此收窄为「生产代码零改动 + fixture 路径豁免」口径）。测试归属按「跟随被测主体」裁决：store 行为测试（disk/memory/world_files/backup/metadata/三个 *_store_test/chunk_keys_test）留根，格式测试随域，`go test -list` 六包并集与迁移前基线逐名零差异（223 Test + 7 Benchmark + 4 Fuzz）。依赖方向（根包 → 五域包、chunk → region/storagedef、companion 域保留 `internal/companion` 边、域包互不依赖）由 archcheck allowed 表登记强制。文档按 `docs/agents-md-style.md` 分层（总纲目录地图 + 依赖方向 + 别名政策，五份子包指南不变量 + 精确路径 + 钉死回归测试名，子包不放 CLAUDE.md）。验证矩阵的盲区由 CI 兜住：微基准步骤真正执行 `-bench`（本地 -list/-race/dev-check 均不运行基准本体），暴露 chunk 域内直装配基准缺 `dimensions/0/regions` 目录创建，补 `MkdirAll` 后 CI 全绿。dev-check、全量 race 36 包、`openspec validate --all --strict`（77 passed）与 PR #119 CI 7/8→8/8 全绿。`openspec/changes/archive/2026-08-29-split-storage-subpackages` 归档并 sync 四条 ADDED Requirement 进 `repository-code-organization` 主规格。协议 v31、玩家 schema v8、区块 schema v9、世界 metadata v3、`companions.ai` schema v4、`hostile_mobs` schema v1、engine ABI v8、client ABI v10、benchmark scenario v19 与 golden 均不变；收益为 storage 六包可单独定点 race（根 17s、chunk 9s、其余 2-4s），重型格式域测试不再互相付费。

`split-network-subpackages`（需求方直发，无规划行）把 `internal/network` 在既有 `tcp/` 之外的单一根包（23 生产 + 30 测试文件、197 个测试入口）拆为薄根包加两个子包：`protocol/`（`packet.go` 密封 ClientPacket/ServerPacket 与 `ProtocolVersion`/`State`、八个域消息 DTO + `Validate`、冻结包 ID 表 `registry.go`、区块 wire DTO `snapshot.go`——密封接口以 unexported marker 钉死四类文件必须同包）与 `codec/`（字节原语 `codec_primitives.go` 保持全 unexported、双向分发 `codec_client/server.go`、`chunk_codec.go` 压缩专路、`frame.go` 帧封装，以及自 message 文件归位的 companion/hostile wire 编解码 `companion_wire.go`/`hostile_wire.go`）；根包保留会话与传输编排（stream 接口、endpoints、Memory transport、登录状态机）并以 `types.go` 别名门面再导出全部迁移符号（类型别名保方法集、错误/常量同值、函数 var 别名）。消费面零改动经终审双向符号比对证明：基线 23 个生产文件的导出面是当前根包导出面的子集，internal/server、internal/client、cmd/mornlea* 与 tcp 生产代码逐字节未动。测试归属按「跟随被测主体」拆分：协议级测试随 protocol、编解码往返/golden/fuzz 随 codec、会话与跨域基准留根（CI M3C 步骤与 Makefile `bench-multiplayer` 经别名继续命中根包），`go test -list` 四包并集与基线 197 项逐名零差异（根 36 Test + 7 Bench、protocol 29 Test、codec 86 Test + 6 Fuzz、tcp 33 Test）；混合测试文件按函数拆分、函数名与 `t.Run` 标签逐名保留（评审抓出 19 个标签被机械加前缀后复原）。依赖方向（根 → {protocol, codec, core}、protocol → {core, companion}、codec → {protocol, core}、tcp → 根不变）由 archcheck allowed 表登记强制。文档按 `docs/agents-md-style.md` 重建根/protocol/codec 三份指南（子包不放 CLAUDE.md），同步 ci.yml 协议门禁行、test-quickstart、architecture.md 与 compatibility.md。dev-check、全量 race 38 包、`-bench . -benchtime 1x` 预演 52 条基准线（数值只记录）、`openspec validate --all --strict` 与 PR #120 CI 8/8 全绿（首轮两例墙钟敏感 integration flake：`TestTCPPlayerAndWorldSaveFailureRecovery` 为 backlog F-05 已记录的既有时序 flake、50ms 服务端探针 deadline 为 runner 调度毛刺，本地 -race -count=10 复现命令全绿后按 F-05 先例重跑通过）。`openspec/changes/archive/2026-08-29-split-network-subpackages` 归档并 sync 四条 ADDED Requirement 进 `repository-code-organization` 主规格。协议 v31、玩家/区块/世界 metadata/伙伴/敌怪 schema、engine ABI v8、client ABI v10、benchmark scenario v19 与 golden 均不变；收益为 protocol/codec/根包可独立定点 race（根 4.8s、protocol 1.6s、codec 2.0s），协议演进锁步契约仍在 AGENTS.md 传输一致性/协议演进节强制。

`client-ui-vanilla-alignment` 把图形客户端的 HUD、容器界面与 egui 菜单对齐原版空间关系并以「像素图标 × 现代面板」统一风格：新增 `internal/render/hud/style.go` 唯一样式令牌来源（深色半透明面板 + 1px 亮边 + 单一琥珀强调色）与 Rust 侧同族 `ui/style.rs`，主菜单/暂停/设置/F3 菜单换肤而 wire layout v1–v4 与全部行为语义不变；生存 HUD 收紧到底边（底边距 24→6），新增屏幕中心十字准星与服务端确认选中变化的物品名弹条（40 权威 tick 窗口、纯 glyph 双层、容器与菜单相位抑制），`core.ItemDisplayName` 提供全量物品中文名；背包/工作台/熔炉/箱子改为屏幕居中浮动面板（面板几何单源、绘制与命中共用），熔炉用原版「输入上/火焰中/燃料下→箭头→输出」图式，个人背包面板右侧保留十条固定配方入口（呈现+悬停提示，点击不触发合成，合成走网格两次点击流程），悬停非空栏位显示中文名 tooltip；HUD 图集 13 个 cell mask 就地重绘提净。benchmark scenario 升到 v20（无窗口场景新增 `hud-item-name-popup`，golden 23→24 张全部重生成并逐图复核），HUD 固定上传契约重钉为 320 quad、768 glyph、15616-byte glyph offset、52480-byte 总容量。协议 v31、玩家 schema v8、区块 schema v9、世界 metadata v3、`companions.ai` schema v4、`hostile_mobs` schema v1、engine ABI v8、client ABI v11 均不变。
`fix-gpu-benchmark-batch` 恢复 scenario v12 `remote_gpu_complete` 的既有批量提交契约：`mornlea_client` 的 client ABI v10→v11，离屏 benchmark 在首个读钟前将合法帧以 `1..=256` 次重复录入一个 prepared command buffer，两个读钟之间只 submit 一次并等待 GPU 完成；重复 prepare 与缺失 submit 稳定拒绝且不覆盖首批，普通 `render_frame` 语义不变。Go 探针不再把 `runtime.GC()` 当作 GPU 回收边界，报告字段测试不装配 renderer；协议、存档、engine ABI 与 benchmark scenario v19 均不变。

`tiered-swords-combat` 在协议 v31 基础上升级到 v32，追加私有 `CombatHit` 确认，注册木/石/铁三级剑及对应损坏形态和 recipe 17..19。玩家与夜行者共用同一 tick 的冻结、容量上限、victim reservation 与结算顺序；玩家成功命中按剑材质造成 4/5/6 点伤害并消耗完好剑耐久，失败或竞争淘汰无副作用，夜行者攻击造成 3 点伤害并带固定击退。确认只发送给攻击者，Memory/TCP 业务事实一致；客户端以确认驱动命中音效、4-quad marker 与 `sword-combat` 无窗口视觉场景。协议 v32、玩家 schema v8、区块 schema v9、世界 metadata v3、`companions.ai` schema v4、`hostile_mobs` schema v1、engine ABI v8、client ABI v11、benchmark scenario v20 与 25 张正式 capture golden 为当前基线。PR #124 以 squash merge `90188fbc` 合入；本地全量 Go 测试、受影响包 race、archcheck、OpenSpec strict 与远端 CI 全部通过。
`client-webview-ui` 把窗口型菜单层（主菜单/设置/暂停/F3 调试面板）从 egui 即时模式整体迁到进程内 WKWebView + Vite + TypeScript + React：前端源码住 `engine/crates/mornlea_client/frontend/`（pnpm 经 corepack `packageManager` 钉版、`--frozen-lockfile` 唯一安装姿势、构建产物 `dist/` 提交入库供 Rust 内嵌，`make frontend-check` 承担冻结安装 + typecheck + vitest + 构建 + dist 一致性门禁）；WKWebView 挂在 winit 窗口 contentView 之上并以 `drawsBackground=false` 透明露出 wgpu 画面（KVC 写入加三级不透明降级守卫），资产经 `mornlea://` scheme handler 从内嵌字节供给（零磁盘写入、零网络、零 CDN），菜单相位 WebView 惰性挂载并接管输入、游戏相位隐藏零参与，`-connect`/benchmark/capture 路径永不创建 WebView。桥协议单源 `frontend/src/bridge/schema.json`（JSON Schema 草案 2020-12）三端钉值：Go 事件驱动 `ui_push_state` 下行 JSON 状态（变化才推送），上行经 WKScriptMessageHandler 汇入 `drain_ui_events` 的版本化 JSON 事件信封（≤64 条、空队列 0 字节），Esc 优先级栈、设置草稿、暂停门与 F3 面板编辑播种等行为语义逐条平移并三端测试守护；设置页脏草稿返回裁决、F3 全精度播种不变量与「变化才推送」均由 Go 与 TS 双侧钉值。世界全景背景：主菜单与设置页相位以固定种子 worldgen 直供区块、经专属 mirror/mesher 调度渲染缓慢环绕的确定性全景（不打开世界存储、不启动本地权威服务端、不登录，源扫描红线测试锁定装配边界），相机推进为 tick 纯函数；途中固化 `SectionScheduler.FlushUploads` 的距离→X→Y→Z 上传序 tiebreak（世界 golden 零差实证中性）。client ABI v11→v12：`upload_ui_font` 出口与帧 tag 9 UI 段（layout v1–v4 编解码）退役，新增 `ui_push_state`，`drain_ui_events` 改 JSON 信封；egui/egui-wgpu 依赖与 `ui.rs`、`src/ui/`、`render/egui.rs` 全部删除（约 5900 行，cargo 测试 188→89 逐文件账目闭合）。golden 重生成 3 张——`main-menu`/`settings-menu` 变为全景底图、`debug-panel` 清除已退役 egui 面板像素，其余 21 张逐字节不变；菜单 chrome 退出像素 golden（系统 WebKit 版本不可钉死），由 vitest 组件断言与桥协议三端钉值测试承担。与 tiered-swords 合并后的基线接轨：协议 v32（该变更升级）、玩家 schema v8、区块 schema v9、世界 metadata v3、`companions.ai` schema v4、`hostile_mobs` schema v1、engine ABI v8 与 benchmark scenario v20 均不变。预存缺陷发现：暂停门置位时 `server.step` 整体跳过（含 KeepAlive 处理）而心跳走墙钟，暂停层停留约 15–20 秒必心跳超时拆链退出（egui 时代已存在，记 backlog F-08，待独立 change 解耦）。

`persistence-scan-hash-optimization`（规划行 F-07，2026-08-30 brainstorming 全链路画像裁决）消除权威服务端持久化路径两个 CPU 热点：`realm.PersistenceStats` 原被 `server.Step` 每 tick 调三次（`Observe` 两处 + `Status()` 一处）、每次 O(全部已加载区块) 全扫并对每脏区块走 24 区段估算（4,489 区块基准下画像 15s/120s ≈ 12.5% 单核，藏在 tick 指标外且随视距线性增长）；`Chunk.Hash` 原对每区块 98,304 次逐体素 `Get` + 2 字节小 SHA-256 写入（画像 11.6s ≈ 9.6% 单核，`MemoryStore.SaveBatch` 每批每区块、磁盘去重再加两次）。现 `Dimension` 级增量记账（`refreshRecord`/`settleRecord`/`rebuildStats` 挂接全部 14 类记录迁移点，估算缓存键 `(revision, chunk 指针)`，`InFlightChunks` 取 `len(inFlightSaves)`）使统计查询零记录访问，`PersistenceSnapshots` 只迭代脏区块索引并复验原有全部过滤与排序；`Chunk.Hash` 改每区段一次缓冲编码 + 一次 `hash.Write`（`PalettedContainer.appendBlocksLE` 线性序批量导出，single 直填快路径），磁盘去重单次哈希复用。SHA-256 摘要、`EstimatedBytes` 数值（含「脏且在途」双计入现行语义）、`SaveBatch` 判定结果逐位不变；旧全量扫描与逐体素实现均移入测试作 oracle，随机操作序列属性测试逐步钉住等价（TDD 期间真抓出估算 diff 顺序 bug 一枚）。协议 v32、全部存档 schema、engine ABI v8、client ABI v12、benchmark scenario v20 与 capture golden 均不变。微基准哈希 2.7–5.1×（UniformAir 621→121µs）；record-only 无窗口对比 persistence p50 −54%、**p99 55.4→24.5ms（−56%）**、max −38%（前后负载 14–21→4.7–6，帧时只作方向参考；tick p50 0.24→0.40ms 单次采样无统计力）。subagent-driven 双任务组各经独立 SPEC/QUALITY 双评审（Task 1 一轮修复：空转子测试改诚实判例；Task 2 零修复，四项偏离经控制会话裁决并同步 design），整分支终审 PASS。

`rust-render-world-cache` 以 main 的 WKWebView/UI client ABI v12 为前代，把组合 ABI 升到 v13，并新增 MRW1 v1 输入与 renderer 独占、原子更新的紧凑 `RenderWorld` 派生缓存；Go bridge 与 C/Rust 版本身份同步，旧 ABI 在缓存改变前拒绝，engine ABI 保持 v8。该入口仅由 Rust/Go 测试驱动，尚未接入实时 app；Go 继续持有生产 mesh 调度、connectivity/visibility、逐 section upload 与 draw 输入，因此 frame layout、`RenderFrame.Visible`、离屏输出、协议/存档 schema、benchmark scenario v20、流体与 visual golden 均未改变。
