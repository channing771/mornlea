interface:
  display_name: "Adaptive Model Router"
  short_description: "Route isolated work across native, Z Code, and OpenCode agents"
  default_prompt: "Use $adaptive-model-router; route architecture, feature-design, and high-level system tasks only to an advanced native OpenAI model before applying Z Code or OpenCode quota preferences."

policy:
  allow_implicit_invocation: true
